{
  "extends": "zotero-types/entries/sandbox/",
  "include": ["src", "typings"],
  "exclude": [
    "build",
    "addon",
    "src/**/*-safeBackup-*",
    ".agents",
    "src/sidebar/components",
    "src/sidebar/**/*.tsx"
  ]
}
