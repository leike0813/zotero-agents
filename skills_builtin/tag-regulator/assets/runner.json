{
  "id": "tag-regulator",
  "name": "🏷️ Tag Regulator",
  "description": "Normalize and infer Zotero tags with an optional controlled vocabulary file.",
  "version": "1.0.1",
  "execution_modes": [
    "auto",
    "interactive"
  ],
  "entrypoint": {
    "prompts": {
      "common": "- file inputs: valid_tags={{ input.valid_tags }} (optional; absent means pure inference mode), digest_markdown={{ input.digest_markdown }} (optional evidence)\n- inline inputs: metadata={{ input.metadata }} (optional), input_tags={{ input.input_tags }} (optional; missing means [])\n- parameters: valid_tags_format={{ parameter.valid_tags_format }} (only applies when valid_tags is provided), infer_tag={{ parameter.infer_tag }} (does not disable pure inference mode when evidence exists), tag_note_language={{ parameter.tag_note_language }}\n\nExecute the tasks strictly according to `SKILL.md`. Output **exactly one valid JSON object** to stdout."
    }
  }
}
