{
  "type": "object",
  "properties": {
    "existing_tags": {
      "type": "array",
      "description": "Existing controlled vocabulary entries.",
      "items": {
        "type": "object",
        "properties": {
          "tag": { "type": "string" },
          "facet": { "type": "string" },
          "note": { "type": "string" }
        },
        "required": ["tag"],
        "additionalProperties": true
      },
      "x-input-source": "inline"
    },
    "protocol": {
      "type": "object",
      "description": "TagVocab protocol constraints.",
      "properties": {
        "facets": {
          "type": "array",
          "items": { "type": "string" }
        },
        "tag_pattern": { "type": "string" },
        "max_tag_length": { "type": "number" }
      },
      "required": ["facets", "tag_pattern", "max_tag_length"],
      "additionalProperties": true,
      "x-input-source": "inline"
    }
  },
  "required": ["existing_tags", "protocol"],
  "additionalProperties": false
}
