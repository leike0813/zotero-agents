{
  "id": "debug-sequence-probe-emit",
  "name": "Debug Sequence Probe Emit",
  "description": "Debug-only sequence probe step that emits handoff markers and optionally writes a workspace sentinel.",
  "version": "0.1.0",
  "debug_only": true,
  "execution_modes": ["auto"],
  "schemas": {
    "parameter": "assets/parameter.schema.json",
    "output": "assets/output.schema.json"
  },
  "entrypoint": {
    "prompts": {
      "common": "Execute this debug-only probe exactly as described in SKILL.md. Inputs: {{ input }}. Parameters: {{ parameter }}. Use the current working directory as the run workspace. Output exactly one JSON object."
    }
  }
}
