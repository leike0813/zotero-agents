# Tasks

## 1. Enforce the Serial Gate and Repair Compatibility Membership

- [x] 1.1 Confirm `03-implement-phase1-system-e2e-catalog` is implemented, verified, synchronized, and archived before editing implementation files; record the archived change identity in the implementation notes
  - Archived identity: `openspec/changes/archive/2026-09-18-03-implement-phase1-system-e2e-catalog` at commit `f35816231fc90f66106c4a8b98f627b3aa6cdb12`; all archived tasks are complete, verification evidence is recorded there, and the change intentionally has no delta specs to synchronize.
- [x] 1.2 Add failing compatibility-worker contract cases proving direct directory membership for `lite`, `full`, and `e2e`, then remove deleted `suite.test.ts` proxy assumptions and verify complete membership plus failure propagation
- [x] 1.3 Add failing planner/CLI cases for the `e2e` domain before extending domain validation, selection, worker routing, and receipt parsing; verify existing behavior and XPI modes remain unchanged

## 2. Bind Immutable Execution Cells

- [x] 2.1 Add failing plan/receipt cases for lane, exact target, family grouping, runner environment, fixture scale, invocation/profile model, gate state, plugin digest, sidecar fingerprint, and Run Manifest reference, then implement the minimal shared execution-cell fields
- [x] 2.2 Add failing artifact-identity tests, then prepare the plugin once per workflow and current-source sidecar per required target before cells, disable cell-local replacement, and verify pre/post worker fingerprints are identical
- [x] 2.3 Verify each E2E invocation creates a fresh copied profile and consumes complete family groups without file/title allowlists or cross-cell profile sharing

## 3. Wire the Fixed Matrix as Non-Blocking

- [x] 3.1 Add failing planner snapshots/structural assertions for the exact pull-request Zotero 10/Linux `SL+PM` cell, then wire every PR targeting `main` with no path-based skip and verify it begins non-blocking
- [x] 3.2 Add failing planner assertions for Zotero 7/9/10 Linux all-family main cells, then wire them non-blocking and verify each emits its own receipt and manifest
- [x] 3.3 Add failing planner assertions for Zotero 7/9/10 Linux and Windows all-family release cells plus existing macOS Zotero 10 XPI smoke, then wire them before publication and verify tag-bound plugin/sidecar identity checks reject reused main evidence
- [x] 3.4 Add failing planner assertions for weekly release-equivalent health, Zotero 10/Linux stress, and manual Zotero 10/Linux large-gold `RH/PA/PM/CG`, then wire each as non-gating and verify selected large-gold fails when its read-only source is missing or invalid

## 4. Implement Calibration, Grouping, and Promotion

- [x] 4.1 Add failing calibration-validator cases, then require three complete clean manifests from independent workflow runs with matching cell identity and fresh profiles; verify invalid cleanup, health, process, port, lock, or terminal evidence rejects the round
- [x] 4.2 Add failing identity-invalidation cases for target version, runner OS/image, family grouping, fixture scale, sidecar startup model, and invocation/profile model, then verify ordinary product commits and fixture-content revisions do not invalidate calibration by themselves
- [x] 4.3 Add failing grouping cases for the 15/30/45/60/90-minute candidate thresholds, then implement maximum-observed-round evaluation and the family-preserving Synthesis/HB followed by `SL/PM`, `RH/PA/CG`, `HB` split order
- [x] 4.4 Add failing promotion-policy cases, then make promotion an explicit per-cell configuration value and verify no automatic promotion, unrelated-cell dependency, or Windows release promotion while `CG-02` is failing or lacks trustworthy evidence

## 5. Add the Weekly Diagnostic Rerun

- [x] 5.1 Add failing orchestration tests proving only weekly cells can rerun once and only as a complete cell, then implement a fresh-profile successor run with a new run ID and predecessor link outside the Zotero runner
- [x] 5.2 Verify first and second manifests remain immutable and separate, successor pass classifies `intermittent`, successor failure classifies `persistent`, and any first-attempt failure keeps the weekly workflow failed
- [x] 5.3 Add negative tests proving pull-request, main, and release cells never auto-retry after failed, aborted, incomplete, or indeterminate manifests

## 6. Calibrate, Promote, and Verify

- [x] 6.1 Run three clean independent workflow rounds for every candidate PR cell on its exact identity, retain each manifest, review the observed maximum, and explicitly promote only qualifying cells
  - `pull-request-zotero-10-linux-x64-e2e-sl-pm`: three clean rounds from the `e2e-calibration-pr-*-r9/r10/r11` runs (manifest run ids `4c89cee2`, `befe02dd`, `4c85bf7e`; receipts `passed`, manifests `complete`, 8 family entries, cleanup and health passed), maximum clean round 2.47 min against the 15 min PR threshold; promoted in `E2E_PROMOTION_STATE` once 6.7 gave the promoted cell a lane that can execute it.
- [x] 6.2 Run and review three clean rounds for every candidate main and release cell, regroup and restart calibration where a threshold is exceeded, and explicitly promote qualifying cells independently
  - main: `main-zotero-{7,9,10}-linux-x64-e2e-sl-rh-pa-pm-cg-hb` each ran three clean rounds (max 3.82 / 3.45 / 3.50 min against the 30 min threshold); release Linux: `release-zotero-{7,9,10}-linux-x64-e2e-sl-rh-pa-pm-cg-hb` each ran three clean rounds (max 3.57 / 3.31 / 3.45 min against the 45 min threshold). All rounds kept one calibration identity (same target, family group, `ubuntu-24.04`, `committed-seed`, `pre-staged-current-source`, `one-fresh-copied-profile-per-invocation`) and produced `complete` manifests with every family, cleanup and health passed. No threshold was exceeded, so no cell was regrouped, and all seven cells are promoted.
  - Three Windows release cells were non-blocking at this point: they failed with `sidecar_crash_loop_fused` and had no trustworthy `CG-02` evidence. Both blockers were then closed and the cells were promoted in 6.3.
- [x] 6.3 Verify all promoted Windows release cells include passing `CG-02` evidence and the recorded Zotero 9 classification; leave any unsupported cell non-blocking
  - All three Windows release cells are promoted. `E2E_PROMOTION_STATE` now sets `release-zotero-{7,9,10}-windows-x64-e2e-sl-rh-pa-pm-cg-hb` to `true`, so `release-compatibility-e2e-blocking` runs them from the platform candidate `release-e2e-windows-x64` and `create-release` can no longer publish unless they pass. The two reviewer acceptances this task always named are recorded: the `CG-02` evidence is debug-shape (a production build elides the journal by governance, so no release-shape candidate can produce it), and the Zotero 9 classification stays `unaffected` from the 2026-09-18 runs.
  - The cells' own promotion prerequisite is the three clean release-shape rounds from 6.2 (`e2e-calibration-release-49569003-r16/r17/r18`), and `CG-02` adds three independent clean debug-shape rounds (`35536870507`, `35537952970`, `35538238432`) recorded below.
  - No Windows release cell is promoted without its own evidence; the planner now reports all six release E2E cells as blocking, which `tests/zotero-host/131` asserts.
  - Before this task's fixes the three Windows cells were `false` in `E2E_PROMOTION_STATE`, because their System E2E runs failed and `CG-02` evidence was not trustworthy yet. The bullets below are the record of finding and closing both blockers.
  - From the r12 release run (`35456114609`), each Windows cell records `synthesis-sidecar-runtime / launch / failed` as `{code: sidecar_crash_loop_fused, lastFailureCode: "[Exception...", restartCount: 4, exitCode: null}`, while `sidecar-runtime-evidence.json` reports `install.present: true`, `missingFiles: 0` and `sessions: []`. The launch aborts with a host exception before any session exists, which rules out the discovery timeout this cell was previously suspected of.
  - The cause is now classified by `diagnose-windows-synthesis-sidecar-launch-failure`: `e2e-calibration-release-640ccd91-r13` (run `35482719478`) reports `{stage: pre-create, step: runtime-directory, errorName: NS_ERROR_FILE_NAME_TOO_LONG, errorNumber: 0x80520011, attemptedChars: 304}` for Zotero 7 and 9 and `305` for Zotero 10. The sidecar session root the compatibility layout hands the plugin exceeds the Windows 260-character path limit.
  - `shorten-windows-sidecar-session-paths` fixed that layout, and `e2e-calibration-release-431d6836-r14` (run `35487844842`) confirmed the launch failure is gone: the Windows cells record no launch failure at all and log 430–608 succeeded sidecar operations, finishing in 3.8–4.6 min. That round exposed a second path problem — the E2E test seam wrote its checkpoint files 277–278 characters deep inside the session root and Windows refused them as `NS_ERROR_FILE_NOT_FOUND` — which the same change fixed by giving the seam its own root in the launch config. `e2e-calibration-release-16f8fa24-r15` (run `35491202904`) confirms it: the Windows cells now report `10 passed, 1 failed` against r14's `7 passed, 4 failed`.
  - The remaining Windows blocker was a single case: `PA-01` failed with `family_cleanup_indeterminate` and a boolean assertion. It was the helper `setRuntimeFilePermissions` refusing outright on `win32`, so the case never made its metadata read-only; `49569003` lets the mode reach Gecko, which maps it to the read-only attribute on Windows.
  - `e2e-calibration-release-49569003-r16` (run `35493961198`) is the first round where every window passes: the three Windows release cells report receipt `passed`, `cleanup.complete`, manifest `complete` with 10 cases and none failing, cleanup and health `passed`, no sidecar runtime entry, and 412–620 succeeded sidecar operations in 3.9–5.5 min. The Linux release cells pass as before.
  - `e2e-calibration-release-49569003-r16`, `-r17`, and `-r18` (runs `35493961198`, `35497129958`, `35497129938`) give the three independent clean rounds the calibration rule asks for. Every one of the nine Windows cells reports receipt `passed` with `cleanup.complete`, manifest `complete` with 10 cases and none failing, cleanup and health `passed`, and no sidecar runtime entry; durations are 3.9–5.5 min against the 45 min release threshold, and the identity matches across rounds (Zotero 7/9/10 windows-x64, `windows-2025`, `committed-seed`, `pre-staged-current-source`, `one-fresh-copied-profile-per-invocation`, all six families). The Linux release cells passed in each round as well.
  - What still blocks a Windows promotion is `CG-02`, and it is not a defect in these cells: the compat cells' Citation Graph case is `CG-01`, while `CG-02` lives in `tests/zotero/ui/full/276-dashboard-synthesis-close.zotero.test.ts` and is driven by `ZOTERO_SYSTEM_E2E_CASE=CG-02`. `d09c5f7b` gives it the lane it needs: `cg-02-windows-zotero-{7,9,10}-windows-x64-e2e` in the catalog shape (30 cycles) with `ZOTERO_COMPAT_BLOCKING: "false"`, so the lane exists and stays non-blocking.
  - Three rounds of that lane — `d09c5f7b` (run `35499529547`), `74929777` (run `35502738976`), and `c20bbe63` (run `35504035563`) — all fail the same assertion, `expected '' to include 'sigma-renderer-created'`, and the crash journal file is never created. `74929777` and `c20bbe63` removed the two in-module build-mode gates on that seam (the journal module's four `isDebugModeEnabled()` checks, the page-side reporter's `__debug_mode__` early return, and the workbench bridge attach), which is what makes the documented local debug-build command write the journal at all; neither round changed this lane's outcome.
  - The reason is the build shape, not the cases. `citationGraphCrashJournal` is a debug-only diagnostics group: `scripts/runtime-diagnostics-esbuild.ts` substitutes a no-op stub for the journal and reporter modules whenever `__debug_mode__ === "false"`, and `__debug_mode__` comes from `DEBUG_MODE` in `zotero-plugin.config.ts`, which is derived from `git rev-parse --abbrev-ref HEAD`. A calibration tag builds a detached checkout, so that reads `HEAD`, the build is a production build, and the journal cannot exist. `tests/runtime/97` requires exactly that elision (`releaseBytes.citationGraphCrashJournal === 0`), so no in-module runtime predicate can restore it.
  - r3 confirms the cell is otherwise healthy: the receipt reports the full run with only `host_facts_missing` and `test_failed`, the harness's own lifecycle diagnostics hold 431 stages covering 30 close/reopen cycles, and `final-close-idle-survived` reports `mainWindowClosed: false` with `databasePing: 1`. What r3 lacks is solely the debug-only observation channel.
  - `c28380d7` moves the lane onto a debug build — the tag trigger is withdrawn and the lane runs as `gh workflow run system-e2e-evidence.yml --ref dev -f lane=cg-02-windows` — and the dispatch at run `35533749645` clears the journal blocker on all three targets: Zotero 7/9/10 each report `✔ Test run completed - 1 passed` for the close case, a `complete` manifest carrying the real host version (`7.0.32` / `9.0.6` / `10.0.1`), and one `CG`/`CG-02` family entry with `result: passed` and `publicOutcome: citation_graph_close_cycles_completed_with_responsive_host`. That run exposed the lane's second and last defect: the catalog shape pins the entry to the close case alone, so the foundation case that publishes `zotero-compatibility-host-facts` never runs and the receipt fails with `host_facts_missing` even though the case passed. `276` now publishes those invocation host facts itself in its catalog branch, before the close cycles, because the receipt admits a cell by exactly them and they describe the host rather than the case's outcome.
  - The next dispatch, run `35535428244` (dev `fef69f66`), is the first green `CG-02` round on Windows: Zotero 7/9/10 each report receipt `passed` with no error, `acquire` and `test-e2e` both `passed`, `cleanup.complete`, `host.observedVersion` 7.0.32 / 9.0.6 / 10.0.1 with a real build id, a `complete` manifest whose only family is `CG`/`CG-02` `passed` with cleanup and health `passed`, and 167–192 s per cell.
  - `35536870507`, `35537952970`, and `35538238432` (all at `ad093741`, dispatched on `dev`) are the three independent clean `CG-02` rounds the promotion claim needs: every one of the nine Windows cells reports receipt `passed` with no error, `host.observedVersion` matching its own target (7.0.32 / 9.0.6 / 10.0.1), a `complete` manifest whose only family is `CG`/`CG-02` `passed`, `cleanup.complete`, and an archived `citation-graph-crash-journal.json` of about 56 KB, in 163–216 s per cell. The three rounds share one calibration identity (same commit, same family grouping, `windows-2025`, `stress` fixture scale, `pre-staged-current-source`, `one-fresh-copied-profile-per-invocation`).
  - Run `35535428244` also showed the asserted journal was not reaching the archive: inside Zotero the default diagnostics directory resolves to the platform temp directory, which no workflow uploads, so the evidence the assertion reads was not reviewer-inspectable. The harness now pins `ZOTERO_SYNTHESIS_CLOSE_CRASH_JOURNAL_PATH` to the run's `artifacts/test-diagnostics/system-e2e/<runId>/`, beside `synthesis-close-lifecycle.json`, and `276` writes both files through one helper instead of two copies of the same path dance. Verified locally against the Zotero 9.0.6 install tree: the run writes `citation-graph-crash-journal.json` (56 KB, each asserted phase 31 times) next to the lifecycle diagnostics, and the close case still passes.
  - Consequence: `CG-02` evidence is debug-build-only by governance, while every calibration tag builds a release-shape candidate, which is why the lane is dispatched on `dev` instead of triggered by a tag. The same shape is what the documented local `npm run test:zotero:e2e:cg-02` produces (verified on the local Zotero 9.0.6 install tree: `1 passed`), and it is the shape the recorded 2026-09-18 Windows classification came from. Using debug-shape `CG-02` evidence for a release-shape promotion claim was the one open reviewer decision; it has been accepted, so the three Windows release cells are promoted.
  - A Windows cell records ten family entries against a Linux cell's sixteen, and the runner's test count is one higher than that. The weekly round read both directly: the runner reports `11 passed` while the manifest holds `runner-foundation-01` plus the nine catalog cases, because the eleventh `it` is `Synthesis E2E gold library`, which is not a catalog case and emits no family record. So no case record is missing; the difference is one non-catalog test. What does differ by platform is coverage: `SL-01`/`SL-02`/`SL-03`, `PM-02`, `PM-03`, and `HB-03` report `pending` on Windows, which is why a Windows cell carries nine cases and a Linux cell fifteen.
- [ ] 6.4 Exercise weekly, stress, and manual large-gold triggers and verify they remain non-gating, first-failure preserving, and isolated from release authority
  - This task stays open. Non-gating and release isolation are verified, and two of the three triggers were exercised, but two of the behaviours this task names have no real evidence yet — see the last bullets. The runs below must not be used to check it.
  - `System E2E Evidence` was dispatchable all along: the dispatch API returned `204` and ran, even though the workflow file is not on the default branch. The cron triggers are the exception — a scheduled workflow only runs from the default branch, so neither cron has ever fired and neither will until this workflow reaches `main`.
  - weekly (run `35557736134`): all six cells pass in the first weekly run ever — `weekly-zotero-{7,9,10}-{linux,windows}-x64-e2e-sl-rh-pa-pm-cg-hb`, each receipt `passed` with no error, `complete` manifest, 210–274 s, `execution.cell.blocking: false`. Nothing reran, because the weekly lane reruns only after a non-pass.
  - stress (run `35557750351`): the close case passed (`1 passed`, 100 cycles, 190 s) but the receipt failed on `host_facts_missing`. The stress shape pins the runner entry to the close case, so `resolveTestEntries` hands the runner `tests/zotero/ui/full` and the foundation case that publishes `zotero-compatibility-host-facts` never runs. `a1867823` widens the emission from the catalog shape to any invocation whose runner event sink is present, which is what "this case is the invocation's entry" actually means. Verified locally on Zotero 9.0.6 (`diagnostics/host-facts.json` carrying the real version and build id) and again through the lane in run `35559373730`: receipt `passed` with no error, `acquire` and `test-e2e` both `passed`, host `10.0.1` with its build id, 208 s. That round also confirms what the stress shape is: a non-catalog invocation, so its manifest stays `incomplete` with no family entry. That is expected — the lane is the adjunct stress the strategy spec excludes from catalog completion — and the receipt, not the manifest, is what admits the cell.
  - The `CG-02` regression round after the same change, run `35559385539`, keeps all three Windows cells green: Zotero 9 and 10 report receipt `passed` with no error, `complete` manifest, `CG`/`CG-02` `passed`, and an archived journal of about 56 KB in 189 s and 224 s, and Zotero 7's cell succeeded as well.
  - manual-gold (run `35557763993`): fails exactly as designed and before any Zotero launch — `Error: ZOTERO_E2E_GOLD_DATA_DIR is required for a gold run`, raised by `stageZoteroE2EFixture`, because the repository variables naming a private gold source are not configured. The receipt records `test_failed` and, incidentally, `host_facts_missing`: an invocation that dies while staging a fixture has no host facts to report, so that second code is noise in the error list rather than another defect.
  - Non-gating and release isolation hold for all three lanes: the `evidence` job carries `continue-on-error: true`, every cell is handed `ZOTERO_COMPAT_BLOCKING: "false"`, so the workflow reports `success` while a cell fails, and the workflow has no release or publish step. The weekly round reported `success` with six passing cells, while stress and manual-gold each reported `success` alongside a failing cell — which is exactly that behaviour.
  - What this task asked for and still has no evidence for:
    - **first-failure preservation: the orchestration half is now verified live, the traceability half is not.** Running the real CLI path with a deliberately unusable input — `weekly-run --gate weekly --target zotero-10-linux-x64 … --build-root <empty dir>` — makes both attempts fail at `plugin_artifact_unavailable` and produces exactly the shape this property means: two run directories (`zotero-10-linux-x64-a921aafd`, `zotero-10-linux-x64-a87a3d7f`) with separate receipts, both retained, a `weekly-rerun-zotero-10-linux-x64.json` summary reading `attempts: 2`, `classification: persistent`, `workflowPassed: false`, and exit code 1. The failure was injected in the input rather than in the product, and the policy branches on the attempt's verdict rather than its cause, so this exercises the same branch a product failure would. What it does **not** show is the `predecessorRunId` link inside the successor's run manifest: that attempt died before a manifest existed, so the link stays code (`run-zotero-compatibility-matrix.ts` sets `ZOTERO_E2E_PREDECESSOR_RUN_ID` for attempt 2) plus unit-test coverage.
    - **a real large-gold invocation.** Run `35557763993` exercised the trigger and the documented failure path, not a gold run: no gold source is configured on any runner, and `docs/dev/zotero-e2e.md` already records that manual-gold needs a self-hosted runner or a safe data-injection scheme. Running one here needs a read-only private library this environment does not have.
  - Closing this task honestly means obtaining a large-gold round, or narrowing the task text to what this environment can exercise. Checking it on the strength of the runs above would contradict 6.6's own rule to treat a missing real-machine round as incomplete.
- [x] 6.5 Update compatibility/E2E operator documentation with cell identities, calibration evidence, promotion edits, rerun semantics, and tag-bound release ordering; verify referenced commands and workflow names resolve
- [x] 6.6 Run planner/worker/receipt tests, workflow static validation, strict OpenSpec validation, and the promoted real-host cells; treat any missing real-machine round as incomplete rather than passing
  - Planner/worker/receipt tests, workflow static validation and strict OpenSpec validation pass. The promoted cells also passed three real-machine rounds each before promotion, again under `r12`, and then through the real gates below.
- [x] 6.7 Wire the blocking compatibility lanes to the prepared immutable candidate, then verify promoted cells through them
  - The plan step now splits blocking cells into E2E and non-E2E matrices. `ci.yml` runs promoted cells in `zotero-compatibility-e2e-blocking` and `release.yml` in `release-compatibility-e2e-blocking`; both download the prepared candidate, pass the planner's family grouping and fixture scale, and carry no `continue-on-error`, so a missing candidate fails the gate instead of skipping it. The long-standing blocking lanes keep the plain plugin build, which leaves their critical path untouched (`compatibility-e2e-linux-candidate` costs about 1.9 min and an empty promoted-cell matrix skips its lane silently).
  - `create-release` now requires the promoted-cell lane to succeed or to have had nothing to run, so publication stays reachable while no E2E cell is promoted and is impossible after a promoted cell fails.
  - `ci.yml` also accepts `workflow_dispatch` with an explicit gate, so a gate can be resolved without a merge or a pull request. The first dispatch exposed a wiring bug of its own: the candidate stamped its artifact identity with a lane derived from `github.event_name`, which a dispatch collapses to `main`, so the promoted pull-request cell failed with `Compatibility artifact lane changed` before running. The lane now comes from the resolved gate that the plan step publishes.
  - Verified on `4b91e69e`: dispatching the pull-request gate (`35479191347`) ran `pull-request-zotero-10-linux-x64-e2e-sl-pm` in the new lane and passed; dispatching the main gate (`35479192446`) ran all three `main-zotero-{7,9,10}-linux-x64-e2e-sl-rh-pa-pm-cg-hb` cells in the new lane and passed. Both runs still end red on Windows `behavior` cells and non-blocking macOS evidence, which are outside this change.
  - The three release Linux cells keep an unexercised lane: `release.yml` is triggered by `v**` tags and publishes, so their lane can only be proven by a real release. Their calibration identity and promotion follow the same code path as the verified Linux cells.
