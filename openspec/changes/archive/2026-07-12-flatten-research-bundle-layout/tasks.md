## 1. Product contract and regression coverage

- [x] 1.1 Extend focused materialization tests for v2 shallow paths, manifest integrity, image-link relocation, and README navigation/index data.
- [x] 1.2 Add focused locale-selection tests for supported output and English fallback.

## 2. Runtime and Product implementation

- [x] 2.1 Add normalized workflow locale to runtime contexts used by hooks and selection validation.
- [x] 2.2 Add the package-local localized README renderer and include it in the built-in workflow manifest.
- [x] 2.3 Materialize the v2 shallow layout, relocate Markdown images locally, and emit manifest schema version `2.0.0`.

## 3. Documentation and verification

- [x] 3.1 Update workflow and main OpenSpec documentation for the v2 Product contract.
- [x] 3.2 Run focused tests and relevant package, type, formatting, lint, and OpenSpec validation checks.
