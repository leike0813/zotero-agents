## 1. Regression Tests

- [x] 1.1 Cover interaction-only WebGL edges, first-render projection, and no-op filter parity
- [x] 1.2 Cover SVG fallback default-node, hover-only neighborhood, and at-rest edge behavior
- [x] 1.3 Cover deep-reading envelope visible and hover-only transport partitions

## 2. Projection and Rendering

- [x] 2.1 Route initial standalone WebGL loading, layout selection, and filtering through one shared projection path
- [x] 2.2 Restore interaction-only Sigma edge rendering while preserving hover reconciliation across page merges
- [x] 2.3 Apply the shared projection and dynamic hover-only neighborhoods in the SVG fallback
- [x] 2.4 Partition the Python export envelope without duplicating filter-sensitive projection rules

## 3. Documentation and Materialization

- [x] 3.1 Synchronize the main specifications and Workbench documentation with interaction-only edge semantics
- [x] 3.2 Regenerate literature-deep-reading renderers and keep source and built-in materializations identical

## 4. Validation

- [x] 4.1 Run focused projection, UI-model, runtime envelope, WebGL, and SVG fallback tests
- [x] 4.2 Run TypeScript, targeted lint and format, materialization identity, diff, and strict OpenSpec checks

## 5. Independent Selection and Pointer Hover

- [x] 5.1 Cover selected-plus-hovered neighborhood union, pointer title rendering, leave cleanup, and same-query page preservation
- [x] 5.2 Separate pointer hover from snapshot selection and reconcile dynamic neighborhoods from their owner union
- [x] 5.3 Keep halo drawing and pointer-hover title drawing active together
- [x] 5.4 Synchronize the main specification, Workbench documentation, and generated renderers with union semantics
- [x] 5.5 Run focused behavior tests and all targeted validation gates

## 6. Residual Graph Interaction Regressions

- [x] 6.1 Cover parallel-edge aggregation, bounded deterministic neighborhoods, dual incoming counts, and real pointer picking
- [x] 6.2 Aggregate visual edges and place interaction-only neighbors without obscuring their owners
- [x] 6.3 Omit absent slice filters, preserve coordinates, and keep continuation-page ownership stable

## 7. Layout Coordination

- [x] 7.1 Cover singleflight layout mutation, frame-source isolation, latest-wins progress, and truthful banner state
- [x] 7.2 Move layout mutation ownership to the host and make busy contention converge without stale Running state

## 8. Synchronization and Validation

- [x] 8.1 Synchronize main specs, Workbench documentation, translations, and generated renderer materializations
- [x] 8.2 Run focused tests, TypeScript, lint/format, materialization identity, diff, and strict OpenSpec checks
