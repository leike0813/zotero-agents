# Tasks

- [x] Add OpenSpec artifacts for result resolution split.
- [x] Add `WorkflowResultContext` and shared result/artifact resolver.
- [x] Pass `resultContext` through `applySeam` and `executeApplyResult`.
- [x] Stop ACP SkillRunner-compatible success path from depending on bundle projection.
- [x] Migrate `literature-digest` apply hook to prefer `resultContext`.
- [x] Migrate `literature-explainer` apply hook to prefer `resultContext`.
- [x] Keep directory-oriented workflows such as `mineru` on `BundleReader.getExtractedDir()`.
- [x] Add resolver and workflow regression tests.
- [x] Run OpenSpec validation, targeted tests, and TypeScript check.
