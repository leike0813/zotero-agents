## 1. Contract Tests

- [x] 1.1 Extend Host Bridge packaging tests with the new bundle structure, independent-surface boundary, version manifest, release workflow, and checksum expectations.
- [x] 1.2 Add focused tests for evidence schema validation, artifact hashing, sensitive-field rejection, and stateless workflow bundle inspection.

## 2. Agent-Neutral Skill Surface

- [x] 2.1 Initialize and author the `zotero-library-agent` semantic Skill with progressive references and agent UI metadata.
- [x] 2.2 Add the shared Host Bridge control-invariants source and route the wrapper, general Skill, and Zotero Librarian profile to rendered copies without sharing task policy.
- [x] 2.3 Add the evidence schema, examples, and Python standard-library helper with explicit stateless subcommands.

## 3. Rendering And Governance

- [x] 3.1 Add the Zotero Library Agent renderer and integrate it with Host Bridge surface render/check commands.
- [x] 3.2 Add bundle version inspection/bump logic and initialize the current CLI release line at bundle version `0.2.0`.
- [x] 3.3 Add structural, semantic-boundary, schema, helper, generated-drift, and prebuild checksum checks.
- [x] 3.4 Extend semantic review, doc-sync, and release coordinator governance for the new source and generated surface.

## 4. Distribution And Release

- [x] 4.1 Add the standalone bundle publisher and manifest for `leike0813/zotero-library-agent-bundle`.
- [x] 4.2 Extend CLI-build and surface-only GitHub workflows to validate and publish all three Host Bridge agent-facing surfaces.
- [x] 4.3 Update the Host Bridge semantic-review and release-pipeline skills with the new current-state workflow and reporting fields.

## 5. Verification

- [x] 5.1 Run semantic review, render/check commands, focused tests, helper validation through `$HOME/.ar`, prebuild freshness, and lint; resolve all in-scope failures.
- [x] 5.2 Validate the OpenSpec change and mark all implementation tasks complete.
