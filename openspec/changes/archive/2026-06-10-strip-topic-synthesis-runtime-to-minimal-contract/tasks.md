# Tasks

- [x] Add delta specs for the minimal runtime, structured artifact, and skill instruction contracts.
- [x] Strip split runtime audit/hash/registry public behavior and remove `--action audit`.
- [x] Remove digest hash requirements from split runtime artifacts and Host apply validation.
- [x] Update renderer, guidance, schemas, and generated skill packages so instructions no longer mention audit/hash/registry/receipt contracts.
- [x] Update focused tests for runtime, renderer/schema, and apply/storage behavior.
- [x] Run focused mocha tests, `tsc --noEmit`, and strict OpenSpec validation.
