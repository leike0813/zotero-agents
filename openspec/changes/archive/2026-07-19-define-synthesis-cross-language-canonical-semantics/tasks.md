## 1. Freeze the Node oracle

- [x] 1.1 Inventory every current v1 OS-process and service/worker boundary DTO and map it to its existing strict TypeScript rebuilder.
- [x] 1.2 Freeze representative durable/canonical bytes and hashes plus positive and negative raw-input behavior before changing canonical ownership.

## 2. Add failing governance coverage

- [x] 2.1 Repair Core 193 to parse the workflow and verify the macOS code-signature gate semantically without requiring `spctl --assess`.
- [x] 2.2 Add Core 218 coverage for strict schema compilation, manifest integrity, corpus conformance, canonical edge cases, dependency direction, and shared schema version.

## 3. Establish the contract set

- [x] 3.1 Add Draft 2020-12 schemas for system/lifecycle, runtime ownership/discovery, service/worker compute and transfer DTOs, canonical inspect, Workbench chrome, and Node bundle/pointer v1.
- [x] 3.2 Add versioned positive/negative corpus documents covering Unicode, key ordering, safe integers, floats, negative zero, unknown fields, malformed JSON, and bounds.
- [x] 3.3 Add the versioned manifest, capability-to-definition map, strict Ajv checker, and computed whole-set fingerprint.

## 4. Consolidate canonical and schema-version ownership

- [x] 4.1 Move canonical JSON bytes/hash implementation to contracts with non-locale UTF-16 key ordering and strict Unicode/value validation; retain the engine path as a re-export.
- [x] 4.2 Move the repository foundation schema version to contracts, retain the repository export, and remove the contracts-to-repository import and boundary allowlist.

## 5. Complete conformance and Stage 1 governance

- [x] 5.1 Make every positive corpus case pass through its existing rebuilder and match canonical JSON/hash; make every negative case return its stable error code.
- [x] 5.2 Extend the blocking Stage 1 suite to Core 175-218 and shard counts `[27, 1, 16]`.
- [x] 5.3 Verify the service boundary remains `108 public methods / 1 direct consumer`, contracts violations remain zero, and emitted service imports contain no Ajv/schema runtime dependency.

## 6. Documentation and final validation

- [x] 6.1 Update the Rust migration plan and Synthesis status documentation: R1 complete, no Rust executable yet, Metrics vertical slice next.
- [x] 6.2 Reuse canonical UTF-8 artifacts across Citation Graph Build transfer pagination, worker frames, strict owner validation, and atomic staging without changing v1 bytes/hash or the 30-second Core 202 deadline.
- [x] 6.3 Add canonical-artifact parity and transfer-frame tamper/round-trip coverage, then make Core 202 pass repeatedly under its existing normal profile and worker resource limits.
- [x] 6.4 Run the contract checker; Core 183, 185, 186, 192, 193, 195, 196, 201, and 218; package TypeScript checks; service build/import inspection; full Stage 1; targeted ESLint/Prettier; `git diff --check`; and strict OpenSpec validation.
