# Tasks

- [x] Update OpenSpec delta specs for runtime report, structured artifact storage,
      synthesis apply, and Topic Details UI.
- [x] Rewrite the split runtime `synthesis_report.body` generator to use the final
      Chinese template and stable source-paper numbering.
- [x] Update Stage 40 schema/guidance examples so report-required fields are
      available at finalize time, then re-render generated split skill packages.
- [x] Remove Host apply/storage generation and hashing of `current/export.md`.
- [x] Remove topic detail DTO fields and Workbench host commands tied to the old
      Markdown export reader.
- [x] Update Topic Details Report UI with Copy and Host save-path Export
      actions.
- [x] Update focused runtime, apply/storage, renderer, and UI tests.
- [x] Run focused tests, TypeScript check, OpenSpec validation, and formatting
      check for changed files.
