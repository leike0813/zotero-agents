## 1. Benchmark contracts and failing tests

- [x] 1.1 Add Core 200 coverage for deterministic profiles, exact request/result envelope classification, small real HTTP/worker parity, and unchanged production ownership.
- [x] 1.2 Add one graph-build benchmark fixture SSOT and migrate Core 150's representative request construction to it.
- [x] 1.3 Extend focused boundary/invariant checks only where needed to prove no runtime capability, public client, method inventory, or authority drift.

## 2. Benchmark harness and command

- [x] 2.1 Implement deterministic byte/JSON-node measurement, wire classification, direct compute, strict rebuild, worker, HTTP, responsiveness, and cancellation phase records.
- [x] 2.2 Add the explicit CLI with isolated normal/target/stress profile execution, bounded child cleanup, and structured resource-failure outcomes.
- [x] 2.3 Add the package benchmark command without adding it to ordinary CI, test, build, runtime packaging, or release publication.

## 3. Baseline capture and governance

- [x] 3.1 Capture a dated report with runtime provenance, commands, measurements, classifications, limitations, and the large-transfer prerequisite.
- [x] 3.2 Update Synthesis performance, runtime, status, root README, and Stage 1 progress docs without claiming production graph-build routing or satisfied target/stress budgets.
- [x] 3.3 Preserve three worker operations, two production routes, `108 methods / 1 direct consumer`, and `mutationEnabled: false`.

## 4. Verification

- [x] 4.1 Run Core 150, 195, 196, 199, and 200 plus contracts/engine/service/root TypeScript checks.
- [x] 4.2 Run service boundary, Synthesis invariants, targeted Prettier/ESLint, help-doc checks, production build, and `git diff --check`.
- [x] 4.3 Run strict OpenSpec validation and complete all tasks without archiving, committing, publishing prebuilds, or touching `reference/Skill-Runner`.
