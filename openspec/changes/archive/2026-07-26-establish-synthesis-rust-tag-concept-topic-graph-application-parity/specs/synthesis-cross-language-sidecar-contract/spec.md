## ADDED Requirements

### Requirement: Tag Concept and Topic Graph typed parity evidence SHALL be versioned
The sidecar contract inventory SHALL include an immutable corpus, development-only Rust driver, Node checker, stable report identity, and complete observation comparison for the three-domain cluster.

#### Scenario: Candidate evidence is audited
- **WHEN** the corpus, driver, checker, workflow, or report contract changes
- **THEN** Core contract tests and every candidate target evaluate the same versioned evidence before smoke
