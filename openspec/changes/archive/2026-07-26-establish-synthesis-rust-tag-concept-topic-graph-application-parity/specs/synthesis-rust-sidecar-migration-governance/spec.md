## ADDED Requirements

### Requirement: R7 knowledge-index typed parity SHALL be separately evidenced
Migration governance SHALL accept Tag Vocabulary, Concept KB, and Topic Graph only when their typed Node/Rust differential passes independently and SHALL keep R8 blocked until Checkpoint, Bundle, WebDAV, and Debug/Maintenance receive equivalent evidence.

#### Scenario: Migration status is reviewed after this cluster
- **WHEN** all three domains pass their local and five-target candidate gates
- **THEN** governance records the knowledge-index cluster as complete
- **AND** does not authorize native manifest v2 or production cutover
