## 1. Contract and baseline

- [x] 1.1 Create and strictly validate proposal/design/delta specs; record fixed baseline, per-domain read contract, caller/result inventory and DEL-01/02/05/16 ownership.
- [x] 1.2 Record affected materialized instruction metrics and explicit semantic replacement units before guidance edits; verify baseline preservation map.

## 2. Broker read slices

- [x] 2.1 Extend existing source-query/Broker seam tests red-before-green for collection, note, attachment and annotation keyset pages; implement page-only hydration, typed failures and remove full-array fallbacks.
- [x] 2.2 Implement Saved Search source pages and portable refs; verify 25/100 bounds, ordering, cursor rejection and duplicate display-name identities.
- [x] 2.3 Implement bounded payload candidate scanning, unknown totals, content/basis limits and complete ambiguity detection; verify empty nonterminal pages, oversized sources and whole-page failure.
- [x] 2.4 Implement process-wide FIFO native slices and trusted cancellation; verify cross-instance serialization, canceled waiters, settle-before-release and external work outside admission.
- [x] 2.5 Adapt readiness, annotation/export, traversal, snapshot and metadata paths to bounded slices; verify complete evidence, unchanged public snapshot limits, cancellation and translator late-result suppression.

## 3. Projections and consumers

- [x] 3.1 Migrate Bridge ordinary reads to complete canonical Broker resolution/control, remove repagination and fallbacks, and project current attachment pages; verify 107/108 and file locality suites.
- [x] 3.2 Replace MCP tool queue with nine inflight admissions and cancellation-aware watchdog; verify 101/105, status bypass, structured tenth-call rejection and retained timed-out handlers.
- [x] 3.3 Update explicit Workflow V12 types/projections/manifest and complete fail-closed Broker harness for read pages and Saved Searches; verify 23/21/88 conformance and scoped controls.
- [x] 3.4 Migrate workflow parameter, research bundle, ACP/Synthesis and built-in hook direct/result consumers; verify later-page data, empty-page continuation, complete task behavior and mid-read failure.
- [x] 3.5 Update executable capability/CLI contracts, Saved Search CLI command and ordinary read schemas; verify CLI schema_mode and cargo tests without compatibility aliases.
- [x] 3.6 Audit remaining legacy symbols/callers and close this change's deletion inventory; verify only assigned selection/navigation/write consumers remain.

## 4. Documentation, governance and acceptance

- [x] 4.1 Update Broker/MCP/Workflow architecture documents and AGENTS constraints, including ordinary-cursor TTL drift; verify changed behavior matches documentation.
- [x] 4.2 Review governed semantic sources, render content and refresh official Chinese review mirror; verify content/mirror gates, relative thickness and zero unmapped/downgraded/unauthorized-dropped/intra-package-duplicate counts.
- [x] 4.3 Run relevant TypeScript, lint, build, Node/Workflow/UI, CLI and Zotero compatibility checks; record exact commands, results and any uncovered environment boundaries without claiming unrun PASS.
- [x] 4.4 Verify implementation against this change with the official skill, synchronize only its delta requirements and archive only when all required work and evidence are complete.
