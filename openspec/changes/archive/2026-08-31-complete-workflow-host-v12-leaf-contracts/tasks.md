# Tasks: complete-workflow-host-v12-leaf-contracts

依据：specs（`specs/workflow-host-api-v12/spec.md`、`specs/zotero-host-broker-capability-api/spec.md`）定 WHAT，`design.md` 定 HOW。四个切片按 M1 → M4 TDD 推进：每切片先写/改契约测试（红），再实现（绿），最后迁移 consumer。所有验证命令中的测试文件若不存在则在该任务中新建。

## 1. M1 metadata.translateIdentifier（ADR §12.0）

- [x] 1.1 在 broker 套件中新增 closed input 契约测试：提交 `normalized` alias、开放 `type`、额外 resource knob、`value` 超 2,048 字符、ISBN checksum 失败均返回 stable `invalid_request` 且不调用 translator。验证（应红）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 1.2 新增三态 outcome 与 exact-match 测试：仅靠后 candidate exact-match 时返回 `matched` 且 evidence 计数完整；两个 match 返回 `ambiguous` 且不暗选 preferred；有 candidate 但无 match 返回 `not_found`/`identifier_mismatch`；无 candidate 返回 `no_candidate`；无 translator 返回 `no_translator`；三分支 evidence 字段同形同必填。验证（应红）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 1.3 新增 hard budget 与 creators 测试：translators &gt; 32、candidates &gt; 64、序列化结果 &gt; 4 MiB 整体 `resource_limited` 且 details 只含受控 `resource/limit/observed`；creator 数超过共享预算（100/item）整体 `resource_limited` 而非截断。验证（应红）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 1.4 新增异常 taxonomy 测试：translator 抛错映射为 stable `execution_failed`（非 `not_found`、非 `ok:false` 正常返回）；`Translate.Search` 缺失映射为 `unavailable`；空 translator list 不伪装 runtime unavailable。验证（应红）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 1.5 改公开类型：`src/workflows/types.ts` 的 `MetadataLookupRequestDto` 固定为 `{type, value}` closed union，`MetadataLookupResultDto` 改为 ADR §12.0 三态判别联合（含 `MetadataTranslationEvidenceDto`），删除 v11 alias `ZoteroHostMetadataTranslateIdentifierResponse` 的公开引用。验证：`npx tsc --noEmit`（允许 1.7 前 consumer 报错，记录待迁移清单）
- [x] 1.6 实现 broker 重写（`src/modules/zoteroHostCapabilityBroker.ts` 的 `translateMetadataIdentifier` 与 creators serializer）：closed 输入校验、per-type normalization table（单一事实源）、序列化后 canonical DTO 上的 exact-match selection（禁 `itemList[0]`）、三态 outcome、统一 evidence、hard budgets、translator 异常进 stable taxonomy、creators 复用 §12.1 唯一 serializer 且不截断。验证（转绿）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 1.7 迁移 consumer：literature metadata curator 删除直接访问 `runtime.zotero.Translate.Search` 的 fallback 与重复的 translator summary normalization，改为消费三态 outcome。验证：`npx tsx node_modules/mocha/bin/mocha "test/workflow-literature-metadata-curator/*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit` 且 `npx tsc --noEmit` 全绿

## 2. M2 attachments.replaceFile（ADR §11.14）

- [x] 2.1 新增 stored 替换契约测试：合法 main + companions 原子切换成功，receipt 记录 before/after revision 与 `outcome: "replaced"`，commit 后清理旧 managed content；companion path-traversal/duplicate-target/超限在 Zotero state 变更前失败；switch 失败保留原文件与原主错误；旧 content 清理无法确认时 outcome 为 `repair_required` 或 `unknown` 且残留进 `residualRefs`。验证（应红）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 2.2 新增 linked relocation 与共同语义测试：新 path 不存在/非 regular readable 时 link 不更新；relocation 只改 Zotero link、绝不复制/修改/删除外部文件；canonical path 相同或 content hash + 完整 companion set 相同返回 `unchanged`；stored/linked source 与目标 link mode 不匹配返回 `invalid_request` 且不转换 mode；linked-URL 与 note image/payload attachment 被拒绝；同 operationId replay 返回原 receipt 且不重复 staging/swap/cleanup。验证（应红）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 2.3 实现生产 `replaceFile` primitive 并接线：在 `ZoteroHostAttachmentMutationPrimitives.replaceFile` 槽位提供真实实现（stored：复用 `attachments.create` 的校验与 managed staging，同目录 rename 为 commit point；linked：canonicalize + link-only 更新；filesystem 一律 `runtimePersistence` 晚绑定；filename/MIME 从实际 source 重识别；结果 DTO `{attachment, outcome}`），删除恒 `unsupported_operation` 路径。验证（转绿）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit` 且 `npx tsc --noEmit` 全绿

## 3. M3 notes.upsertPayload（ADR §11.7/§11.1/§11.2）

- [x] 3.1 新增幂等与 result DTO 契约测试：logical content + schema identity hash 相同返回 `outcome: "unchanged"` 且不重写 note/attachment；无该 payloadType 返回 `created`；identity 不同返回 `replaced`；result 形状恰为 `{note, payload, outcome}`（`note` 为 NoteSummaryDto、`payload` 为 NotePayloadSummaryDto）。验证（应红）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 3.2 新增冲突与输入契约测试：同 note 多个同 payloadType payload 时抛 stable `conflict` + details reason `ambiguous_state` 且无任何修改；提交 `encoding`/`source`/`attachmentRef`/storage preference 返回 `invalid_request`；`markdown`/`text` 非 string value 与 decoded &gt; 1 MiB 被拒绝。验证（应红）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 3.3 新增补偿与 replay 测试：新 attachment 已建而 note 更新失败时补偿删除、原失败为主错误、删除失败时孤儿 attachment 进 `residualRefs`；note 已更新而旧 attachment 清理无法确认时 outcome 为 `repair_required`/`unknown`；同 operationId replay 返回原结果且不重复创建 attachment。验证（应红）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 3.4 改公开类型：`NotePayloadUpsertRequestDto` 改为 `{operationId, noteRef, expectedRevision?, payload: LogicalNotePayloadDto}`（含 `schemaVersion` 与 `format: "json" | "markdown" | "text"`），`NotePayloadUpsertResultDto` 改为 `{note, payload, outcome}`。验证：`npx tsc --noEmit`（允许 3.6 前 consumer 报错，记录待迁移清单）
- [x] 3.5 实现：删除 `payloadFormat: "json"` 硬编码；canonical payload hash 单一事实源并用于 unchanged 短路（对 stored payload 重算，不信任缓存 hash）；同 payloadType 计数在 reservation 前判定（0 创建 / 1 替换 / ≥2 conflict）；note 更新失败补偿删除新 attachment；result DTO 对齐；OQ1（schemaVersion 持久化位置）在本任务定夺并记录于实现注释。验证（转绿）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 3.6 迁移 v11 legacy mutations adapter 与 Host Bridge note payload 复用路径对新 result 形状的投影（保持各自 wire 形状不变），并迁移 `workflowHostOwners.ts` 中 `notes.upsertPayload` 调用点到新 request DTO。验证：`npx tsx node_modules/mocha/bin/mocha "test/core/194-*.test.ts" "test/workflow-literature-workbench-package/*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit` 且 `npx tsc --noEmit` 全绿

## 4. M4 notes.create（ADR §11.4）

- [x] 4.1 新增 placement union 契约测试：`top_level` 缺省 libraryId 落 user library、`collectionRefs` 全部同库 active 时一次 receipt 完成放置；`child` 由 parent 决定 library 且拒绝任何 library/collections 输入；missing/trashed/跨库 collection 在 commit 前失败；扁平 `parentRef` 混排与 standalone `title` 返回 `invalid_request`。验证（应红）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 4.2 新增 initialTags 与幂等测试：`initialTags` 与 note 在同一 operation 提交（单一 receipt，无可观察的未打 tag 中间态）；同 operationId 安全重试返回原 receipt 且不产生第二个 note；空内容与 &gt; 50,000 字符在 commit 前失败。验证（应红）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 4.3 改类型并实现：`NoteCreateRequestDto` 改为 placement 判别联合 + `initialTags?`；broker 在 admission/validation 完成 placement/collection/content 校验，tag 在 note 的同一次 Zotero save 中应用。验证（转绿）：`npx tsx node_modules/mocha/bin/mocha "test/core/102-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit`
- [x] 4.4 迁移 research import 编排：删除 `src/workflows/workflowHostOwners.ts` 中 create 后另行 `item.updateTags` 的路径，`note.tags` 改作 `initialTags` 随 `notes.create`（`child` placement）提交。验证：`npx tsx node_modules/mocha/bin/mocha "test/core/194-*.test.ts" "test/workflow-literature-workbench-package/*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit` 且 `npx tsc --noEmit` 全绿

## 5. 收尾验证

- [x] 5.1 全量 core 套件与 node 契约治理套件通过，确认 23 keys / 87 members manifest identity 未变。验证：`npx tsx node_modules/mocha/bin/mocha "test/node/core/187-*.test.ts" --require test/setup/zotero-mock.ts --timeout 120000 --exit` 与 `npm run test:zotero:core`
- [x] 5.2 规格一致性收尾：`openspec validate "complete-workflow-host-v12-leaf-contracts"` 通过；逐条核对四个切片的 spec scenario 均有对应测试覆盖（scenario → 测试映射记入实现备注或测试名）

