# Tasks

- [x] Add delta specs for split runtime context view generation and skill
      instruction scope.
- [x] Replace split `write_prepare_views()` with deterministic cross/external
      context rendering from SQLite/workspace runtime sources.
- [x] Add source evidence index enrichment from triage, workset metadata, and
      digest locators.
- [x] Update guidance and generated split packages.
- [x] Extend focused runtime and renderer tests.
- [x] Run focused mocha tests, `tsc --noEmit`, strict OpenSpec validation, and
      Prettier checks for changed files.
