# Tasks

- [x] 1. Create OpenSpec artifacts and runtime persistence SSOT documentation.
- [x] 2. Add centralized runtime persistence resolver, usage scanner, cleanup API, and tests.
- [x] 3. Move plugin state DB path to managed `state/`, with safe copy migration from the old Zotero data location.
- [x] 4. Move runtime log persistence from prefs JSON to managed `logs/`, with legacy pref import.
- [x] 5. Update ACP chat and ACP SkillRunner-compatible workspace paths to managed runtime subdirectories.
- [x] 6. Add preferences UI monitoring and categorized cleanup actions.
- [x] 7. Add or update tests for path resolution, migration, cleanup categories, and preferences smoke coverage.
- [x] 8. Run OpenSpec validation, targeted tests, UI/core smoke tests, and `npx tsc --noEmit`.
