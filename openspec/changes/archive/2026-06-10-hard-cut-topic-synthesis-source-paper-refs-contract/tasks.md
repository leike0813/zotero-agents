# Tasks

- [x] Add OpenSpec deltas for current-state source paper refs contract.
- [x] Update split runtime final section materialization to emit `source_papers`
      and preserve `source_paper_refs`.
- [x] Update generated split skill packages from source.
- [x] Update Host manifest/artifact validation and topic detail DTOs.
- [x] Update Topic Details UI to render `source_papers`.
- [x] Update focused runtime, apply/storage, renderer, and UI tests.
- [x] Run focused mocha tests, `tsc --noEmit`, strict OpenSpec validation, and
      formatting checks for touched files.
