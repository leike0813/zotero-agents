## 1. Contract and root-cause TDD

- [x] 1.1 Add the ACP Workspace surface architecture SSOT and OpenSpec proposal, design, and delta specifications.
- [x] 1.2 Extend the paired publication conformance test with snapshot-upsert-followup mutation continuity, atomic commit, projected count, and single-rebase failures.
- [x] 1.3 Extend Chat/Skills runtime and UI tests for ordinary progress parity, first-open delivery, DOM identity, owner cleanup, and forbidden materialization.

## 2. Canonical protocol and host runtime

- [x] 2.1 Atomically migrate publication, transcript page, profiler, Shell, and child fields to the v4 visible-count and revision vocabulary without compatibility aliases.
- [x] 2.2 Implement the shared ACP surface adapter/runtime with exhaustive paired mappings, owner-scoped scheduling, typed initialization, lane cleanup, and coordinator-owned rebase.
- [x] 2.3 Replace Chat and Skills panel read-model producers with owner-scoped source adapters and remove full-panel/frontend materialization from ACP Workspace paths.

## 3. Transactional browser surface

- [x] 3.1 Replace the old transcript publication receiver/client with the source-neutral ACP surface controller and one canonical region state.
- [x] 3.2 Implement plan-render-commit transcript transactions and targeted rollback-safe renderer effects for both children.
- [x] 3.3 Remove child automatic rebase requests, duplicated FIFO/RAF/revision state, source-specific region projection, and the superseded browser module.

## 4. Profiler, Replay, and cleanup

- [x] 4.1 Align profiler and Replay lifecycle/barrier aggregation with the v4 protocol, coordinator-owned rebase, and no-storm acceptance.
- [x] 4.2 Remove obsolete Host schedulers, DTO conversion, old field vocabulary, stale tests, and update profiler/parity documentation.

## 5. Verification

- [x] 5.1 Run focused Node conformance, Chat, Skills, UI, memory, profiler, and Replay tests and fix all change-related failures.
- [x] 5.2 Run Zotero UI/runtime tests available in this environment, `npm run lint:check`, `npm run build`, strict OpenSpec validation, and generated-help-doc drift checks.
  - `eslint`, build, strict OpenSpec, touched-file formatting, and help-doc drift checks pass. Full `lint:check` reaches three unchanged baseline Prettier failures in tests 172, 173, and workflow test 48.
- [x] 5.3 Run or prepare same-provenance boundary formal Replay for Chat and Skills, record unavailable-host limitations, and keep the change active unless all formal budgets pass.
  - 2026-07-16: Zotero UI lite production-path and nested-frame lifecycle gates pass. The repository does not contain the user's saved round4 trace/report group, and formal comparison requires that exact trace digest plus the user-retained boundary setting, so no replacement performance claim was generated.

