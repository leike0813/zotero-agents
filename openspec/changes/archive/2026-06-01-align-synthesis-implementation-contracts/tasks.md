## 1. Spec Alignment

- [x] 1.1 Align identity anchor, reference matcher, discovery hint, runtime gate, related-items sync, and performance requirements with `doc/synthesis-layer`.
- [x] 1.2 Remove or rewrite stale `accepted`/`filtered` discovery hint and `paper_ref`-derived identity requirements.
- [x] 1.3 Validate the change with `openspec validate align-synthesis-implementation-contracts --strict`.
