## ADDED Requirements

### Requirement: R7 Citation and Reference typed parity SHALL be separately evidenced
R7 SHALL record Citation Graph, Reference Refresh, and Reference Matching/Review as complete only after typed Node/Rust differential evidence covers their bounded public behavior, durable rows, and reopen state. R8 SHALL remain blocked by Tag/Concept/Topic Graph and Checkpoint/Bundle/WebDAV/Debug application clusters.

#### Scenario: Migration status is reviewed after the Citation/Reference slice
- **WHEN** the cluster's local and five-target candidate evidence is accepted
- **THEN** the migration inventory SHALL record these three application families as complete
- **AND** it SHALL not claim R8 runtime/lifecycle completion or production ownership cutover.
