{
  "schema": "host-bridge.semantic-domain.v2",
  "domain": "connectivity-context",
  "families": {
    "surface": {
      "domain": "connectivity",
      "intents": ["inspect the CLI contract", "discover a canonical command"],
      "useWhen": ["The task needs offline identity or command metadata before connecting to Zotero."],
      "avoidWhen": ["Do not use discovery commands as evidence that the Host Bridge is currently reachable."],
      "evidence": ["surface identity or the described command entry"],
      "followups": ["bridge status"]
    },
    "bridge": {
      "domain": "connectivity",
      "intents": ["check Host Bridge connectivity", "diagnose profiles and backends"],
      "useWhen": ["The task needs current bridge, profile, manifest, or backend state."],
      "avoidWhen": ["Do not use bridge diagnostics for Zotero library facts."],
      "evidence": ["health, manifest, profile, or backend diagnostic result"],
      "followups": ["context current", "library item search"]
    },
    "operation": {
      "domain": "connectivity",
      "intents": ["inspect a durable operation receipt", "resolve an uncertain write outcome"],
      "useWhen": ["A state-changing command returned an operationId or reported unknown state or handle outcomes."],
      "avoidWhen": ["Do not infer operation completion from transport status when a receipt is available."],
      "evidence": ["operation state, request digest, stateChange, handleConsumption, and original response"],
      "followups": ["surface describe", "bridge status"]
    },
    "context": {
      "domain": "context",
      "intents": ["resolve the current Zotero view or selection", "navigate to a known Zotero object"],
      "useWhen": ["The request refers to this item, the current collection, selection, note, or pane."],
      "avoidWhen": ["Navigation changes the visible Zotero context but is not a metadata mutation."],
      "evidence": ["current view, stable Zotero refs, or navigation result"],
      "followups": ["library item get", "workflow describe"]
    }
  },
  "commands": {
    "operation get": {
      "intents": ["get operation receipt", "check uncertain mutation"],
      "evidence": ["the exact operationId-bound durable receipt"]
    },
    "context selection get": {
      "intents": ["selected items", "current selection", "read selection"],
      "evidence": ["ordered top-level item refs and selection summaries"]
    }
  }
}
