{
  "id": "zotero-bridge-cli",
  "name": "🔌 Zotero Bridge CLI",
  "description": "Operate the Zotero Bridge CLI when exact Zotero commands or structured recovery are needed.",
  "version": "__HOST_BRIDGE_SURFACE_VERSION__",
  "execution_modes": [
    "auto"
  ],
  "schemas": {
    "output": "assets/output.schema.json"
  },
  "entrypoint": {
    "prompts": {
      "common": "Read SKILL.md first. After selecting a canonical command, read only its matching reference under references/commands/. Never expose bearer tokens. Return one JSON object that preserves the CLI result or structured failure."
    }
  }
}
