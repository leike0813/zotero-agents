{
  "schema": "zotero-library-task.runner-template.v1",
  "id": "__SKILL_ID__",
  "name": "__SKILL_NAME__",
  "description": "__DESCRIPTION__",
  "execution_modes": ["auto", "interactive"],
  "schemas": {
    "output": "assets/output.schema.json"
  },
  "entrypoint": {
    "result_json_filename": "__SKILL_ID__.result.json",
    "prompts": {
      "common": "Treat SKILL.md as the authoritative procedure and use the bundled zotero-bridge-cli Skill for exact commands. A pending Runner response uses __SKILL_DONE__: false only while a concrete user decision is required. A final Runner response uses __SKILL_DONE__: true as transport metadata and carries one business payload matching assets/output.schema.json. The Runner removes the marker before Schema validation: __SKILL_DONE__ is not a business-result field and must not appear in a result file. Emit no Markdown fence, preface, suffix, or second JSON object."
    }
  }
}
