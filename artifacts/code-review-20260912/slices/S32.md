| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/modules/synthesis/builtinTagPolicy.ts | 159 | OK | 内建 status 策略常量与保护函数；未发现可证据化缺陷 |
| src/modules/synthesis/citationGraph.ts | 621 | 问题 | P3: promotions 去重 O(n²)（F6）；isbn 字段从不参与身份键（F7）；canonical 分组非传递闭包（见 NOISE） |
| src/modules/synthesis/digestRepresentativeImage.ts | 162 | 存疑 | 块提取正则 `<div …>[\s\S]*?</div>` 遇块内嵌套 div 会提前截断，caption 兜底同时失效；需确认笔记 HTML 实际形状 |
| src/modules/synthesis/exportDeliveryAdapter.ts | 110 | OK | 失败时删根目录且保留稳定 unavailable 结果；路径前缀用 capability 白名单清洗 |
| src/modules/synthesis/foundation.ts | 171 | 存疑 | `buildSynthesisStoragePaths(root, topicId)` 直接把 topicId 拼入路径，是否已由 `canonicalSynthesisTopicPathId` 归一需确认（调用方不在本切片） |
| src/modules/synthesis/itemObserver.ts | 180 | 问题 | P3: 返回的 `recorded` 恒为 0，函数名/返回值与行为不符（F5） |
| src/modules/synthesis/libraryAdapter.ts | 1467 | 问题 | P2: artifacts.read 不校验 configuredLibraryId（F1）；digest payload 形状假设与 registry 冲突（F2）；metadataFingerprint 中 arxiv 恒为 ""（见 NOISE） |
| src/modules/synthesis/registry.ts | 507 | 问题 | 同 F2（digest 要求 object.markdown）；`rolesByReference` 对同一 sourceReferenceId 覆盖而非累积（见 NOISE） |
| src/modules/synthesis/relatedItemsEffectAdapter.ts | 127 | OK | ensure_present/移除双向处理齐备，失败转 receipt |
| src/modules/synthesis/representativeImageReadAdapter.ts | 249 | OK | stat 后二次校验长度，规避读放大 |
| src/modules/synthesis/reviewInput.ts | 377 | OK | 输入归一化、过滤、hash 自洽 |
| src/modules/synthesis/runWorkspaceMaterializationAdapter.ts | 96 | 问题 | P2: run_root 仅做字符串前缀比较，未消解 `..`（F4，条件性） |
| src/modules/synthesis/syncRecovery.ts | 205 | OK | 状态优先级与 allowedActions 一致；autoOverwriteCanonical 固定 false |
| src/modules/synthesis/syncRuntimeCleanup.ts | 40 | OK | 退役路径/偏好清理，失败不影响返回 |
| src/modules/synthesis/tagEffectAdapter.ts | 122 | 存疑 | 恒提交 `add: [effect.tag]`，从不依据 `effect.action` 分支；需确认契约是否只有 ensure_present |
| src/modules/synthesis/uiModel.ts | 4198 | OK | 纯归一化/派生模块，按函数粒度走读；未发现带证据的缺陷（见 COVERAGE 说明） |
| src/modules/synthesis/webDavSyncAdapter.ts | 266 | 问题 | P3: MKCOL 把 409 当“已存在”接受（F8） |
| src/modules/synthesis/webDavSyncClient.ts | 110 | 问题 | P3: 凭据读取失败被吞成空串（F9） |
| src/modules/synthesis/webDavSyncCredentialPrefs.ts | 193 | 问题 | P3: envelope.iterations/kdf 写入但读取时忽略（F10） |
| src/modules/synthesis/webDavSyncPrefs.ts | 337 | 问题 | P2: 连接测试把 404/其它 4xx 判为 ok（F3） |
| src/modules/synthesis/webDavSyncRemote.ts | 31 | OK | 脱敏与 URL 拼接；relativePath 由契约层校验 |
| src/modules/synthesis/webDavSyncTypes.ts | 13 | OK | 纯类型再导出（contracts 为 SSOT） |
| src/modules/synthesis/zoteroItemRefAdapter.ts | 51 | 存疑 | `findZoteroItemByRef` 回退 `getAll(libraryId)` 全库扫描，热路径代价取决于调用频率 |

### F1 [P2][信任边界] src/modules/synthesis/libraryAdapter.ts:1418 —— artifacts.read 未校验 locator 的 libraryId 是否落在配置范围内
- 证据：
```
        const item = itemByLibraryAndKey(locator.libraryId, locator.itemKey);
```
（对照 :1264 `if (libraryId !== configuredLibraryId) { invalidHostRead("Host libraryId is outside the configured scope", {`）
- 分析：同一 port 的 `syncSnapshot`/`listItemsPage`/`getItemsByRef` 都以 `configuredLibraryId` 做作用域闸门，唯独 `read()` 只经 `decodeArtifactLocator`（正数 libraryId、非空 key、artifactType 在 `PAPER_ARTIFACT_TYPES` 内）后直接按 locator 取值。locator 只是 `synthesis-artifact:v1:` + URI 编码段的可构造字符串，因此调用方可以传别的 `libraryId` 读取配置库之外的条目工件（digest 文本、canonical JSON）。
- 建议：`decodeArtifactLocator` 之后立刻加 `locator.libraryId !== configuredLibraryId → invalidHostRead(...)`，与其他三个方法保持同一闸门。
- 置信度：中（缺检查这一事实确定；越权可达性取决于调用方能否自造 locator）

### F2 [P2][契约] src/modules/synthesis/libraryAdapter.ts:641 / src/modules/synthesis/registry.ts:309 —— 同一 digest payload 在两处被要求成不同形状
- 证据：
```
            ...(directType === "digest" && typeof note.payload === "string"
              ? { markdown: note.payload, decodedText: note.payload }
              : {}),
```
```
  const markdown =
    payload && typeof payload === "object" && !Array.isArray(payload)
      ? payload.markdown
      : undefined;
```
- 分析：两处消费的都是 `broker.library.getNoteDetail(...).payload`。libraryAdapter 只有在 payload 是 **string** 时才把它当 digest 正文；registry.discoverArtifact 只有在 payload 是 **object 且带 markdown 字段** 时才判 digest 为 available。payload 为 object 时，host read 的 digest 内容走 `cleanString(artifact.markdown || artifact.decoded_text || artifact.payload)`（:1235）会得到 `"[object Object]"`；payload 为 string 时 registry 会报 `payload_decode_failed`。两侧不可能同时正确，且都会静默降级而不是报错。
- 建议：在 contracts 中固化唯一 digest payload codec（string 或 `{markdown}`），两处共用同一解析函数并据此判 valid。
- 置信度：中（不一致可证；哪一侧正确需 contracts 确认）

### F3 [P2][正确性] src/modules/synthesis/webDavSyncPrefs.ts:301 —— 连接测试把 404 等 4xx 视为成功
- 证据：
```
    if (response.status === 401 || response.status === 403) {
...
    } else if (response.status >= 500 || response.status === 0) {
...
    ok: !diagnostics.some((entry) => entry.severity === "error"),
```
- 分析：PROPFIND 只有 401/403/5xx/0 才产生 error 诊断；remote path 写错时的 404、405、400 都落空，`ok` 为 true，设置面板显示“连接测试通过”，而真正同步会在后续 read/write 处失败，用户被误导。
- 建议：把 207/2xx 之外的非 401/403 状态（尤其 404）也记为 error（如 `webdav_sync_remote_not_found`）。
- 置信度：高

### F4 [P2][信任边界] src/modules/synthesis/runWorkspaceMaterializationAdapter.ts:32 —— run_root 校验只做大小写折叠的字符串前缀，未消解 `..`
- 证据：
```
function pathContains(parent: string, child: string) {
  const base = normalizeNativeLocalPath(parent)
    .replace(/\\/g, "/")
    .toLowerCase();
  const target = normalizeNativeLocalPath(child)
    .replace(/\\/g, "/")
```
- 分析：`pathContains` 用 `target.startsWith(`${base}/`)` 判包含，不解析 `..` 段。若 `normalizeNativeLocalPath` 不折叠 `..`（该实现不在本切片），`runRoot = <acpSkillRunsDir>/../../<any>/acp-skill-x` 既通过包含判定，其基名又满足 `ACP_SKILL_RUN_ID_RE`（:54），随后 :80 `writeText(joinPath(runRoot, entry.path), entry.text)` 会写到 skill-runs 目录之外。同类问题也适用于 `entry.path` 自身是否含 `..`。
- 建议：用解析后的绝对路径做比较（`path.relative(root, target)` 不以 `..` 开头），并显式拒绝含 `..` 的段；写入前再断言最终路径仍在 runRoot 内。
- 置信度：中（依赖 `utils/path.normalizeNativeLocalPath` 是否折叠 `..`）

### F5 [P3][可维护性] src/modules/synthesis/itemObserver.ts:156 —— `recorded` 从不自增，返回值恒为 `{recorded: 0}`
- 证据：
```
  const recorded = 0;
```
```
  return { recorded };
```
- 分析：函数名 `recordSynthesisZoteroItemNotifications` 与返回字段承诺“记录了几条”，循环内只 `await client.notifications.consumeRelatedItemsSyncEcho(...)` 并在 consumed 时 `continue`，计数从不递增。任何依据该值判断是否发生 echo 消费的调用方都会走空分支（不可观测的静默失败路径）。
- 建议：consumed 分支计数，或删掉该字段并把函数改名为 `consumeSynthesisZoteroItemNotifications` 同步调用方。
- 置信度：高（返回恒 0 可证），对调用方影响中

### F6 [P3][性能] src/modules/synthesis/citationGraph.ts:471 —— promotions 去重为 O(n²) 主路径同步扫描
- 证据：
```
        if (
          !promotions.some(
            (entry) => entry.from === refKey && entry.to === target,
          )
        ) {
```
- 分析：每命中一条 reference 就对已累积的 promotions 线性扫描；引用总数 n 时最坏 O(n²) 字符串比较（万级引用即千万级比较），且全部在 `buildUnifiedCitationGraph` 同步执行，直接抬高建图延迟。
- 建议：改用 `Set<string>` 存 `${refKey}\u0000${target}` 去重键，最后统一排序输出。
- 置信度：高

### F7 [P3][契约] src/modules/synthesis/citationGraph.ts:15 —— isbn 被传入但从不参与身份键计算
- 证据：
```
  isbn?: string;
```
```
  const url = normalizeUrl(input.url);
  if (url) {
    keys.push(`ref:url:${slug(url)}`);
  }
```
- 分析：`provisionalReferenceKey` 与 `referenceIdentityKeys` 枚举了 citekey/doi/arxiv/url/title-year-author/raw，唯独没有 `input.isbn`；而 `CitationGraphPaperInput` 也带 isbn，libraryAdapter.ts:796 明确把 `entry.matching.ISBN` 投进来。结果：只有 ISBN 的图书类引用永远无法被提升为库内节点，恒定落入 external/unresolved，且没有任何注释说明这是有意省略（判断）。
- 建议：确认 ISBN 匹配语义后补 `ref:isbn:`（复用 `registry.normalizeIsbnValue` 归一化），或从两个 DTO 中移除 isbn 以免误导下游。
- 置信度：中（判断性）

### F8 [P3][错误处理] src/modules/synthesis/webDavSyncAdapter.ts:72 —— MKCOL 把 409 当“集合已存在”接受
- 证据：
```
function acceptedCollectionStatus(status: number) {
  return (
    status === 200 ||
    status === 201 ||
    status === 204 ||
    status === 405 ||
    status === 409
  );
}
```
- 分析：409 Conflict 在 WebDAV 语义是“父集合不存在、未创建”，而 405 才是“已存在”。把 409 当成功会让 `ensureCollection` 在父路径缺失时返回 `ready`，随后的 PUT 失败被归到泛化 `webdav_sync_host_write_failed`，定位成本高。
- 建议：成功集合收敛为 200/201/204/405，409 走 `webdav_sync_host_collection_failed`。
- 置信度：中（判断性）

### F9 [P3][错误处理] src/modules/synthesis/webDavSyncClient.ts:107 —— 凭据读取失败被吞成空串，丢失失败码
- 证据：
```
export async function webDavCredentialForRequest() {
  const credential = await readSynthesisWebDavSyncCredential();
  return credential.ok ? credential.credential : "";
}
```
- 分析：missing / crypto_unavailable / decrypt_failed 三种失败码在此被压成 `""`，adapter 只要 config 为 configured 就照常发请求（空口令 Basic），最终只报 `webdav_sync_auth_failed`。host bridge token 轮换导致密文不可解的场景下，用户无法区分“未配置口令”和“口令无法解密”。
- 建议：改为返回结果对象，把 `credential.code` 映射为 `webdav_sync_credential_*` 诊断（描述与连接测试都要透出）。
- 置信度：中

### F10 [P3][契约] src/modules/synthesis/webDavSyncCredentialPrefs.ts:89 —— envelope 的 iterations/kdf 写入但不参与解密
- 证据：
```
      name: "PBKDF2",
      salt,
      iterations: PBKDF2_ITERATIONS,
      hash: "SHA-256",
```
```
    const key = await deriveKey(base64ToBytes(envelope.salt));
```
- 分析：写入时把 `iterations`、`kdf` 存进必填字段，读取时只校验 `schema_id/schema_version/algorithm`，`deriveKey` 固定用常量 `PBKDF2_ITERATIONS`，`kdf` 完全不校验。序列化/反序列化不对称：将来调整迭代数会让旧密文以新参数解密失败并被归为 `decrypt_failed`，而字段本身永远读不出来。
- 建议：解密时用 `envelope.iterations` 并校验 `kdf === "PBKDF2-SHA256"`，或把参数变更绑定到 schema_version 并删除未使用字段。
- 置信度：中（判断性）

SLICE: S32
FILES: 23 个 / 9792 行；ledger: /tmp/review/ledger/S32.md
COVERAGE: 已细读 22 个；uiModel.ts（4198 行）按函数粒度走读归一化/派生逻辑，未逐行核对全部 wire 字段
TOP FINDINGS（只列 P0/P1/P2，按严重度排序）:
- [P2][信任边界] libraryAdapter.ts:1418 artifacts.read 不校验 locator.libraryId 是否等于 configuredLibraryId，可跨库读工件
- [P2][契约] libraryAdapter.ts:641 / registry.ts:309 digest payload 一处要求 string、一处要求 object.markdown，必有一侧错
- [P2][正确性] webDavSyncPrefs.ts:301 连接测试只把 401/403/5xx 当失败，404 等 4xx 直接 ok:true
- [P2][信任边界] runWorkspaceMaterializationAdapter.ts:32 run_root 仅字符串前缀校验，`..` 未消解即可越出 skill-runs 目录
NOISE: 本切片最值得主代理复核的 3 个可疑点：
1) citationGraph.ts:373 groupCanonicalPapers 按单键取 canonical，未做传递闭包（A~B、B~C 时 A/C 仍是不同节点）；
2) tagEffectAdapter.ts:52 恒 `add: [effect.tag]`，从不依据 `effect.action` 分支（若契约含“移除/取消”动作即误写）；
3) libraryAdapter.ts:481 `metadataFingerprintFromItem` 固定 `arxiv: ""`，arXiv 变化不会改变 metadataHash，而 registry 侧同一类型会带 arxiv。