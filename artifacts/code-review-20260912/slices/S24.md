## A. 文件级台账

| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| rust/zotero-bridge/tests/schema_mode.rs | 272 | OK | 纯契约/黑盒测试，逐条读完未发现缺陷 |
| skills_builtin/collection-collector/scripts/gate_runtime.py | 138 | OK | 仅做 gate 指令拼装与 CLI 分发 |
| skills_builtin/collection-collector/scripts/stage_runtime.py | 974 | 问题 | P3: `page_cursor` 静默截断（F7）；另有 `run_root_from_db` 隐式回退 cwd 的可疑行为（NOISE） |
| skills_builtin/create-topic-synthesis-prepare/scripts/gate.py | 127 | OK | — |
| skills_builtin/create-topic-synthesis-prepare/scripts/topic_synthesis_db.py | 4231 | 问题 | P2: 工件 key 不匹配致 handoff 丢项（F1）；P2: 工件注册表进程内失效/hash 丢弃（F4）；P2: triage 层级读取不一致（F3）；P3 若干（F5/F6/F9） |
| skills_builtin/export-research-bundle/scripts/gate_runtime.py | 197 | OK | — |
| skills_builtin/export-research-bundle/scripts/stage_runtime.py | 1951 | 问题 | P2: manifest content_file 未做路径约束（F2）；P3: selection 的 O(n²) 成员判断（F8）；P3: script_command 硬编码 `python`（F9） |
| skills_builtin/literature-analysis/scripts/run_analysis.py | 342 | OK | — |
| skills_builtin/literature-analysis/scripts/runtime_db.py | 10 | 未细读 | 纯 re-export shim（`from analysis_runtime.runtime_db import *`），无自有逻辑 |
| skills_builtin/literature-analysis/tests/test_citation_materialization.py | 66 | OK | 测试文件 |

## B. Findings

### F1 [P2][契约] skills_builtin/create-topic-synthesis-prepare/scripts/topic_synthesis_db.py:2038（另见 4095）—— handoff manifest 与最终 source_artifacts 静默丢失 Stage 30 payload 引用
- 证据：
  ```
  2036	          "resolver_manifest",
  2037	          "paper_artifacts_manifest_batch_1",
  2038	          "prepare_analysis_context_payload",
  ```
  以及 4095：`"prepare_handoff": artifact_entry(conn, "prepare_analysis_context_payload"),`
  而真正写进注册表的名字是 2984：`key=stage["id"] + "_payload",`（即 `stage_30_prepare_analysis_context_payload`，默认表见 78 行 `("stage_30_prepare_analysis_context", "runtime/payloads/prepare-analysis-context.json")`）。
- 分析：`artifact_entry` 对未知 key 直接 `return None`（587-591），`write_handoff` 的 `if entry:` 会静默跳过，`write_complete_sections` 的 `source_artifacts.prepare_handoff` 会写成 `null`。触发条件是任意一次正常的 Stage 30 submit + handoff 生成，属于确定性缺陷；影响是跨 skill handoff 的引用清单缺一项，下游只能靠约定路径猜测。
- 建议：把两处字符串改为 `stage_30_prepare_analysis_context_payload`（或让 `submit_current_payload_stage` 额外注册一个无前缀别名），并让 `artifact_entry` 对未命中 key 记诊断而非静默 None。
- 置信度：高

### F2 [P2][信任边界] skills_builtin/export-research-bundle/scripts/stage_runtime.py:767 —— artifact manifest 的 `content_file` 未做 run_root 约束，可读取任意路径
- 证据：
  ```
  766	            content_file = clean(artifact.get("content_file"))
  767	            target = run_root / content_file
  768	            if ref and content_file and target.is_file():
  ```
- 分析：`Path("/run") / "/etc/passwd"` 在 Python 中等于 `/etc/passwd`；`../../...` 同样逃逸。`content_file` 是 `is_file()` 通过后即 `posix(target)` 存入 `digest_path`，随后 789 行 `Path(digest_path).read_text(...)[:12000]` 会被写进 Stage 50 的 assessment packet。同仓库 `topic_synthesis_db.normalize_paper_artifact_manifest_paths`（348-362）用的是会抛错的 `require_run_file/resolve_run_path`，此处缺少同等校验。触发条件：Host Bridge 返回（或被篡改）的 manifest 携带绝对路径或 `..`。
- 建议：统一改为 `resolve_run_path(run_root, content_file)` + 存在性校验，越界即记诊断并丢弃该 artifact。
- 置信度：中

### F3 [P2][正确性] skills_builtin/create-topic-synthesis-prepare/scripts/topic_synthesis_db.py:2071（另见 3407）—— update 流程只认扁平 `relevance_level`，与 canonical 访问器分叉，会把新发现文献误判为 screened_out 并删除
- 证据：
  ```
  2070	        paper_triage = triage.get(paper_ref, {})
  2071	        relevance_level = clean_text(
  2072	            paper_triage.get("relevance_level"), "unknown"
  2073	        ).lower()
  ```
  对照同文件 canonical 访问器 2291-2296：`triage_level(triage, flat_key, nested_key)` 会回退到 `triage[nested_key]["level"]`，而 2441/2536/2591 都走该访问器。
- 分析：若 Stage 30 payload 用嵌套形式（`topic_relevance.level`，访问器显式支持该形状），2071 会得到 `"unknown"`，`"unknown" not in {"core","related"}`（2075）→ 判为 `screened_out`；随后 2104-2107 会把这些 paper 从 `paper_workset` 删除，属于破坏性后果。是否可达取决于 stage-30 schema 是否强制扁平字段——无法在本切片内确认，故标中置信度。
- 建议：2071 与 3407 改用 `triage_level(paper_triage, "relevance_level", "topic_relevance")`，与其余渲染/选择路径保持单一事实源。
- 置信度：中

### F4 [P2][可维护性/契约] skills_builtin/create-topic-synthesis-prepare/scripts/topic_synthesis_db.py:574-584 —— `register_artifact` 只改进程内全局字典，不落库且丢弃 hash
- 证据：
  ```
  574	def register_artifact(
  ...
  583	    ARTIFACT_PATHS[key] = path
  584	    return None
  ```
- 分析：`conn` / `skill_id` / `stage_id` / `hash_value` 全部被忽略。每个 stage 都以新进程运行（gate.py `--action` 逐次调用），因此该注册表每次都被重置为 51-85 行的硬编码默认值；只要某个 key 不在默认表里，写进注册表的动作在下一个进程就消失（`artifact_manifest` 于 4193 注册即属此类，只是恰好无人回读）。同时 `write_complete_sections`（4102）与 `write_json` 精心算出的 `sha256:` 哈希被 `hash_value` 吞掉，`section_manifest_entry` 只输出 path。
- 建议：把 (key → path, hash, stage, skill) 落到 `runtime_metadata` 或新表，`artifact_entry` 从库读；或在启动时显式校验所有用到的 key 都存在于默认表并加断言，避免 F1 这类静默丢项。
- 置信度：高

### F5 [P3][可维护性] skills_builtin/create-topic-synthesis-prepare/scripts/topic_synthesis_db.py:4212-4218 —— `normalize_update_operation` 对所有输入都返回 `"update_full"`（死代码）
- 证据：
  ```
  4213	    operation = str(value or "").strip()
  4214	    if operation == "update_full":
  4215	        return operation
  4216	    if default == "update_full":
  4217	        return default
  4218	    return "update_full"
  ```
- 分析：三条分支的返回值全是 `"update_full"`，函数恒等；当前全文件无调用点，属死代码，一旦被复用会把 create 运行错误地标成 update。
- 建议：删除，或补一条 `return operation if operation in {...} else default` 的分支。
- 置信度：高

### F6 [P3][可维护性] skills_builtin/create-topic-synthesis-prepare/scripts/topic_synthesis_db.py:562-571 / 667-672 —— `record_action`、`write_gate_transcript`、`write_action_transcript` 均为空实现
- 证据：
  ```
  569	) -> None:
  570	    return None
  ```
  ```
  667	def write_gate_transcript(run_root: Path, instruction: dict) -> None:
  668	    return None
  ```
- 分析：调用点（1850、1871、2958、2997、696）看起来在记录动作/转写，实际什么都不写；审计链是空的，排障时容易误以为有证据。属"名字与行为不符"。
- 建议：要么实现落盘，要么删除这些桩并同时删掉调用点，避免保留语义错觉。
- 置信度：高

### F7 [P3][正确性] skills_builtin/collection-collector/scripts/stage_runtime.py:308-313 —— `hasMore=true` 但缺 `nextCursor` 时静默结束分页，导致清单静默截断
- 证据：
  ```
  311	    has_more = value.get("hasMore") is True
  312	    next_cursor = value.get("nextCursor")
  313	    return has_more and next_cursor is not None, next_cursor
  ```
- 分析：同文件 `snapshot_bridge`（350-355）在同一情形下会 `raise RuntimeError("library-snapshot returned incomplete continuation state")`，而 `page_bridge`（316-335）只静默 break，于是 collection-items / topic-list 的分页可能少收数据而不报错，`existing_refs` 与 topics 清单会不完整。
- 建议：`page_bridge` 增加与 `snapshot_bridge` 相同的完整性检查（hasMore 为真却无新 cursor → 抛错并写 diagnostic）。
- 置信度：中

### F8 [P3][性能] skills_builtin/export-research-bundle/scripts/stage_runtime.py:1687（另见 1159）—— 以 `list in list` 做成员判断，O(n²) 深比较
- 证据：
  ```
  1687	    optional = [paper for paper in papers if paper not in mandatory]
  ```
- 分析：`papers` 每项是含 `graph_metrics`/`artifact_manifest`/`readiness_evidence` 的深层 dict，`in`/`not in` 走 `dict.__eq__`，整体 O(n²) 次深比较；n 由 `candidate_budget`（≤250）与 `max_related_papers` 决定，量级可接受但纯浪费。
- 建议：用 `id()` 或 `paper_ref` 集合做去重/划分。
- 置信度：高（代价判断，非缺陷）

### F9 [P3][可维护性] skills_builtin/create-topic-synthesis-prepare/scripts/topic_synthesis_db.py:606 / skills_builtin/export-research-bundle/scripts/gate_runtime.py:76 —— 回给 agent 的命令硬编码 `python`
- 证据：
  ```
  606	    return f'python "{portable_path(skill_root / "scripts" / "gate.py")}" --db "{db_path}" --action {action}'
  ```
  ```
  76	    parts = [f'python "{script.as_posix()}"', f'--db "{Path(db_path).resolve().as_posix()}"']
  ```
- 分析：`collection-collector/gate_runtime.py:24-25` 用的是 `sys.executable`，同一仓库两种约定。在只有 `python3`（或无 `python`）的环境里，agent 照抄 `command`/`submit_command` 会直接失败。
- 建议：统一用 `sys.executable`（必要时 shlex.quote）。
- 置信度：中

---

SLICE: S24
FILES: 10 个 / 6308 行；ledger: /tmp/review/ledger/S24.md
COVERAGE: 已细读 9 个；略读 1 个（literature-analysis/scripts/runtime_db.py：10 行纯 re-export shim）
TOP FINDINGS（只列 P0/P1/P2，按严重度排序）:
- [P2][契约] topic_synthesis_db.py:2038/4095 工件 key `prepare_analysis_context_payload` 非实际注册名，handoff 与 source_artifacts 静默丢项
- [P2][信任边界] export-research-bundle/stage_runtime.py:767 `run_root / content_file` 未约束，manifest 可用绝对路径/`..` 读取任意文件入 packet
- [P2][正确性] topic_synthesis_db.py:2071 update 流程只读扁平 `relevance_level`，与 `triage_level` 分叉，会把 triage 成嵌套形状的新文献删出 workset
- [P2][可维护性] topic_synthesis_db.py:583 `register_artifact` 只改进程内全局，不落库并丢弃 hash，是 F1 能隐形的原因
NOISE: 本切片最值得主代理复核的 3 点 ——
1) export-research-bundle/stage_runtime.py:1494 `available = bool(data.get("ok")) and ...`；若 `unwrap_bridge` 已剥掉外层 `ok`，`graph_ready` 将恒 False、永远走 fallback 权重（需对照 bridge 的 get-metrics 返回形状确认）。
2) collection-collector/stage_runtime.py:57-61 `run_root_from_db` 在 DB 不在 `runtime/` 下时静默回退 `Path.cwd()`（其余两个 runtime 均抛错），可能把产物写到意外目录。
3) topic_synthesis_db.py:3395/3407 与 2071 同源：`paper_year_from_metadata`/`synthesis_role` 也绕过 `triage_level` 语义，属同一处一致性风险。