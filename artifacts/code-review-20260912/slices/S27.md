## A. 文件级台账

| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| skills_builtin/topic-synthesis-finalize/scripts/gate.py | 127 | OK | 薄 CLI 适配层：参数解析 → 4 个 action 分派 → 统一异常兜底返回 `{error:{code,message}}` + exit 2。未发现缺陷；`--action cancel` 只打印终态而不落库，属既有设计（与 db 层 `write_canceled_output` 分工不同）。 |
| skills_builtin/topic-synthesis-finalize/scripts/topic_synthesis_db.py | 4231 | 问题 | F1–F7（见下）。 |
| skills_builtin/update-topic-synthesis-prepare/scripts/topic_synthesis_db.py | 4231 | 问题 | 与上表 topic-synthesis-finalize 副本逐行相同（同 4231 行，文本一致），F1–F7 同样适用，行号相同；修复需同时落到两份生成物/其生成器。 |
| skills_builtin/update-topic-synthesis-prepare/scripts/gate.py | 127 | OK | 与 finalize 的 gate.py 逐行相同，薄适配层，无额外缺陷。 |
| skills_src/literature-deep-reading/renderer/render_literature_deep_reading_skill.ts | 169 | 问题 | F8、F9（见下）；另见文件级存疑（`npx tsx` 依赖、frontmatter 定界符未找到时静默不加 notice）。 |

### B. Findings

### F1 [P2][契约] skills_builtin/topic-synthesis-finalize/scripts/topic_synthesis_db.py:2038（同 update-topic-synthesis-prepare 副本同址）—— 制品 key 名不匹配，handoff 静默丢条目、最终产物 `prepare_handoff` 恒为 null
- 证据：
  ```
            artifact_keys=[
              "resolver_manifest",
              "paper_artifacts_manifest_batch_1",
              "prepare_analysis_context_payload",
  ```
  与注册侧：
  ```
        key=stage["id"] + "_payload",
  ```
  与 `ARTIFACT_PATHS` 填充：
  ```
        ARTIFACT_PATHS[f"{_stage_id}_payload"] = _path
  ```
  与消费侧：
  ```
            "prepare_handoff": artifact_entry(conn, "prepare_analysis_context_payload"),
  ```
- 分析：实际注册的 key 是 `stage_30_prepare_analysis_context_payload`（`ARTIFACT_PATHS` 中只有这个名字），而 `write_handoff` 的 `artifact_keys` 与 `write_complete_sections` 的 `source_artifacts` 都查询 `prepare_analysis_context_payload`。该 key 不存在于 `ARTIFACT_PATHS`，`artifact_entry` 返回 `None`，而 `write_handoff` 对 `None` 条目是 `if entry:` 静默跳过（不报错、不产生缺项提示）。后果：prepare handoff manifest 少了 prepare payload 的绝对路径（与 core handoff 包含 `stage_40/stage_50` payload 形成不对称），且 finalize 产出的 `result/sections/source_artifacts.json` 里 `prepare_handoff` 永远是 `null`，下游无法回溯 prepare payload。
- 建议：把两处 key 统一为 `stage_30_prepare_analysis_context_payload`（或让 `write_handoff` 对缺失 key 抛错，避免再出现静默丢条目）。
- 置信度：高

### F2 [P2][正确性] skills_builtin/topic-synthesis-finalize/scripts/topic_synthesis_db.py:376（同 update-topic-synthesis-prepare 副本同址）—— `stage_state` 主键只有 stage_id，但读取按 skill_id 过滤，多 skill 共用同一 run DB 时状态归属被覆盖
- 证据：
  ```
        create table if not exists stage_state (
          stage_id text primary key,
  ```
  与
  ```
        "select stage_id from stage_state where skill_id = ? and state = 'completed'",
  ```
  两个 skill 契约共用同名 stage id：
  ```
                "id": "stage_00_runtime_state_check",
  ```
  （`topic-synthesis-core-enrichment` 与 `topic-synthesis-finalize` 各出现一次；`stage_00_runtime_setup`、`stage_30_prepare_analysis_context` 亦在 create/update prepare 之间同名）
- 分析：`record_stage` 用 `on conflict(stage_id) do update set skill_id = excluded.skill_id`，即后写入的 skill 会夺走该行的归属。同一 run 目录（handoff manifest 里 `db_path` 明确是同一个 `runtime/topic-synthesis.sqlite`）里 finalize 记录 `stage_00_runtime_state_check` 后，core-enrichment 的 `completed_stages()` 不再包含该 stage，`current_stage()` 会回退到 stage_00 要求重跑（幂等命令，只造成一次多余往返）；更糟的是任何"按技能恢复/续跑"的判定都建立在这个被污染的表上。同一 stage_id 在两个技能里语义不同（core 校验 `prepare-analysis-context.json`，finalize 校验 `core-enrichment.json`），冲突后语义已失真。
- 建议：主键改为 `(skill_id, stage_id)`（或写入时带 skill 前缀），并 `completed_stages` 一致化。
- 置信度：中

### F3 [P2][并发/资源] skills_builtin/topic-synthesis-finalize/scripts/topic_synthesis_db.py:1173（同 update-topic-synthesis-prepare 副本同址）—— 调用 Host Bridge 的 `subprocess.run` 无超时，挂死进程会无限阻塞 gate
- 证据：
  ```
      completed = subprocess.run(
          [str(bridge), *subcommand, "--query", f"@runtime/payloads/{input_name}"],
          cwd=str(run_root),
          capture_output=True,
  ```
- 分析：`run_bridge_json` 是每次 preflight/resolver/metrics/artifact export 的唯一出口，全部无 `timeout=`。bridge 侧卡住（Host 忙、Zotero 弹窗、端口占用）时 `capture_output=True` 还会持续缓冲 stdout，gate 进程永不返回，agent 侧没有可取消点，也无超时错误码；这与仓库对 Host 短片段的超时/取消语义要求相冲突（同类路径在 TS 侧有 deadline 概念，此处没有）。
- 建议：加 `timeout=` 并把 `subprocess.TimeoutExpired` 映射为稳定的可重试错误（含 subcommand 标识）。
- 置信度：高

### F4 [P2][正确性] skills_builtin/topic-synthesis-finalize/scripts/topic_synthesis_db.py:3545（同 update-topic-synthesis-prepare 副本同址）—— 归一化把 `taxonomy.nodes` 压成"第一条 axis 的 nodes"，最终报告与统计静默丢弃其余 axis 的技术路线
- 证据：
  ```
        normalized_axes.append(
            {
                **axis,
  ```
  ```
        if axis_index == 0:
            normalized_nodes = axis_nodes
  ```
  消费侧：
  ```
      for node_raw in as_list(taxonomy.get("nodes")):
  ```
- 分析：校验强制要求 `taxonomy.axes` 有 2–5 条且每条 axis 的 `nodes` 非空（`validate_core_synthesis_apply_fields`），因此多 axis 是常态；但归一化后顶层 `nodes` 只等于 axis[0] 的 routes，`report_body` 的 `## 技术路线` 与 `statistics.route_coverage.routes` 都只读 `nodes`，于是 axis[1..] 的路线在最终报告中整体消失（不是标注丢失，是内容丢失）。附带边界：`if not axis_nodes: continue` 在赋值之前，若首条 axis 为空（历史/未过校验的存量 payload），`normalized_nodes` 会停在 `[]` 而后续 axis 明明有节点。
- 建议：`nodes` 改为所有 axis 节点的并集（或让 `report_body`/statistics 直接遍历 `axes[].nodes`）。
- 置信度：中

### F5 [P3][可维护性] skills_builtin/topic-synthesis-finalize/scripts/topic_synthesis_db.py:562（同 update-topic-synthesis-prepare 副本同址）—— "制品注册表"只写进程内全局字典，`hash_value` 被丢弃，跨进程只剩硬编码默认路径
- 证据：
  ```
        ARTIFACT_PATHS[key] = path
        return None
  ```
  ```
        path = ARTIFACT_PATHS.get(key)
        if not path:
            return None
        return {"path": path}
  ```
- 分析：`register_artifact` 接收 `conn`/`hash_value` 却既不入库也不记 hash；`artifact_entry` 只查模块级字典。gate 每次是独立子进程，因此跨进程时注册信息全部丢失，只能靠 `ARTIFACT_PATHS` 里预置的默认路径"碰巧"正确——本切片所有被消费的 key 恰好都有默认值（唯一例外见 F1，立刻表现为静默丢条目）。任何"注册路径 ≠ 默认路径"的场景（用户把 payload 写到别的路径、将来新增 key）都会跨阶段失配且无报错，且制品完整性（sha256）实际不参与任何校验。
- 建议：把 (key → path, hash) 落进 SQLite 表，或至少对未注册/未预置的 key 直接报错。
- 置信度：高

### F6 [P3][可维护性] skills_builtin/topic-synthesis-finalize/scripts/topic_synthesis_db.py:562（同 update-topic-synthesis-prepare 副本同址）—— 多个被真实调用的动作/脚本是空实现，另有若干死函数
- 证据：
  ```
  def record_action(
      conn: sqlite3.Connection,
      ...
  ) -> None:
      return None
  ```
  ```
  def write_gate_transcript(run_root: Path, instruction: dict) -> None:
      return None
  ```
  未被任何调用点引用的函数：`first_paper_ref`（3322）、`normalize_rows_with_refs`（3632）、`normalize_update_operation`（4212）。
- 分析：`record_action`/`write_gate_transcript`/`write_action_transcript` 在提交/运行路径上被调用（2997、696），但恒为 no-op，读代码者会误以为存在动作审计与 transcript；`normalize_update_operation` 还含明显无意义分支（`if default == "update_full": return default` 之后 `return "update_full"` 等价）。属真实的可维护性代价（误导 + 死代码），非风格问题。
- 建议：删除未被调用的函数；把 `record_action`/`*_transcript` 要么实现要么去掉调用点，避免"看起来有审计"。
- 置信度：高（事实），影响判断为 P3。

### F7 [P2][正确性] skills_builtin/topic-synthesis-finalize/scripts/topic_synthesis_db.py:1892（同 update-topic-synthesis-prepare 副本同址）—— 远端制品缓存完全不校验 `refs`，可能把上一次 export 的清单当成当前 workset 的清单
- 证据：
  ```
  def export_filtered_paper_artifacts_manifest(run_root: Path, refs: list[str]) -> dict:
      cached_manifest = cached_remote_artifact_manifest(run_root)
      if cached_manifest is not None:
          return cached_manifest
  ```
  缓存函数签名不含 refs：
  ```
  def cached_remote_artifact_manifest(run_root: Path) -> dict | None:
  ```
- 分析：`refs` 是这次 resolver 级联算出的论文集合，但缓存命中只看 `runtime/payloads/paper-artifacts-export-delivery.json` 是否存在、其 `manifest_file` 是否存在，与 `refs` 无关。触发条件：第一次 stage_20 的 artifact export 返回 `bridge-download`（此时该 delivery 文件被写入并抛错让用户下载），用户随后修改 resolver proposal / 前置输入再重提 stage_20（stage 未记录完成，允许重提），此时不重新 export，直接把旧 bundle 的 manifest 写进 `paper-artifacts-manifest-batch-1.json`；DB 里 `paper_workset` 已是新集合，而 manifest 的 `papers[]` 是旧集合，stage_30 的 `content_file` 校验与 triage 会按错位集合进行。
- 建议：把 `refs` 的规范化摘要写进 delivery 文件，命中时比对不一致则重新 export（或直接报错要求重下）。
- 置信度：中

### F8 [P3][可测试性] skills_src/literature-deep-reading/renderer/render_literature_deep_reading_skill.ts:54 —— 前端名校验喂的是目录常量而非模板实际 frontmatter，校验恒真
- 证据：
  ```
      validateRunnerManifestShape({
        runnerJson: runner,
        skillDirName: SKILL_ID,
        skillFrontmatterName: SKILL_ID,
      }),
  ```
- 分析：`skillFrontmatterName` 应为 `templates/SKILL.md` frontmatter 里的 `name:`，这里直接传 `SKILL_ID` 常量，与 `skillDirName` 永远相等，因此"目录名与 frontmatter 名一致"这条断言不可能失败。渲染器随后把这个 frontmatter 原样写进 `skills_builtin/literature-deep-reading/SKILL.md`（`renderSkillMd` 只 trim + 插 notice），所以模板里 `name:` 与目录名不一致时会被原样发布（违反 Skill 产物的命名契约），且预渲染校验给出了虚假的通过信号。
- 建议：解析模板 frontmatter 的 `name:` 再传入，或显式断言不存在 frontmatter 名字字段。
- 置信度：中（代码事实高，是否算缺陷取决于模板是否允许改名）

### F9 [P3][正确性] skills_src/literature-deep-reading/renderer/render_literature_deep_reading_skill.ts:83 —— 目录拷贝对所有文件走 utf8 文本往返，二进制模板会被破坏
- 证据：
  ```
      } else if (entry.isFile()) {
        await copyTextFile(sourcePath, targetPath);
  ```
  配合：
  ```
  async function copyTextFile(sourcePath: string, targetPath: string) {
    await writeText(targetPath, await fs.readFile(sourcePath, "utf8"));
  ```
- 分析：`renderer/templates` 目录整体拷贝（line 131）时逐个文件 `readFile(..., "utf8")` → `writeFile`，非 UTF-8 可逆的内容（图片、字体、压缩资源）会被解码替换字符后再编码，产物静默损坏且无告警。当前若该目录确为纯文本则无影响，但拷贝逻辑本身不设防。
- 建议：拷贝二进制安全的目录时用 `fs.cp(..., {recursive:true})`，或按扩展名分流 `Buffer` 拷贝。
- 置信度：中（需确认 templates 目录当前是否含二进制）

### 存疑（不作为缺陷）
- `skills_builtin/*/scripts/topic_synthesis_db.py:606` `script_command` 硬编码 `python "..."`；在只提供 `python3` 的环境（部分 Linux/macOS）该命令串会直接失败，而同一文件对 Windows/非 Windows 已做分支处理。需确认 gate 执行方是否保证 `python` 在 PATH 上。
- `topic_synthesis_db.py:2122-2125` `finalize_update_source_membership` 直接改写 `runtime/payloads/resolver.json` 的 `resolved_paper_set`/`paper_refs`，与 `resolver_manifest["resolution_result"]`（原始 resolve 输出）不再一致；消费方若读 `resolution_result` 与 `paper_refs` 会得到互相矛盾的集合，需确认是否有意为之。
- `topic_synthesis_db.py:542`（finalize 契约的 `stage_00_runtime_state_check`）与 core-enrichment 契约同名同语义位——见 F2，若 run DB 实际按技能隔离则影响面显著缩小。
- `render_literature_deep_reading_skill.ts:149` 依赖 `npx tsx ...` 且 `stdio:"inherit"`：无本地 tsx / 无网络时渲染直接崩，且构建脚本路径写死在字符串里；需确认 CI 与本地均预装。

---

SLICE: S27
FILES: 5 个 / 8885 行；ledger: /tmp/review/ledger/S27.md
COVERAGE: 已细读 5 个（其中两份 `topic_synthesis_db.py` 内容逐行相同，等同细读 1 份 + 交叉核对）
TOP FINDINGS（只列 P0/P1/P2，按严重度排序）:
- [P2][契约] finalize/scripts/topic_synthesis_db.py:2038 制品 key `prepare_analysis_context_payload` 与注册 key 不匹配 → handoff 静默丢条目、`source_artifacts.prepare_handoff` 恒 null
- [P2][并发] 同文件:1173 `run_bridge_json` 的 `subprocess.run` 无 timeout，bridge 挂死即 gate 永久阻塞、无取消语义
- [P2][正确性] 同文件:376 `stage_state` 主键仅 stage_id 而读取按 skill_id 过滤，同名 stage 在共享 run DB 中互相夺权
- [P2][正确性] 同文件:3545 `taxonomy.nodes` 只保留首条 axis，报告与 statistics 静默丢弃其余 axis 的技术路线
- [P2][正确性] 同文件:1892 远端制品缓存不校验 `refs`，重提 stage_20 可能复用旧 workset 的 artifact manifest
NOISE: 1) `register_artifact`/`artifact_entry` 是纯内存注册表（F5），F1 只是它暴露出来的第一个实例，建议主代理顺着这条线检查其他 skill 副本；2) 两份 4231 行文件完全重复，任何修复都要落到两份生成物及其生成器，评审时注意别只改一份；3) TS 侧 frontmatter 名校验恒真（F8），若模板确实允许改名则是发布级问题。