| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/modules/skillRunner/runtime/skillRunnerAsyncLifecycle.ts | 58 | OK | 顺序 shutdown，每步独立 try/catch + `appendRuntimeLog`，单步失败不阻断后续清理；重复调用无副作用（lease 释放有 `acquired` 守卫）。 |
| src/modules/skillRunner/runtime/skillRunnerCtlBridge.ts | 2242 | 问题 | F7：preflight 把 `readable` 直接等于 `exists`（名实不符的 DTO 字段）。另：`appendCtlStreamChunks` 在 debug 关闭时仍全量切分 stdout/stderr（见 F4 分析）；`resolveTempRoot` 在本文件内无调用点（死代码，仅 lint 级）。 |
| src/modules/skillRunner/runtime/skillRunnerLocalDeployDebugStore.ts | 113 | 问题 | F4（emit 全量 clone + 全量广播，O(n²)、entries 无上限）、F6（listener 异常未隔离）。 |
| src/modules/skillRunner/runtime/skillRunnerLocalRuntimeManager.ts | 5048 | 问题 | F2（升级部署不停旧 runtime，旧版本继续服务却写入新 versionTag）、F3（uninstall 先清 state，失败路径不回滚 → 半提交）。另：本地 `getParentPath`(3928) 与 `platform/path` 的实现并存（见 NOISE）；`quoteShellArg` 的 Windows 分支用 `\"` 转义（PowerShell 非法转义，仅影响生成的“手工命令”文本）。 |
| src/modules/skillRunner/runtime/skillRunnerReleaseInstaller.ts | 422 | 问题 | F1：`keepTempOnFailure` 对提前 `return createFailure(...)` 的失败路径无效，temp 被删。 |
| src/modules/skillRunner/runtime/skillRunnerRuntimeFeed.ts | 304 | 问题 | F5：feed 的 `skillrunner` 版本串只做非空校验即流向文件路径/URL。 |

### F1 [P2][错误处理] src/modules/skillRunner/runtime/skillRunnerReleaseInstaller.ts:219 —— 提前返回的失败路径不会保留 temp，`keepTempOnFailure` 契约失效

- 证据：
```ts
226	    if (!artifactResponse.ok) {
227	      return createFailure({
228	        stage: "deploy-release-download",
```
```ts
415	    const shouldKeepTemp = keepTempOnSuccess
416	      ? true
417	      : !!failure && keepTempOnFailure;
418	    if (!shouldKeepTemp) {
419	      await removePathIfExists(tempDir);
```
- 分析：`failure` 只在 `catch` 块（第 393 行）里被赋值；下载失败、checksum 下载失败、checksum 解析失败、SHA256 不匹配、extract 失败、artifact 缺失这 6 条失败路径都是直接 `return createFailure(...)`，`failure` 保持 `null`。调用方 `deployAndConfigureLocalSkillRunner` 传的是 `keepTempOnSuccess: false, keepTempOnFailure: true`（runtimeManager:3358-3359），于是 `shouldKeepTemp = false && true = false`，`finally` 删掉整个 tempDir。后果：这些最常见的失败模式下，返回的 `tempDir/artifactFile/checksumFile` 指向的目录/文件已经不存在，用户按 UI 提示去 temp 抓下载产物会扑空；同时 `keepTempOnFailure` 参数形同虚设（只有抛异常路径才生效）。
- 建议：把 6 处 `return createFailure(...)` 改为 `failure = createFailure(...); return failure;`，或在 `finally` 里以“是否 ok 返回”而非 `!!failure` 判定。
- 置信度：高

### F2 [P2][正确性] src/modules/skillRunner/runtime/skillRunnerLocalRuntimeManager.ts:3511 —— 升级部署不停止旧 runtime，旧进程继续服务但 state 已写成新版本

- 证据：
```ts
3511	      const stagedState = writeManagedLocalRuntimeState({
3512	        ...previousState,
3513	        versionTag: version,
3514	        installDir: normalizedInstallDir,
3515	        ctlPath,
3516	        baseUrl: buildBaseUrl(previousEndpoint.host, previousEndpoint.port),
```
（同对象第 3524 行硬写 `runtimeState: "stopped"`）
- 分析：整条 deploy 分支（3126-3700）没有任何 `runLocalBridgeDown`/`downLocalRuntime` 调用，而 stagedState 明确复用了 `previousEndpoint.host/port`，只把 `runtimeState` 改成 `"stopped"`（声称已停，实际没停）。触发条件：feed 版本变化（或用户强制 deploy）而旧 runtime 仍在运行时——`planLocalRuntimeOneclick` 在版本不一致时正是选择 deploy。后果链：新版本被解压到 `<installRoot>/<新版本>`，但旧进程仍占用 `previousEndpoint.port`；随后 `triggerManagedRuntimeAutoEnsureTickAsync()` → `ensureManagedLocalRuntimeForBackend` 先跑 status，`isStatusRunning` 为真（旧进程健康响应），于是完全跳过 up；更早的 `runLocalBridgeUp`（ctlBridge:1781-1784）也会因 `status === "running"` 直接返回 `alreadyRunning`。最终 state 记的是新 `versionTag/installDir`，实际服务的是旧版本，且后续 one-click 会判为“版本已最新”而不再纠正，只能靠 stop/start 或重启才能生效。
- 建议：deploy 在覆盖 installDir/写 stagedState 之前显式 down（或对旧 endpoint 做 health 校验并要求端口释放），失败即中止；至少要保证 `runtimeState: "stopped"` 是事实而非声明。
- 置信度：中（若调用方在 deploy 前已 stop，则不触发）

### F3 [P2][错误处理] src/modules/skillRunner/runtime/skillRunnerLocalRuntimeManager.ts:4431 —— uninstall 先清 state，失败路径不回滚，留下半提交状态

- 证据：
```ts
4430	      setAutoStartEnabledInSession(false);
4431	      clearManagedLocalRuntimeState();
4432	      const localRootResolution = resolveManagedLocalRoot({
```
- 分析：state 在任何危险动作（down、删目录、清 profile）之前就被清空，而失败分支只有 `return { ok:false, ... }`：`uninstall-down`（4540-4551）、`uninstall-delete`（4616-4630）、`uninstall-configure-profile`（4652-4668）都不恢复 state。触发条件：down 失败、任一目标目录删不掉、或 backend profile 写入失败。后果：托管运行时配置彻底丢失（`getManagedInstallDir()`→""），但进程可能仍在跑、目录只删了一半；此时 `previewLocalRuntimeUninstall`/`resolveManagedLocalRoot` 都因无 `installDir` 而返回“managed installDir is missing”，UI 无法重试清理，用户只能手工收拾。清 state 的动机（防止 auto-ensure 在卸载期间把 runtime 拉起来）可以保留，但失败必须回滚。
- 建议：把 `clearManagedLocalRuntimeState()` 移到删除与 profile 清理都成功之后；或在每个失败分支写回 `stateBeforeUninstall`（至少写回 installDir/managedBackendId）。
- 置信度：高

### F4 [P2][性能] src/modules/skillRunner/runtime/skillRunnerLocalDeployDebugStore.ts:97 —— 每次 append 都全量深拷贝并广播，流式日志下退化为 O(n²)

- 证据：
```ts
97	  entries.push(entry);
98	  emitChanged();
99	  return cloneEntry(entry);
```
（`emitChanged` 第 51 行 `entries.map((entry) => cloneEntry(entry))`，`cloneEntry` 用 `JSON.parse(JSON.stringify(...))` 深拷贝 `details`）
- 分析：`entries` 无上限（只在 `reset…` 时清空），且每次 append 都会把“全部历史条目的 details/error 深拷贝一遍”交给每个监听器。放大点在上游：`appendCtlStreamChunks`（ctlBridge:686-707）把 stdout/stderr 按 1800 字符切成 N 块逐块 append，bootstrap/install 的日志常见 MB 级 → N 数千，拷贝量约 N²/2 × 1.8KB，在正是最需要 debug 的场景（失败后打开 debug）里会明显卡顿/占内存。注意 debug 关闭时 `appendSkillRunnerLocalDeployDebugLog` 会在第 84-86 行提前返回，但 ctlBridge 侧的切分/数组分配仍然发生。
- 建议：append 走增量通知（只把新条目或增量 diff 给监听器），或对 `entries` 设上限（环形缓冲）并在 emit 时复用不可变快照；ctlBridge 侧把 chunk 切分放到 `isDebugModeEnabled()` 之后。
- 置信度：高

### F5 [P2][信任边界] src/modules/skillRunner/runtime/skillRunnerRuntimeFeed.ts:145 —— 远程 feed 的 skillrunner 版本串未做格式/白名单校验即进入文件路径

- 证据：
```ts
145	  for (const entry of args.feed.matches) {
146	    if (
147	      semver.validRange(entry.plugin) &&
148	      semver.satisfies(pluginVersion, entry.plugin, { includePrerelease: true })
149	    ) {
150	      return entry.skillrunner;
```
- 分析：对 `plugin` 做了 `semver.validRange` 校验，对 `skillrunner`（第 120 行只做 `normalizeString` + 非空）却原样返回，`normalizeSkillRunnerRuntimeFeedDocument` 同样只查非空。该值随后被当成版本目录名/文件名使用：`releaseInstaller.ts:193 const installDir = joinPath(installRoot, version);`、`artifactName = skill-runner-${version}.tar.gz`（第 182 行）、runtimeManager:3731 `joinPath(installRoot, version)`。若 feed 被投毒或用户把 `skillRunnerRuntimeFeedUrl` 指到自建源，版本串里的 `..`/分隔符会参与路径拼接（写目录由 `ensureDirectory` 创建、tar 解压到该目录），属于远程内容决定本地写入位置。同源 checksum 也在同一次下载里取得，sha256 校验挡不住这一层。`explicitVersion`（用户偏好/UI 传参）走的是同一条未校验路径（第 205-212 行）。
- 建议：在 `normalizeSkillRunnerRuntimeFeedDocument`/`selectSkillRunnerRuntimeVersionFromFeed` 对 `skillrunner` 做 `semver.valid`（或 `^v?\d+\.\d+\.\d+(-[0-9A-Za-z.-]+)?$`）校验，并拒绝含 `/ \ .. :` 的串；消费侧 `joinPath` 前后再做一次“解析结果必须位于 installRoot 之下”的断言。
- 置信度：中（可利用性取决于 `joinPath` 是否归一 `..`，其实现不在本切片内）

### F6 [P3][错误处理] src/modules/skillRunner/runtime/skillRunnerLocalDeployDebugStore.ts:52 —— 监听器异常未隔离，会把日志调用方一起打断

- 证据：
```ts
52	  for (const listener of listeners) {
53	    listener(snapshot);
54	  }
```
- 分析：`emitChanged` 同步调用监听器且没有 try/catch（对比同类的 `notifyManagedLocalRuntimeStateChanged`，runtimeManager:204-212 明确 `try { listener() } catch {}`）。监听器抛错会沿 `appendSkillRunnerLocalDeployDebugLog` → `appendCtlLog` → 调用方传播：例如 ctlBridge:1599-1607 是“先 `appendCtlLog(...)` 再 `return result`”，一旦抛出，`preflightLocalRuntime` 直接 reject 而不返回失败结果，runtimeManager 里 `await runLocalBridgePreflight(...)`（如 3059、3158、4825）随之抛出，one-click/deploy/ensure 失败且错误信息与真实原因无关。
- 建议：逐个监听器包 try/catch（与 runtimeManager 的既有做法一致）。
- 置信度：中

### F7 [P3][契约] src/modules/skillRunner/runtime/skillRunnerCtlBridge.ts:1454 —— preflight 把 `readable` 直接赋值为 `exists`，DTO 字段语义失真

- 证据：
```ts
1450	      const exists = await pathExists(target.path);
1451	      requiredFileChecks[target.id] = {
1452	        path: target.path,
1453	        exists,
1454	        readable: exists,
```
- 分析：`readable` 从未做读取尝试（也没有权限/编码校验），却对被检查文件一律返回 `true`。该结果进了 `details.checks.required_files` 并被 UI/诊断复用，会在“文件存在但不可读（权限、路径过长、被占用）”时给出错误结论——正是 preflight 想提前拦住的场景。属于投影字段反向成为事实来源的典型（维度 4/7）。
- 建议：要么真实尝试一次受控读取（复用 `readUtf8File` 的失败语义）再填 `readable`，要么改名为 `exists` 并从 DTO 中移除 `readable`。
- 置信度：高

```
SLICE: S52
FILES: 6 个 / 8187 行；ledger: /tmp/review/ledger/S52.md
COVERAGE: 已细读 6 个（skillRunnerAsyncLifecycle 58 / skillRunnerCtlBridge 2242 / skillRunnerLocalDeployDebugStore 113 / skillRunnerLocalRuntimeManager 5048 / skillRunnerReleaseInstaller 422 / skillRunnerRuntimeFeed 304）；略读 0 个
TOP FINDINGS（只列 P0/P1/P2，按严重度排序）:
- [P2][错误处理] skillRunnerReleaseInstaller.ts:219 keepTempOnFailure 对 6 条提前 return 失败路径无效，temp 被删
- [P2][正确性] skillRunnerLocalRuntimeManager.ts:3511 升级部署不停旧进程：旧版本继续服务、state 写新 versionTag
- [P2][错误处理] skillRunnerLocalRuntimeManager.ts:4431 uninstall 先清 state，down/delete/profile 失败路径不回滚 → 半提交
- [P2][性能] skillRunnerLocalDeployDebugStore.ts:97 每次 append 全量深拷贝+广播，chunk 流式日志下 O(n²)、entries 无上限
- [P2][信任边界] skillRunnerRuntimeFeed.ts:145 feed 的 skillrunner 版本未校验即进入 joinPath/文件路径
NOISE:
- runtimeManager 本地 getParentPath(3928) 与 platform/path 的 getParentPath 并存，且 resolveManagedLocalRoot 的安全校验依赖它：两套语义若在 UNC/盘根尾分隔符上分歧，会误判 releases/<version> 布局（建议主代理对比两实现）。
- runtimeManager quoteShellArg(3702) 的 Windows 分支用 \" 转义（PowerShell 非法），仅影响生成的手工命令文本，输入含引号时才可见。
- ctlBridge parseJsonObjectCandidate 只接受单行 JSON；ctl 脚本若输出 pretty-print JSON，会静默退化为 exitCode 判定并丢掉 details（无证据表明当前脚本如此）。
```