| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/modules/hostBridge/server/hostBridgeAdvertisedHostDetection.ts | 231 | 问题 | P2: advertised host 校验过宽（F8）；fetch 无超时/AbortSignal（F10） |
| src/modules/hostBridge/server/hostBridgeAuth.ts | 314 | 问题 | P1: 无效 token 触发 PBKDF2 100k 派生（F2）；P3: envelope.iterations/kdf 未被校验/使用（F11） |
| src/modules/hostBridge/server/hostBridgeCapabilityContract.ts | 309 | 问题 | P2: 运行时改写导入的 JSON 契约，SSOT 分裂（F5） |
| src/modules/hostBridge/server/hostBridgeFileRegistry.ts | 530 | 问题 | P2: 上传临时文件只删句柄不删磁盘（F3）；contentType 未净化，是否可达需确认 |
| src/modules/hostBridge/server/hostBridgeMutationAdapter.ts | 176 | 问题 | P3: 主错误存在时清理错误被静默丢弃（F9） |
| src/modules/hostBridge/server/hostBridgeNotificationInbox.ts | 423 | 存疑 | `pruneHostBridgeNotificationInbox` 为空实现，需确认调用方是否依赖其清理 |
| src/modules/hostBridge/server/hostBridgeOperationStore.ts | 226 | OK | — |
| src/modules/hostBridge/server/hostBridgePagination.ts | 287 | 问题 | P2: UTF-16 切片可能截断代理对（F6）；cursor 无签名，TTL 可被绕过（存疑） |
| src/modules/hostBridge/server/hostBridgeProtocol.ts | 374 | 问题 | P3: 与 capability contract 重复定义 capability entry，字段命名漂移（F12） |
| src/modules/hostBridge/server/hostBridgeRouteContract.ts | 19 | OK | — |
| src/modules/hostBridge/server/hostBridgeServer.ts | 1944 | 问题 | P1: 鉴权前读满请求体且连接无上限（F1）；P2: 文件响应 operation 记为 unknown（F4）；P2: rotate 忽略 profile 写入结果（F7） |
| src/modules/hostBridge/server/hostHttpRequestReader.ts | 656 | 问题 | 见 F1：默认 16MB body 在鉴权前生效；无独立发现 |
| src/modules/hostBridge/server/runtimeHttpResponse.ts | 458 | 存疑 | `contentType` 直接拼入响应头，需确认 upload 路径是否传入客户端 Content-Type |

### F1 [P1][并发/资源] src/modules/hostBridge/server/hostBridgeServer.ts:1348 —— 未认证请求在鉴权前被完整读入最多 16MB，且连接数无上限
- 证据：
```
const request = await connection.requestRead.completion;
request.signal = connection.requestControl.signal;
if (connection.generation !== serverGeneration) {
  return;
}
const rawResponse = await handleHttpRequest(
```
（hostHttpRequestReader.ts:54 `maxBodyBytes: 16 * 1024 * 1024`；hostBridgeServer.ts:907 `if (!(await isHostBridgeAuthorizationValid(request.headers, state.token)))`）
- 分析：`processAcceptedConnection` 先等待请求体读取完成，再调用 `handleHttpRequest` 做 bearer 校验；reader 允许 16MB，`acceptedConnections` 无并发上限。LAN 暴露时未认证攻击者可并发发送大体积请求，造成内存/连接耗尽。
- 建议：鉴权前限制连接数与累计读入量，或让 reader 在校验前仅读 header 并在鉴权后按需消费 body。
- 置信度：高

### F2 [P1][信任边界/资源] src/modules/hostBridge/server/hostBridgeAuth.ts:312 —— 无效 bearer token 触发 PBKDF2 100000 次派生，未认证 CPU DoS
- 证据：
```
const masterToken = await readHostBridgeMasterToken();
return masterToken.ok && timingSafeEqualString(token, masterToken.token);
```
（hostBridgeAuth.ts:10 `const MASTER_PBKDF2_ITERATIONS = 100000;`）
- 分析：`isHostBridgeAuthorizationValid` 先用常规 token 比较，失败后无条件解密密文 master token；`readHostBridgeMasterToken` 每次都执行 100k PBKDF2 + AES-GCM。只要 master token 已配置，攻击者用随机 token 反复请求即可让服务器每次做高成本 KDF。
- 建议：缓存派生 key，或在派生/解密前增加按来源的限流；考虑用可快速比较的 HMAC token。
- 置信度：高

### F3 [P2][资源/隐私] src/modules/hostBridge/server/hostBridgeFileRegistry.ts:511 —— 上传文件过期/消费/释放时只删句柄，不删除磁盘临时文件
- 证据：
```
for (const fileId of lease.fileIds) {
  leaseByFileId.delete(fileId);
  if (consume) handles.delete(fileId);
}
```
（另见 hostBridgeFileRegistry.ts:175-177 `if (isExpired(handle, now) && !leaseByFileId.has(fileId)) { handles.delete(fileId); }`；hostBridgeFileRegistry.ts:286 `await writeRuntimeBytes(uploadPath, bytes, { overwrite: false });`）
- 分析：`bridge-upload` 写入 tmpDir 下的真实文件，但 `cleanupExpiredHandles`、`markHostBridgeUploadedFileConsumed`、`releaseHostBridgeUploadedFileLease` 都只从 Map 删句柄。上传文件会无限期留在磁盘，最多 16MB/个，既泄漏空间也保留敏感内容。
- 建议：对 `sourceKind === "bridge-upload"` 的 handle，在删除句柄时同步 unlink `localPath`；失败时记录清理告警。
- 置信度：高

### F4 [P2][正确性] src/modules/hostBridge/server/hostBridgeServer.ts:1113 —— 带 operationId 的文件下载响应被记为 outcome_unknown，重试返回 202 而非重放
- 证据：
```
const completed = operationResponseFromRaw(result);
if (completed) {
  completeHostBridgeOperation({ operationId, response: completed });
} else {
  markHostBridgeOperationOutcomeUnknown(operationId);
}
```
（hostBridgeServer.ts:570 `if (raw.kind !== "memory") return null;`）
- 分析：只要客户端在文件下载请求上带 `X-Zotero-Bridge-Operation-Id`，`operationReserved` 为 true，但 `operationResponseFromRaw` 对 `kind === "file"` 返回 null，于是成功下载被记录为 `outcome_unknown`。后续同 operationId 只能得到 202，而不是实际文件响应。
- 建议：文件响应也写一条可重放的 operation receipt，或在文件下载路由明确拒绝 operationId。
- 置信度：高

### F5 [P2][契约/SSOT] src/modules/hostBridge/server/hostBridgeCapabilityContract.ts:95 —— 运行时直接改写导入的 JSON 契约，使 JSON 文件不再是该 capability 输出 schema 的单一事实源
- 证据：
```
contract.capabilities["library.get_note_detail"] = {
  ...contract.capabilities["library.get_note_detail"],
  outputSchema: HOST_BRIDGE_NOTE_DETAIL_OUTPUT_SCHEMA,
};
```
- 分析：`capabilityContractJson` 是导入的模块级对象；这里在原地覆盖 `library.get_note_detail` 的 outputSchema。任何直接导入 `contracts/host-bridge/capabilities.v2.json` 的消费者会看到不同 schema，契约验证结果也取决于是否先加载本模块，破坏 SSOT。
- 建议：把覆盖后的 schema 移回 JSON 契约文件，或用显式投影在读取边界合并并保持唯一来源。
- 置信度：高

### F6 [P2][正确性/编码] src/modules/hostBridge/server/hostBridgePagination.ts:275 —— 文本分页按 UTF-16 code unit 切片，可能切断代理对
- 证据：
```
const chunk = text.slice(offset, offset + maxChars);
const nextOffset = offset + chunk.length;
const hasMore = nextOffset < text.length;
```
（hostBridgePagination.ts:282 `totalChars: text.length,`）
- 分析：`String.prototype.slice` 以 UTF-16 code unit 为单位；当 `maxChars`/`offset` 落在高-低代理对之间时，会返回未配对代理，JSON 序列化后客户端得到非法字符。`totalChars` 也把 code unit 数当字符数。
- 建议：按 code point 迭代分块（如 `Array.from` 或迭代器），或明确文档说明 offset 以 UTF-16 code unit 计并保证不切断代理对。
- 置信度：中高

### F7 [P2][资源] src/modules/hostBridge/server/hostBridgeServer.ts:1691 —— rotateHostBridgeToken 忽略 well-known profile 写入结果，且未处理 rejection
- 证据：
```
if (state.status === "running" && state.endpoint) {
  void writeHostBridgeWellKnownProfile({
    endpoint: buildLocalProfileEndpoint(state.bindMode, state.port),
    token: rotated.token,
    updatedAt: rotated.rotatedAt,
  });
}
```
- 分析：token 已在 prefs/state 中轮换，但 profile 写失败既不会更新 `lastError`，也不会阻止 rotation；CLI 仍可能读到旧 token 而认证失败。`void` 还会把 rejection 变成未处理拒绝。
- 建议：await 并检查 `result.ok`，失败时更新状态或回滚/重试。
- 置信度：中高

### F8 [P2][验证] src/modules/hostBridge/server/hostBridgeAdvertisedHostDetection.ts:81 —— advertised host 校验仅排除少数地址，接受 APIPA/多播/保留地址
- 证据：
```
return !(
  host === "0.0.0.0" ||
  host === "255.255.255.255" ||
  host === "127.0.0.1" ||
  host.startsWith("127.")
);
```
- 分析：`isUsableAdvertisedIPv4` 接受 `169.254.*`、`224.*`–`239.*`、`240.*` 等不可达或保留地址。反射端点若返回这些 client_ip，会被当作可用 advertised host，远程客户端得到不可连接的 endpoint。
- 建议：只接受私网/链路本地可路由的单播地址（10/8、172.16/12、192.168/16，必要时排除 169.254/16、多播和 240/4）。
- 置信度：高

### F9 [P3][错误处理] src/modules/hostBridge/server/hostBridgeMutationAdapter.ts:100 —— 主错误存在时 cleanupError 被静默丢弃
- 证据：
```
if (hasPrimaryError) {
  throw primaryError;
}
if (cleanupError !== undefined) {
  throw createWorkflowHostError(
    "execution_failed",
    "Prepared Host Bridge resource cleanup failed",
    { phase: "cleanup", recovery: "retry_same_operation" },
  );
}
```
- 分析：execute 失败且 `preparedFiles.dispose()` 也失败时，只抛 primaryError，cleanupError 不记录也不上报。资源泄漏在调用侧看起来像普通执行失败。
- 建议：把 cleanupError 作为 `cause`/详情附加到主错误，或至少写入可观测日志。
- 置信度：高

### F10 [P2][超时] src/modules/hostBridge/server/hostBridgeAdvertisedHostDetection.ts:145 —— fetch 反射端点无超时/AbortSignal，可无限挂起
- 证据：
```
response = await fetchImpl(reflectionEndpoint, {
  method: "GET",
  headers: {
    accept: "application/json",
  },
});
```
- 分析：调用方无法取消；后端接受连接但不返回响应时，该检测会一直 pending，可能卡住设置流程。
- 建议：传入 AbortSignal.timeout 或内部 race 超时，并返回稳定的 reflection_unavailable。
- 置信度：高

### F11 [P3][契约] src/modules/hostBridge/server/hostBridgeAuth.ts:251 —— 信封包含 iterations/kdf 但读取时忽略，未来轮换参数会使旧密文不可解
- 证据：
```
if (
  envelope.schema_id !== MASTER_TOKEN_SCHEMA_ID ||
  envelope.schema_version !== MASTER_TOKEN_SCHEMA_VERSION ||
  envelope.algorithm !== "AES-GCM"
) {
```
（hostBridgeAuth.ts:212 `iterations: MASTER_PBKDF2_ITERATIONS,`；hostBridgeAuth.ts:257 `const key = await deriveMasterTokenKey(base64ToBytes(envelope.salt));`）
- 分析：`envelope.kdf` 和 `envelope.iterations` 被写入但从不校验或用于派生；解密固定用当前常量。若以后修改 `MASTER_PBKDF2_ITERATIONS` 或 KDF，旧密文会静默变成 `decrypt_failed`。
- 建议：用 `envelope.iterations`/`envelope.kdf` 驱动派生并校验支持范围，或在 schema_version 升级时显式迁移。
- 置信度：高

### F12 [P3][重复] src/modules/hostBridge/server/hostBridgeProtocol.ts:228 —— capability entry 类型与 hostBridgeCapabilityContract.ts 重复定义，字段命名漂移
- 证据：
```
export type HostBridgeCapabilityManifestEntry = {
  name: string;
  category: HostBridgeCapabilityCategory;
  summary: string;
  approval: HostBridgeApprovalRequirement;
  requestEffect: "read" | "state-change";
```
（hostBridgeCapabilityContract.ts:39 `export type HostBridgeCapabilityContractEntry = { ... effect: HostBridgeRequestEffect; ... }`）
- 分析：同一能力描述 shape 有两套类型，`requestEffect` vs `effect` 已经漂移；新增字段需同步修改两处，容易造成 manifest 与 contract 不一致。
- 建议：从 contract entry 派生 manifest entry 类型，或统一为一个 shared DTO。
- 置信度：高