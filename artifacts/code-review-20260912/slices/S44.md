| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/modules/acp/skillRun/acpAgentFamilyResolver.ts | 279 | OK | 家族推断/技能根归一化；`normalizeAcpProjectSkillRoot` 显式拒绝绝对路径、盘符与 `..`，信任边界正确。 |
| src/modules/acp/skillRun/acpPermissionQueue.ts | 52 | OK | FIFO 队列语义清晰；重复 requestId 直接 resolve(cancelled)。 |
| src/modules/acp/skillRun/acpRuntimeDependencyWrapper.ts | 491 | OK | uv/system-python 探测与包装；Python 探测脚本用 `JSON.stringify` 嵌入，未发现可注入通道。 |
| src/modules/acp/skillRun/acpRuntimePromptTemplates.ts | 142 | 问题 | P3：与 acpSkillPatchTemplates.ts 大段重复（见 F7）。 |
| src/modules/acp/skillRun/acpSharedSkillCatalog.ts | 223 | 问题 | P2：复用分支下 resourceManifest 绝对路径来自源目录（见 F3）。 |
| src/modules/acp/skillRun/acpSkillMaterializer.ts | 109 | 存疑 | 本地 `readJsonFile` 无 try/catch，runner.json 缺失/损坏即抛错，而 acpThinProxySkillMaterializer 的同名函数容错返回 `{}`；需确认“请求技能缺 runner.json”是否应 fail-fast。 |
| src/modules/acp/skillRun/acpSkillOutputConvergence.ts | 210 | OK | JSON 候选提取与 `__SKILL_DONE__` 分支判定符合契约。 |
| src/modules/acp/skillRun/acpSkillOutputValidator.ts | 188 | 问题 | P2：相对 artifact 路径保留 `..`，可越出 workspaceDir（见 F1）。 |
| src/modules/acp/skillRun/acpSkillPatchTemplates.ts | 162 | 问题 | P3：模板加载/渲染逻辑与 acpRuntimePromptTemplates.ts 重复（见 F7）。 |
| src/modules/acp/skillRun/acpSkillReferenceRewriter.ts | 159 | 问题 | P3：`./assets/x` 形态既不改写也不告警（见 F6）。 |
| src/modules/acp/skillRun/acpSkillResourceManifest.ts | 67 | OK | 纯投影，`runtimeTreeManifest` 优先级导致的问题源自调用方（见 F3）。 |
| src/modules/acp/skillRun/acpSkillResultFileFallback.ts | 158 | OK | 候选按 mtime/深度/字典序选择，多候选有告警；逻辑自洽。 |
| src/modules/acp/skillRun/acpSkillRunActions.ts | 1100 | 存疑 | `cancelAcpSkillRun` 只 unregister run controller，未 `unregisterAcpSkillRunSetupController`（shutdown 路径有做）；且 cancel 设置 `removedAt` 使其从默认列表消失，需确认语义。 |
| src/modules/acp/skillRun/acpSkillRunAuditTrail.ts | 723 | 存疑 | `append*` 系列受 `shouldWriteDetailedAcpAuditArtifacts()` 门控，但 `writeAcpSkillRunAuditPrompt/StderrTail/RuntimeLogs/FinalState` 无门控且 prompt 无条件写入 runtimeDir；与 README“debug-only”描述存在漂移，需确认是否有意。 |
| src/modules/acp/skillRun/acpSkillRunControllerRegistry.ts | 108 | OK | null 注册走统一清理（计时器/权限队列）；setup controller 身份校验正确。 |
| src/modules/acp/skillRun/acpSkillRunExecutionSupport.ts | 1275 | OK | 硬超时监视器 pause/resume/triggered 语义自洽；MCP 预检、运行时选项解析未见缺陷。 |
| src/modules/acp/skillRun/acpSkillRunForeground.ts | 98 | OK | 仅 `void` 触发 selection/sidebar（潜在 unhandled rejection，影响低）。 |
| src/modules/acp/skillRun/acpSkillRunHosts.ts | 126 | OK | 叶子模块 host 槽，避免 store/host 循环 TDZ，设计正确。 |
| src/modules/acp/skillRun/acpSkillRunInteractionFiles.ts | 313 | OK | 分阶段 staging + 临时目录失败清理 + in-flight 去重；文件名净化/冲突处理完整。 |
| src/modules/acp/skillRun/acpSkillRunPayloadStore.ts | 166 | 问题 | P3：run-context.json 读-改-写无串行化（见 F8）。 |
| src/modules/acp/skillRun/acpSkillRunPermissionFacade.ts | 29 | OK | 单处理器注册，语义单一。 |
| src/modules/acp/skillRun/acpSkillRunPermissionQueue.ts | 314 | OK | stale 清理与 active 匹配逻辑完备，非法 resolve 抛错。 |
| src/modules/acp/skillRun/acpSkillRunPromptBuilder.ts | 326 | OK | 按 family 选择引擎目录/指令文件/调用行；Hermes 分支单独处理。 |
| src/modules/acp/skillRun/acpSkillRunRequestAdapter.ts | 140 | OK | upload_files/绝对路径/相对上传路径校验完整。 |
| src/modules/acp/skillRun/acpSkillRunRuntimeCatalog.ts | 86 | OK | 归一化 + 变更发射，无副作用泄漏。 |
| src/modules/acp/skillRun/acpSkillRunState.ts | 54 | OK | 共享可变状态集中，注释与代码一致。 |
| src/modules/acp/skillRun/acpSkillRunStatus.ts | 112 | OK | 终态/可恢复判定为纯函数，边界清晰。 |
| src/modules/acp/skillRun/acpSkillRunTaskProjection.ts | 78 | OK | 纯投影，字段默认值合理。 |
| src/modules/acp/skillRun/acpSkillRunTranscriptStore.ts | 996 | 问题 | P2：`transcriptIndexStates` 无淘汰，跨 run 无界增长（见 F4）。 |
| src/modules/acp/skillRun/acpSkillRunWorkspaceDataPlane.ts | 759 | 存疑 | `emitWorkspaceChanged` 同步遍历监听器且无 try/catch，某监听器抛错会中断后续监听并回灌到调用方（store/selection）链路；需确认监听器是否保证不抛。 |
| src/modules/acp/skillRun/acpSkillRunWorkspaceSelection.ts | 54 | OK | owner-first：先写 selected 再 prune 再 emit。 |
| src/modules/acp/skillRun/acpSkillRunnerWorkspace.ts | 237 | OK | 复用/新建命名空间分配与扫描一致。 |
| src/modules/acp/skillRun/acpSkillSchemaAssets.ts | 571 | 存疑 | parameter 分支用原始 `requestParameter` 做 AJV 校验，而返回的 `parameterContext` 已被 `properties` 过滤（input 分支则校验过滤后的 `inputContext` 并对未知键报错）；不一致是否有意需确认。 |
| src/modules/acp/skillRun/acpStartupPromptPreambles.ts | 81 | OK | 模板必需占位符校验到位。 |
| src/modules/acp/skillRun/acpThinProxySkillMaterializer.ts | 563 | 问题 | P2：full-snapshot 分支复制前不清理 targetDir（见 F2）；P3：runner.json 每 root 重复读（见 F5）。 |

### F1 [P2][信任边界] src/modules/acp/skillRun/acpSkillOutputValidator.ts:23 —— 模型输出的相对 artifact 路径保留 `..`，可越出 workspaceDir 读文件
- 证据：
```ts
function normalizeRelativeArtifactPath(value: string) {
  return normalizeSlashes(value)
    .replace(/^\.\/+/, "")
    .split("/")
    .filter((segment) => segment && segment !== ".")
    .join("/");
}
```
```ts
  return readRuntimeTextFile(
    joinPath(workspaceDir, normalizeRelativeArtifactPath(rawPath)),
  );
```
- 分析：过滤只去掉空段和 `.`，`..` 原样保留，`../../...` 会经 `joinPath(workspaceDir, "..")` 拼出 workspace 之外路径并被实际读取（`collectOutputBundleArtifactPaths` 以 `allowAbsolutePaths: true` 调用该 reader）。触发条件：最终 payload 是模型产出，artifact 字段路径由模型决定；同仓库 acpSkillSchemaAssets.ts:74-81 的 `splitSafeRelativePath` 明确拒绝 `..`，说明这是有意的边界而不是不必要的严格。另 `isAbsolutePath(rawPath)` 分支直接读任意绝对路径，进一步放宽了读取面。
- 建议：对相对路径复用 `splitSafeRelativePath` 式校验，拒绝任何 `..` 段；绝对路径分支仅在显式白名单（workspace/result 根）下放行或改为返回诊断错误。
- 置信度：中

### F2 [P2][正确性] src/modules/acp/skillRun/acpThinProxySkillMaterializer.ts:508 —— full-snapshot 分支复制前不清理目标目录，复用 workspace 下会残留旧文件
- 证据：
```ts
      if (shouldUseFullSnapshot(runnerJson)) {
        await copyRuntimeDirectory({
          sourceDir: entry.catalogSkillRoot,
          targetDir,
        });
```
对照同文件 422 行的 proxy 分支：`await removeRuntimePath(args.targetDir);`
- 分析：`materializeAcpThinProxySkills` 的目标是 `<workspaceDir>/.agents/skills/<skillId>` 这类共享技能根；workflow workspace `reuse` 模式下同一 skillId 会被多次物化（`acpSkillRunnerWorkspace.createAcpSkillRunnerWorkspace` 的 reuse 分支只换 `.acp`/`result` 命名空间，技能根不变）。proxy 分支会先删除目标目录，full-snapshot 分支不会，因此复制可能合并进旧内容或直接失败，导致 harness 看到上一轮残留（含已删除引用）。`appendFeedbackPatchToSnapshotSkill` 在复制后读取 `SKILL.md` 也假定目标为“新拷贝”。
- 建议：full-snapshot 分支在 `copyRuntimeDirectory` 前同样执行 `removeRuntimePath(targetDir)`，或让 `copyRuntimeDirectory` 支持“先清空”语义并在两处统一。
- 置信度：中

### F3 [P2][契约] src/modules/acp/skillRun/acpSharedSkillCatalog.ts:116 —— 复用目录时 resourceManifest 的绝对路径来自插件源目录而非 catalog
- 证据：
```ts
    if (!canReuse || !(await runtimePathExists(catalogSkillRoot))) {
      catalogTree = entry.runtimeTreeManifest
        ? await copyRuntimeTree({
            manifest: entry.runtimeTreeManifest,
            targetDir: catalogSkillRoot,
          })
        : await copyRuntimeDirectory({
```
```ts
    const resourceManifest = await buildAcpSkillResourceManifest(
      catalogEntry,
      catalogTree,
    );
```
- 分析：复用命中时 `catalogTree` 保持 `undefined`；`catalogEntry` 由 registry entry 展开，其 `runtimeTreeManifest` 仍指向插件源目录，`buildAcpSkillResourceManifest` 的 `runtimeTreeManifest || entry.runtimeTreeManifest || scanRuntimeTree(entry.sourceDir)` 会命中这一份，于是 `files[].absolutePath` 指向源目录（未走 catalog 拷贝）。该 manifest 会被写入 index.json（162-171 行）与 proxy 的 `zotero-skill-proxy.json`，并被后续 reader 当作 catalog 事实使用，形成“同一次构建结果随复用与否不同”的契约漂移。
- 建议：复用分支在生成 manifest 时显式传入以 `catalogSkillRoot` 为根的树（例如对该目录 `scanRuntimeTree`），或写入 index 后不要从 registry 的 manifest 回填绝对路径。
- 置信度：中

### F4 [P2][并发/资源] src/modules/acp/skillRun/acpSkillRunTranscriptStore.ts:78 —— `transcriptIndexStates` 只增不删，跨 run 无界占用内存
- 证据：
```ts
const transcriptIndexStates = new Map<string, TranscriptIndexState>();
```
```ts
export function resetAcpTranscriptWritesForTests() {
  for (const key of transcriptWriteKeys) {
    discardBufferedWriteKey(key);
  }
  transcriptWriteKeys.clear();
  transcriptIndexStates.clear();
}
```
- 分析：全文只有测试用 `resetAcpTranscriptWritesForTests`（851 行）与 `rebuildAcpSkillRunTranscriptIndex` 的 `.set`（989 行）操作该 Map，生产路径没有 `delete`/淘汰。每个 state 持有 `AcpSkillRunTranscriptIndex`，其中每个 item 的 `eventOffsets/eventLengths` 按事件数增长，等价于把该 run 的全部 transcript 事件以数字数组形式常驻内存；长期运行（多个 run）会线性累积，且与 `pruneInactiveAcpSkillRunTranscriptMirrors` 对 mirror 的 LRU 策略不一致。
- 建议：给 `transcriptIndexStates` 加容量上限（LRU 或按 `flushBufferedWriteOwner`/run 结束时机删除），并保留 index 文件作为冷数据来源。
- 置信度：中

### F5 [P3][性能] src/modules/acp/skillRun/acpThinProxySkillMaterializer.ts:503 —— runner.json 被按“技能根 × 技能”重复读取与解析
- 证据：
```ts
  for (const root of args.injectionPlan.skillRoots) {
    for (const entry of args.catalog.entries) {
      const targetDir = joinPath(root, entry.skillId);
      const runnerJson = await readJsonFile(entry.runnerJsonPath);
```
- 分析：`entry.runnerJsonPath` 与 root 无关，但每个 root 都重新读盘并 `JSON.parse` 一次，读盘次数 = roots × entries；同时每个条目都要重写 SKILL.md 与 proxy 元数据。catalog 越大、技能根越多，物化阶段 IO 成倍放大。
- 建议：在进入 root 循环前一次性构建 `Map<skillId, runnerJson>` 并复用。
- 置信度：高

### F6 [P3][正确性] src/modules/acp/skillRun/acpSkillReferenceRewriter.ts:44 —— `./assets/x` 形态的引用既不被改写也不产生告警
- 证据：
```ts
  const pattern = new RegExp(
    `(^|[^A-Za-z0-9_:/\\\\.-])${args.prefix}/([^\\s'")<>]+)`,
    "g",
  );
```
（`collectUnresolvedReferences` 84-85 行使用同一前导字符排除集）
- 分析：前导字符类排除了 `.` 与 `/`，因此 `./assets/foo`、`../assets/foo`、`x/assets/foo` 这类引用不会被改写；而 collect 阶段同样排除前导 `.`/`/`，所以既不改写也不进 warnings。proxy 的 SKILL.md 只含 SKILL.md 自身，未被改写为 catalog 绝对路径的相对引用会解析到不存在的路径，问题被静默吞掉。
- 建议：把 `./` 前缀单独归一化后再匹配，或在 collect 阶段放宽前导字符限制并对未解析引用（含 `./` 形态）一律产出 warning。
- 置信度：中

### F7 [P3][可维护] src/modules/acp/skillRun/acpRuntimePromptTemplates.ts:89 —— 模板加载/渲染逻辑与 acpSkillPatchTemplates.ts 完全重复
- 证据：
```ts
async function readTemplateFromNode(template: AcpRuntimePromptTemplate) {
  const path = joinPath(
    getRuntimeCwd(),
    ACP_RUNTIME_PROMPT_TEMPLATE_ROOT,
    template.filename,
  );
```
对照 acpSkillPatchTemplates.ts:109-119 的 `readTemplateFromNode`（仅常量名不同），两文件另有 `getRuntimeCwd`/`hasNodeRuntime`/`resolveChromeTemplateUri`/`readTemplateFromChrome`/`load*Template`/`render*Template` 六处成对重复。
- 分析：同一 shape 出现两处，任何一处修复（如 chrome fetch 缓存、路径分隔符、占位符转义）都需同步改两份，属于典型漂移源；两处实现目前逐字相同，重复代价可量化（约 60 行 ×2）。
- 建议：抽出一个私有的 template-loader 工厂（chrome/node 双源 + 占位符渲染），两个模块只保留各自的 root 常量与 ID 联合类型。
- 置信度：高

### F8 [P3][并发] src/modules/acp/skillRun/acpSkillRunPayloadStore.ts:63 —— run-context.json 读-改-写未串行化（判断）
- 证据：
```ts
  const existing = await readAcpSkillRunContextPayload(args.runtimeDir);
  const merged: Omit<AcpSkillRunContextPayload, "schema" | "updatedAt"> = {
    ...(existing || {}),
  };
```
- 分析：`writeAcpSkillRunContextPayload` 是“读整文件 → 合并 → 整文件覆盖写”。同一 runtimeDir 两次写入若 await 点交错（不同 field 批量落盘），后写者会以旧快照覆盖先写者的新增字段，造成字段丢失；文件本身也只有普通写，没有复用同仓库的 buffered-write/owner 串行机制（transcript、audit 都用了）。当前调用点是否天然串行未在本切片可见，故标为判断。
- 建议：按 runtimeDir 用同一 key 的 owner 串行写（如 `enqueueBufferedWrite`/审计 core 的 owner 语义），或在内存持有已合并快照避免每次重读。
- 置信度：低

---

SLICE: S44
FILES: 35 个 / 10550 行；ledger: /tmp/review/ledger/S44.md
COVERAGE: 已细读 35 个（全部）；略读 0 个（数据/模板常量未单独展开，已计入对应文件）
TOP FINDINGS（只列 P0/P1/P2，按严重度排序）:
- [P2][信任边界] acpSkillOutputValidator.ts:23 相对 artifact 路径保留 `..`，模型输出可越 workspaceDir 读取文件
- [P2][正确性] acpThinProxySkillMaterializer.ts:508 full-snapshot 复制前不清理 targetDir，复用 workspace 下残留旧文件
- [P2][契约] acpSharedSkillCatalog.ts:116 复用分支 resourceManifest 绝对路径取自插件源目录而非 catalog
- [P2][资源] acpSkillRunTranscriptStore.ts:78 `transcriptIndexStates` 只增不删，跨 run 无界占用内存
NOISE: ①`acpSkillRunAuditTrail` 中 prompt/stderr/final-state 未受 debug 门控，与 README “debug-only” 描述漂移；②`emitWorkspaceChanged` 同步遍历监听器无 try/catch，监听器抛错会中断整条 store/selection 链路；③`acpSkillSchemaAssets` parameter 校验用原始对象、input 用过滤后对象，两分支语义不一致。