| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/providers/skillrunner/client.ts | 1424 | 问题 | P2: poll 无上限/不可取消（F2）；P2: request_id 未编码插值进 URL（F3）；P3: 失败路径 20 次投机 IO（F8） |
| src/providers/skillrunner/errors.ts | 93 | OK | 纯类型/判定辅助；status 提取有 `typeof object` 守卫，无静默吞错 |
| src/providers/skillrunner/managementClient.ts | 1101 | 问题 | P3: `limit` 非数值生成 `limit=NaN`（F9）；P3: 两份完全相同的 history payload 类型（F7） |
| src/providers/skillrunner/modelCache.ts | 675 | 问题 | P3: pref 读-改-写无串行化，重叠刷新会丢条目（F6）；模型 DTO/解析与 modelCatalog 重复（F4） |
| src/providers/skillrunner/modelCatalog.ts | 744 | 问题 | P3: `SkillRunnerModelEntry` 与 modelCache 的 `SkillRunnerModelCacheModel` 同形重复（F4）；其余为静态模型表 |
| src/providers/skillrunner/provider.ts | 683 | 问题 | P2/P1-conditional: 选项守卫会拒绝 normalize 后未回显的合法 key（F1）；P3: 三个完全相同返回分支（F5） |
| src/providers/skillrunner/skillPackageBundler.ts | 120 | 存疑 | `registry.entriesById[skillId]` 为无 hasOwnProperty 的字典下标；目录遍历未见符号链接/环防护；整树读入内存后同步打包 |
| src/providers/skillrunner/uploadMapping.ts | 72 | OK | 相对路径构造 + 绝对路径判定；非法段由下游 `ensureUploadRelativePath` 拒绝 |
| src/providers/skillrunner/zipTransport.ts | 168 | OK | 逐字节核对 local/central/EOCD 布局与 CRC32 正确；入口名做 `..`/绝对路径清洗 |
| src/sidebar/components/ActionControls.tsx | 269 | OK | pending/aria-busy 走 DOM 侧属有意设计；键盘 roving 索引有 values.length 保护 |
| src/sidebar/components/BannerRegion.tsx | 219 | OK | 边界恰为 {context,lifecycle}，符合既有守卫；onAction 稳定由调用方保证 |
| src/sidebar/components/ContextDrawerRegion.tsx | 562 | OK | container.onclick 闭包捕获首帧 onAction（与 imperative 原实现一致，调用方传稳定句柄） |
| src/sidebar/components/DetailsDrawerRegion.tsx | 195 | OK | 折叠态走原生 `open`/class，未进入 selection；overlay dismiss 与旧实现同构 |
| src/sidebar/components/EmptyStateRegion.tsx | 16 | OK | — |
| src/sidebar/components/HintRegion.tsx | 440 | OK | 边界仅 interaction；labelOf 有意在边界外 |
| src/sidebar/components/MessageCountsRegion.tsx | 95 | OK | 保留上次 items 的 quirk 与旧实现一致；见 F7（其 selection 来自 regionEquality） |
| src/sidebar/components/PermissionDrawerRegion.tsx | 127 | OK | open 进入边界（与 details/context 不同）符合注释；关闭时残留 onclick 因 hidden 不可达 |
| src/sidebar/components/PlanRegion.tsx | 132 | OK | 隐藏保留最后可见 DOM，同旧实现 |
| src/sidebar/components/ReplyRegion.tsx | 288 | OK | 双层结构/实时边界与 textarea 非受控同步逻辑正确，焦点态不回写 |
| src/sidebar/components/ToolbarRegion.tsx | 56 | OK | — |
| src/sidebar/components/TranscriptRegion.tsx | 75 | OK | 只吃 state/message/mode/ownerKey，未引入 transcript revision/chunk 计数 |
| src/sidebar/components/ViewModeToggle.tsx | 63 | OK | — |
| src/sidebar/components/chromeRenderer.ts | 297 | OK | 每区域独立 render 到自己的 mount；permission overlay 首次创建后复用 |
| src/sidebar/components/regionEquality.ts | 200 | 问题 | P3（判断）: `revision` 进入 messageCounts 签名但渲染未使用，chunk 级重渲染（F7）；其余 selection 未混入他区域计数 |
| src/sidebar/components/replyHistory.ts | 131 | OK | 上限 50/键，键受 kind\|context\|action 约束，越界与草稿恢复逻辑正确 |

### F1 [P2][契约/正确性] src/providers/skillrunner/provider.ts:482 —— 选项守卫会把 normalize 后未回显的合法 key 判为"不可用"并终止执行
- 证据：
```
      if (
        !Object.prototype.hasOwnProperty.call(
          normalizedProviderOptions,
          optionKey,
        ) ||
```
（配合 `const rawLegacyModelProvider = source.model_provider;` 与 391-442 的返回对象中只回显 `hard_timeout_seconds`（当且仅当解析出正整数））
- 分析：循环遍历的是**原始** `args.providerOptions`，而判定用的是 `normalizeRuntimeOptions` 的输出。凡是「原始存在、归一化后被丢弃」的 key 都会命中第一条分支抛 `provider_profile_option_unavailable`：(a) `hard_timeout_seconds: ""`——schema 自己在 200-203 行明确写 "Empty means backend default"、placeholder 也要求留空，但 `normalizePositiveInteger("")` 返回 undefined 导致该 key 不回显，于是留空即直接失败；(b) 特性开关关闭时的 `interactive_auto_reply`/`interactive_reply_timeout_sec`；(c) 归一化函数显式支持读取的遗留别名 `model_provider`（282 行读、返回对象只给 `provider_id`）。触发即整个 run 在 provider 入口硬失败，无任何降级。若上游确实会把空字符串/遗留键传进 `execute`，本条为 P1。
- 建议：守护条件改为「原始 key 归一层级不识别时才拒绝」，或让 `normalizeRuntimeOptions` 对空值 key 回显 `undefined`/缺省值；对 `model_provider` 在回显中保留别名。
- 置信度：中（代码事实高；调用方是否会传空串/遗留键无法在本切片内证实）

### F2 [P2][并发/资源] src/providers/skillrunner/client.ts:713 —— 轮询循环无总时限且不可取消，0 间隔会变成忙等
- 证据：
```
    const intervalMs = Math.max(0, request.poll?.interval_ms ?? 2000);
    let pollRetry = 0;
    while (true) {
```
```
      pollRetry += 1;
      await sleep(intervalMs);
```
- 分析：`executePollStep` 仅在 backend 给出 succeeded/failed/canceled/waiting 时退出，没有任何尝试次数或截止时间；`getJobState`（749-796）从不设置 signal，`fetchWithTimeout` 只透传在 upload/bundle/result 里也从未赋值的 `args.init.signal`，因此即便上层想取消，也只能等 backend 自己终止，`executeHttpSteps`/`settleExistingRun` 的 await 链会永久挂住。`interval_ms: 0`（`Math.max(0, …)`）时退化为无退避忙等，直接吃满 CPU 并持续打 get-state 请求。
- 建议：为轮询加总 deadline（与 `hard_timeout_seconds` 对齐）+ 最小间隔下限（如 250ms），并把 AbortSignal 从 provider 入口贯穿到 `getJobState`。
- 置信度：高（行为事实）/ 影响中

### F3 [P2][信任边界] src/providers/skillrunner/client.ts:42 —— 后端返回的 request_id 未编码就插入 URL 路径
- 证据：
```
function interpolatePath(template: string, values: Record<string, string>) {
  return template.replace(
    /\{([^}]+)\}/g,
    (_, key: string) => values[key] || "",
  );
}
```
使用点：`interpolatePath(step.request.path, { request_id: requestId })`（651 行）、`getJobState`（756 行）、bundle/result（802/909 行）
- 分析：`requestId` 来自 create 步骤响应 `$.request_id`（579-584），即由（可远端、可被攻陷的）backend 决定。含 `../`、`?`、`#` 的 id 会改变后续请求的实际 path/query，使客户端带着 `content-type` 等头访问非预期端点；同一仓库的 `managementClient` 对同类输入使用 `encodeURIComponent(requestId)`（701/733/818 等），两处契约不一致，说明这里属遗漏而非有意。
- 建议：统一走 `encodeURIComponent`（或对 `interpolatePath` 的替换值编码）。
- 置信度：中

### F4 [P3][契约] src/providers/skillrunner/modelCatalog.ts:13 —— 模型 DTO 与解析逻辑在两文件重复定义
- 证据：
```
type SkillRunnerModelEntry = {
  id: string;
  display_name: string;
  deprecated?: boolean;
  provider_id?: string;
  provider?: string;
  model?: string;
  supported_effort?: string[];
};
```
（modelCache.ts:8-16 的 `SkillRunnerModelCacheModel` 字段逐一相同；modelCache.ts:104-177 `parseModelCacheEntry` 与 300-340 `parseEngineModelsPayload` 是同一套 id/provider/model/supported_effort 归一化；modelCache.ts:52-73 `splitProviderModel` 与 modelCatalog.ts:473-494 `splitSkillRunnerModelId` 同形）
- 分析：catalog 与 cache 是同一份模型数据的生产/消费两端，形状与归一化规则已出现 ≥3 处副本，任一侧新增字段（例如新的 effort 语义）都会静默漂移；离线快照（MODEL_SNAPSHOTS）与在线缓存走不同解析路径时行为可能不一致。
- 建议：把模型条目类型与归一化函数提到单一模块（如 modelCatalog），modelCache 只做持久化。
- 置信度：高

### F5 [P3][可维护性] src/providers/skillrunner/provider.ts:391 —— 三个返回分支内容完全相同
- 证据：
```
    if (typeof rawNoCache === "boolean") {
      return {
        engine: normalizedEngine,
        provider_id: normalizedProviderId,
```
（409 行 `if (typeof rawNoCache === "string")`、427 行无守卫 return 的 body 与本分支逐字段一致）
- 分析：`normalizedNoCache` 已在 378 行归一化完成，分支对结果没有任何影响，属死分支；后续任何字段调整都要改三处，是典型漂移源。
- 建议：删除前两个分支，只保留单一 return。
- 置信度：高

### F6 [P3][并发] src/providers/skillrunner/modelCache.ts:393 —— 整份 pref 的读-改-写无串行化
- 证据：
```
  const current = readModelCacheDocument();
  const nextEntries = current.entries.filter(
    (row) => row.backendId !== normalized.backendId,
  );
  nextEntries.push({
```
- 分析：`upsertSkillRunnerModelCacheEntry` 是「读全量文档 → 过滤 → push → 写回全量」。`refreshSkillRunnerModelCaches` 内是顺序 await，但 640 行的定时器与手动刷新可以并发，两个 backend 的刷新交错时后写者会用旧快照覆盖前者，导致某 backend 的缓存条目丢失（随后回落静态快照或空）。
- 建议：进程内串行化（单 promise 链/互斥），或把每个 backend 的条目拆分存储。
- 置信度：中

### F7 [P3][契约] src/providers/skillrunner/managementClient.ts:82 —— 两份完全相同的 history payload 类型
- 证据：
```
export type SkillRunnerManagementChatHistoryPayload = {
  request_id: string;
  count: number;
  events: Array<Record<string, unknown>>;
  cursor_floor: number;
  cursor_ceiling: number;
  source?: string;
};
```
（91-98 行 `SkillRunnerManagementEventHistoryPayload` 逐字段相同；listRunChatHistory 746-753 与 listRunEventHistory 799-806 也是同一份取值代码）
- 分析：两个端点共用结构却没有共享类型/解析函数，字段演进时容易只改一处。
- 建议：合并为一个 `SkillRunnerManagementHistoryPayload` 并抽出共用的 normalize 函数。
- 置信度：高

### F8 [P3][健壮性] src/providers/skillrunner/client.ts:209 —— 终态 bundle 解析在失败路径做最多 20 次投机读
- 证据：
```
    for (let index = 1; index <= 20; index += 1) {
      add(`result/${skillSegment}.${index}/result.json`);
    }
```
- 分析：`normalizeBundleTerminalResult` 对每个候选 `await bundleReader.readText(candidate)`，全部失败时是串行 20+ 次（含 zip 条目查找/解压）IO，且把每个错误字符串拼进最终错误消息；成功路径通常命中最前候选，代价落在真正出错的场景（放大失败延迟与日志体积）。
- 建议：先用 bundle 的条目索引（一次列目录）定位 `result/**/result.json`，再按候选校验。
- 置信度：高

### F9 [P3][边界] src/providers/skillrunner/managementClient.ts:627 —— 非数值 limit 生成 `limit=NaN`
- 证据：
```
    const limit = Math.max(1, Math.min(1000, Number(args?.limit || 200)));
    query.set("limit", String(limit));
```
- 分析：`Number("abc")` 为 NaN，`Math.min/Math.max` 对 NaN 的传播结果是 NaN，于是查询串写成 `limit=NaN`，源端行为未定义；同类归一化在别处（如 modelCache `normalizeTimeoutMs`）都有 `Number.isFinite` 守卫，这里缺失。
- 建议：加 `Number.isFinite` 回退到默认值，再 clamp。
- 置信度：高

### F10 [P3][判断][可维护性] src/sidebar/components/regionEquality.ts:89 —— `revision` 进入 messageCounts 签名但不参与渲染
- 证据：
```
    completeness: counts.completeness,
    revision: counts.revision,
    labels: panelTranscriptLabels(panel),
```
（MessageCountsRegion 的 items memo 只比较 `counts`，渲染只读 current/cumulative/completeness/labels）
- 分析：判断性建议而非已证实缺陷——该 region 的 memo 边界因此对不可见的 revision 变化敏感（若 revision 随每个 streaming chunk 递增，则每 chunk 触发一次该 region 重渲染；其子项 memo 会吸收 DOM 变更，代价主要是外层 render）。与「region 只以自己的可见数据比较 signature」的约束方向相反。
- 建议：从 messageCounts 的 equality input 中移除 `revision`（或确认其确为 region 可见数据后再保留）。
- 置信度：中

---

SLICE: S36
FILES: 25 个 / 8245 行；ledger: /tmp/review/ledger/S36.md
COVERAGE: 已细读 25 个（含 4 个静态数据/类型表按数据快速过：modelCatalog 的 MODEL_MANIFESTS/MODEL_SNAPSHOTS 逐项核对了 engine 对应关系）；略读 0 个。
TOP FINDINGS（只列 P0/P1/P2，按严重度排序，每条 ≤ 25 词）:
- [P2][契约] provider.ts:482 选项守卫拒绝 normalize 后未回显的 key：空 `hard_timeout_seconds`、遗留 `model_provider`、开关关闭的 auto-reply 键都会硬失败。
- [P2][并发] client.ts:713 轮询 `while(true)` 无 deadline 无 AbortSignal，backend 不终态即永久挂起；`interval_ms:0` 退化忙等。
- [P2][信任边界] client.ts:42 后端返回的 request_id 未编码直插 URL 路径（managementClient 同场景用 encodeURIComponent，两处契约不一致）。
NOISE:
- managementClient.ts:27 `new Function("specifier","return import(specifier)")` 仅为 import("util") 兜底、specifier 非外部输入，但 `new Function` 在受限 CSP 的插件页面可能被禁，建议复核该 fallback 的可达性。
- skillPackageBundler.ts:96 `registry.entriesById[skillId]` 为无 `hasOwnProperty` 的字典下标：`skillId="constructor"` 会命中原型属性，报错信息误导（需确认 entriesById 是否 `Object.create(null)`）。
- zipTransport.ts:150-161 multipart 的 filename/fieldName 未做引号/CRLF 转义（当前调用方只传常量名，属潜在而非已证实）。