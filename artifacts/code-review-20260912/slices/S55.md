## A. 文件级台账

| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/modules/workflow/catalog/builtinWorkflowSync.ts | 650 | 问题 | P3: 替换已提交后被清理失败回滚（F2）、catch 中清理失败吞掉主错误（F4）、manifest 路径未拒绝 `..`（F8）、6 个同形死包装函数（F7） |
| src/modules/workflow/catalog/contentPackageSubscription.ts | 1294 | 问题 | P3: `feed_mirror_mismatch` 死分支 + 镜像比对只看 `packages[0]`（F6）；存疑：`copyRuntimeDirectory` 是合并还是替换（陈旧文件残留）、`setContentPackageInstallProgress` 单测外的全局进度未被 install 驱动 |
| src/modules/workflow/catalog/pluginSkillRegistry.ts | 646 | 问题 | P2: `inspectCandidate` 的 IO/扫描异常未兜住，单目录失败使整次扫描 reject（F1）；优先级覆盖顺序与 workflowRuntime 一致，非缺陷 |
| src/modules/workflow/catalog/workflowPackageDiagnostics.ts | 203 | OK | 调试门控 + 逐字段 trim 的日志投影；`zoteroDebug(JSON.stringify)` 有 try/catch 兜底 |
| src/modules/workflow/catalog/workflowProductStore.ts | 1199 | 问题 | P2: `removeWorkflowProduct` 不回收对象树（F3）；P3: 单条 legacy 失败即整库 fail-closed（F5） |
| src/modules/workflow/catalog/workflowRequestKind.ts | 33 | OK | 声明式 kind 优先、显式抛错，无静默默认 |
| src/modules/workflow/catalog/workflowRuntime.ts | 655 | 存疑 | 需确认：`latestBuiltinSync` 类型/语义漂移（类型是 `ContentPackageInstallState`，实际只是 `latestContentInstall` 的别名）；技能依赖过滤只识别 `skillrunner.job.v1`/`sequence.v1`；生产路径读 `ZOTERO_TEST_WORKFLOW_DIR` 环境变量 |
| src/modules/workflow/catalog/workflowRuntimeBridge.ts | 79 | OK | 仅写 global/addon 桥；无授权语义，纯转发 |
| src/modules/workflow/catalog/workflowVisibility.ts | 28 | OK | 纯判定，`debug_only` 门控清晰 |

## B. Findings

### F1 [P2][错误处理] src/modules/workflow/catalog/pluginSkillRegistry.ts:409 —— 单个坏技能目录会让整次注册表扫描 reject，进而拖垮工作流注册表
- 证据：
```ts
  const directoryChecksum = await computeDirectoryChecksum(candidate.sourceDir);
```
```ts
  if (runtimeTreeManifest.issues.length) {
    throw new Error("skill runtime tree scan was incomplete");
  }
```
- 分析：`inspectCandidate` 只对 `readJsonFile` 和 schema 校验做了 catch，`computeDirectoryChecksum`（扫描不完整即 throw、`readRuntimeTextFile` 也可能 throw）与 `readSkillFrontmatter`（读取 SKILL.md）都是裸调用；`scanPluginSkillRegistryImpl` 的候选循环 `const inspected = await inspectCandidate(candidate);` 也无 try/catch。触发条件：任一候选目录下存在不可读文件/子目录、或扫描策略判定 incomplete（如异常条目），此时 `scanPluginSkillRegistry` 整—个 Promise reject；`workflowRuntime.rescanWorkflowRegistry` 里 `await scanPluginSkillRegistry({...})` 同样没有兜底，于是 rescan 整体失败、状态不更新，用户看到的是“工作流全部加载不出来”，而不是“跳过这一个坏技能”。注意 `collectCandidates` 是做了 try/catch 的，说明作者知道这里是 IO 边界，但 `inspectCandidate` 漏了。
- 建议：把 `inspectCandidate` 的调用包进 try/catch，失败时产出一条 `skill_scan_error`/`skill_candidate_invalid` 诊断并跳过该候选（`computeDirectoryChecksum` 失败也应如此）。
- 置信度：中

### F2 [P3][正确性] src/modules/workflow/catalog/builtinWorkflowSync.ts:507 —— 已完成的目录替换会被“删除备份失败”回滚，成功安装被报成失败
- 证据：
```ts
    await movePath(args.stagingRoot, args.targetRoot);
    await removeDirectory(backupRoot);
  } catch (error) {
```
```ts
      if (hadTarget && (await pathExists(backupRoot))) {
        await movePath(backupRoot, args.targetRoot);
      }
```
- 分析：`removeDirectory(backupRoot)` 属于提交后的清理步骤，但它处在同一个 `try` 里；一旦它抛错，catch 会把备份再搬回 `targetRoot`（`overwrite: true`），于是刚刚 move 进去的新版本被旧版本覆盖，而 staging 已被消费。触发条件是备份目录删除失败（占用/权限/瞬时 IO），结果等价于“什么都没更新但报了失败”，并且外层 `syncBuiltinWorkflowsOnStartup` 的 catch 再尝试 `removeDirectory(stagingRoot)`（已不存在），诊断信息指向清理而非真实原因。
- 建议：把 `removeDirectory(backupRoot)` 移出回滚 try（或单独 try/catch 只记录告警），只在 `movePath(stagingRoot, targetRoot)` 失败时回滚。
- 置信度：中

### F3 [P2][资源] src/modules/workflow/catalog/workflowProductStore.ts:503 —— 删除产品只删行、不删对象树，磁盘占用无界增长
- 证据：
```ts
export function removeWorkflowProduct(productIdRaw: string) {
  const productId = safeId(productIdRaw, "");
  if (!productId) return false;
  return deletePluginTaskRowEntry(
    PLUGIN_TASK_DOMAIN_WORKFLOW_PRODUCTS,
    productId,
  );
```
- 分析：对象内容写在 `objects/<productDigest>/<storageRevision>/<assetDigest>`（`objectsRoot()`/`assetObjectPath`）。本模块内针对这些目录的删除只出现在三处：`registerProduct` 删除被覆盖的旧 revision（`:903`）、失败回滚删除新 revision（`:920`）、legacy 迁移后删除 `oldRoot`（`:1115`）。`removeWorkflowProduct` 不计算也不删除 `await productObjectRoot(productId)`，因此每个被删除产品的全部资产永久残留；配合 `readRuntimeBytes` 落盘的是完整文件拷贝（非引用计数），长期使用必然无界增长。
- 建议：在删除行成功（durable 之后）删除 `await productObjectRoot(productId)`，失败只记诊断；或引入显式 GC 步骤回收无行引用的对象目录。
- 置信度：高

### F4 [P3][错误处理] src/modules/workflow/catalog/builtinWorkflowSync.ts:622 —— catch 内清理失败会顶掉主错误并跳过失败结果记录
- 证据：
```ts
  } catch (error) {
    await removeDirectory(stagingRoot);
    latestBuiltinWorkflowSyncResult = {
```
- 分析：`await removeDirectory(stagingRoot)` 在写入 `latestBuiltinWorkflowSyncResult` 之前且未加 catch；一旦清理抛错，函数直接以“清理错误”外抛，原始的写入/读取失败（含 `diagnostics`）既没有被 `throw`、也没有被记进 `latestBuiltinWorkflowSyncResult`，`getLatestBuiltinWorkflowSyncResult()` 仍是上一次的结果。相同模式也出现在 contentPackageSubscription 的 `installContentPackageFromFeed`（`:1249-1257` `finally { await removeRuntimePath(stagingRoot); }` 抛错会替换真实安装错误）。
- 建议：`removeDirectory(stagingRoot).catch(() => undefined)`，先记录/抛出主错误。
- 置信度：高（模式确定；触发需清理失败）

### F5 [P3][错误处理] src/modules/workflow/catalog/workflowProductStore.ts:1160 —— 单条 legacy 记录失败即整库 fail-closed，读全空、写全抛
- 证据：
```ts
      } catch {
        failed.push(row.taskId);
      }
```
```ts
    if (failed.length) {
      migrationStatus = {
        state: "failed",
```
- 分析：任一条 legacy payload 解析/迁移失败（例如 `migrateLegacyProduct` 里 `assertManagedRelativePath(source.relativePath || source.path || assetId)` 抛错、或 `legacy workflow product asset ownership is invalid`）都会把 `migrationStatus` 置为 failed；随后 `listWorkflowProducts()` 直接 `return []`（`:336`），`assertWorkflowProductStorageReady()` 对所有写入抛 `workflow_product_store_migration_incomplete`（`:1129-1136`）。该状态只能靠人工修 payload 解除，且每次注册都会重跑全量迁移再失败。判断：这是有意的 fail-safe，但“一条脏数据 = 整个产品功能不可用且不可自愈”代价偏大，且失败行本身不会从读取路径中剔除。
- 建议：对失败产品做逐条隔离（跳过并在 `failedProductIds`/诊断中暴露），仅对真正的存储级错误才整体 fail-closed。
- 置信度：中

### F6 [P3][正确性] src/modules/workflow/catalog/contentPackageSubscription.ts:880 —— `feed_mirror_mismatch` 分支不可达；镜像一致性只比对 `packages[0]`
- 证据：
```ts
      code:
        compactError(error) === "content feed mirror mismatch"
          ? "feed_mirror_mismatch"
          : "feed_unavailable",
```
```ts
function packageSignature(feed: ContentFeedDocument) {
  const entry = feed.packages[0];
```
- 分析：`resolveFeed` 只在 `successes.length === 0` 时 `throw new Error("all content feeds failed")`（`:778`），镜像不一致是 `failures.push({... error: "content feed mirror mismatch"})`（`:790-794`）后正常返回，因此 check 结果永远是 `feed_unavailable`（或 ok），`feed_mirror_mismatch` 这个 code 永远不会产生——调用方无法据此区分“断网”和“镜像被篡改/不同步”。另外 `packageSignature`、`latestPackage` 都只看 `packages[0]`，镜像若只在第 2..n 个包上不同，会被判为“一致”并静默接受。
- 建议：镜像不一致时给出稳定 code（例如通过带 code 的 error 对象或返回结构化 mismatch 标记）；若 feed 允许多包，签名应对全部包做规范化摘要。
- 置信度：中（第一点高；第二点取决于 feed 是否支持多包）

### F7 [P3][可维护性] src/modules/workflow/catalog/builtinWorkflowSync.ts:184 —— 三组同形包装函数各留两个死副本
- 证据：
```ts
async function removeDirectoryNode(targetDir: string) {
  await removeRuntimePath(targetDir);
}

async function removeDirectoryZotero(targetDir: string) {
  await removeRuntimePath(targetDir);
}
```
- 分析：`removeDirectoryNode/Zotero`、`pathExistsNode/Zotero`（`:200-206`）、`movePathNode/Zotero`（`:212-218`）与保留的同名函数体逐字相同，且前者在本文件内除声明处外零引用（本文件只用 `removeDirectory`/`pathExists`/`movePath`）。共 6 个死函数、3 组重复实现，读代码时无法判断跨 runtime 分支到底在哪一层切换（实际都指向 `runtimePersistence` 的同一实现）。
- 建议：删除这 6 个未引用包装，仅保留在用的同名函数。
- 置信度：高

### F8 [P3][信任边界] src/modules/workflow/catalog/builtinWorkflowSync.ts:485 —— manifest 条目未拒绝 `..`，`toLocalPath` 会写出 staging/target 之外
- 证据：
```ts
function toLocalPath(rootDir: string, posixRelativePath: string) {
  const segments = toPosixRelativePath(posixRelativePath).split("/");
  return joinPath(rootDir, ...segments);
}
```
```ts
        .map((entry) => toPosixRelativePath(String(entry || "")))
        .filter(Boolean)
```
- 分析：`toPosixRelativePath` 只做 trim/去空段，不剔 `..`/`.`，`parseBuiltinManifest` 也只过滤空字符串，于是 `manifest.json` 中形如 `"../../x"` 的条目会让 `writeTargetFile` 写到 staging 根之外（`joinPath(stagingRoot, "..", "..", ...)`），后续 `replaceTargetDirectory` 仍会照常提交。可达性取决于谁能提供 `manifest.json`：生产走 chrome:// XPI 资源（可信），但 `readPackagedTextWithDiagnostics` 还会从 `resourceURI`/`devCwd`（开发/本地目录）读取；同仓库的内容包路径校验 `normalizePackageEntryName`（`:929-968`）是显式拒绝 `..` 的，这里缺少同等校验，属防御深度不一致。
- 建议：在 `parseBuiltinManifest`/`toLocalPath` 复用 `normalizePackageEntryName` 式校验（拒绝绝对路径、盘符、`.`/`..` 段），非法条目直接使同步失败。
- 置信度：中

---

SLICE: S55
FILES: 9 个 / 4787 行；ledger: /tmp/review/ledger/S55.md
COVERAGE: 已细读 9 个（builtinWorkflowSync、contentPackageSubscription、pluginSkillRegistry、workflowPackageDiagnostics、workflowProductStore、workflowRequestKind、workflowRuntime、workflowRuntimeBridge、workflowVisibility）；略读 0 个
TOP FINDINGS（只列 P0/P1/P2，按严重度排序，每条 ≤ 25 词）:
- [P2][错误处理] pluginSkillRegistry.ts:409 单个坏技能目录让整次 registry 扫描 reject，工作流加载全挂
- [P2][资源] workflowProductStore.ts:503 removeWorkflowProduct 只删行不删 objects 树，磁盘无界增长
NOISE: 本切片最值得主代理复核的 3 个可疑点：
1) contentPackageSubscription 的 `copyRuntimeTreeTransactional` 是否会把 `content/official` 里已被新包移除的陈旧 workflow/skill 目录留下（`copyRuntimeDirectory` 是合并还是替换语义无法在本切片确认）。
2) 同一文件中 `setContentPackageInstallProgress`/`subscribeContentPackageInstallProgress` 从未被 `installContentPackageFromFeed` 驱动（它只调 `args.onProgress`），且 `refresh-registry`/`complete` 两个 stage 在本文件内不可达——需确认是否由切片外调用方补齐进度，否则进度条会停在 write-state。
3) workflowRuntime.ts:46-74 生产路径读取 `ZOTERO_TEST_WORKFLOW_DIR`（仅靠全局 `__zoteroSkillsDisableWorkflowDirOverride` 关闭），可重定向工作流加载目录，需确认是否符合测试 seam 约定。