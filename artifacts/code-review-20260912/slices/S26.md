## A. 文件级台账

| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| skills_builtin/literature-translator/scripts/blockify.py | 675 | 问题 | P2: 代码围栏闭合分支不可达（F1）；`state` 守卫缺失导致 `#`/`<table`/图片行可打断进行中的 code 块 |
| skills_builtin/literature-translator/scripts/build_block_sentences.py | 140 | 问题 | P2: BLOCK_START_RE 强制 heading 字段，与 blockify 可选输出不匹配 → 无 heading 块被静默丢弃（F2） |
| skills_builtin/literature-translator/scripts/concatenate.py | 196 | 问题 | P2: 译文 sentences 为空时块内容被静默删除且仍计为 applied（F7） |
| skills_builtin/literature-translator/scripts/export_alignment.py | 296 | 存疑 | 句对按位置配对（`tgt_sentences_raw[i]`），译文丢句时会整体错位，是否由上游 quality_gate 的 sentence_count 兜住需确认；mode.txt 解析与 quality_gate 重复（F13） |
| skills_builtin/literature-translator/scripts/export_qa_report.py | 210 | 问题 | P2: `--block-stats` 收 sentences.json 时 `total_blocks` 变 dict，第 110 行 `dict - int` 抛 TypeError（F3） |
| skills_builtin/literature-translator/scripts/parse_input.py | 319 | 问题 | P3 空文件 IndexError（F12）；P2 `\cite{key}` 不被剥离（F8）；P2 LaTeX 分支自造 BLOCK 标记与 sentencify 冲突（F9） |
| skills_builtin/literature-translator/scripts/partition_batches.py | 197 | OK | 依赖模块级 global `target_size`（main 里赋值、write_batch_files 里读）才能运行，属可测试性隐患，非缺陷 |
| skills_builtin/literature-translator/scripts/protect_placeholders.py | 339 | OK | 保护优先级与占位符自洽；`get_placeholder_map` 的 by_type 对 `<EQ_REF_001>` 这类两段式类型统计成 "EQ"（无消费者，仅元数据失真） |
| skills_builtin/literature-translator/scripts/quality_gate.py | 831 | 问题 | P3: `resolve_mode` 与 export_alignment 逐字重复、`check_language` 死代码（F13） |
| skills_builtin/literature-translator/scripts/restore_placeholders.py | 135 | 问题 | P2: 占位符表为空时 exit 0 且不产出任何文件，下游 concatenate 直接失败（F6） |
| skills_builtin/literature-translator/scripts/sentencify.py | 277 | 问题 | P2: list 块内换行被替换为空格 → 多行列表塌缩为单句、项目符号丢失（F10）；文件头 docstring 与 NO_SPLIT_TYPES 对 heading 的描述互相矛盾 |
| skills_builtin/manuscript-literature-framing/scripts/gate_runtime.py | 233 | OK | gate 检查字段集合与 stage_runtime 写入字段一一对应；状态机无非法跃迁 |
| skills_builtin/manuscript-literature-framing/scripts/runtime_state.py | 90 | OK | `write_json` 非原子（直接覆盖），单写者场景下可接受 |
| skills_builtin/manuscript-literature-framing/scripts/stage_runtime.py | 503 | OK | `require_run_root_path` 有路径逃逸守卫；payload 校验充分；错误以 traceback 而非 JSON 契约返回（低） |
| skills_builtin/tag-bootstrapper/scripts/normalize_output.py | 89 | OK | 非法输入返回 None 时不动原字段，交给 validate_output 报错，行为一致 |
| skills_builtin/tag-bootstrapper/scripts/validate_output.py | 94 | OK | 校验键集合与 normalize 输出形状一致 |
| skills_builtin/tag-regulator/scripts/normalize_output.py | 90 | 存疑 | 第 55 行用 input_tags 过滤 remove_tags：请求删除但不在 input_tags 中的标签被静默丢弃，是否符合语义待确认 |
| skills_builtin/tag-regulator/scripts/validate_valid_tags.py | 68 | OK | auto 模式回退合理；宽 except 仅用于探测格式 |
| skills_builtin/topic-planner/scripts/topic_planner.py | 258 | OK | 覆盖度分母/材料化节点/关系规范化检查完整，错误统一返回 code=2 的 JSON |
| skills_builtin/topic-synthesis-core-enrichment/scripts/gate.py | 127 | OK | 参数与 action 分派清晰，异常统一转 JSON 错误 |
| skills_builtin/topic-synthesis-core-enrichment/scripts/topic_synthesis_db.py | 4231 | 问题 | P2: handoff artifact key 拼写不一致导致静默丢路径（F4）、stage_state 主键只有 stage_id 跨 skill 互相覆盖（F5）、schema 校验器静默忽略未实现关键字（F11）、locator 重复实现（F14）、artifact 注册表为全局 dict 且丢弃 hash（F15） |

## B. Findings

### F1 [P2][正确性] skills_builtin/literature-translator/scripts/blockify.py:396 —— 代码围栏的闭合分支不可达，` ``` ` 结尾被当成新 code 块起点
- 证据：
```
        if CODE_FENCE_RE.match(stripped):
            flush_block()
            start_block("code", i)
```
```
        if state == "CODE":
            current_block.add_line(line)
            if CODE_FENCE_RE.match(stripped):
```
- 分析：第 396 行的围栏判断没有 `state` 守卫且排在 `if state == "CODE"`（404 行）之前，而闭合围栏行必然匹配 `CODE_FENCE_RE`，因此永远走 396 分支：当前 code 块被 flush，闭合围栏反而成为**新 code 块的开始行**，其后直到下一个围栏（或 EOF）的全部正文都被吞进 code 块。同一顺序问题还让 `classify_heading`（320 行，无 state 守卫）把代码里的 `# comment` 当成 heading，把 372 行 `<table`、430 行 `![...]` 当成块起点，直接打断进行中的 code 块。触发条件：normalized.md 含任意 fenced code block。影响：blocked.md 块边界错误 → 大段正文被判为 `code` 类型进入 NO_TRANSLATE/NO_SPLIT，既不翻译也不分句。
- 建议：把 `state == "CODE"` 的分支整体上移到围栏判断之前，并在 TABLE/IMAGE/heading 判断上加 `state in ("IDLE","PARAGRAPH",...)` 守卫。
- 置信度：高

### F2 [P2][契约] skills_builtin/literature-translator/scripts/build_block_sentences.py:16 —— BLOCK_START_RE 强制要求 heading 字段，与 blockify 的可选输出不匹配，导致无 heading 块被静默丢弃
- 证据：
```
BLOCK_START_RE = re.compile(
    r'<!-- BLOCK: (b_\d+) \| type: (\S+) \| heading: (.*) -->'
)
```
（blockify.py:86）`heading_attr = f" | heading: {self.heading}" if self.heading else ""`
- 分析：blockify 对 `heading == ""` 的块输出 `<!-- BLOCK: b_001 | type: paragraph -->`，该行无法匹配上面这个把 heading 组写成必需的正则；`parse_blocked_md` 于是既不开始块也不收集内容，这些块从 sentences.json 中消失且无任何报错（只有“全部块都缺失”才触发 `no blocks found`）。首个小节标题之前的正文（标题、摘要、front matter）恰好都是 heading 为空的块。同一切片里 concatenate.py:21 与 sentencify.py:26 都用了 `(?:\s*\|\s*heading:\s*(.*?))?` 可选组，本文件是唯一例外。另外本文件的 `parse_blocked_md` 末尾不像 sentencify.parse_blocks:111 那样 flush 未闭合块。
- 建议：heading 组改为可选 `(?:\s*\|\s*heading:\s*(.*?))?\s*-->`，并在循环结束后 flush 未闭合块。
- 置信度：高

### F3 [P2][契约] skills_builtin/literature-translator/scripts/export_qa_report.py:94 —— `--block-stats` 声明可传 sentences.json，实际会 TypeError
- 证据：
```
    total_blocks = block_stats.get("blocks",
                   block_stats.get("total_blocks", 0))
```
```
            "translated_blocks": total_blocks - passthrough,
```
- 分析：sentences.json 形状是 `{"format":"v2","blocks":{...},"stats":{...}}`。传入该文件时 `total_blocks` 取到的是 **blocks 字典**，第 110 行 `dict - int` 抛 TypeError，脚本以 traceback 退出（main 无 try 包裹），而 CLI help（143 行）明确写着 “Path to block_stats.json or sentences.json for stats”。同时 `total_units = block_stats.get("total_sentences", ...)` 在 sentences.json 里位于 `stats` 子对象，取不到 → 统计恒为 0。
- 建议：识别 `format == "v2"` 时改用 `data["blocks"]` 的长度与 `data["stats"]`；或直接禁用 sentences.json 入口。
- 置信度：高

### F4 [P2][契约] skills_builtin/topic-synthesis-core-enrichment/scripts/topic_synthesis_db.py:2038 —— handoff artifact key 与静态表不一致，路径被静默丢弃
- 证据：
```
          "prepare_analysis_context_payload",
```
```
    for key in artifact_keys:
        entry = artifact_entry(conn, key)
        if entry:
```
- 分析：`submit_current_payload_stage` 注册的 key 是 `f"{stage['id']}_payload"`（2984 行）＝`stage_30_prepare_analysis_context_payload`，与 ARTIFACT_PATHS 静态表（75-85 行循环生成的 `${stage_id}_payload`）一致；但 `register_prepare_triage` 的 handoff 清单与 `source_artifacts` 用的是 `prepare_analysis_context_payload`（4095 行同样如此）。`artifact_entry` 查不到就返回 None，`write_handoff` 的 `if entry:` 直接跳过，**没有任何日志或失败**：prepare 阶段的 handoff manifest 少一个关键 artifact，final `source_artifacts.prepare_handoff` 恒为 null（同段落 `core_handoff` 用了正确 key，形成对照）。
- 建议：统一为 `stage_30_prepare_analysis_context_payload`，或让 `artifact_entry` 在 key 未注册时抛错而非静默 None。
- 置信度：高

### F5 [P2][正确性] skills_builtin/topic-synthesis-core-enrichment/scripts/topic_synthesis_db.py:376 —— stage_state 主键只有 stage_id，两个 skill 共用同一 stage_id 时互相覆盖完成记录
- 证据：
```
        create table if not exists stage_state (
          stage_id text primary key,
          skill_id text not null,
```
```
        on conflict(stage_id) do update set
          skill_id = excluded.skill_id,
```
- 分析：SKILL_STAGE_CONTRACT 里 `topic-synthesis-core-enrichment`（197 行）与 `topic-synthesis-finalize`（230 行）都用 `stage_00_runtime_state_check`，而两者共享同一个 run_root 的 SQLite。finalize 调 `record_stage` 时 upsert 会把该行的 `skill_id` 改成 finalize，之后 `completed_stages(conn, "topic-synthesis-core-enrichment")` 不再包含该 stage → core-enrichment 的 gate 会重新执行这个 command stage。当前该 stage 幂等，故影响有限；但状态机已不再表达“每个 skill 各自的完成度”，任何新增非幂等 command stage 都会被重放。
- 建议：主键改为 `(skill_id, stage_id)`，upsert 冲突目标同步改。
- 置信度：高

### F6 [P2][错误处理] skills_builtin/literature-translator/scripts/restore_placeholders.py:90 —— 占位符表为空时返回 ok 却不产出任何文件
- 证据：
```
    if not entries:
        print(json.dumps({"warning": "placeholder map is empty, nothing to restore",
                          "status": "ok"}))
        sys.exit(0)
```
- 分析：high_quality 模式下 protect_placeholders 对“无内联公式/URL/>3 位数字/实体”的文本会写出 `entries: {}`，此时本脚本提前退出，output-dir 仍为空；流水线下一步 concatenate.py 以该目录作 `--translations-dir`，会在 `load_translations` 后命中 `no translations loaded` 并 exit 1。也就是说一个成功的上游步骤会把失败以“译文缺失”的形式转嫁给下游，错误信息指向错误原因。
- 建议：entries 为空时按 `--pattern` 原样复制（透传）文件到 output-dir，而不是提前退出。
- 置信度：中

### F7 [P2][正确性] skills_builtin/literature-translator/scripts/concatenate.py:135 —— 译文 sentences 为空时整块内容被静默删除
- 证据：
```
                    if trans_sentences:
                        output_lines.extend(trans_sentences)
```
- 分析：块类型不在 `NO_TRANSLATE_TYPES` 且 `block_id in translations` 时，只写 start marker + 译文 + end marker；若译文文件的 `sentences` 是空数组（模型返回空数组、文件被截断等），原块的内容被完全删除，而 `applied_count += 1` 且 `missing_blocks` 不包含该块 → 最终返回 `status: ok`。这是无告警的内容丢失，比“缺失翻译”更难发现。
- 建议：`trans_sentences` 为空时计入 missing_blocks（或保留原文），并在结果里单独计数。
- 置信度：高

### F8 [P2][正确性] skills_builtin/literature-translator/scripts/parse_input.py:211 —— `\cite{key}` 不会被剥离，LaTeX 命令与 citekey 流进译文
- 证据：
```
        # \cite, \ref, \label
        line = re.sub(r'\\(cite|ref|label)\[.*?\]?\{.*?\}', '', line)
```
- 分析：模式里的 `\[` 是必需字面量，`\]?` 只是可选右括号，没有把整段 `[.*?]?` 变成可选组。因此最常见写法 `\cite{smith2020}`、`\ref{fig:1}` 都不匹配（下一个字符是 `{`），只有 `\cite[p.3]{key}` 这种带可选参数的形式才会被删除。结果：normalized.md 里残留 LaTeX 命令，被 blockify 当正文、被送入翻译，citekey 会原样进入译文。
- 建议：改为 `r'\\(?:cite|ref|label)(?:\[[^\]]*\])?\{[^}]*\}'`（同时去掉 `\[.*?\]?` 的隐式必需量词）。
- 置信度：高

### F9 [P2][正确性] skills_builtin/literature-translator/scripts/parse_input.py:220 —— LaTeX 分支自造 BLOCK 标记，会被 sentencify 当成真实块边界
- 证据：
```
            output_lines.append("<!-- BLOCK: abstract | type: abstract -->")
            continue
```
```
        start_match = BLOCK_START_RE.search(line)
        if start_match:
            if current_block:
```
- 分析：parse_input 在 normalized.md 里写 `<!-- BLOCK: abstract|table|figure ... -->`/`BLOCK_END`（220-243 行），而 sentencify.BLOCK_START_RE 的 id 组是 `(\S+)`，任何 id 都能匹配。blockify 先把这些注释当普通段落文本收进某个 `b_NNN` 块，sentencify 解析 blocked.md 时命中内嵌标记 → 隐式关闭外层块（67-77 行）并开一个 id 为 `abstract`/`table` 的伪块；随后外层的 `<!-- BLOCK_END: b_NNN -->` 因 id 不等而无法闭合，行又被 `BLOCK_END_RE.search` 过滤掉（105-108 行），后续正文继续挂在伪块上。块结构静默错位，最终 concatenate 会把这些伪块报为 missing。
- 建议：normalize_latex 只输出语义注释（如 `<!-- MATH -->`）或直接把 abstract/table/figure 留给 blockify 识别，不要复用 BLOCK 标记命名空间。
- 置信度：中

### F10 [P2][正确性] skills_builtin/literature-translator/scripts/sentencify.py:208 —— list 块内换行被替换为空格，多行列表塌缩成单句
- 证据：
```
            para = para.replace('\n', ' ')
            para = re.sub(r' +', ' ', para)  # collapse multiple spaces
```
- 分析：`list` 不在 `NO_SPLIT_TYPES`（30 行），因此 `- item1\n- item2\n- item3` 走“按空行切段 → 段内换行替换为空格”的路径，变成一个句子 `- item1 - item2 - item3`；concatenate 又把每条句子写成单独一行，输出里的第二条起项目符号永久丢失，markdown 列表结构被破坏。文件头 docstring 声称 list 会被分句，但真正落在 NO_SPLIT_TYPES 里的却是 heading，文档与实现也不一致。
- 建议：对 `list` 类型按行切分（每行一个 sentence），不要做段内换行归并；同步修正 docstring。
- 置信度：中

### F11 [P2][契约] skills_builtin/topic-synthesis-core-enrichment/scripts/topic_synthesis_db.py:758 —— 手写 schema 校验器静默忽略未实现的 JSON Schema 关键字
- 证据：
```
    if "anyOf" in schema:
        options = schema.get("anyOf")
        if isinstance(options, list):
```
- 分析：`validate_schema_node`（752-823 行）只实现 `$ref/anyOf/type(list)/enum/minLength/minimum/maximum/minItems/items/required/additionalProperties/properties`。`oneOf`、`not`、`pattern`、`patternProperties`、`uniqueItems`、`minProperties`、`exclusiveMinimum/Maximum`、`format` 等关键字既不校验也不报“不支持的 schema 关键字”，而是被完全忽略——只要 assets/schemas 里用到其中之一，该约束就形同不存在，且没有任何诊断（对比 `resolve_schema_ref` 对不支持 ref 会显式抛错）。这是 payload 准入边界的静默弱化；最终定级需核对 assets/schemas 是否使用这些关键字。
- 建议：遇到未支持关键字时 raise，或补齐 oneOf/pattern/uniqueItems 等实现。
- 置信度：中

### F12 [P3][错误处理] skills_builtin/literature-translator/scripts/parse_input.py:119 —— 空文件触发 IndexError，绕过 JSON 错误契约
- 证据：
```
def is_binary(header: bytes) -> bool:
    """Check if bytes indicate a binary file."""
    if b"\x00" in header[:256]:
        return True
    first = header[0] if header[0] < 128 else 65
```
- 分析：0 字节文件 `f.read(512)` 返回 `b""`，`b"\x00" in b""[:256]` 为 False，随后 `header[0]` 抛 IndexError；该异常未被 classify_file/main 捕获，脚本以 traceback 退出，也不会写 `source_meta.json`，破坏了其余路径统一的 `{"status":"error",...}` 契约。
- 建议：`if not header: return False`（或按空文本处理）。
- 置信度：高

### F13 [P3][可维护性] skills_builtin/literature-translator/scripts/quality_gate.py:552 —— mode 解析逻辑在两个文件里逐字重复；`check_language` 为死代码
- 证据：
```
def resolve_mode() -> str:
    for candidate in [".literature_translator_tmp/mode.txt", "workspace/mode.txt"]:
        p = Path(candidate)
        if p.exists():
```
（export_alignment.py:199 的 `_resolve_mode` 与之逐字相同，仅函数名/尾注释不同）另有 quality_gate.py:323 的 `def check_language(text: str, target_lang: str) -> dict:`，全切片无任何调用点（main 只用 `check_language_v2`）。
- 分析：mode.txt 的路径与取值域（`fast|high_quality`）是同一份隐式契约，分散在两处，任一处改动另一处就会静默漂移（例如新增 mode）。死代码 `check_language` 会诱导后续维护者误改。
- 建议：抽到一个共享模块并删除 `check_language`。
- 置信度：高

### F14 [P3][可维护性] skills_builtin/topic-synthesis-core-enrichment/scripts/topic_synthesis_db.py:2277 —— digest locator 的同一形状被实现两份
- 证据：
```
def digest_locator_from_artifact(paper_ref: str, artifact: dict) -> dict:
```
```
def digest_ref_for_paper(run_root: Path, paper_ref: str, artifact_manifest: dict) -> dict:
```
- 分析：两者产出相同 key 组合（`paper_ref`、`payload_type`、可选 `note_key`、可选 `path`），差异仅在入参形态（单个 artifact vs manifest 查找），复制了 `payload_type` 默认值 `"digest-markdown"` 与 note_key/path 的组装逻辑。字段语义将来一变就要改两处，且 evidence_index 与 source_papers 两路输出的 `digest_ref` 容易漂移。
- 建议：`digest_ref_for_paper` 复用 `digest_locator_from_artifact`。
- 置信度：高

### F15 [P3][可维护性] skills_builtin/topic-synthesis-core-enrichment/scripts/topic_synthesis_db.py:583 —— artifact 注册表退化为模块级全局 dict，hash 一律丢弃
- 证据：
```
def register_artifact(
    conn: sqlite3.Connection,
    *,
    skill_id: str,
    stage_id: str,
    key: str,
    path: str,
    hash_value: str,
) -> None:
    ARTIFACT_PATHS[key] = path
    return None
```
- 分析：`register_artifact` 忽略 `conn`/`skill_id`/`stage_id`/`hash_value`，直接改写模块级的静态常量 `ARTIFACT_PATHS`；`artifact_entry(conn, key)` 也只回 `{"path": ...}`。后果有三：(1) `write_json/write_text` 每写一个文件都算一次 sha256（含 `write_complete_sections` 的 15 个 section）却从不落盘，纯浪费 IO；(2) handoff/manifest 只有路径、没有摘要，无法检测 artifact 漂移；(3) 返回值依赖同一进程内的注册顺序，`artifact_entry` 对未注册 key 返回 None 并让 F4 那类拼写错误静默通过。`record_action`（562 行）与 `write_gate_transcript`/`write_action_transcript`（667/671 行）同样是空实现。
- 建议：要么把 hash 写进 registry 并返回，要么删掉 `hash_value` 参数与空实现，明确该表就是静态路径表。
- 置信度：高