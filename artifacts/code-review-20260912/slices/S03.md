| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| scripts/synthesis/check-synthesis-artifact-library-debug-surface-parity.ts | 84 | OK | 语料身份/bounds/12 操作/access 对账/边界与 reopen 用例/ready 白名单检查链完整；`!` 断言仅在语料缺失时抛错。 |
| scripts/synthesis/check-synthesis-citation-graph-surface-parity.ts | 114 | OK | 窗口读用例、durable mutation、Host revision 例外白名单、ready 去重均自洽。 |
| scripts/synthesis/check-synthesis-concept-topic-graph-surface-parity.ts | 117 | OK | mutation 集合硬编码与 operations.json access 交叉校验；`missing Concept query case` 错误串丢 id（单条匹配，无实害）。 |
| scripts/synthesis/check-synthesis-cross-language-contracts.ts | 690 | OK | 注册表/引用解析/开放对象开放数组逃逸/负例生成逻辑闭合；`validator` 用 `${schemaId}#${fragment}` 解析依赖 Ajv 归一化，未发现翻转。 |
| scripts/synthesis/check-synthesis-native-runtime-contract-parity.ts | 168 | OK | node/rust 双跑比对；`nodeCode` 忽略错误细节（只比对稳定 code），符合设计。 |
| scripts/synthesis/check-synthesis-native-worker-transfer-parity.ts | 264 | OK | 所有权断言用源码字符串匹配（刻意钝化）；协议语料双向比对并回收 expected，未见漏检。 |
| scripts/synthesis/check-synthesis-production-capabilities.ts | 394 | OK | manifest/TS 常量/AST 端口/语料五方对账，operation 元数据字段集与 receipt/workDeadline 双向一致性检查到位。 |
| scripts/synthesis/check-synthesis-production-route-performance.ts | 995 | 问题 | P3: `cachedSetup` 缓存 rejection（见 F8）；另存疑：`FORMAL_SAMPLE_COUNT = 11` 时 `nearestRank(…,95)` 取到第 11 个即最大值，"p95 预算"实为 max 预算，需确认是否有意。 |
| scripts/synthesis/check-synthesis-reference-canonical-surface-parity.ts | 169 | OK | Host job / worker / batch / dedicated port 四类族用例覆盖检查完整。 |
| scripts/synthesis/check-synthesis-rust-license-inventory.ts | 160 | OK | Cargo.lock 块解析、stale/missing 双向、rusqlite 声明与 provenance 字段存在性检查；属文本门禁，无运行期逻辑。 |
| scripts/synthesis/check-synthesis-service-boundary.ts | 188 | OK | 正则边界检查（可能误报注释/类型名，属钝化设计）；仅 runtime_service.rs 允许 open_production 的约束与 AGENTS 条款一致。 |
| scripts/synthesis/check-synthesis-sidecar-runtime-freshness.ts | 66 | OK | 逐 target 校验并把读取异常降级为 `bundle_unreadable` 诊断；CLI 显式传 `undefined` 会回落到默认 assetRoot（正确）。 |
| scripts/synthesis/check-synthesis-sidecar-runtime-xpi.ts | 120 | 问题 | P3(判断): 第一段 forbidden 过滤硬编码 `bin/synthesis-sidecar/`（见 F5）。 |
| scripts/synthesis/check-synthesis-tag-surface-parity.ts | 119 | OK | special 族用例、ready 白名单、baseline stableErrors 反查齐全。 |
| scripts/synthesis/check-synthesis-topic-workbench-surface-parity.ts | 98 | OK | 与 baseline fixture 的 observable id/access 双向对账。 |
| scripts/synthesis/check-synthesis-webdav-maintenance-surface-parity.ts | 89 | OK | 控制操作 cancel/continue/retry/restart 覆盖检查到位。 |
| scripts/synthesis/dispatch-synthesis-sidecar-prebuild.ts | 407 | OK | ref/SHA/source 前置校验（check-ref-format + upstream 比对 + 全 SHA）、结果身份断言、dirty bundle 拒绝覆盖、失败提示 resume-run-id；`--overwrite-dirty-bundles=value` 形式会被忽略成裸 flag（无实害）。 |
| scripts/synthesis/dispatch-synthesis-sidecar-release.ts | 131 | OK | 仅 clean main、prepared==remote、ancestor 校验由 execFile 非零码兜底。 |
| scripts/synthesis/download-synthesis-sidecar-runtime-cache.ts | 267 | 问题 | P3: bearer token 进入 curl argv（见 F7）；另存疑：解压用 `unzip`，但同文件谱系对 tar 专门做了 Windows 分支（`synthesisSidecarRuntimeTar`），Windows runner 上 `unzip` 是否可用需确认。 |
| scripts/synthesis/package-synthesis-sidecar-runtime-symbols.ts | 179 | OK | manifest 形状/字节数/SHA 校验与 gzip 体积上限自洽。 |
| scripts/synthesis/package-synthesis-sidecar-runtime.ts | 204 | OK | 符号链接/非文件条目拒绝、provenance 与 manifest 生成后立即 candidate 校验。 |
| scripts/synthesis/prepare-synthesis-sidecar-release.ts | 82 | OK | 强制 clean main 且与 origin/main 同步，无 verification receipt 即拒绝生成 release set。 |
| scripts/synthesis/publish-synthesis-sidecar-runtime-prebuild.ts | 376 | 问题 | P3: token 拼进 remote URL 传给 git（见 F7）。 |
| scripts/synthesis/resolve-synthesis-sidecar-runtime-cache.ts | 349 | 问题 | P3: 内联 `--exclude-run-id=` 不做整数校验，NaN 静默退化为"不过滤"（见 F4）。 |
| scripts/synthesis/resolve-synthesis-sidecar-verification.ts | 345 | OK | receipt 重建 + run 元数据/workflow path/身份四重比对；分页上限 10×100 明确。 |
| scripts/synthesis/smoke-synthesis-rust-durable-candidate.ts | 1000 | 问题 | P3: finally 中清理失败会掩盖主错误并跳过后续清理（见 F10）。 |
| scripts/synthesis/smoke-synthesis-rust-sidecar-worker.ts | 115 | OK | worker ready/result 帧与 fingerprint 校验；失败靠顶层 await 的未捕获 rejection 退出码，可接受。 |
| scripts/synthesis/stage-synthesis-sidecar-runtime-prebuilds.ts | 261 | 存疑 | rename 失败后只要目标目录存在就吞掉错误并返回 `reused:false`（未再比对目标内容）；因 aggregate 由归档字节派生，实际风险需确认 aggregate 覆盖范围。 |
| scripts/synthesis/sync-synthesis-sidecar-runtime-prebuilds.ts | 236 | OK | 先解包到 bundleStaging 校验再原子换入，失败回滚 backup；catch 中回滚自身失败属边缘路径。 |
| scripts/synthesis/synthesis-native-stage1-suite.ts | 86 | OK | 必需编号集合与三段 segment 划分互补且不重叠，缺号即抛错。 |
| scripts/synthesis/synthesis-sidecar-runtime-release-controller.ts | 161 | OK | 步骤顺序、不可回退（complete→非 complete）、失败态与全完成态收敛逻辑自洽。 |
| scripts/synthesis/synthesis-sidecar-runtime-release-governance.ts | 495 | 问题 | P3: `verifySynthesisSidecarRuntimeBundleDirectory` 的 `policy` 参数被接受但正文从不读取（见 F6）；另存疑 `computeSynthesisSidecarRuntimeBundleId` 依赖 `JSON.stringify` 字段序，与 `rebuild…` 归一化是否一致未验证。 |
| scripts/synthesis/synthesis-sidecar-runtime-release-plan.ts | 64 | OK | 仅做 receipt/plan 一致性投影。 |
| scripts/synthesis/synthesis-sidecar-runtime-release-set.ts | 84 | OK | 读写 release set 的薄封装。 |
| scripts/synthesis/synthesisProductionSurfaceCorpora.ts | 295 | OK | 语料 SSOT 常量 + 指纹 + baseline/normalization/migration YAML 一致性检查；`inspectStableValue` 递归无界（fixture 受控，可接受）。 |
| src/backends/displayName.ts | 23 | OK | managed-local 优先返回受治理本地化名，其余走 configured→id；行为与命名意图一致。 |
| src/backends/identity.ts | 146 | OK | 指纹/ID 生成逻辑自洽；`normalizeManagedLocalBackendId` 实为 trim 直通（命名偏强），无运行期缺陷。 |
| src/backends/managementAuth.ts | 161 | 问题 | P2: 写回只保留 schemaVersion/backends，丢弃其他顶层字段（见 F2）；另与 registry 对同一 pref 存在两套解析/容错语义。 |
| src/backends/registry.ts | 934 | 问题 | P2: 选择路径忽略 `enabled:false`（见 F1）；P3: 死诊断计数器（F3）、错误串正则反解 id（F9）。 |
| src/backends/types.ts | 80 | OK | 类型定义，无逻辑。 |
| src/config/defaults.ts | 44 | OK | 纯常量表，无逻辑。 |

## B. Findings

### F1 [P2][正确性] src/backends/registry.ts:862 —— `enabled:false` 的 backend 仍会被选择，禁用形同虚设
- 证据：
```
  return sortBackendsByCompatibilityPriority(
    loaded.backends.filter((backend) =>
      compatibleBackendTypes.includes(backend.type),
    ),
```
- 分析：加载器显式保留了禁用状态（registry.ts:683 `...(enabled === false ? { enabled: false } : {}),`），但 `listBackendsForWorkflow`（861-866）、`resolveBackendForWorkflow`（890-892）、`listBackendsForProvider`（848）都只按 `type` 过滤，完全没有读取 `enabled`。触发条件：某 workflow 唯一兼容的 profile 被用户置 `enabled:false`，或 `preferredBackendId` 指向已禁用 profile —— 该 profile 仍会被返回并用于运行，用户"禁用"不生效。假设：若意图是"enabled 仅由 UI 侧过滤"，则调用方必须先剔除；本文件内看不到这种剔除。
- 建议：在选择/列举入口统一以 `backend.enabled !== false` 过滤（或在 `resolveBackendForWorkflow` 中对命中但禁用的 preferred 显式报错），并把该语义固化为 registry 的不变量。
- 置信度：中

### F2 [P2][契约] src/backends/managementAuth.ts:157 —— 更新 management_auth 会静默丢弃文档其他顶层字段
- 证据：
```
  setPref(
    BACKENDS_CONFIG_PREF_KEY,
    JSON.stringify(
      createBackendsPrefsDocument(nextEntries as BackendInstance[]),
    ),
  );
```
- 分析：`createBackendsPrefsDocument`（registry.ts:713-718）只产出 `{ schemaVersion, backends }`，而同一 pref 的文档类型 `BackendsDocument` 声明了 `defaultBackendId?: unknown`（registry.ts:18）。任何 "backends/schemaVersion 之外" 的顶层字段（例如默认 backend 选择结果，由本 slice 之外写入）在用户修改任一 backend 的 management_auth 后被删除；同时非法（非对象）条目在 132-134 行 `continue` 被丢弃，schemaVersion 被强制改写为 2。触发条件：prefs 文档含额外顶层字段时执行一次 auth 更新 → 默认 backend 选择回退。此外同一 pref 存在两套解析语义（本文件 95-106 容错返回 []，registry.ts:720+ 返回 fatalError/整条 backend 作废），加剧漂移。
- 建议：更新时以"读原文档 → 只替换目标条目的 management_auth → 原样写回其他字段"为原则（保留未知顶层字段与原始 schemaVersion），或让 registry 暴露唯一的文档读写入口。
- 置信度：中

### F3 [P3][可维护性] src/backends/registry.ts:44 —— `builtinRewriteCount` 是只清零不递增的死诊断计数
- 证据：
```
  builtinRewriteCount: 0,
```
（另一处为 registry.ts:138 `backendRegistryReadDiagnostics.builtinRewriteCount = 0;`；全文件再无写入。）
- 分析：该计数器通过 `getBackendsRegistryReadDiagnosticsForTests()` 对外暴露，但**没有任何代码路径递增它**，任何断言它的测试都会恒为 0 而静默通过；同时暗示曾存在的 "builtin backend 重写" 路径已被删除或未实现。
- 建议：删除该字段，或在实际发生 builtin 重写/迁移时补计数，避免假可观测性。
- 置信度：高

### F4 [P3][错误处理] scripts/synthesis/resolve-synthesis-sidecar-runtime-cache.ts:306 —— 内联 `--exclude-run-id=` 不校验，NaN 静默退化为"不排除"
- 证据：
```
  const excludeRunId = (() => {
    const inline = args.find((arg) => arg.startsWith("--exclude-run-id="));
    if (inline) return Number(inline.slice(17).trim());
```
（对照非内联分支 `return Number.isSafeInteger(value) ? value : undefined;`；消费点 175 行 `const filteredRuns = args.excludeRunId ? … : …`。）
- 分析：`--exclude-run-id=abc` / `--exclude-run-id=` 得到 NaN，NaN 在 `args.excludeRunId ? …` 中为 falsy，于是**完全不做排除**并把本应排除的 run 作为缓存候选参与解析；两种写法（`=` 与空格）语义不一致，且无任何诊断。触发条件：CI 传入拼错/空的 exclude 值。
- 建议：内联分支同样做 `Number.isSafeInteger && > 0` 校验，非法值直接抛错（而非静默 undefined）。
- 置信度：高

### F5 [P3][可维护性] scripts/synthesis/check-synthesis-sidecar-runtime-xpi.ts:66 —— bundle 路径字面量与合同 SSOT 重复（判断）
- 证据：
```
  const forbidden = Array.from(entries).filter((entry) =>
    entry.startsWith("bin/synthesis-sidecar/"),
  );
```
- 分析：本次检查其余地方都用 `bin/${synthesisSidecarRuntimeTargetBundlePath(target)}`（同一文件 69 行）取路径，唯独这条守卫把布局知识硬编码成字面量。判断：若该合同的 bundle 路径以 `synthesis-sidecar/` 开头（即 `bin/synthesis-sidecar/<target>`），则下方 required 列表里的 `…/manifest.json` 必然同时落入 `forbidden`，`ok` 恒为 false（门禁自反）；若布局改名（多一级目录/改名），这条守卫会静默不再匹配应当拒绝的条目。两种情况都属于"布局知识非 SSOT"的代价，建议改为从合同常量派生 `bin/<bundlePath>` 前缀或直接校验"bin 下除 target bundle 根之外无其它 synthesis-sidecar 相关目录"。
- 建议：用 `synthesisSidecarRuntimeTargetBundlePath` 派生的前缀集合来表达 forbidden 集合，并把"legacy node 目录"作为显式命名常量。
- 置信度：中

### F6 [P3][契约] scripts/synthesis/synthesis-sidecar-runtime-release-governance.ts:421 —— `policy` 参数被接受但从不使用
- 证据：
```
  policy?: "candidate" | "production";
  nowMs?: number;
}) {
  const manifestPath = path.join(args.root, "manifest.json");
```
（该函数体内无任何 `args.policy` 引用；返回值 482-486 行仅含 `ok/manifest/diagnostics`。）
- 分析：`download-…-cache.ts:209`、`package-synthesis-sidecar-runtime.ts:182` 等调用点显式传 `policy: "candidate"`，语义上 `"production"` 应比 candidate 更严（例如有效期/provenance 强制），但两者行为完全一致 —— 调用方以为在选择校验强度，实际是无效参数，未来想收紧发布校验时无处落点。
- 建议：要么删除该参数，要么实现差异化校验（如 production 强制 `expiresAt` 非空/未过期、强制 sourceFingerprint 校验），并让调用点显式选择。
- 置信度：高

### F7 [P3][信任边界] scripts/synthesis/publish-synthesis-sidecar-runtime-prebuild.ts:121 —— GH token 进入子进程 argv
- 证据：
```
  const token = process.env.GH_TOKEN?.trim();
  return token
    ? `https://x-access-token:${token}@github.com/${repository}.git`
    : `https://github.com/${repository}.git`;
```
（同类：download-synthesis-sidecar-runtime-cache.ts:76 `-H`, `Authorization: Bearer ${token}` 交给 curl。）
- 分析：该 URL 会作为 `git clone/fetch/ls-remote/push` 的参数出现在命令行，任何同机进程（CI runner 上的旁路进程、进程审计日志）可读到明文 token；curl 那处同理。虽然 `runGit` 的失败信息只回传 `args[0]`、不打印 URL，但 argv 暴露本身是可被利用的秘密面。
- 建议：改用 credential helper / `GIT_ASKPASS`，或把 header 走 curl 的 `--config -`（stdin）而不进 argv；至少不要把 token 拼进 URL 后继续出现在 argv 中。
- 置信度：高

### F8 [P3][正确性] scripts/synthesis/check-synthesis-production-route-performance.ts:234 —— `cachedSetup` 把 rejection 也永久缓存
- 证据：
```
  const existing = cache.get(key);
  if (existing) return existing;
  const pending = setup().then(() => undefined);
  cache.set(key, pending);
```
- 分析：`setup()` 失败时被拒绝的 promise 仍写入 `cache` 且永不失效，`runSynthesisProductionRoutePerformanceDataset` 的每个操作都只 catch 自己的 setup（705-714 行），于是首个失败（如 reference-refresh）会让后续所有依赖同一 key 的操作立刻复用同一个 rejection，报告里全部变成同样的 `setup_failed:…`，真实首个失败因被重复上报而弱化诊断；此外 `key` 无失效条件，长时间运行会持有陈旧结果。
- 建议：失败时 `cache.delete(key)`（或在 rejection 上挂 `finally` 清理），让每个操作独立重试/独立报错。
- 置信度：中

### F9 [P3][健壮性] src/backends/registry.ts:786 —— 用正则从错误串反解 backend id，id 含空格/引号即错配
- 证据：
```
    const idMatch = reason.match(/(?:\(|id ")([^)"\s]+)(?:\)|")?/);
    if (idMatch?.[1]) {
      invalidBackends[idMatch[1]] = reason;
    }
```
- 分析：`normalizeBackendEntry` 只要求 id 非空（`isNonEmptyString` + trim），因此 id 可含空格或 `"`。此时捕获组 `[^)"\s]+` 只取到第一个片段（如 `entry[1] (my backend) …` → key `my`），`invalidBackends` 的键与真实 id 不一致；`resolveBackendForWorkflow` 的 `loaded.invalidBackends[preferredBackendId]`（916 行）因此查不到，用户看到的是"Unknown or incompatible backendId"而不是真实失效原因，UI 侧展示的失效 id 也是错的。
- 建议：让 `normalizeBackendEntry` 直接在返回结构里带出 `id`/`reason`（结构化错误），不要在格式化后的字符串上做反向解析。
- 置信度：高

### F10 [P3][错误处理] scripts/synthesis/smoke-synthesis-rust-durable-candidate.ts:983 —— finally 内清理失败会掩盖主错误并中断后续清理
- 证据：
```
  } finally {
    await closeChildForCleanup(child, childClosed, "candidate_cleanup");
    lines.close();
    await reverseHost.close();
    await fs.rm(root, { recursive: true, force: true });
  }
```
（`closeChildForCleanup` 内 `await withDeadline(closed, 5_000, label)` 超时会 reject，见 169 行。）
- 分析：子进程未在 5s 内退出时，`closeChildForCleanup` 抛出 `candidate_deadline_exceeded:…`，该异常在 finally 中替换掉真正的断言失败/超时错误（丢失主错误与栈），并且后续 `lines.close()`、`reverseHost.close()`、临时目录删除全部被跳过（监听器与 /tmp 目录残留）；内层 try/finally（969 行）同理。
- 建议：finally 中逐项 try/catch 记录清理诊断，或在清理失败时以 `AggregateError`/附加 cause 形式保留主错误。
- 置信度：高

---

SLICE: S03
FILES: 41 个 / 10360 行；ledger: /tmp/review/ledger/S03.md
COVERAGE: 已细读 41 个（逐个读过逻辑分支、参数校验、清理路径与门禁语义）；略读 0 个（生成物/纯数据文件不存在于本切片）。
TOP FINDINGS（只列 P0/P1/P2，按严重度排序，每条 ≤ 25 词）:
- [P2][正确性] src/backends/registry.ts:862 禁用(enabled:false)的 backend 仍会被 list/resolve 选中，禁用不生效。
- [P2][契约] src/backends/managementAuth.ts:157 写回 backends 文档只保留 schemaVersion/backends，静默丢弃 defaultBackendId 等顶层字段。
NOISE: 本切片最值得主代理复核的 1-3 个可疑点：
- `SYNTHESIS_SIDECAR_RUNTIME_TARGET_BUNDLE_PATH` 的真实取值决定 check-synthesis-sidecar-runtime-xpi.ts:66 的字面量是"失效"还是"门禁自反"（F5），请以合同源码确认。
- `verifySynthesisSidecarRuntimeBundleDirectory` 的 `policy` 参数完全无效（F6），需确认是否有意保留"production 更严"的预留位。
- `stage-synthesis-sidecar-runtime-prebuilds.ts:154-159` 在 rename 失败但目标目录已存在时吞错并返回 `reused:false`，需确认 aggregate 是否覆盖归档内容（否则是竞态下的静默错配）。