| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/modules/acp/skillRun/acpSkillRunPersistence.ts | 1279 | 问题 | P3: 软持久化 flush 不清 timer/记录（F4）；P3: invalidate hydration 不解除订阅（F3）；另 `lastPersistedEventIds` 无删除路径（会话内单调增长） |
| src/modules/acp/skillRun/acpSkillRunRecovery.ts | 2358 | 问题 | P2: 早期失败路径泄漏 setup controller 注册（F1）；P3: 取消被记为 recovery failed（F5） |
| src/modules/acp/skillRun/acpSkillRunStore.ts | 2085 | OK | 未发现独立缺陷；仅作为 F4 的删除调用方参与（flush→delete 顺序） |
| src/modules/acp/skillRun/acpSkillRunTranscriptMirror.ts | 1439 | OK | 未发现缺陷；`readFullTranscript` 在非 live 模式下每次分页读都全量扫读属既有设计（存疑点见 NOISE） |
| src/modules/acp/skillRun/acpSkillRunnerOrchestrator.ts | 3219 | 问题 | P2: 等待用户回复期间被 interrupt 会把 run 置为 failed（F2） |
| src/modules/acp/skillRun/acpSkillsWorkspaceSurface.ts | 608 | 存疑 | details DTO 内 `collapsed` 为静态字面量，需确认 UI 是否把它当作折叠状态来源（每次 run 变更重读会重置） |

### F1 [P2][资源/生命周期] src/modules/acp/skillRun/acpSkillRunRecovery.ts:1001 —— 恢复流程在早期失败路径上永久泄漏 setup controller 注册

- 证据：
```
  if (!postTerminalConversation) {
    registerAcpSkillRunSetupController(requestId, setupController);
  }
```
（同文件 1058 `throw new Error("ACP skill run has no remote session id.");`、1063、1132 的 rethrow、1160 的 dependency error 也属同类早期退出）
- 分析：`registerAcpSkillRunSetupController` 在 1001 行注册，而唯一对应的 `unregisterAcpSkillRunSetupController(requestId, setupController)` 位于 2336 行——它属于 2034 行才开始的 `try` 的 catch。本模块在进入该 try 之前有多处 `throw`（1013 读 context 失败、1058 无 sessionId、1063 缺 workspace/runtime、1116-1133 host bridge 注入失败、1147-1161 dependency diagnostic 为 error），这些路径既不 unregister，也不把 run 置为终态（只写 `conversationRecoveryState: "failed"`）。store 只在终态时释放 setup controller（acpSkillRunStore.ts:813-815 `if (isTerminalAcpSkillRunStatus(next.status)) setupControllers.delete(record.requestId);`），因此该条目会一直挂着，直到（可能永远不会发生的）终态跃迁。可达路径：任一次“连接/回复”触发的恢复在 host bridge 或依赖探测阶段失败 → 面板 Cancel 会命中这个指向已关闭 adapter 的 setup controller（cancel 变成静默 no-op），且若 `acpSkillRunControllerRegistry` 拒绝重复注册，下一次恢复会在 2232/2238 直接以 `BoundedWaitError` 失败（需在 registry 中复核该语义）。
- 建议：把 setup controller 的注销放到覆盖全部失败路径的 `try/finally`（或在 1013/1058/1063、1116-1133、1147-1161 的 throw 前显式 unregister），使注册生命周期与函数体一致。
- 置信度：中

### F2 [P2][正确性] src/modules/acp/skillRun/acpSkillRunnerOrchestrator.ts:3169 —— 等待交互回复时被 interrupt，会把可恢复的 waiting_user run 置为 failed

- 证据：
```
    upsertAcpSkillRun({
      requestId: workspace.requestId,
      status: cancellationRequested ? "canceled" : "failed",
      statusReason: cancellationRequested
        ? "cancel_task"
        : "prompt_failed_terminal",
```
（前置分支 3154-3168 只写 `promptInterruptState: "unconfirmed"`）
- 分析：`interruptTurn` 会主动 reject 待回复的 resolver（1747-1753 `pendingReplyRejecter(new Error("ACP skill run interrupted while waiting for user reply."))`）。主循环在 `convergence.kind === "pending"` 时 `const reply = await replyPromise;`（2865 行）会因此抛出，进入 3063 行外层 catch。此时 `disconnectRequested=false`、`interruptionForced=false`（等待回复期间 `activePromptTimeoutDrain` 已在 promptExistingSession 的 finally 里置 null，179-1839 的 force watchdog 不会被安排），于是只走 3154 的 unconfirmed 分支，随后无条件执行上面的终态 upsert：`status:"failed" / statusReason:"prompt_failed_terminal"`，并 `throw error` 让 job/sequence 视为失败（store 的 `propagateAcpSkillRunTerminalState` 会把 sequence step 置 failed）。对照同一机制在 detached 回复路径的处理（2331-2348 使用 `CONFIRMED_ACP_SKILL_PROMPT_INTERRUPTION_STATE` 并 `return`），两条路径语义不一致；store 的合法跃迁表也把 `interrupt_turn` 明确映射为 `waiting_user`。
- 建议：在 catch 的 `interruptionRequested` 分支里按 detached 路径处理（写 confirmed 中断态并 `return` 一个 deferred/waiting_user 结果，不写 failed 终态），仅在没有活动 prompt 语义时保留 unconfirmed。
- 置信度：中

### F3 [P3][资源/清理] src/modules/acp/skillRun/acpSkillRunPersistence.ts:736 —— invalidate hydration 不解除执行显示模式订阅，重复 hydrate 会不断叠加订阅者

- 证据：
```
export function invalidateAcpSkillRunPersistenceHydration() {
  hydrated = false;
}
```
- 分析：`ensureAcpSkillRunStoreHydrated`（695 行）把 `subscribeAssistantExecutionDisplayMode(...)` 的返回值写入模块级 `unsubscribeExecutionDisplayMode`；`invalidateAcpSkillRunPersistenceHydration` 只重置 `hydrated`，不调用旧 unsubscriber。下一次 hydrate 会覆盖该变量，旧订阅永久残留（只能靠 `resetAcpSkillRunPersistenceForTests` 解除最新一个）。调用方 `clearAcpSkillRunsForRuntimePersistence`（acpSkillRunStore.ts:2082）在运行时持久化切换/清理时会被反复触发，导致订阅者列表随切换次数单调增长；由于回调共享 `lastExecutionDisplayMode`，功能影响有限，但属于无界资源泄漏且会让测试隔离失效。
- 建议：invalidate 时先 `unsubscribeExecutionDisplayMode?.(); unsubscribeExecutionDisplayMode = undefined;` 再重置 `hydrated`。
- 置信度：高

### F4 [P3][并发/清理] src/modules/acp/skillRun/acpSkillRunPersistence.ts:964 —— flushSoftRunPersists 不清 timer/记录，flush 后重复写入，删除后可能复活已删 run

- 证据：
```
function flushSoftRunPersists() {
  for (const record of Array.from(softRunPersistRecords.values())) {
    persistRun(record);
  }
}
```
- 分析：函数只做 persist，既不 `clearTimeout`，也不 `softRunPersistTimers.delete` / `softRunPersistRecords.delete`。因此：(1) 每次 flush（`flushAcpSkillRunRuntimeFileWrites` 会调用它）之后，挂起的 2s timer 仍会再 `persistRun(pending)` 一次，产生重复 JSON 序列化 + SQLite upsert；(2) 更严重的是 `deletePluginRunStoreEntry` 之后 timer 仍持有同一 record 引用（acpSkillRunStore.ts:2019-2022 先 flush 再 delete、acpSkillRunPersistence.ts:1250-1251 retention 清理同理），timer 触发时 `persistRun` 会再次 `upsertPluginRunStoreEntry("acp", ...)`，把刚删除/归档的 run 重新写回持久化表，下次启动 hydrate 时复活。
- 建议：flush 时同步 `clearTimeout` 并清空对应 `softRunPersistTimers/softRunPersistRecords` 条目（或让删除路径先 `cancelSoftRunPersist(requestId)`）。
- 置信度：中

### F5 [P3][错误处理] src/modules/acp/skillRun/acpSkillRunRecovery.ts:2337 —— 用户取消恢复会被记为 recovery failed，且 `canceled` 分支不可达

- 证据：
```
    const current = getAcpSkillRunRecord(requestId);
    if (current?.status === "canceled") {
      return;
    }
```
- 分析：本模块的取消路径（994-999 `setupController.cancel`、2046-2050 `recoveredController.cancel`）只置 `recoveryCanceled` / `recoveredCancellationRequested`、abort 并关闭 adapter，从不写 `status: "canceled"`（`detach` 也不改 status）。因此 2337 的 `current?.status === "canceled"` 在同模块内不可达，取消会落到 2341-2349：`conversationRecoveryState: "failed"` + `lastRecoveryError` 为取消原因，并向外 rethrow。结果是用户主动取消被上报为“恢复失败”，run 停留在非终态且 recovery state 为 failed（只能靠下一次连接尝试覆盖）。
- 建议：取消路径显式写入终态（`status:"canceled"`, `statusReason:"cancel_task"`）或在 catch 中以 `recoveryCanceled` 判定为“已取消”而不写 `failed`。
- 置信度：中

SLICE: S43
FILES: 6 个 / 10988 行；ledger: /tmp/review/ledger/S43.md
COVERAGE: 已细读 6 个（acpSkillRunPersistence、acpSkillRunRecovery、acpSkillRunStore、acpSkillRunTranscriptMirror、acpSkillRunnerOrchestrator、acpSkillsWorkspaceSurface）；略读 0 个
TOP FINDINGS（只列 P0/P1/P2，按严重度排序）:
- [P2][资源] acpSkillRunRecovery.ts:1001 恢复早期失败路径不 unregister setup controller，注册泄漏至终态
- [P2][正确性] acpSkillRunnerOrchestrator.ts:3169 等待回复时被 interrupt 会把 waiting_user run 置为 failed（detached 路径则正确置 confirmed）
NOISE:
- acpSkillsWorkspaceSurface.ts:366/374/381 details DTO 硬编码 `collapsed: true/false`，若 UI 以其为折叠来源，每次 run 变更重读会重置用户展开状态（需对 publication 层复核）
- acpSkillRunTranscriptMirror.ts:1251 非 live 显示模式下每次分页读取都走 `readFullTranscript` 全量扫读并建 Map，页面翻页有 O(n) 代价（是否为有意权衡需确认）