| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/modules/hostBridge/mcp/zoteroMcpProtocol.ts | 1502 | 问题 | P1 broker 错误投影丢失 code/retryable/details（F1）；审批拒绝响应形态不一致（F9） |
| src/modules/hostBridge/mcp/zoteroMcpServer.ts | 2656 | 问题 | P2 JSON-RPC batch 被当作单工具做 admission/circuit（F3）；descriptorStale 只置位不清理（F5）；超时分支遗漏控制信号清理（F8） |
| src/modules/hostBridge/permissions/acpConversationHostBridgePermissionRegistry.ts | 57 | 问题 | P2 同一 conversation 并发审批会覆盖 handler，旧请求只能等超时（F2） |
| src/modules/hostBridge/permissions/hostBridgePermissionManager.ts | 649 | 问题 | P2 parseHostBridgePermissionScope 丢弃 connectionMode，导致 auto-approval 在 wire 路径永不生效（F4）；P3 permissionProjections 无界增长（F7） |
| src/modules/hostBridge/permissions/hostBridgeWriteAutoApprovalRegistry.ts | 110 | 问题 | P2 参与 F4：registry 要求 scope.connectionMode === "local"，但解析路径永不产生该字段 |
| src/modules/hostBridge/permissions/skillRunnerHostBridgePermissionRegistry.ts | 141 | 问题 | P2 仅凭 runRequestId 解析时会选中最早 resolver，而不是当前 pending，可能批准错误请求（F6） |

### F1 [P1][契约] src/modules/hostBridge/mcp/zoteroMcpProtocol.ts:1453-1494 —— ZoteroHostCapabilityError 除 not_found 外全部丢失稳定 code/retryable/details
- 证据：
```
        const structuredCode =
          brokerError?.code === "not_found"
            ? brokerDetails?.kind === "note"
              ? "zotero_note_not_found"
...
        return jsonRpcError(request.id ?? null, -32602, message, {
          toolName,
          errorName: error instanceof Error ? error.name : "Error",
          details:
            error instanceof ZoteroMcpToolInputError
              ? error.details
              : undefined,
        });
```
- 分析：只有 `brokerError.code === "not_found"` 会走 `buildToolErrorResult`；其他 broker 稳定错误（如冲突、不可用、限流、参数错误）走 `jsonRpcError`，且因 `error` 不是 `ZoteroMcpToolInputError`，`details` 传 `undefined`，`code`、`retryable` 被整体丢弃。这直接违反“broker 失败统一 `ZoteroHostCapabilityError`（稳定 code/retryable/strict-JSON details）”的硬约束，MCP 客户端无法按稳定语义重试或分类。
- 建议：将 `ZoteroHostCapabilityError` 统一投影为带 `error_code`/`retryable`/`details` 的结构化工具错误，或至少把 `code`/`retryable` 放进 JSON-RPC error data。
- 置信度：高

### F2 [P2][并发] src/modules/hostBridge/permissions/acpConversationHostBridgePermissionRegistry.ts:30 —— 同一 ACP conversation 的并发审批会覆盖 pending handler
- 证据：
```
  handlersByConversationId.set(conversationId, handler);
  return () => {
```
```
  const handler = handlersByConversationId.get(conversationId);
  if (!handler) {
    return false;
  }
  handler(request);
```
- 分析：registry 每个 conversationId 只保存一个 handler。两个需要审批的 Host Bridge 调用若属于同一 ACP Chat conversation 并发进入，第二个 `setAcpConversationHostBridgePermissionRequest` 会覆盖第一个 handler；第一个 `requestScopedPermission` 的 promise 永远等不到 resolve，只能等 5 分钟 timeout。MCP admission 允许最多 9 个普通工具并发，因此该路径可达。
- 建议：按 conversationId 维护 pending 队列，或第二个请求存在未决审批时返回 `ui_unavailable`/排队，禁止静默覆盖。
- 置信度：中高

### F3 [P2][正确性] src/modules/hostBridge/mcp/zoteroMcpServer.ts:1656-1657 —— JSON-RPC batch 只按第一个工具名做 admission/circuit/timeout
- 证据：
```
  const shouldAdmit = payloadContainsAdmittedToolCall(payload);
  const toolName = firstToolName(payload);
```
```
      if (canRecordCircuit && toolOutcome === "success") {
        recordCircuitSuccess(toolName);
      } else if (canRecordCircuit && toolOutcome === "error") {
        recordCircuitFailure(
          toolName,
          toolErrorName,
          summarizeJsonRpcResponse(response).error,
        );
      }
```
- 分析：对 `[{status},{failingTool}]` 这类 batch，`firstToolName` 只取第一个 tools/call；`responseContainsError(response)` 又扫描整个响应。结果是：失败会被记到第一个工具（例如 `diagnostic.get_status`）的 circuit 上，真正失败的工具反而不进 breaker；客户端可借 batch 前缀 status 绕过失败工具的 circuit 保护，同时 batch 内多个工具共用一次 admission/timeout。
- 建议：要么把 batch 拆成逐条目 admission/circuit，要么禁止 batch 内混合多个 tools/call，至少按每个 tools/call entry 单独归因。
- 置信度：高

### F4 [P2][契约] src/modules/hostBridge/permissions/hostBridgePermissionManager.ts:563-569 —— parseHostBridgePermissionScope 丢弃 connectionMode，导致 write auto-approval 永不匹配
- 证据：
```
  return {
    kind,
    requestId: normalizeString(source.requestId) || undefined,
    runId: normalizeString(source.runId) || undefined,
    autoApproveWrites: source.autoApproveWrites === true,
    grantId: normalizeString(source.grantId) || undefined,
  };
```
```
  if (
    !scope?.autoApproveWrites ||
    scope.kind !== "acp-skill-run" ||
    scope.connectionMode !== "local"
  ) {
    return false;
  }
```
- 分析：`HostBridgePermissionScope` 声明了 `connectionMode?: "local" | "remote"`，`isHostBridgeWriteAutoApprovalScope` 也强制要求 `"local"`；但唯一的 wire 反序列化函数 `parseHostBridgePermissionScope` 从不写入 `connectionMode`。因此任何从 header/JSON 解析出来的 scope 都拿不到 `"local"`，`requestAcpRunScopedPermission` 里的 auto-approval 分支实际不可达，acp-skill-run 自动审批退化为每次都要 UI 审批。
- 建议：在 parser 中解析并校验 `connectionMode`，只允许 `"local" | "remote"`，再进入 registry 判断。
- 置信度：中高

### F5 [P2][正确性/可观测性] src/modules/hostBridge/mcp/zoteroMcpServer.ts:2311-2313 —— descriptorStale 置位后没有成功注入/启动时的清理路径
- 证据：
```
    if (previousEndpoint && previousEndpoint !== endpoint) {
      descriptorStale = true;
    }
```
```
export function markZoteroMcpServerDescriptorInjected() {
  descriptorInjected = true;
  descriptorInjectedAt = nowIso();
}
```
- 分析：endpoint 变化只把 `descriptorStale` 置 true；`markZoteroMcpServerDescriptorInjected()` 不清理该标志，`startServer` 成功、`ensureZoteroMcpServer` 成功返回、`syncMcpRouteStateFromHostBridge` 都不清理。结果是重新注入新 descriptor 后 health 仍长期报 `descriptor_stale`/warning。反向问题也存在：状态已是 running 时走 `syncMcpRouteStateFromHostBridge` 更新 endpoint，不会设置 `descriptorStale`，真正 stale 时可能漏报。
- 建议：在 descriptor 成功注入/重新启动并发布新 endpoint 时清 stale；在 `syncMcpRouteStateFromHostBridge` 中比较旧 endpoint，变化时置 stale。
- 置信度：高

### F6 [P2][正确性] src/modules/hostBridge/permissions/skillRunnerHostBridgePermissionRegistry.ts:106-110 —— 只给 runRequestId 时会解析最早的 resolver，而不是当前 pending
- 证据：
```
  const matched = permissionRequestId
    ? resolversByPermissionRequestId.get(permissionRequestId)
    : Array.from(resolversByPermissionRequestId.values()).find(
        (entry) => entry.runRequestId === runRequestId,
      );
```
- 分析：`pendingByRunRequestId` 保存的是该 run 最新 pending 请求；但 fallback 在 `resolversByPermissionRequestId` 中找“第一个 runRequestId 匹配”的 resolver（Map 插入序），若同一 run 先后有两个 pending，用户批准当前显示的请求时可能 resolve 旧请求，当前请求继续挂起到 timeout。触发条件是 UI/调用方只传 `runRequestId` 而不传 `permissionRequestId`。
- 建议：fallback 应以 `pendingByRunRequestId.get(runRequestId)` 的 requestId 为准，或要求调用方必须传 `permissionRequestId`；旧请求在覆盖时应被明确 cancel。
- 置信度：中

### F7 [P3][资源] src/modules/hostBridge/permissions/hostBridgePermissionManager.ts:270-286 —— permissionProjections 无界增长，resolved/timeout 条目永不删除
- 证据：
```
  permissionProjections.set(request.requestId, {
    permissionRequestId: request.requestId,
...
  });
```
```
export function listHostBridgePendingPermissions() {
  return Array.from(permissionProjections.values())
    .filter((entry) => entry.state === "pending")
    .sort((left, right) => right.createdAt.localeCompare(left.createdAt));
}
```
- 分析：每轮审批都写入 Map；`resolvePermissionProjection` 只更新 state，不删除；生产路径没有 TTL/容量/清理，只有测试 reset 会 clear。长时间运行的 Zotero 会话中该 Map 持续增长，`listHostBridgePendingPermissions` 每次还要对全量历史条目排序。
- 建议：为 resolved/timeout 条目加保留上限或 TTL；pending 列表不要遍历全量历史。
- 置信度：高

### F8 [P3][资源] src/modules/hostBridge/mcp/zoteroMcpServer.ts:1860-1872 —— tool_timeout 分支未清理 requestControl 的外部 abort 监听
- 证据：
```
      recordCircuitFailure(
        toolName,
        "ZoteroMcpToolTimeoutError",
        `Timed out after ${toolCallAdmission.getPolicy().runningTimeoutMs}ms`,
      );
      return {
        response: jsonRpcToolTimeoutError(firstJsonRpcId(payload), toolName),
```
- 分析：`requestControl.cleanup()` 只在 `run` 的 `finally` 中调用。超时分支直接返回；如果被超时的 `item.run()` 因宿主操作不可取消而长期不 settle，`externalSignal` 上的 abort listener、run 闭包和 admission 槽位会一起泄漏。
- 建议：在 timeout 分支也执行 `requestControl.cleanup()`（注意与 run finally 的重复清理要幂等），或把清理统一挂到 admission 的最终释放路径。
- 置信度：中高

### F9 [P2][契约] src/modules/hostBridge/mcp/zoteroMcpProtocol.ts:1250-1255 —— canonical mutation 的用户拒绝抛 ZoteroMcpToolInputError，最终变成 JSON-RPC error 而不是 isError 工具结果
- 证据：
```
            if (decision.outcome !== "approved") {
              throw new ZoteroMcpToolInputError(
                "Zotero-side approval was denied for this MCP capability.",
                { capability: capability.name, approval: decision.outcome },
              );
            }
```
- 分析：非 canonical 路径在预审批阶段拒绝时返回 `buildToolResult({ isError: true, structuredContent: { capability, approval, data: null } })`；canonical 路径在预览后拒绝则抛 `ZoteroMcpToolInputError`，被 `handleZoteroMcpJsonRpc` 转成 `-32602` JSON-RPC error。同一语义（用户拒绝）产生两种 wire 形态，MCP 客户端可能把后者当协议/传输错误处理，而不是工具失败。
- 建议：canonical mutation 的审批拒绝也走结构化 `isError` 工具结果，或在 `callHostBridgeCapabilityAsMcpTool` 捕获该错误并投影为 `buildToolErrorResult`。
- 置信度：高