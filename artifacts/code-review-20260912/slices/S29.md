| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/dashboard/components/AcpTraceReplayRegion.tsx | 1172 | OK | 投影/生命周期与 memo 边界一致；250ms 计时器与原生 change 监听器都可清理 |
| src/dashboard/components/BackendManagerRegion.tsx | 1373 | OK | 非阻塞观察：`buildAcpPresetDisplayName` 在 useNpx+isolated 时拼出 `"(npm)(Isolated)"`，疑似逐字沿用 legacy 字符串，未达 finding 门槛 |
| src/dashboard/components/BackendRegion.tsx | 773 | OK | 非阻塞观察：TaskTable 由空态切到有行时 wrap 是新节点而 `[scrollKey]` 未变，滚动位置不回填（影响极小） |
| src/dashboard/components/HomeRegion.tsx | 364 | OK | 文档视图与 running 表均为纯投影；文档滚动位置经 props 回灌 |
| src/dashboard/components/MigrationsRegion.tsx | 662 | 问题 | P2：搜索框完全受控于异步 host 回显，快速输入丢字（见 F1） |
| src/dashboard/components/ProductsRegion.tsx | 1442 | OK | 树构建/折叠/滚动记忆均在本地，签名比较只吃 selection |
| src/dashboard/components/RuntimeLogsRegion.tsx | 952 | 问题 | P2：勾选与 level 切换基于「上次回显」计算，快速连点吞掉前一次（见 F2） |
| src/dashboard/components/SkillrunnerAuditRegion.tsx | 258 | OK | 只读渲染，条形宽度对 max=0 有 `Math.max(…,1)` 兜底 |
| src/dashboard/components/SynthesisSidecarRegion.tsx | 736 | OK | 行复用/签名一致；150ms 防抖 + `lastSentRef` 与外部回显的同步逻辑正确 |
| src/dashboard/components/TabBarRegion.tsx | 109 | OK | — |
| src/dashboard/components/WorkflowOptionsRegion.tsx | 1263 | 存疑 | 需确认：schema（如 `disabled`/`allowCustom`）变化而 `workflowOptionsDraftResetKey` 不变时，`useLayoutEffect(…, [])` 注册的原生 change/blur 会残留在被替换掉的旧 input 上，导致该字段 blur 不再提交（resetKey 只含 selectedWorkflowId/selectedProfile/三个 value bag） |
| src/dashboard/components/WorkflowSettingsDialogRegion.tsx | 893 | 问题 | P2：hostOptions 读扁平 `maxConcurrency`、写/发送嵌套 `queue.maxConcurrency`，形状不自洽（见 F3） |

### F1 [P2][正确性] src/dashboard/components/MigrationsRegion.tsx:319 —— 搜索框完全受控于异步 host 回显，快速输入会被回显覆盖丢字
- 证据：
```
                value={candidatePage.query.search}
                placeholder={selection.searchPlaceholder}
                aria-label={selection.searchPlaceholder}
                onInput={(event) =>
                  updateQuery({ search: event.currentTarget.value })
                }
```
- 分析：每次按键都 dispatch `literature-migration-set-candidate-query`，而输入框的值来源是 host 快照回显（`candidatePage.query.search`），页面没有本地 draft。用户连续输入 "ab" 时，若第一次按键的回显在第二次按键之后到达，Preact 会把 DOM 值写回旧串 "a"（`dom.value !== props.value` 即赋值），"b" 与其后光标位置一起丢失；本切片其它面（`SynthesisSidecarFilterInput` 的本地 draft+防抖、`ReplayDraft`、`WorkflowFieldRow` 的非受控 + native change 提交）都刻意避开了这一模式，此文件是唯一受控文本输入。触发条件：输入速度快于一次 IPC 快照往返。
- 建议：像 `SynthesisSidecarFilterInput` 那样保留本地 draft，仅在 `props.filterValue !== lastSentRef.current` 时回灌；或对 dispatch 做短防抖。
- 置信度：中

### F2 [P2][正确性/并发] src/dashboard/components/RuntimeLogsRegion.tsx:464 —— 连续勾选两行（或两个 level）时第二次提交基于上次 host 回显，吞掉第一次选择
- 证据：
```
      const nextIds = new Set(latest.selectedEntryIds);
      if (checkbox.checked) {
        nextIds.add(row.id);
      } else {
        nextIds.delete(row.id);
      }
```
- 分析：`latest` 是「最后一次 host 回显」的 `view`，不是本页最后发出的集合。用户在回显到达前点第二行，payload = 旧集合 ∪ {B}，A 被丢掉；host 落库后回显，A 的勾选被回滚（`checked` 受控于回显）。`toggleLevel`（第 756 行 `const active = new Set(selection.activeLevels);`）同一根因：levels 是整数组替换，第二次切换会把第一次的 level 从数组里剔除。触发条件：两次点击落在同一次快照往返内。
- 建议：像 `filterPatchRef` 一样维护一个 pending selection ref，第 n+1 次计算基于该 ref 而非 `this.view`/`selection`。
- 置信度：中

### F3 [P2][契约] src/dashboard/components/WorkflowSettingsDialogRegion.tsx:242 —— hostOptions 读扁平 `maxConcurrency`、写嵌套 `queue.maxConcurrency`，形状不自洽，可能清空最大并发
- 证据：
```
      typeof maxConcurrency === "number" &&
      Number.isSafeInteger(maxConcurrency) &&
      maxConcurrency > 0
        ? { queue: { maxConcurrency } }
        : {},
```
- 分析：第 235 行 `const maxConcurrency = form.hostOptions.maxConcurrency;` 按扁平形状读入，而第 464 行 `const queue = asRecord(props.draft.hostOptions.queue);`、第 431 行写回、第 493 行控制键 `data-workflow-settings-control-key="hostOptions.queue.maxConcurrency"` 以及 `buildWorkflowSettingsDialogExecutionOptions` 全部使用嵌套 `queue`。若 `form.hostOptions` 承载的是「上一次 update-draft 发出的形状」（控制键即此意），则 `form.hostOptions.maxConcurrency` 恒为 undefined → 输入框空白、draft.hostOptions 被初始化成 `{}`，用户原设置的最大并发在 confirm/cache-refresh 时被静默清除。同文件另一处（`createWorkflowSettingsDialogDraft`）与 `WorkflowOptionsRegion.createWorkflowSettingsDraft`（逐字 clone）不一致，说明两者对同一 wire 字段形状的判断不同，至多一个是错的。
- 建议：统一为 `asRecord(form.hostOptions.queue).maxConcurrency`（或统一为扁平写出），并让控制键与读写形状一致。
- 置信度：中

SLICE: S29
FILES: 12 个 / 9997 行；ledger: /tmp/review/ledger/S29.md
COVERAGE: 已细读 12 个；略读 0 个
TOP FINDINGS（只列 P0/P1/P2，按严重度排序，每条 ≤ 25 词）:
- [P2][正确性] MigrationsRegion.tsx:319 搜索框受控于异步回显，快速输入被回显覆盖丢字/跳光标
- [P2][并发] RuntimeLogsRegion.tsx:464 行勾选/level 基于上次回显计算，快速连点吞掉前一次选择
- [P2][契约] WorkflowSettingsDialogRegion.tsx:242 hostOptions 读扁平写嵌套，可能清空 hostOptions.queue.maxConcurrency
NOISE:（1）WorkflowOptionsRegion 原生 change/blur 用 `[]` 依赖 + 分支可换 input 元素，schema 变更但 resetKey 不变时可能残留旧监听（已列存疑）；（2）BackendManagerRegion `buildAcpPresetDisplayName` 拼出 `"(npm)(Isolated)"` 缺空格；（3）多个 vendor 句柄（createMultiSelect/createCustomSelect）契约里没有 destroy，重建时只移除 DOM 节点，若组件在 document 上挂了监听则会泄漏。