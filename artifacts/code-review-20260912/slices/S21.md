| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| packages/synthesis-engine/src/canonicalJson.ts | 15 | OK | 纯 re-export 别名映射，未发现问题 |
| packages/synthesis-engine/src/citationGraphBuild.ts | 1469 | 问题 | P2: F1 aggregateEdges.sourceRefs 生产/校验规范化不一致；P2: F2 同一 targetId 的 targetKind 冲突被静默合并 |
| packages/synthesis-engine/src/citationGraphBuildTransfer.ts | 496 | OK | 分页/清单重建自洽；但 page rebuild 复用 citationGraphBuild 的验证器，实际受 F1 影响 |
| packages/synthesis-engine/src/conceptKbIndex.ts | 941 | 问题 | P2: F3 派生 search.normalized 可超 stringMax；P2: F4 normalizedKey 未固定 locale |
| packages/synthesis-engine/src/index.ts | 1282 | OK | 布局/度量请求与结果校验自洽；仅 BFS O(n²) 性能观察（NOISE） |
| packages/synthesis-engine/src/referenceMatcher.ts | 4269 | 问题 | P2: F5 suggestedCandidates 排序校验过严；P2: F6 可选布尔被强制必填；P2: F7 canonical_count 与静默丢弃 |
| packages/synthesis-engine/src/tagVocabulary.ts | 898 | 问题 | P3（判断）: F11 请求提供的 tagPattern 被直接编译并逐条执行 |
| packages/synthesis-engine/src/topicGraphIndex.ts | 441 | OK | 索引重建与结果校验一致，未发现问题 |
| packages/synthesis-engine/src/topicStructuredArtifact.ts | 1733 | 问题 | P2: F8 冲突只报第一条；P2: F9 校验结果不重算；P2: F10 source_paper_refs 非数组绕过校验 |

## B. Findings

### F1 [P2][契约] packages/synthesis-engine/src/citationGraphBuild.ts:542 —— 重建校验对 aggregateEdges.sourceRefs 强制排序/去重/≤256，生产端不满足，导致合法结果被拒
- 证据：
  ```
    sourceRefs: stringList(object.sourceRefs ?? [], `${location}.sourceRefs`, {
      unique: true,
      sort: true,
    }),
  ```
  ```
        sourceRefs: [reference.sourceRef || reference.referenceId],
  ```
  ```
    entry.sourceRefs.push(...edge.sourceRefs);
  ```
  ```
  if (
    JSON.stringify(result.aggregateEdges) !== JSON.stringify(expectedAggregates)
  ) {
    return invalid("result aggregate edges are inconsistent");
  }
  ```
- 分析：`rebuildAggregateEdge` 通过 `stringList`（内部 `requiredString(..., ROLE_MAX=256)`）对 `sourceRefs` 做排序+去重+单串 256 上限；而 `computeRebuiltSynthesisCitationGraphBuild` 按 referenceId 顺序 `push` 且不去重（992/867 行），而 `reference.sourceRef` 的校验上限是 `TEXT_MAX=4096`。触发条件任一成立即失败：(a) 同一 (source,target) 有两条 reference 且 sourceRef 相同 → 生产端 ["X","X"]，校验端 ["X"]；(b) 两条 reference 的 sourceRef 顺序与 referenceId 顺序相反 → 生产端未排序；(c) 任一 sourceRef/回退 referenceId 超过 256 字符（referenceId 允许到 512）。`expectedAggregates` 由未排序的请求侧数据计算，因此 `JSON.stringify` 比较必然失败；`citationGraphBuildTransfer` 的 page rebuild 正是复用这些函数（`rebuildSynthesisCitationGraphBuildAggregateEdgePage`），会抛 `page_descriptor_mismatch` 或 `result aggregate edges are inconsistent`。
- 建议：让生产端与校验端使用同一规范化（在 `aggregateSynthesisCitationGraphBuildEdges`/evidence 组装处对 sourceRefs 去重排序，并把单串上限统一为 IDENTIFIER_MAX/TEXT_MAX），或去掉校验端的 `unique/sort` 并按同一顺序生成 `expectedAggregates`；当前 `stringList` 的 ROLE_MAX 默认上限对 sourceRefs 明显过小。
- 置信度：高

### F2 [P2][正确性] packages/synthesis-engine/src/citationGraphBuild.ts:955 —— 同一 targetId 的 targetKind 冲突时按 referenceId 顺序静默定型
- 证据：
  ```
    const existingTarget = nodes.get(reference.targetId);
    if (existingTarget) {
      mergeNodeMetadata(existingTarget, {
  ```
  ```
    if (
      reference.targetKind === "library_paper" &&
      !libraryIds.has(reference.targetId)
    ) {
      return invalid("library reference target is missing");
    }
  ```
- 分析：`rebuildSynthesisCitationGraphBuildRequest` 对非 library target 只把 targetId 收进 `targetIds` 集合做数量限制，从不要求同一 targetId 的所有 reference 声明一致的 `targetKind`；`computeRebuiltSynthesisCitationGraphBuild` 在第二次遇到同一 targetId 时只 `mergeNodeMetadata`，不更新 `kind`。因此“external_reference”和“unresolved_reference”会静默折叠成先访问者（references 按 referenceId 排序）的 kind，另一条 reference 的 kind 语义丢失；结果校验也只对 library_paper 节点核对 kind，不会发现。下游（metrics/ownership 消费者）会把该目标当成固定 kind，且 kind 依赖 referenceId 排序这一实现细节。
- 建议：在 `rebuildSynthesisCitationGraphBuildRequest` 中维护 `targetId → targetKind` 映射，冲突即 `invalid`；或让节点 kind 由所有引用确定性推导。
- 置信度：高

### F3 [P2][契约] packages/synthesis-engine/src/conceptKbIndex.ts:550 —— 派生 search.normalized 可远超 stringMax，rebuildSearchRow 却强制上限，校验必拒绝引擎自身输出
- 证据：
  ```
      normalized:
        `${concept.label} ${concept.aliases.join(" ")} ${concept.shortDefinition || ""} ${concept.definition || ""}`.toLowerCase(),
  ```
  ```
    normalized: boundedString(
      row.normalized,
      `search[${index}].normalized`,
      bounds,
    ),
  ```
  ```
  if (!sameJson(rebuilt, computeIndex(request, {}))) {
    invalid("index result does not match the request");
  }
  ```
- 分析：请求校验允许每个 concept 最多 `perConceptAliasMax=256` 个别名、每个字符串 ≤`stringMax=4096`，因此拼接后的 search 文本可轻松超过 4096（例如 200×100 字符别名）。`computeIndex` 不做任何长度检查就产出该 `normalized`；而 `rebuildSynthesisConceptKbIndexResult`→`rebuildSearchRow` 用 `boundedString` 拒绝 >stringMax，于是对合法请求的合法引擎输出会抛 “exceeds the string bound”。同仓库 tagVocabulary 在 `rebuildEntry` 对等价派生文本做了 `if (normalizedSearchText.length > bounds.stringMax) invalid(...)`（tagVocabulary.ts:327-329），可确认是有意为之的不变量。
- 建议：在 `computeIndex` 组装 search 行时对派生文本做显式上限检查（或按契约截断/拒绝），并让 rebuild 端与声明上限保持一致。
- 置信度：高

### F4 [P2][正确性/确定性] packages/synthesis-engine/src/conceptKbIndex.ts:532 —— normalizedKey 使用未指定 locale 的 toLocaleLowerCase
- 证据：
  ```
  function normalizedKey(value: string) {
    return value.trim().toLocaleLowerCase().replace(/\s+/g, " ");
  }
  ```
- 分析：`computeQuery` 用 `normalizedKey` 建立 concepts/aliases 的匹配键；无 locale 的 `toLocaleLowerCase()` 依赖宿主默认 locale（土耳其语下 "I"→"ı"），同一请求在不同默认 locale 的宿主上会得到不同 `matches`，与仓库其它地方的确定性约定不一致（referenceMatcher 的 `normalizeSynthesisLiteratureTitle` 固定 `toLocaleLowerCase("en-US")`）。查询结果虽未在本文件哈希，但会直接影响匹配语义与任何上层缓存/比对。
- 建议：改为 `.toLowerCase()` 或 `.toLocaleLowerCase("en-US")`。
- 置信度：中

### F5 [P2][契约] packages/synthesis-engine/src/referenceMatcher.ts:4004 —— suggestedCandidates 的排序校验比候选比较器更严，会拒绝本引擎产生的合法结果
- 证据：
  ```
    if (
      previous.score < current.score ||
      (previous.score === current.score &&
        compareSynthesisEngineStrings(previous.paperRef, current.paperRef) >= 0)
    ) {
      return matcherInvalid(`${location}.suggestedCandidates is not ordered`);
  ```
  ```
  return (
    right.score - left.score ||
    Number(right.evidence.author_overlap_count || 0) -
      Number(left.evidence.author_overlap_count || 0) ||
    yearRank(right) - yearRank(left) ||
    compareSynthesisEngineStrings(left.paperRef, right.paperRef)
  );
  ```
- 分析：`resolveReferenceWithPolicy` 用 `candidateSort` 排序：同分时先比 `author_overlap_count`、再比 `yearRank`、最后才比 `paperRef`；`rebuildMatcherBindingDecision` 要求同分时严格按 `paperRef` 升序。两个同分候选（例如 ambiguous identifier 路径对所有候选都赋 score=1；或多条 exact title 匹配同为 0.999）如果 overlap/yearRank 排序与 paperRef 顺序相反，生产端会把 paperRef 较大者排在前，校验端立即判 “suggestedCandidates is not ordered”，拒绝一个由同一算法产生的合法结果。ambiguous 路径正是多候选场景。
- 建议：让校验端复用 `candidateSort`（或直接重算比较），或在 `candidateSort` 中把 `paperRef` 提前到同分 tie-break 的第一位。
- 置信度：高

### F6 [P2][契约] packages/synthesis-engine/src/referenceMatcher.ts:3599 —— acceptedBinding/stickyRepresentative 在类型中可选，运行时却强制必填
- 证据：
  ```
    acceptedBinding?: boolean;
    stickyRepresentative?: boolean;
  ```
  ```
      acceptedBinding: matcherBoolean(
        object.acceptedBinding,
        `${location}.acceptedBinding`,
      ),
      stickyRepresentative: matcherBoolean(
        object.stickyRepresentative,
        `${location}.stickyRepresentative`,
      ),
  ```
- 分析：公开类型 `ReferenceCanonicalDedupeInput` 把这两个字段声明为可选，且算法本身容忍缺失（`acceptedBinding: Boolean(input.acceptedBinding)`）；但 `computeSynthesisReferenceDedupe` → `rebuildSynthesisReferenceDedupeRequest` → `rebuildDedupeCanonical` 对每个 canonical 强制 `matcherBoolean`，TS 调用方按类型省略字段后在运行期抛 `SynthesisReferenceMatcherContractError`。这是类型/运行时契约漂移，且最容易在测试或跨模块调用中触发。
- 建议：校验端默认 `false`（缺失即 false），或把 DTO 改为必填并同步类型。
- 置信度：高

### F7 [P2][错误处理/契约] packages/synthesis-engine/src/referenceMatcher.ts:2445 —— 被静默丢弃的 canonical 无诊断，且 counters.canonical_count 与请求数不一致使重建必失败
- 证据：
  ```
    if (!canonicalReferenceId || !normalized || tokens.length < 2) {
      return null;
    }
  ```
  ```
      .filter((record): record is CanonicalDedupeRecord => Boolean(record))
  ```
  ```
        canonical_count: records.length,
  ```
  ```
    counters.canonical_count !== request.canonicals.length ||
  ```
- 分析：`rebuildDedupeCanonical` 允许只有 `canonicalReferenceId`、没有 title/normalizedTitle 的记录通过请求校验；`canonicalDedupeRecord` 对没有标题或不足 2 个内容 token 的输入返回 null，被 `.filter(Boolean)` 丢弃且不产生任何 diagnostics（只有 eligibility==="excluded" 才进 diagnostics）。于是 `computeSynthesisReferenceDedupe` 产出的 `counters.canonical_count` 小于 `request.canonicals.length`，而 `rebuildSynthesisReferenceDedupeResult` 要求两者相等，校验器会拒绝本引擎的输出；同时调用方无法知道多少记录被丢弃。
- 建议：在请求校验阶段对这种 canonical 直接报错，或保留 `canonical_count = 输入数` 并新增 dropped 计数与 diagnostics。
- 置信度：高

### F8 [P2][正确性/契约] packages/synthesis-engine/src/topicStructuredArtifact.ts:1195 —— 冲突只返回第一条 mismatch，重建校验要求完整列表
- 证据：
  ```
    if (current[section] !== hash) {
      return {
        status: "conflict",
        mismatches: [
          {
            name: `section:${section}`,
            base: hash,
            current: current[section] || "",
          },
        ],
      };
  ```
  ```
    const mismatches = Object.entries(read)
      .filter(([name, hash]) => current[name] !== hash)
      .map(([name, base]) => ({
        name: `section:${name}`,
        base,
        current: current[name] || "",
      }));
  ```
  ```
      invariantValid =
        mismatches.length > 0 &&
        sameCanonicalJson(rebuilt.mismatches, mismatches);
  ```
- 分析：`applyTopicSectionPatch` 在遇到第一个 hash 不一致时立刻 return，只上报一条 mismatch；`rebuildSynthesisTopicSectionPatchResult` 从 request 重算全部 mismatch 并要求 `rebuilt.mismatches` 与之完全相等。当存在 ≥2 个冲突 section 时，引擎自身输出无法通过重建校验（抛 “section patch result violates request invariants”），并且即使不校验，调用方也只会看到部分冲突信息。
- 建议：先收集所有 mismatch 再返回 conflict；或让校验端只要求报出的 mismatch 是全部的子集（但语义上应报全量）。
- 置信度：高

### F9 [P2][契约] packages/synthesis-engine/src/topicStructuredArtifact.ts:1490 —— 校验结果重建不重算，伪造的 ok/errors 可通过
- 证据：
  ```
  export function rebuildSynthesisTopicManifestValidationResult(
    input: unknown,
    requestInput: unknown,
    bounds: SynthesisTopicStructuredArtifactContractBounds = {},
  ): SynthesisTopicValidationResult {
    rebuildSynthesisTopicManifestValidationRequest(requestInput, bounds);
    return rebuiltValidationResult(
      input,
      SYNTHESIS_TOPIC_MANIFEST_VALIDATION_VERSION,
    );
  }
  ```
  ```
    if (
      row.contractVersion !== rebuilt.contractVersion ||
      row.algorithmVersion !== rebuilt.algorithmVersion ||
      rebuilt.ok !== (errors.length === 0)
    ) {
      contractInvalid("validation result is inconsistent");
    }
  ```
- 分析：manifest/artifact 两个 rebuild 入口只校验请求本身与结果的结构一致性（版本、errors 类型、ok 与 errors 长度匹配），从不重跑 `validateTopicAnalysisManifest`/`validateTopicSynthesisArtifact` 做等值比较；而 conceptKbIndex、tagVocabulary 的同类 rebuild 都会 `compute + sameJson` 严格比对。结果是一个声称 `ok:true, errors:[]` 的 payload 可对任意非法 manifest 通过 ingress 校验，使“校验结果”在信任边界上失去意义。
- 建议：在这两个 rebuild 中重算 validate* 并比较（必要时比较错误指纹），与其它 engine 的 rebuild 语义对齐。
- 置信度：中

### F10 [P2][正确性] packages/synthesis-engine/src/topicStructuredArtifact.ts:672 —— source_paper_refs 存在但非数组时绕过必填与存在性校验
- 证据：
  ```
      const hasRefs = "source_paper_refs" in row;
      const refs = Array.isArray(row.source_paper_refs)
        ? row.source_paper_refs.map(cleanString).filter(Boolean)
        : [];
      if (args.requireProperty && !hasRefs) {
  ```
  ```
      if ("source_paper_refs" in entry) {
        const refs = Array.isArray(entry.source_paper_refs)
          ? entry.source_paper_refs.map(cleanString).filter(Boolean)
          : [];
  ```
- 分析：`requireProperty` 只检查键存在；当值为字符串/数字/null 时 `hasRefs` 为真、`refs` 为空，于是既不会报 “requires source_paper_refs”，也不会发现缺失/未知的 source_papers 引用；`validateNestedSourcePaperRefs`（taxonomy/review_outline 路径）有同样的漏洞。结果是类型错误的字段被当作合法通过，下游按数组使用时会出错或静默丢失引用检查。
- 建议：键存在时强制 `Array.isArray`，否则报类型错误。
- 置信度：高

### F11 [P3][信任边界]（判断） packages/synthesis-engine/src/tagVocabulary.ts:405 —— 请求提供的 tagPattern 被直接编译并逐条执行，未防灾难性回溯
- 证据：
  ```
    const tagPattern = cleanString(row.tagPattern, "protocol.tagPattern", bounds);
    try {
      new RegExp(tagPattern);
    } catch {
      invalid("protocol.tagPattern must be a valid regular expression");
    }
  ```
  ```
      if (!pattern.test(tag) || tag.length > request.protocol.maxTagLength) {
  ```
- 分析：校验只要求可编译，随后用该正则对最多 25000 条 tag 逐条 `test`。形如 `(a+)+$` 的模式会在长 tag 上触发指数级回溯，阻塞本进程。`stringMax` 只限制模式长度，无法阻止回溯爆炸。属于判断性硬化建议（本地引擎，未见网络入口）。
- 建议：限制 tagPattern 的允许语法/长度，或改用非回溯匹配/带超时的执行策略。
- 置信度：中

## NOISE

- `packages/synthesis-engine/src/index.ts:1027-1038`：`computeMetricsComponents` 的 BFS 用 `queue.shift()`，单组件最坏 O(n²)（n ≤ COMPUTE_NODE_MAX=10000，约 5×10⁷ 次元素搬移），且每个节点都对邻接集复制+排序。量级尚可，但作为 metrics 热路径值得观察。
- `packages/synthesis-engine/src/referenceMatcher.ts:3313`：`Math.max(1, Math.floor(value))` 对 `NaN` 返回 `NaN`，会让 `matcherArray` 的 `length > max` 恒为 false 而静默取消上限。当前 bounds 仅来自内部 engine options，暂未发现外部可达路径，列为存疑。
- `packages/synthesis-engine/src/citationGraphBuild.ts:1122` 起的 `canonicalizeResultValue` 无任何调用点（死代码）。另 `scope.kind === "source_slice"` 时 `computeRebuiltSynthesisCitationGraphBuild` 仍计算全部 libraryNodes/references，仅把 scope 原样回填；需确认这是契约有意（scope 只作元数据）还是应为切片过滤。