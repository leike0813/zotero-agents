| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/modules/acp/diagnostics/acpAuditAppendCore.ts | 140 | OK | 缓冲写入键/owner 映射与 flush/discard 语义一致；TextEncoder 每次 append 新建属微优化，未达 finding 门槛。 |
| src/modules/acp/diagnostics/acpBackendRefreshCacheDiagnostic.ts | 4169 | OK | 逐路径读完主要探针逻辑（base64/PowerShell 引号/路径处理正确）；embedded 探针脚本为模板，略读。大量 `compact*` 序列化属同 shape 重复，仅记录不报。 |
| src/modules/acp/diagnostics/acpChatDiagnosticAuditTrail.ts | 170 | OK | owner 丢弃集 + 调试门控逻辑自洽；`discardedOwners` 理论可随会话数增长，规模有限，不作 finding。 |
| src/modules/acp/diagnostics/acpDiagnosticRouter.ts | 51 | OK | 日志与审计 sink 均 try/catch 包裹，不污染执行流。 |
| src/modules/acp/diagnostics/acpDiagnostics.ts | 213 | OK | 证据投影/脱敏/错误序列化逻辑完整；`safeJson` 有兜底。 |
| src/modules/acp/diagnostics/acpRuntimeDiagnosticsMode.ts | 29 | OK | 单模式全局互斥，acquire/release 对称。 |
| src/modules/acp/diagnostics/acpRuntimePerformanceBaseline.ts | 466 | 问题 | P3：`BYTE_METRICS` 漏列 `semantic_event_bytes`（见 F3）。 |
| src/modules/acp/diagnostics/acpRuntimePerformanceProfiler.ts | 915 | 存疑 | `recordPublicationLifecycle` 对 shell_forward/child_apply/render_ack 提前返回 false 会丢弃这些计数（见「存疑」）。 |
| src/modules/acp/diagnostics/acpRuntimeReplayController.ts | 440 | OK | 运行态互斥、completion promise、取消传播均正确；视图克隆为浅拷贝但无可达缺陷。 |
| src/modules/acp/diagnostics/acpRuntimeReplayIdentity.ts | 116 | OK | phase 校验、slug、artifact stem 唯一性正确。 |
| src/modules/acp/diagnostics/acpRuntimeReplayLogicalTime.ts | 157 | OK | 逻辑时钟批处理/释放语义自洽。 |
| src/modules/acp/diagnostics/acpRuntimeReplayProductionPorts.ts | 522 | OK | 生产端口实现与接口一致；R2 合成负载计数与校验常量对应。 |
| src/modules/acp/diagnostics/acpRuntimeReplayProfileContext.ts | 24 | OK | 简单上下文持有，拷贝隔离。 |
| src/modules/acp/diagnostics/acpRuntimeReplayProfiler.ts | 1619 | OK | 矩阵状态机、phase provenance、acceptance 规则读毕；硬编码 33/536/8 为对合成负载的守护常量。 |
| src/modules/acp/diagnostics/acpRuntimeReplayPublicationSidecar.ts | 421 | 问题 | P3：`inspectCurrent` 中存在不可达分支（见 F4）。 |
| src/modules/acp/diagnostics/acpRuntimeReplayTargets.ts | 415 | 问题 | P2：连接失败路径泄漏 adapter factory 注册（见 F2）。 |
| src/modules/acp/diagnostics/acpRuntimeSemanticTrace.ts | 275 | OK | NDJSON 完整性/摘要重算与 recorder 写出口径一致。 |
| src/modules/acp/diagnostics/acpRuntimeSemanticTraceRecorder.ts | 747 | 问题 | P2：shutdown 丢弃未决 finishWaiters 导致挂起（见 F1）。 |

### F1 [P2][正确性/并发] src/modules/acp/diagnostics/acpRuntimeSemanticTraceRecorder.ts:737 —— shutdown 丢弃未决完成等待者，使等待中的完成 Promise 永不 settle
- 证据：
  ```
  export async function shutdownAcpRuntimeSemanticTraceRecorder() {
    await writeChain;
    releaseAcpRuntimeDiagnosticsMode("recording");
  ...
    finishWaiters = [];
  ```
  （对照 `freezeIncomplete` 第 205 行：`for (const resolve of finishWaiters.splice(0)) resolve(view);`）
- 分析：当 `finishAcpRuntimeSemanticTraceRoot({ waitForActivities: true })` 在 `stopping` 状态下把 resolver 压入 `finishWaiters` 并返回一个 Promise（第 626-630 行）。若此时调用 `shutdownAcpRuntimeSemanticTraceRecorder`（插件卸载/禁用路径），第 737 行直接 `finishWaiters = []` 而不 resolve/reject，等待方永远挂起。`resetAcpRuntimeSemanticTraceRecorder` 虽在第 669-673 行对 recording/stopping 抛错而规避，但 shutdown 没有该保护。
- 建议：在清空前像 `freezeIncomplete` 一样 `finishWaiters.splice(0).forEach(r => r(recorderView()))`，或显式 reject。
- 置信度：中

### F2 [P2][资源] src/modules/acp/diagnostics/acpRuntimeReplayTargets.ts:100 —— adapter factory 注册后连接失败不回滚，注册项泄漏
- 证据：
  ```
  registerAcpConnectionAdapterFactory({
    backend,
    conversationId,
    factory: async () => {
  ...
  await connectAcpConversation({ backendId, conversationId });
  if (!adapter) {
    throw new Error("Synthetic ACP Chat adapter was not created");
  }
  ```
- 分析：工厂在 `connectAcpConversation` 之前注册，但该 await 若抛错，函数直接抛出（矩阵记为 `target-activation` 失败，`target` 为 undefined，`target.cleanup()` 不会执行）。`unregisterAcpConnectionAdapterFactory` 只在 cleanup 路径调用（第 241 行）。由于每次 replay 的 `syntheticRootId` 带 nonce（`acp-replay-${matrixNonce}-${ownerNonce}`），backendId/conversationId 各不相同，失败注册会随重放次数无界累积。
- 建议：把注册+连接放入 try，失败时 `unregisterAcpConnectionAdapterFactory(backendId, conversationId)` 后再抛。
- 置信度：中

### F3 [P3][契约] src/modules/acp/diagnostics/acpRuntimePerformanceBaseline.ts:152 —— BYTE_METRICS 漏列 semantic_event_bytes，bytes 汇总少计
- 证据：
  ```
  const BYTE_METRICS = new Set<AcpRuntimeMetricName>([
    "run_persist_bytes",
    "host_input_bytes",
  ...
    "runtime_log_persist_bytes",
  ]);
  ```
  （`semantic_event_bytes` 在风险组表第 93 行声明为 `"R1"`，却未进入该集合）
- 分析：所有其它 `*_bytes` 指标都通过 BYTE_METRICS 归入 `bytes` 字段（第 376 行 `const field = BYTE_METRICS.has(metric.name) ? "bytes" : "counter";`），唯独 `semantic_event_bytes` 被当作 `counter`，使 governance baseline 的字节总量系统性少计该项。
- 建议：把 `"semantic_event_bytes"` 加入 `BYTE_METRICS`。
- 置信度：中

### F4 [P3][可维护性] src/modules/acp/diagnostics/acpRuntimeReplayPublicationSidecar.ts:284 —— inspectCurrent 中不可达分支
- 证据：
  ```
      const barrierPublications = targetPublicationId
        ? sourcePublications.filter(
  ...
      if (!targetPublicationId) {
        lastPublicationDetail = `target=unassigned,pending=${
  ```
- 分析：第 256 行已在 `!targetPublicationId` 时 `return`，故第 284 行的同一判断恒为假，`barrierPublications` 的三元 else 分支亦恒不可达。属死代码，掩盖了本意，易误导后续修改。
- 建议：删除该分支及三元 else，改为直接使用 `sourcePublications.filter(...)`。
- 置信度：高

## 存疑
- src/modules/acp/diagnostics/acpRuntimePerformanceProfiler.ts:419 `const stage = name === "panel_post" ? "post" : null;` 与 421-427 行的提前 `return false`：`panel_shell_forward`/`panel_child_apply`/`panel_render_ack` 会让 `incrementAcpRuntimeMetric` 直接 return（第 744 行 `if (!ownedPublicationStage) return;`），从而不建立这些计数序列。若这是为避免与 `recordAcpRuntimePublicationAck` 双计而有意为之则正常；否则任何以这些名字上报的计数都会被静默丢弃。需确认调用方是否会用这些名字调用 `incrementAcpRuntimeMetric`（切片外文件）。

SLICE: S42
FILES: 18 个 / 10889 行；ledger: /tmp/review/ledger/S42.md
COVERAGE: 已细读 18 个；略读 1 个（acpBackendRefreshCacheDiagnostic.ts 内嵌 PowerShell/Node 探针脚本文本，其余逻辑已细读）
TOP FINDINGS（只列 P0/P1/P2，按严重度排序，每条 ≤ 25 词）:
- [P2][并发] acpRuntimeSemanticTraceRecorder.ts:737 shutdown 丢弃未决 finishWaiters，等待中的完成 Promise 永不 settle。
- [P2][资源] acpRuntimeReplayTargets.ts:100 连接失败不回滚 adapter factory 注册，随重放无界累积。
NOISE: 主代理可复核 acpRuntimePerformanceProfiler.ts:419 早期 return 是否有意丢弃 shell_forward/child_apply/render_ack 计数（存疑，未列为缺陷）。