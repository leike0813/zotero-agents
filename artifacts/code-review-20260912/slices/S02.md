| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| scripts/host-bridge/check-host-bridge-agent-language.ts | 124 | OK | 全局正则 lastIndex 已重置；无运行期缺陷 |
| scripts/host-bridge/check-host-bridge-consumer-guidance.ts | 262 | OK | 仅 `--baseline-ref` 空值时静默降级（判断性，噪声级） |
| scripts/host-bridge/check-host-bridge-skill-packages.ts | 698 | OK | 计数基线为 ratchet 用途；每卡重复读文件 3 次，仅 CI 成本 |
| scripts/host-bridge/check-plugin-host-bridge-assets.ts | 367 | OK | 同一 xpi 读两次；ACP 侧车无外部期望值（见存疑） |
| scripts/host-bridge/dispatch-host-bridge-release.ts | 274 | OK | 本地门禁 splice 索引硬编码 3，行为不变 |
| scripts/host-bridge/host-bridge-agent-surface.ts | 259 | 问题 | P3: 内容寻址 canonical 化重复（F7）；P3: 空 intent 全命中（F8）；`_catalog` 参数未被使用（F4 引用） |
| scripts/host-bridge/host-bridge-command-contracts.ts | 481 | OK | protocol/cliSchema 字面量与 src/shared 常量分离（F7 同类，未单列） |
| scripts/host-bridge/host-bridge-release-controller.ts | 297 | 问题 | P2: `--surface-status` 未校验即落盘（F1） |
| scripts/host-bridge/host-bridge-release-plan.ts | 192 | OK | git 失败静默降级为空串，仅影响降级精度 |
| scripts/host-bridge/host-bridge-release-set.ts | 375 | 问题 | P3: 第二份 stableJson + localeCompare 排序参与 releaseSetId（F7） |
| scripts/host-bridge/host-bridge-review-mirror.ts | 608 | OK | 路径均经 assertInside/无符号链接校验；swap 失败会重抛但目标已更新（低） |
| scripts/host-bridge/host-bridge-semantic-review-context.ts | 318 | OK | 分类优先级清晰；未分类项落到 unclassifiedChanges 属设计 |
| scripts/host-bridge/host-bridge-surface-catalog.ts | 287 | OK | 与 command-contracts 的 CommandTarget 形状重复；execFileSync 未设 maxBuffer（存疑） |
| scripts/host-bridge/host-bridge-surface-model.ts | 336 | 问题 | P2: skill.id/mount 缺穿越校验（F2） |
| scripts/host-bridge/host-bridge-surface-version.ts | 58 | OK | — |
| scripts/host-bridge/host-bridge-version-intent.ts | 30 | 问题 | P3: 版本文法与另两处不一致（F5） |
| scripts/host-bridge/host-bridge-workflow-catalog.ts | 132 | OK | — |
| scripts/host-bridge/materialize-host-bridge-surfaces.ts | 329 | OK | 模块级 `ROOT=process.cwd()` 与顶层 argv 副作用（可测试性，低） |
| scripts/host-bridge/prebuild-zotero-bridge-cli.ts | 381 | OK | ref/sha 校验完整、顺序正确 |
| scripts/host-bridge/prepare-host-bridge-release.ts | 125 | OK | `run()` 未检查 `result.error`，ENOENT 时错误信息失真（低） |
| scripts/host-bridge/render-host-bridge-release-set.ts | 122 | 问题 | P3: catalog 构建后被丢弃（F4） |
| scripts/host-bridge/render-host-bridge-surfaces.ts | 1410 | 问题 | P2: `mode`/`--content-only` 完全无效（F3） |
| scripts/host-bridge/render-host-mutation-contract.ts | 124 | OK | 顶层读写为 CLI 入口；--check 幂等比较 |
| scripts/host-bridge/stage-host-bridge-cli-prebuilds.ts | 162 | 问题 | P3: 生产路径依赖 `...ForTests` seam（F6） |
| scripts/host-bridge/sync-host-bridge-cli-prebuilds.ts | 719 | 问题 | P3: 导出 `...InternalsForTests` 成为生产 API（F6） |
| scripts/host-bridge/zotero-bridge-cli-release.ts | 34 | OK | 不校验 buildFingerprint 形状，由下游 buildHostBridgeReleaseSet 兜底 |
| scripts/internal/cleanup-runtime-category-cli.ts | 279 | 存疑 | 需确认消费方 `bindByIndex` 是否 0 基（mozIStorage 真实 API 为 1 基） |

### F1 [P2][信任边界] scripts/host-bridge/host-bridge-release-controller.ts:282 —— CLI 未校验 `--surface-status`，非法枚举被原子写进 v2 receipt

- 证据：
  ```
  282	    const surfaceStatus = option("surface-status") as SurfaceStatus;
  ```
  ```
  146	    surfaces: { ...receipt.surfaces, ...(event.surfaces || {}) },
  ```
- 分析：`advance` 分支只校验 `--step`/`--status`（`STEPS.includes(step)` / `["pending","complete","failed"]`），`--surface-status` 仅做 TS 断言。`surfaceUpdates()` 把原样字符串塞进 `{status, contentDigest}`，`advanceHostBridgeReleaseReceipt()` 只检查 digest 一致、published/verified 需 commit、verified 不可回退，**从不校验 `update.status ∈ {pending,published,verified,failed}`**。因此 `--surface-status=complete`（与步状态同名，极易误用）会通过 `writeJsonAtomic` 生成一份 surfaces.status 非法的 `host-bridge.release-receipt.v2`，随后 publish/verify 完整性检查报出误导性错误（“Publish completion requires all immutable surface facts”），而真正的坏值已持久化。
- 建议：在 `surfaceUpdates()` 或 `advanceHostBridgeReleaseReceipt()` 入口对 surface status 做白名单校验并抛错，禁止写盘。
- 置信度：高

### F2 [P2][信任边界] scripts/host-bridge/host-bridge-surface-model.ts:109 —— 只校验 `skill.source` 的路径穿越，`skill.id`/`skill.mount` 未校验，可越出 bundle/仓库根

- 证据：
  ```
  109	      requireNonEmpty(skill.id, `${surface.id}.skills.id`);
  110	      requireNonEmpty(skill.mount, `${surface.id}.skills.mount`);
  ```
  ```
  132	      if (skill.mount !== `skills/${skill.id}`) {
  ```
- 分析：`source` 明确被禁止 `..`/`../`（116-121 行），但 `id`/`mount` 只要求非空且 `mount === skills/${id}`，该等式对任何 `id` 都成立。下游把二者直接 `join` 使用：本文件 `hostBridgeSkillGeneratedRoot()` 第 55-57 行 `join(surface.generatedRoot, skill.id)`；`materialize-host-bridge-surfaces.ts:127-130` `join(targetRoot, skill.mount)`；`render-host-bridge-surfaces.ts:104` 用 `skill.id` 作为 ContentMap 前缀，最终经 `applyContent` 的 `join(outputRoot, target)` 写盘（无 assertInside）。构造 `id: "../../evil"`, `mount: "skills/../../evil"` 即可让生成/物化写到仓库根之外（且 prune 只作用于 targetRoot，逃逸文件不会被清理）。输入是受版本控制的 `contracts/host-bridge/surfaces.json`，属供应链/笔误面。
- 建议：对 `skill.id`/`skill.mount` 复用 `source` 的同一套穿越规则（禁止绝对路径、`..` 段），或断言 resolve 后的目标仍位于对应 root 之内。
- 置信度：高

### F3 [P2][契约/死代码] scripts/host-bridge/render-host-bridge-surfaces.ts:1268 —— `mode` 参数与 `--content-only` 完全无效

- 证据：
  ```
  1267	    check?: boolean;
  1268	    mode?: RenderMode;
  ```
  ```
  1407	    mode: process.argv.includes("--content-only") ? "content" : "release",
  ```
- 分析：`renderHostBridgeSurfaces` 参数表里声明了 `mode`，CLI 入口也按 `--content-only` 计算并传入，但函数体内从未读取 `args.mode`（全文只有第 41 行的类型定义与第 1268 行的声明/第 1407 行的传入）。任何以 `--content-only` 调用的发布路径（npm script/CI）都不会改变行为，仍执行含 prune 的完整 release 渲染；调用方以为“只渲染内容”的意图被静默吞掉。
- 建议：要么删除 `mode`/`--content-only`（若确已废弃），要么在 `renderHostBridgeSurfaces` 内真正分支（例如 content-only 跳过 release 面向的 applyContent/prune）。
- 置信度：高

### F4 [P3][性能] scripts/host-bridge/render-host-bridge-release-set.ts:37 —— 白构建一次 surface catalog（触发 `cargo run`）后被 `_catalog` 丢弃

- 证据：
  ```
  36	  const descriptor = buildHostBridgeAgentSurfaceDescriptor(
  37	    buildHostBridgeSurfaceCatalog(root),
  38	  );
  ```
  对应形参（host-bridge-agent-surface.ts:143-146）：
  ```
  143	export function buildHostBridgeAgentSurfaceDescriptor(
  144	  _catalog?: unknown,
  145	  root = process.cwd(),
  ```
- 分析：`buildHostBridgeSurfaceCatalog()` 会 `execFileSync("cargo", [..., "run", "--example", "export-command-inventory"])` 并 AJV 编译两份 contract；而 `buildHostBridgeAgentSurfaceDescriptor` 完全忽略该实参（注释也说明 Node 只调 Rust exporter）。即每次渲染 release-set 都多付一次 cargo 进程（含潜在编译检查）与两次契约加载。同一切片内 `render-host-bridge-surfaces.ts:1300` 构建 catalog 是**必要**的（供 `renderDocSurface` 用），说明该 API 形状掩盖了这里的纯浪费。
- 建议：删除该实参调用（`buildHostBridgeAgentSurfaceDescriptor(undefined, root)` 或去掉形参）。
- 置信度：高

### F5 [P3][契约] scripts/host-bridge/host-bridge-version-intent.ts:2 —— CLI 版本文法与本切片另两处不一致

- 证据：
  ```
  2	  const match = value.match(/^(\d+)\.(\d+)\.(\d+)$/);
  ```
  对照（sync-host-bridge-cli-prebuilds.ts:348）：
  ```
  348	    !/^\d+\.\d+\.\d+(?:[-+][0-9A-Za-z.-]+)?$/.test(manifest.cliVersion)
  ```
  以及（host-bridge-surface-model.ts:260）：
  ```
  260	  const match = /^(\d+)\.(\d+)\.\d+(?:[-+].*)?$/.exec(cliVersion);
  ```
- 分析：同一切片中“CLI 版本”被三处解析，其中两处接受 `-rc.1`/`+build` 后缀，`resolveExactCliReleaseIntent` 只接受纯三段。一旦 `releases/host-bridge/cli-release.json` 出现预发布版本，`prepare-host-bridge-release.ts:36` 调用会以 “Invalid exact CLI version” 失败——即便调用方传的是 `--cli-version` 等于当前值（本应返回 `auto`），失败点与真实原因（版本文法分裂）不符。
- 建议：把版本文法抽成单一来源（共享正则/parse 函数），或明确禁止预发布并在 release 校验处同时收紧。
- 置信度：中

### F6 [P3][可维护性] scripts/host-bridge/stage-host-bridge-cli-prebuilds.ts:15 —— 生产脚本依赖 `syncHostBridgeCliPrebuildInternalsForTests`

- 证据：
  ```
  15	import { syncHostBridgeCliPrebuildInternalsForTests } from "./sync-host-bridge-cli-prebuilds";
  ```
  ```
  60	      await syncHostBridgeCliPrebuildInternalsForTests.verifyArchiveSet(
  ```
  导出侧（sync-host-bridge-cli-prebuilds.ts:700）：
  ```
  700	export const syncHostBridgeCliPrebuildInternalsForTests = {
  ```
- 分析：名为 “ForTests” 的 seam 实际是生产 staging 路径的唯一实现入口（`expectedPlatforms`、`verifyArchiveSet` 均被生产调用，见 60/83/135 行）。这既让命名与行为不符，也使“为测试扩大 production API”的反向版本出现：测试 seam 变成生产契约，任何为测试做的签名调整都会外溢到发布脚本。（判断）
- 建议：把 `expectedPlatforms`/`verifyArchiveSet` 以生产语义命名导出（如 `hostBridgeCliPrebuildInternals`），测试对象仅再导出这些引用。
- 置信度：中

### F7 [P3][可维护性] scripts/host-bridge/host-bridge-agent-surface.ts:178 —— 两份 stable-JSON canonical 化各写一遍，且都用 localeCompare 排序决定内容寻址 digest

- 证据：
  ```
  178	    return `{${Object.entries(value as Record<string, unknown>)
  179	      .sort(([left], [right]) => left.localeCompare(right))
  ```
  对照（host-bridge-release-set.ts:179-181）：
  ```
  179	    return `{${Object.entries(value as Record<string, unknown>)
  180	      .sort(([left], [right]) => left.localeCompare(right))
  181	      .map(([key, entry]) => `${JSON.stringify(key)}:${stableJson(entry)}`)
  ```
- 分析：两处实现同一套“键排序 + 紧凑 JSON”，分别喂给 `hostBridgeAgentSurfaceChecksum()`（→ commandCatalogChecksum，进入 release-set 与 XPI 身份）和 `payloadDigest`/`releaseSetId`。任一处改动（例如换排序比较函数）都会静默改变身份摘要的语义；此外 `localeCompare` 依赖运行环境默认 locale，理论上不同 LANG/ICU 下同一对象可能序列化出不同字节，导致跨机构建摘要不一致。同一 shape 出现 2 处，属可量化重复。
- 建议：抽出一个共享的 canonical JSON 工具（并用码点比较 `<`/`>` 替代 localeCompare），两处共用。
- 置信度：中

### F8 [P3][正确性] scripts/host-bridge/host-bridge-agent-surface.ts:236 —— 空/纯空白 intent 时每个命令都得 100 分，返回任意前 N 条

- 证据：
  ```
  236	      const phraseFields = fields.filter((field) =>
  237	        field.normalize("NFKC").toLowerCase().includes(normalized),
  238	      );
  ```
  与归一化入口（223）：
  ```
  223	  const normalized = intent.trim().normalize("NFKC").toLowerCase();
  ```
- 分析：`intent` 为空时 `normalized === ""`，`String.prototype.includes("")` 恒真，因此 `phraseFields` 非空 → `score = 100`，过滤器 `score > 0` 全部通过，最终按命令名字母序返回前 `limit` 条。调用方（如把用户空搜索词透传）会拿到看似“高匹配”的任意结果，而不是空集，属静默错误默认值。
- 建议：`if (!normalized) return [];`（或在调用前校验）。
- 置信度：高（机制）；影响范围取决于消费方，判定为中。

存疑（不写成缺陷）：
- `check-plugin-host-bridge-assets.ts:193-197`：ACP bridge 二进制没有外部期望 digest，只与同包侧车互校；需确认其真实性是否由签名/另一层校验覆盖。
- `host-bridge-surface-catalog.ts:126-137`：`execFileSync` 未设 `maxBuffer`，而同切片 `host-bridge-agent-surface.ts:160` 显式设 16MB；需确认 `export-command-inventory` 输出规模是否可能超 1MB 默认上限。
- `scripts/internal/cleanup-runtime-category-cli.ts:193`：`bindByIndex(index, value) { params[index] = value; }` 为 0 基，而 mozIStorage 真实 API 为 1 基；需对照 `src/modules/runtimePersistenceGovernance` 的调用约定确认 shim 与消费方一致。