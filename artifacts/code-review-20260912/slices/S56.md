| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/modules/workflow/settings/backendManager.ts | 2959 | 问题 | F3/F5：postMessage 动作通道未校验来源窗口；ACP 指纹失效逻辑与行收集校验各重复 3~4 份（见 F3、F5）。另见「存疑」中 idMapping 恒空、managed-local else 分支 |
| src/modules/workflow/settings/genericHttpBackendPresets.ts | 77 | OK | 纯预设表；`listGenericHttpBackendPresets` 只浅拷贝数组、元素对象可被外部改写（判断项，未达缺陷阈值） |
| src/modules/workflow/settings/workflowParameterOptions.ts | 164 | OK | 分页有 `nextCursor === cursor` 守卫、limit=100 合规；仅缺页数上限（判断项，非缺陷）。存疑项：非法 library 被静默降级为“全部库” |
| src/modules/workflow/settings/workflowSettings.ts | 1187 | 问题 | F1：provider 运行时选项投影与 workflowSettingsDialogModel 重复且行为分叉；F6：run-once seam 是空实现 |
| src/modules/workflow/settings/workflowSettingsDialog.ts | 1161 | 问题 | F7：每次重渲染向 document 追加 click 监听且从不移除；F6：调用空实现后弹“已保存 run-once” |
| src/modules/workflow/settings/workflowSettingsDialogModel.ts | 471 | 问题 | F1：第二套 provider schema 投影（effort 回退语义与 workflowSettings.ts 不一致） |
| src/modules/workflow/settings/workflowSettingsDomain.ts | 471 | OK | 解析/合并/规范化逻辑自洽；runOptions 只在 override 侧生效与“run-once 不持久化”一致 |
| src/modules/workflow/settings/workflowSettingsNormalizer.ts | 108 | OK | hook 前后剥离/回挂 hostOptions，非对象返回值安全回退 |
| src/modules/workflow/settings/workflowSettingsOptionLocalization.ts | 149 | OK | 纯查表 + fallback，无副作用 |
| src/modules/workflow/settings/workflowSettingsWebDialog.ts | 970 | 问题 | F3：动作消息不校验来源/来源窗口，confirm 可携带 runOptions |
| src/modules/workflow/ui/selectionSample.ts | 141 | OK | debug-only 菜单与采样写盘，路径来自用户 pref，错误路径均有 alert |
| src/modules/workflow/ui/workflowDebugProbe.ts | 534 | 问题 | F4：特权对话框内 innerHTML 插值未转义（同块内其它值已转义） |
| src/modules/workflow/ui/workflowEditorHost.ts | 840 | 问题 | F8：脏数据关闭提示只挂在 Cancel 按钮，窗口 X 直接丢弃；另见存疑（context 有界校验与注释矛盾、全局开放 seam 绕过 bounds） |
| src/modules/workflow/ui/workflowExecute.ts | 408 | OK | 选择快照先于设置门捕获并全程复用，持久化剥离 runOptions，日志/提示路径完整 |
| src/modules/workflow/ui/workflowMenu.ts | 415 | 问题 | F2：popup 重建可重入，异步等待期间二次 `popupshowing` 会产生重复菜单项 |

### F1 [P2][契约/可维护性] src/modules/workflow/settings/workflowSettingsDialogModel.ts:275 —— provider 运行时选项投影存在两套实现，同一输入结果不同
- 证据：
  ```
        if (entry.key === "effort") {
          return {
            ...entry,
            enumValues: ["default"],
            disabled: true,
          };
        }
  ```
  对照 src/modules/workflow/settings/workflowSettings.ts:753-764：
  ```
    const resolvedEnumValues =
      key === "effort" && entry.type === "string"
        ? toStringEnum(
            dynamicEnum.length > 0
              ? dynamicEnum
              : enumValues.length > 0
                ? enumValues
                : ["default"],
          )
  ```
- 分析：两处都在把 `ProviderRuntimeOptionSchema` 投影成 UI 条目，并各自复制了 effort 回退、`acpModelProvider` 空枚举过滤、`provider_id` 作用域过滤、动态枚举解析（workflowSettings.ts:711-822 vs workflowSettingsDialogModel.ts:218-299）。差别是实质性的：当 provider 的 `effort` 在 schema 里声明了静态 `enum`（长度>1）而动态枚举为空时，legacy 逻辑（dialogModel）会把枚举强制成 `["default"]` 并置 `disabled`，而 descriptor 逻辑（workflowSettings）保留静态枚举且不禁用。同一个工作流走经典设置对话框还是 Web/提交对话框，会看到不同的可选 effort 与不同的可提交状态；后续任何 provider schema 变更（如新增 `hard_timeout_seconds` 之外的键）也只会在其中一处生效。
- 建议：删除 `workflowSettingsDialogModel.resolveProviderSchemaEntries`/`fromProviderOptionSchema`，让经典对话框复用 workflowSettings 的 `toProviderSchemaEntries`（或把它下移到一个共享投影模块），保持单一事实源。
- 置信度：高

### F2 [P3][并发] src/modules/workflow/ui/workflowMenu.ts:276 —— 弹出菜单重建可重入，异步等待期间再次展开会产生重复菜单项
- 证据：
  ```
  clearPopupChildren(popup);
  ```
  （line 276）与 line 306：
  ```
  const selectionContext = await readSelectionContext(
    createZoteroHostCapabilityBroker(() => win),
  );
  ```
- 分析：`rebuildWorkflowActionPopup` 由 `popupshowing`（line 398-405）触发，先同步清空再 `await` 选择上下文，并在每个工作流里再次 `await evaluateWorkflowSelection`（line 332）。若在等待期间用户再次展开（或菜单被重新渲染），第二次调用会先清空、随后两次调用的后续逻辑各自 append，最终 popup 中出现两份工作流条目；由于清空发生在各自的 await 之前，先完成的那次无法清掉后一次的追加。
- 建议：给 popup 重建加“最后一次请求胜出”的代际号或 per-popup 串行队列（复用 workflowEditorHost 的 enqueue 思路），过期调用在 append 前直接返回。
- 置信度：中

### F3 [P3][信任边界] src/modules/workflow/settings/workflowSettingsWebDialog.ts:898 —— 设置对话框动作通道不校验消息来源，confirm 可携带 runOptions
- 证据：
  ```
        const onMessage = (event: MessageEvent) => {
          const data = event.data as { type?: unknown };
          if (!data || data.type !== "workflow-settings-dialog:action") {
            return;
          }
          void handleAction(data as WorkflowSettingsDialogActionEnvelope);
        };
  ```
  对照 src/modules/workflow/settings/backendManager.ts:2662-2669：
  ```
        const currentFrameWindow = resolveFrameWindow(frame);
        if (
          sourceWindow &&
          currentFrameWindow &&
          sourceWindow !== currentFrameWindow
        ) {
          return;
        }
  ```
- 分析：同一个仓库里 backendManager 至少比对 `event.source` 与 iframe 窗口，而 Web 设置对话框对 `event.source`/`event.origin` 均无校验，任何能向该对话框窗口 postMessage 的窗口（对话框内的其它 frame、同进程窗口）都可驱动 `update-draft` / `confirm` / `cancel`。`confirm` 分支会把 `payload.executionOptions` 直接交给调用方（workflowExecute.ts:231），其中 `runOptions.zoteroHostAccess.autoApproveWrites` 是该通道的最高价值字段；虽然下游 `buildWorkflowRunOptionsForUi(manifest,...)` 仍按 manifest 门控，但这层审批语义不应依赖单一后端兜底。
- 建议：在 `onMessage` 里与 backendManager 一致，记录 `frameWindow` 并校验 `event.source === frameWindow`（或校验 `event.origin` 为 `chrome://<addonRef>`），不符合直接丢弃。
- 置信度：中

### F4 [P3][信任边界] src/modules/workflow/ui/workflowDebugProbe.ts:344 —— 特权对话框内 innerHTML 插值未转义，同一块内其它值却已转义
- 证据：
  ```
      summary.innerHTML = [
        `<div><strong>Debug Mode:</strong> ${result.debugMode ? "on" : "off"}</div>`,
        `<div><strong>Selection:</strong> ${result.selectionSummary.selectionType} / refs=${result.selectionSummary.selectedRefs.map((ref) => `${ref.libraryId}:${escapeHtml(ref.key)}`).join(",") || "-"}</div>`,
        `<div><strong>Workflows Root:</strong> ${result.runtimeSummary.workflowsDir || "-"}</div>`,
  ```
  对照 line 352：`` `<div><strong>Zotero Version:</strong> ${escapeHtml(result.runtimeSummary.zoteroVersion || "-")}</div>` ``
- 分析：`workflowsDir`/`officialWorkflowsDir` 来自注册表目录配置（含用户可控制的路径/目录名），`selectionType` 来自 selection 项的 `kind`，二者未转义就拼进 `innerHTML`；该渲染运行在 chrome 特权窗口（openWorkflowEditorSession），一旦路径/名称含 `<`、`"` 等字符，注入的标签（含事件属性）会在特权文档里解析执行。同一段代码对 `zoteroVersion`、`key`、JSON 都做了转义，说明作者本意是转义，属遗漏。
- 建议：把 `workflowsDir`/`officialWorkflowsDir`/`selectionType` 也走 `escapeHtml`（或改用 `textContent` 逐节点构建）。
- 置信度：中

### F5 [P3][可维护性] src/modules/workflow/settings/backendManager.ts:1449 —— ACP 指纹失效逻辑与后端行收集/校验各重复 3~4 份，易漂移
- 证据（同一段逻辑的第 1 份，另有 1680-1693、1878-1891）：
  ```
  const expectedFingerprint = computeAcpBackendConfigFingerprint(backend);
  if (
    backend.acp?.connectionTest?.configFingerprint &&
    backend.acp.connectionTest.configFingerprint !== expectedFingerprint
  ) {
  ```
- 分析：`resolveAcpBackendFromRow`（1402-1464）、`collectBackendsFromDialog`（1660-1696）、`collectBackendsFromDraftRows`（1859-1894）三处逐字重复“重建 ACP backend → 计算指纹 → 置 stale + 固定英文 error”。同时存在两套几乎同构的整表校验器：DOM 版 `collectBackendsFromDialog`（1602-1766）与草稿版 `collectBackendsFromDraftRows`（1806-1961），包含相同的库/URL/bearer/timeout/重复 id 规则。任何规则调整（例如指纹输入字段、stale 文案、timeout 上限）必须改 2~3 处，漏改会让“配置已变更/仍新鲜”判定在同一后端的不同入口下不一致。
- 建议：抽出一个 `finalizeAcpBackend(backend)` 与一个共用的 `validate+collect(rows)` 内核，DOM 版只负责把控件读成草稿行（`readRowField` 层）后调用它。
- 置信度：高

### F6 [P3][正确性/UX] src/modules/workflow/settings/workflowSettings.ts:1034 —— run-once seam 是空实现，但经典对话框仍提示“已保存”
- 证据：
  ```
  export function applyRunOnceWorkflowSettingsDraft(args: {
    workflowId: string;
    draft: WorkflowExecutionOptions;
  }) {
    void args;
  }
  ```
  调用点 src/modules/workflow/settings/workflowSettingsDialog.ts:1148-1153：
  ```
    applyRunOnceWorkflowSettingsDraft({
      workflowId,
      draft: draft.runOnce,
    });
    refreshWorkflowMenus();
    alertWindow?.alert?.(getString("workflow-settings-run-once-saved" as any));
  ```
- 分析：`setRunOnceWorkflowOverrides`/`clearRunOnceWorkflowOverrides` 同样是空实现，`resetRunOnceOverridesForSettingsOpen` 直接返回持久化快照。因此用户点击 “Apply (run once)” 后：没有任何状态被写入，却收到成功提示；下次打开对话框时 run-once 区域又被重置为持久化值。是否可达取决于 `openWorkflowSettingsDialog` 是否仍被 prefs/菜单事件引用（本切片内无引用），但至少是“成功提示 + 无效果”的确定性不一致。
- 建议：若经典对话框仍在用，实现 override 存储（或让按钮变灰并去掉成功提示）；若已废弃，直接删除 `openWorkflowSettingsDialog` 与这三个空 seam，避免误导。
- 置信度：中

### F7 [P3][资源] src/modules/workflow/settings/workflowSettingsDialog.ts:186 —— 每个 choice 控件向 document 追加 click 监听且从不移除，随重渲染累积
- 证据：
  ```
    doc.addEventListener("click", (event) => {
      const target = event.target as Node | null;
      if (!target || !root.contains(target)) {
        closeAllChoiceLists(doc);
      }
    });
  ```
  重渲染路径（同文件 926-939）：
  ```
          control.addEventListener("change", () => {
            const currentValues = {
              ...args.values,
              ...collectSchemaValues(args.container),
            };
            renderProviderOptionsFields({
  ```
  以及 341：`container.innerHTML = "";`
- 分析：`createChoiceControl` 每次创建都在 `doc`（对话框文档）上挂一个永久监听，而 `renderSchemaFields` 在 dynamic 控件（engine/provider_id/model/acpModelProvider/acpModelId）change 时会整体清空并重建容器，于是每次重渲染都给文档新增 N 个闭包（引用已被移除的节点）、旧闭包永不释放；每次点击又会遍历全部累积监听。交互越多泄漏越大，且这些挂在已分离节点上的闭包对关闭下拉毫无作用。
- 建议：把“点击外部关闭”的监听提到对话框初始化时注册一次（或使用一次性委托到根容器），控件本身不注册 document 级监听。
- 置信度：高

### F8 [P3][正确性] src/modules/workflow/ui/workflowEditorHost.ts:506 —— 未保存更改提示只挂在 Cancel，窗口关闭按钮可静默丢弃
- 证据：
  ```
    unloadCallback: () => {},
  ```
  与 564-569（唯一调用脏检查的地方）：
  ```
      .addButton(labels.cancel, "cancel", {
        noClose: true,
        callback: () => {
          handleAttemptClose();
        },
      });
  ```
- 分析：`handleAttemptClose`/`resolveDirtyCloseDecision` 只在 Cancel 按钮回调里调用；`unloadCallback` 为空，也没有像 backendManager.ts:1250-1267 那样的 `beforeunload` 守卫。用窗口 X（或 Esc 由宿主关闭）关闭时，宿主走 unload，`_lastButtonId` 为空 → 最终返回 `{saved:false, reason:"canceled"}`（line 655-661），用户以为只是取消，实际编辑内容被丢弃且没有任何确认。
- 建议：为编辑器窗口安装与 backendManager 同款的 `beforeunload` 守卫（脏时 `preventDefault`），或在 `unloadCallback` 中无按钮 id 但脏时阻止关闭。
- 置信度：中

### 存疑
- `src/modules/workflow/settings/backendManager.ts:2075`：`const idMapping = new Map<string, string>();` 全程只读为空并原样传给 `syncBackendReferenceState({ idMapping, removedIds })`（2150-2153）。若该 map 的语义是“旧 id → 新 id”，则任何 id 重写都不会同步到引用方；需确认 `syncBackendReferenceState` 是否真的接受空映射、以及是否存在 id 变更路径。
- `src/modules/workflow/settings/backendManager.ts:2110-2127`：非规范 managed-local id 走 `removedIds.add(...)` 删除，而 `id === MANAGED_LOCAL_BACKEND_ID` 分支里的 `normalizedManaged` 只重算了 displayName（id 与 `managed.id` 相同）。需确认这是“清理历史 managed id”的既定策略，还是原本应做 id 迁移（配合上面的空 idMapping）。
- `src/modules/workflow/settings/workflowSettingsDialog.ts:1063-1064`：`await unloadLock` 之后仍读取 `dialogHelper.window?.document` 来取表单值。若 ztoolkit 在 unload 后才 resolve，则该路径会抛“窗口不可用”。无法在只读环境中确认 ztoolkit 生命周期语义，故不作为缺陷。
- `src/modules/workflow/settings/workflowParameterOptions.ts:93-99`：`normalizeLibraryId` 把非法值（`"abc"`、`0`）归一成 0，配合 `requestedLibrary || undefined` 会静默退化为“列出全部库”，而不是报诊断。需确认 `WorkflowParameterOptionsSource.library` 是否已在 workflow 校验层限制。
- `src/modules/workflow/ui/workflowEditorHost.ts:718-720` 对 `args.context` 做 `assertBoundedEditorValue`（严格 JSON），而同文件 422-423 注释声称 context “may carry non-serializable capabilities (callbacks, schedulers)”。需确认 Host 路径是否确实禁止携带回调（若是，注释与实现二选一修正）。
- `src/modules/workflow/ui/workflowMenu.ts:34-38`：`getMenuLabel` 未包 try/catch，而 `getMenuLabelWithArgs` 包了；若 `getString` 在 i18n 未就绪时会抛，菜单构建（含根菜单标签）会在 `popupshowing` 中中断。需确认 `getString` 的失败语义是“返回 key”还是“抛异常”。

SLICE: S56
FILES: 15 个 / 10055 行；ledger: /tmp/review/ledger/S56.md
COVERAGE: 已细读 15 个（backendManager.ts、workflowSettings.ts、workflowSettingsWebDialog.ts、workflowEditorHost.ts、workflowSettingsDialog.ts 及其余 10 个全部逐段核对；无生成物/纯数据文件需略读）
TOP FINDINGS（只列 P0/P1/P2，按严重度排序，每条 ≤ 25 词）:
- [P2][契约] workflowSettingsDialogModel.ts:275 provider 选项投影与 workflowSettings.ts:753 重复且 effort 回退语义分叉，URL/枚举/禁用态会随入口不同（F1）
NOISE: 本切片最值得主代理复核的 3 点：
- backendManager.ts:2075 `idMapping` 恒空 + 2110 非规范 managed id 走删除而非迁移，可能是 id 迁移功能的残留；
- workflowSettingsDialog.ts:1063 在 unloadLock 之后读 DOM 取表单值，可达性取决于 ztoolkit 关闭时序；
- workflowEditorHost.ts:718 对 `context` 强制严格 JSON 与同文件 422 注释冲突，属契约表述不一致。