| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/modules/harness/assistantReadonlyPublication.ts | 1966 | 问题 | P2: ackRoutes 无界增长（F2）；其余只读投影逻辑与生产 DTO 语义一致 |
| src/modules/harness/backendsReadonly.ts | 168 | OK | 只读 prefs 解析，错误累计到 errors/invalidBackends，未发现缺陷 |
| src/modules/harness/dashboardReadonlyModel.ts | 1071 | 问题 | P2: normalizeProduct 的 backendType 回退到 row.backendId（F1）；snapshot 有副作用但属 harness 设计 |
| src/modules/harness/env.ts | 66 | OK | 仅解析白名单 PATH_KEYS；引号/注释处理未见可达缺陷 |
| src/modules/harness/pluginStateReadonly.ts | 435 | OK | safeRows 吞错返回 []（只读诊断场景），未构成缺陷 |
| src/modules/harness/prefsReadonly.ts | 76 | OK | 覆盖 prefs.get，set/clear 抛错，符合只读意图 |
| src/modules/harness/skillRunnerReadonlyProjection.ts | 455 | 问题 | P3: stateSemantics 恒等三元（F6）；其余投影字段与生产 run store 对齐 |
| src/modules/harness/sqliteReadonly.ts | 200 | 问题 | P3: 每次打开整库 backup 到 tmp（F10）；SQL 白名单仅浅层判定但写保护实测由 readOnly 连接兜底 |
| src/modules/harness/synthesisReadonlyClient.ts | 65 | OK | close 幂等，失败路径均释放 hostReadPort/database |
| src/modules/harness/synthesisReadonlyPort.ts | 847 | 问题 | P2: topicPage.next_cursor 无对应翻页实现（F4） |
| src/modules/harness/synthesisWorkbenchI18nEnvelope.ts | 141 | OK | 缓存按 rootDir+locale 有界；FTL 续行处理未污染相邻键 |
| src/modules/harness/zoteroReadonlyLibraryAdapter.ts | 544 | OK | locatorEntries 随扫描累积（会话内小量，未单列 finding）；分页/游标校验完整 |
| src/modules/literatureArtifactMigration/converter.ts | 1318 | 问题 | P2(判断): 32 位 FNV 作为 basisHash（F9）；其余归一化/冲突判定逻辑自洽 |
| src/modules/pluginStateStore/core.ts | 70 | OK | 类型与 normalize 工具，测试 seam 走 Symbol.for 全局注册表 |
| src/modules/pluginStateStore/literatureMigrationTables.ts | 377 | OK | 游标与 ORDER BY 一致，limit 上限 100 |
| src/modules/pluginStateStore/mutationAuthorityTable.ts | 30 起 228 | OK | INSERT OR IGNORE + changes() 判 winner，settle 用 state='started' 守卫 |
| src/modules/pluginStateStore/runTables.ts | 629 | 问题 | P3: 事件/运行列表可无 LIMIT 全量读取（F7） |
| src/modules/pluginStateStore/taskTables.ts | 1167 | 问题 | P2: 字节估算整域全量读（F3）；P2: ACP 会话记录子串启发式删除（F5） |
| src/modules/preferences/skillRunnerLocalRuntimePreferences.ts | 617 | 问题 | P3: confirm 缺失时默认放行、卸载选项默认勾选清除（F8） |

### F1 [P2][正确性] src/modules/harness/dashboardReadonlyModel.ts:532 —— product 的 backendType 回退到了 backendId
- 证据：`backendType: cleanString(product.backendType || row.backendId),`
- 分析：同一对象的 `backendId` 已由上一行 `product.backendId || row.backendId` 取得（531 行），532 行的回退项写成了 `row.backendId`。当产品记录没有 `payload.backendType` 时，productStorageView 里的 `backendType` 会显示/透传成后端 id（如 `skillrunner-local`），与同文件 `normalizeDashboardRow` 的 `cleanString(row.backendType)` 语义不一致，属于可观测的字段语义漂移（消费方若用 backendType 做类型分支会走错分支）。
- 建议：改为 `cleanString(product.backendType || row.backendType)`，并核对 products 视图消费方是否依赖该字段做分派。
- 置信度：高

### F2 [P2][资源] src/modules/harness/assistantReadonlyPublication.ts:1558 —— ackRoutes 只写不删，随发布量无界增长
- 证据：
```
      post(publication) {
        deliveryQueue.push(publication);
        ackRoutes.set(publication.publicationId, runtime);
```
- 分析：`post` 每次发布都写入 `ackRoutes`，而 `acknowledge()`（1708-1728）只 `get` 从不 `delete`；`post` 自己在 queueMicrotask 里直接调 `coordinator.acknowledge(...)`，因此这些路由项在发布后立刻变成纯垃圾。`ackRoutes` 只在 `bootstrap()` 中 `clear()`。长驻侧栏会话反复切 tab / 翻 transcript 时 Map 无界增长，且持续持有已 deactivate 的 runtime 引用，延迟旧 channel 的回收。
- 建议：在 `acknowledge()` 命中后（或在 post 的 microtask 里）`ackRoutes.delete(publicationId)`；`close()` 里一并清空。
- 置信度：高

### F3 [P2][性能] src/modules/pluginStateStore/taskTables.ts:800 —— 字节估算把整个 domain 的 payload 全量拉进 JS
- 证据：
```
    const rows = db.all(
      `
        SELECT domain, scope, task_id, request_id, backend_id, state, updated_at, payload_json
        FROM plugin_task_rows
        WHERE domain=@domain
```
- 分析：`sumPluginTaskRowBytesForScopes` 为了“估算字节数”读取该 domain 全部行的 `payload_json`，再在 JS 里 reduce 求和；`args.scope` / `excludedScopes` 过滤也是读完之后在内存里做的。它被 `estimatePluginTaskDomainBytes` / `estimatePluginTaskDomainExceptRowScopesBytes` / `estimatePluginTaskScopeBytes` 调用，即每次配额估算都做一次全量物化（与大 payload 场景最不兼容）。同仓库 `runTables.estimatePluginRunStoreBytes` 用的是 `SUM(LENGTH(payload_json))`，说明存在低成本写法。
- 建议：改为 `SELECT COALESCE(SUM(LENGTH(payload_json)),0)`，scope/excluded 过滤下推到 SQL（`scope=@scope` / `scope NOT IN (...)`），只对确实需要列级估算的路径保留 JS 计算。
- 置信度：高

### F4 [P2][契约] src/modules/harness/synthesisReadonlyPort.ts:186 —— topicPage.next_cursor 承诺翻页，实现完全忽略 cursor
- 证据：
```
    topicPage: {
      cursor: "",
      next_cursor: total > rows.length ? String(rows.length) : "",
      has_more: total > rows.length,
```
- 分析：行来自 `boundedRows`（第 34-42 行，固定 `ORDER BY rowid LIMIT 200`），`readonlySurfaceProjection` 收到的 `state` 只用于 `reviews.activeTab` / `reviews.cursor` / `reader.topicId`，topics 分页字段从不读取请求侧 cursor。结果：当库里 topic 超过 200 条时，客户端拿 `next_cursor="200"` 再请求会永远拿到同一批前 200 行，`has_more` 恒为 true，形成重复页/无法收敛的分页循环（deletedArtifacts 同理）。
- 建议：要么让 `boundedRows`/`readonlyTopics` 接受并按 cursor（如 `topic_id > @cursor`）分页并保持一致排序，要么明确返回 `next_cursor: ""` / `has_more: false`，不要给出无法兑现的游标。
- 置信度：中

### F5 [P2][正确性/信任边界] src/modules/pluginStateStore/taskTables.ts:1024 —— 以 payload 子串 "conversationid" 判定并删除 ACP 记录
- 证据：
```
    const payload = normalizeString(entry.payload).toLowerCase();
    return (
      isAcpConversationRecordRef(entry.requestId) ||
      payload.includes("conversationid")
```
- 分析：`listAcpConversationTaskRecords` 把该谓词用于 `plugin_task_requests` / `plugin_task_contexts`（这两张表没有 scope 维度，1030-1038 只对 rows 做了 `scope !== "skill-runs"` 过滤），`clearAcpConversationTaskRecords`（1042-1058）随后在事务内逐条删除命中项。任何 payload 中嵌套出现 `conversationId`/`activeConversationId` 的 ACP 记录（例如携带 conversation 上下文的 skill-run 请求）都会被判为“会话记录”并被删除，这是不可逆的数据删除，误伤面无法从记录种类上区分。
- 建议：删除前要求结构化判定（解析 payload 并检查 `conversationId` 字段位于顶层会话形状，或校验 `domain+scope` 与会话索引键），至少对 requests/contexts 增加 scope/record-kind 白名单。
- 置信度：中

### F6 [P3][可维护性] src/modules/harness/skillRunnerReadonlyProjection.ts:305 —— 恒等三元，分支语义完全一致
- 证据：`const normalized = status === "queued" ? "queued" : status;`
- 分析：两个分支产出相同值，`normalized` 就是 `status`；看起来原本想做的是 queued 的语义归一化（例如映射为 `waiting_*` 或 `pending`），现在是无效果代码，会让读者误以为存在分支语义。
- 建议：直接 `const normalized = status;`，若确有 queued 的特殊语义则补齐分支并加注释。
- 置信度：高

### F7 [P3][性能] src/modules/pluginStateStore/runTables.ts:600 —— 运行事件按 run_key 全量读取，无 LIMIT
- 证据：
```
        SELECT event_id, run_key, request_id, backend_id, type, created_at, payload_json
        FROM ${tables.events}
        WHERE run_key=@run_key
        ORDER BY created_at DESC
```
- 分析：`listPluginRunEventStoreEntries` 没有 limit 参数，长跑 run 的事件表会随事件数线性膨胀并一次性物化全部 payload；同文件 `listPluginRunStoreEntriesFiltered` 有 limit（`normalizeRowLimit` 为 0 时又退化为无 LIMIT），`listPluginRunStoreEntries(kind)` 也直接走无界分支。事件/运行数的增长由业务时长决定，缺少背压。
- 建议：给事件列表加 `limit`（默认值 + 上限），并为无界入口 `listPluginRunStoreEntries` 明确默认上限或保留显式 `limit=0` 语义的注释。
- 置信度：中

### F8 [P3][信任边界] src/modules/preferences/skillRunnerLocalRuntimePreferences.ts:71 —— 确认框缺失时默认放行，且卸载选项默认清除
- 证据：
```
    return typeof host.confirm === "function" ? host.confirm(message) : true;
```
- 分析：`confirm` 在宿主没有 `confirm` 时返回 `true`，而它被 `confirmDeploy` / `confirmUninstall` 用于破坏性动作（部署落盘、卸载删目录）的最终确认；同时 `showUninstallOptions` 在对话框元素缺失时退化为 `clearData: confirm(...)` / `clearAgentHome: confirm(...)`（384-395），即“默认清除数据 + 默认清除 agent home”，默认值方向对破坏性操作是 fail-open。
- 建议：`confirm` 缺省应返回 `false`（或在缺失时走一个显式不可用的错误路径），无对话框回退时两个布尔默认取 `false`。
- 置信度：中

### F9 [P2][正确性][判断] src/modules/literatureArtifactMigration/converter.ts:249 —— basisHash 使用 32 位 FNV-1a
- 证据：
```
function hashText(value: string): string {
  let hash = 2166136261;
  for (let index = 0; index < value.length; index += 1) {
    hash ^= value.charCodeAt(index);
    hash = Math.imul(hash, 16777619);
  }
  return (hash >>> 0).toString(16).padStart(8, "0");
```
- 分析（判断）：`basisHash = hashText(stableJson(basisInput))`（1133 行）并持久化到 `plugin_literature_artifact_migration_sets.basis_hash`；note 级事实也用同一函数（262、288 行）。basis 在本仓库是“证据是否变化”的判定依据，32 位非密码学哈希在批量 note（上万条）下生日碰撞概率可达百分比量级，碰撞会把“已变化的 legacy 证据”判为未变化，从而跳过重转换/重校验。同仓库其它地方（`zoteroReadonlyLibraryAdapter` 的 metadataHash）使用 `hashCanonicalJson`（sha256）。
- 建议：basis 与 note 事实改用 `hashCanonicalJson`/sha256，若需短标识则截断（如 16 字节 hex）并同步更新存储列语义与比较处。
- 置信度：中（取决于 basis_hash 的比较/消费方语义，需与迁移 runner 对照确认）

### F10 [P3][性能][判断] src/modules/harness/sqliteReadonly.ts:116 —— 每个 harness 连接都把整库 backup 到 tmp
- 证据：
```
  const snapshotDir = mkdtempSync(
    path.join(tmpdir(), "zs-harness-sqlite-snapshot-"),
  );
  const snapshotPath = path.join(snapshotDir, path.basename(dbPath));
  try {
    await sqlite.backup(source, snapshotPath);
```
- 分析（判断）：只要不是 `:memory:`/`file:` 且 `sqlite.backup` 可用，就会对整库做一次全量备份再以只读打开。`createSynthesisReadonlyClient` 对 plugin/synthesis DB、`createZoteroReadonlyHostReadPort` 对 zotero.sqlite 都走这条路径，即每次 harness 会话都要在 /tmp 复制一份可能上 GB 的库（磁盘 + 启动延迟），且进程被 kill 时 tmp 目录不会被清理（只有 `close()` 才 `rmSync`）。
- 建议：若只为一致性快照，评估直接 `readOnly: true` + WAL 快照读是否满足；否则至少在备份前检查源库大小/增加显式开关，并考虑启动时清理陈旧 `zs-harness-sqlite-snapshot-*` 目录。
- 置信度：中

SLICE: S31
FILES: 19 个 / 9934 行；ledger: /tmp/review/ledger/S31.md
COVERAGE: 已细读 19 个；略读 0 个（无生成物/纯数据文件）
TOP FINDINGS（只列 P0/P1/P2，按严重度排序，每条 ≤ 25 词）:
- [P2][正确性] dashboardReadonlyModel.ts:532 product.backendType 回退写成 row.backendId，类型字段语义漂移
- [P2][资源] assistantReadonlyPublication.ts:1558 ackRoutes 只写不删，随发布量无界增长并吊住旧 runtime
- [P2][性能] taskTables.ts:800 字节估算全量读 domain 的 payload_json，未用 SQL SUM(LENGTH)
- [P2][契约] synthesisReadonlyPort.ts:186 next_cursor/has_more 承诺翻页，实现完全忽略 cursor，客户端重复取同一页
- [P2][正确性] taskTables.ts:1024 以 payload 子串 "conversationid" 判定并删除 ACP 记录，误删面过宽
- [P2][判断] converter.ts:249 32 位 FNV 作为 migration basisHash，碰撞可导致漏判证据变化
NOISE: 本切片最值得主代理复核的 1-3 个可疑点（若有）
- basis_hash 的消费方（迁移 runner）是否真按等值判定“证据未变”——决定 F9 是 P2 还是纯风格。
- `clearAcpConversationTaskRecords` 的调用方与是否存在 tests 覆盖子串谓词，决定 F5 的严重度。
- sqliteReadonly 的整库 backup 是否为离线 harness 的有意取舍（决定 F10 是否接受为设计代价）。