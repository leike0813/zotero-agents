| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| skills_src/literature-deep-reading/renderer/templates/citation-graph-standalone.css | 231 | OK | 纯样式；所有 `var()` 均带 fallback，未发现缺陷 |
| skills_src/literature-deep-reading/renderer/templates/citation-graph-standalone.js | 1 | OK | 压缩产物；逐一核对 HTML 转义 `h()`、边合并 `k()`、坐标投影 `fe()`、hover 定时器清理，未发现缺陷 |
| skills_src/literature-deep-reading/renderer/templates/citation-graph-synthesis-app.js | 393 | 存疑 | 内嵌 Preact/Sigma/Graphology 压缩库未逐行审阅；产品侧 `Qn` 控制器与 `Ad` 导出入口已读，未见 P1/P2；`ni()` 强制 ready 见存疑栏 |
| skills_src/literature-deep-reading/renderer/templates/citation-graph-synthesis-theme.js | 71 | OK | 主题归一化/回退/DOM 属性写入自洽；storage 事件只 apply 不重发 change（当前无消费者受影响） |
| skills_src/literature-deep-reading/renderer/templates/citation-graph-synthesis.css | 6909 | 问题 | P3 同文件重复定义（见 F3）；`.main{overflow:auto}` 与页面滚动所有权条款疑似冲突，见存疑 |
| skills_src/literature-deep-reading/renderer/templates/deep-reading.css | 1402 | OK | 纯样式 |
| skills_src/literature-deep-reading/renderer/templates/deep-reading.js | 1173 | 问题 | P2 digest outline 锚点失效（见 F1） |
| skills_src/literature-deep-reading/renderer/templates/markdown-renderer.css | 128 | OK | 纯样式 |
| skills_src/literature-deep-reading/renderer/templates/markdown-renderer.js | 434 | 问题 | P2 消毒器 URL 属性覆盖不全（见 F2）；负缓存见存疑栏 |
| skills_src/literature-deep-reading/renderer/templates/topic-timeline-shared.css | 567 | OK | 纯样式 |
| skills_src/literature-deep-reading/renderer/templates/topic-timeline-shared.js | 3 | 存疑 | 同年多个 event 共用同一 `left`，标记完全重叠且无法分别交互，见存疑栏 |

### F1 [P2][正确性] skills_src/literature-deep-reading/renderer/templates/deep-reading.js:84 —— Digest outline 的锚点在渲染器可用时全部失效
- 证据：
```js
  if (window.ZoteroSkillsMarkdownRenderer?.renderToHtml) {
    return window.ZoteroSkillsMarkdownRenderer.renderToHtml(markdown || "", {
      profile: "standaloneDigest",
      headingIdPrefix: "digest-heading",
    });
```
- 分析：`renderToHtml`（markdown-renderer.js:368-380）只做 `parser.render` + `sanitizeRenderedHtml`，**从不写 heading id**；`headingIdPrefix` 只被 `ensureHeadingIds` 读取，而 `ensureHeadingIds` 只在 `renderInto`（markdown-renderer.js:387-388）里调用。`openDigest` 用的是 `renderMarkdown` 的返回值直接塞 `innerHTML`（deep-reading.js:733），也没走 `bindLocalAnchors`。于是 `renderDigestOutline`（deep-reading.js:158-167）产出的 `<a href="#digest-heading-…">` 在同页无对应元素：只要 markdown-renderer 脚本在场（正常路径），点击 outline 条目完全不滚动；只有渲染器缺失走本地 fallback 分支（deep-reading.js:136 才生成 id）时才可用，路径恰好相反。
- 建议：在 `renderToHtml` 返回前对模板内容调用 `ensureHeadingIds`（或在 deep-reading 改用 `renderInto`），使 outline 与产物 id 由同一函数生成。
- 置信度：高

### F2 [P2][信任边界] skills_src/literature-deep-reading/renderer/templates/markdown-renderer.js:292 —— 属性级 URL 白名单只覆盖 href/src，SVG 等载体可绕过
- 证据：
```js
        if (name === "href" || name === "src") {
          const resolved = safeResolvedUrl(value, options);
          if (!resolved) {
            node.removeAttribute(attr.name);
```
- 分析：`UNSAFE_TAGS`（第 42-43 行）不含 `svg`/`link`/`meta`，而属性循环只对 `href`/`src` 做协议白名单（`javascript:` 会被 `safeResolvedUrl` 拦掉）。`xlink:href` 名称既不等于 `href` 也不等于 `src`，因此被原样保留；`standaloneDigest`/`document`/`synthesis` 三个 profile 是 `html: true`（第 26-39 行），所以 LLM 生成（可被 PDF 内容注入驱动）的 markdown 里 `<svg><a xlink:href="javascript:…">` 能通过消毒并在点击时执行，绕过 `ALLOWED_PROTOCOLS`。同理 `<link rel="stylesheet" href>` 合法通过（`link` 未被移除），可外联加载第三方 CSS。
- 建议：把 URL 类属性名做成白名单（`href`/`src`/`xlink:href`/`poster`/`data`…）统一走 `safeResolvedUrl`，并对 `svg`/`link`/`meta` 等非内容元素整体剥离或移除其 URL 属性。
- 置信度：中

### F3 [P3][可维护性] skills_src/literature-deep-reading/renderer/templates/citation-graph-synthesis.css:5280 —— 同文件内 4 个选择器各定义两次且取值冲突，前一份是死代码
- 证据：
```css
.overview-dashboard {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: 12px;
  margin-bottom: 12px;
}
```
（另一处为 6068-6073：`minmax(200px, 1fr)` / `gap: 16px` / `margin-bottom: 24px`）
- 分析：`.overview-dashboard`（5280 / 6068）、`.dashboard-metric`（5294 / 6074）、`.metric-label`（5301 / 6087）、`.metric-value`（5309 / 6094，`font-size` 20px vs 14px，`font-weight` 700 vs 500）在同一文件各出现两次，后者静默覆盖前者。改动落在被覆盖的定义上不会生效，且两套卡片视觉（padding/box-shadow/border-radius）不一致，属于典型“投影层反向成为定义来源”的重复定义成本（4 组重复、至少 8 处死声明）。
- 建议：合并为单一定义（保留后一份或按设计稿统一），并删除前一份，避免后续修改继续落在无效块上。
- 置信度：高

### 存疑（证据不足，需人工确认，非缺陷）
- citation-graph-synthesis.css:1412 `.main { position: relative; min-height: 0; overflow: auto; padding: 16px; }`：若该文件即 hosted Synthesis 页面使用的样式（它是被捆绑进本模板的整页样式，含 `.synthesis-root`/`.sidebar`/`.topbar`），则与 AGENTS.md「页面根与 `.main` 永不滚动」直接冲突；若该 `.main` 仅属于另一页面骨架，则无害。需确认本文件在运行时被哪个页面加载。
- citation-graph-synthesis-app.js `ni()`（约 393 行内）把 `layoutStatus:"ready"`、`diagnostics.cache_status:"ready"` 写死：standalone 导出会把“布局失败/缺失”掩盖成 ready，只显示 `synthesis-graph-diagnostic-missing`，是否属预期的只读导出行为需确认。
- markdown-renderer.js:107-113 对 `markdownit` 缺失的负结果做永久 `parserCache` 记录：脚本加载顺序若把渲染器放在 markdown-it 之前，会永久退化为 fallback；当前模板加载顺序未能从本切片确认。
- topic-timeline-shared.js:1 `y.forEach(m=>{s.push({item:m,left:o.end})})`：同年多个 `event` 全部落在同一 `left`，标记完全重叠，只有最后一个可 hover/点击；是否认为“同年事件本就该合并展示”需确认。

```
SLICE: S40
FILES: 11 个 / 11312 行；ledger: /tmp/review/ledger/S40.md（内容见上）
COVERAGE: 已细读 9 个；部分审阅/略读 2 个（citation-graph-synthesis-app.js 内嵌 Preact/Sigma/Graphology 压缩库未逐行读；citation-graph-synthesis.css 为多文件拼接产物，按段抽样核对了重复定义与滚动规则）
TOP FINDINGS（只列 P0/P1/P2，按严重度排序）:
- [P2][正确性] deep-reading.js:84 Digest outline 链接全失效：renderToHtml 从不写 heading id，headingIdPrefix 仅 renderInto 使用
- [P2][信任边界] markdown-renderer.js:292 消毒器只重写 href/src，xlink:href 等可携带 javascript: URL；svg/link 未列入禁表
NOISE: 请主代理复核 1) citation-graph-synthesis.css:1412 `.main{overflow:auto}` 是否即 AGENTS.md 明令“永不滚动”的 .main；2) citation-graph-synthesis-app.js 的 ni() 是否应保留 layoutFailure 语义。
```