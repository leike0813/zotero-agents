| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/modules/zoteroHostCapabilityBroker.ts | 18292 | 问题 | P1: markWritten 全量重算导致 O(N²) 原生读取（F1）；另 F2 并发门禁旁路、F3 取消语义覆盖已提交效果、F4 arXiv 字段 SSOT 漂移、F5 stored_file 公共路径抛未类型化 Error、F6 伪造 observed、F7 literature.ingest 字段校验缺失、F8 未绑定 image slot 落盘。836–839 行为内嵌 base64 PNG 常量（纯数据），未逐字节细读；其余按段细读。 |

### F1 [P1][性能] src/modules/zoteroHostCapabilityBroker.ts:9550 —— markWritten 忽略传入实体并对整个 mutation 输入做全量重算，批量成员操作退化为 O(N²) 原生读取
- 证据：
```
        beforeEffect.markWritten = (entities) => {
          if (record.trashPrepared) {
            return;
          }
          if (record.destructivePrepared || record.ingestPrepared) {
            refreshExpectedEntities(entities);
            return;
          }
          expectedObservations = collectCanonicalMutationObservations(
            args.input,
          );
        };
```
- 分析：`collection.updateMembership` / `collection.create` 在每个 add/remove 项之后各调用一次 `beforeEffect?.markWritten([...])`（6804、6819、6591），而该实现无视 `entities`，改为对 `args.input` 重新执行 `collectCanonicalMutationObservations`；后者对 `input.add`/`input.remove` 的每个 ref 做 `requireItem(ref, "mutation target")` + `canonicalItemVersion`（8736–8754），即真实原生读。触发条件：`collection.updateMembership` 带 N 个成员（`normalizeCollectionMembershipRefs` 上限 `mutationPreviewTargetLimit` 默认 10_000，line 13690）→ N 次 × 每次 N 项原生读 = O(N²)（N=1000 即 10^6 次 Item 读 + toJSON），且每次都发生在持有 Host slice 的窗口内，直接击穿“native 循环至多 100 items 或 50 ms”的片段预算，拖垮共享 FIFO。可达路径：`broker.mutations.execute({operation:"collection.updateMembership"})` 正常公开调用。
- 建议：非破坏路径也改为按 `entities` 增量刷新（复用 `refreshExpectedEntities`），或仅在 `entities` 为空时回退全量重算。
- 置信度：高（机制与调用点明确）；数值影响取决于 N，故业务严重度中高。

### F2 [P2][并发] src/modules/zoteroHostCapabilityBroker.ts:4362 —— markWritten 触发的原生读取在 Host slice 之外执行，破坏单一 FIFO 片段串行
- 证据：
```
    beforeEffect?.markWritten([{ kind: "item", ref: canonicalItemRef(item) }]);
    return {
      status: "attached" as const,
```
（`attachLandingUrlWhenMissingPdf` 中该调用位于 `nativeMutations.attachments.createLinkedUrlAttachment` 的 `admit` 包装之外；`markWritten` 体见 9550–9561，非 ingest 分支直接 `collectCanonicalMutationObservations`，其中 `const item = requireItem(ref, "mutation target");`（8737）与 `canonicalItemVersion`（8740）都是同步原生读，且没有 `withZoteroHostSlice` 包裹）
- 分析：`beforeEffect(...)` 的读断言由调用方包在 `withZoteroHostSlice(control, ...)` 内，但 `markWritten` / `markRemoved` 是同步回调，调用点并未统一包 slice（4362 是明确的一处）；destructive/ingest 分支走 `refreshExpectedEntities → currentMutationEntityObservation → requireItem/canonicalItemVersion`（8774–8789）同样是无门禁原生读。触发条件：任何走到 4362 的 literature ingest 落地页补挂路径，或在其它未包 slice 的 markWritten 调用点。影响：与其它 broker 的处于活动 Host 片段/原生事务并发读，可能读到撕裂状态（观测快照被写成“当前值”，从而让后续 stale 判定失效），也违反“所有 broker 共享进程级 FIFO Host 短片段 admission”的约定。
- 建议：把 `markWritten`/`markRemoved` 内部的读操作统一经 `withZoteroHostSlice`（或要求调用方已在 slice 内并加断言），至少在 4362 等处补 slice。
- 置信度：中（门禁旁路与调用点确证；实际撕裂后果依赖运行时并发时机）。

### F3 [P2][错误处理] src/modules/zoteroHostCapabilityBroker.ts:17492 —— slice 的 run 已成功完成后仍可能 reject “canceled”，把已提交的原生效果上报为取消/失败
- 证据：
```
    .then(
      (value) => {
        if (waiter.canceled || waiter.control.signal?.aborted) {
          waiter.reject(
            capabilityError("canceled", "workflow call was canceled", {
              reason: "caller_signal",
            }),
          );
```
（`abort` 监听器只置 `waiter.canceled = true`，已完成/进行中的 `run()` 继续执行：17541–17557）
- 分析：`abort` 事件在 `run()` 已开始后到达时只标记 `canceled`，随后 `.then` 的 fulfillment 分支无条件改为 reject canceled。触发条件：write 效果执行中 signal 被 abort（例如 `brokerMutationPrimitives.tag.update` 已落盘）。影响：调用方（如 11130–11137、11649–11661 的 `error.code === "canceled" ? "canceled" : "failed"`）会把一次已生效的写入判定为 canceled/failed，durable receipt 与真实状态不一致；重试虽多可幂等，但对非幂等效果（collection.user add/remove、item.create 等）会重复执行或误判为“未发生”。可达路径：任何传入带 AbortSignal 的 `WorkflowCallControl` 的 mutation/读调用。
- 建议：区分“未开始即取消”与“已开始后取消”；已开始的情况下以 `run()` 结果（或 `unknown`/reconcile 语义）结算，而不是覆盖为 canceled。
- 置信度：中（行为可从代码确证，影响取决于上层对 canceled 的处理）。

### F4 [P2][契约] src/modules/zoteroHostCapabilityBroker.ts:2683 —— snapshot 的 `identifiers.arxiv` 读取字段名与文件内其它 arXiv 投影不一致，静默恒为 null
- 证据：
```
    identifiers: {
      doi: readField(item, "DOI") || null,
      isbn: readField(item, "ISBN") || null,
      issn: readField(item, "ISSN") || null,
      arxiv: readField(item, "arXiv") || null,
```
对比同一文件 2441–2446：
```
  const field =
    type === "arXiv" ? "archiveID" : type === "PMID" ? "PMID" : type;
```
- 分析：元数据翻译路径把 arXiv 归一到 `archiveID` 字段，literature ingest 又把 arXiv 写进 `extra`（3961–3966），而 snapshot 读的是 `"arXiv"`。三者至多一个与 Zotero 实际字段一致；`readField` 捕获异常后返回 `""`（1179–1183），因此字段名错误不会报错，只会让 `identifiers.arxiv` 静默为 null，导致 snapshot/对照/去重侧丢标识。触发条件：任何对含 arXiv 条目的 `library.syncSnapshot`。
- 建议：把 arXiv 的字段名抽成单一定义（与 metadata 归一化共用 `archiveID`/`extra` 解析），避免每个投影各自猜字段名。
- 置信度：中（SSOT 漂移确证；具体哪个字段名正确需运行时/上游规范确认，见“存疑”）。

### F5 [P2][错误处理] src/modules/zoteroHostCapabilityBroker.ts:13206 —— 公共 broker 成员的 stored_file 路径必然缺失 prepared 资源并以未类型化 Error 逃出
- 证据：
```
              if (input.source.kind === "stored_file") {
                if (!primitives.createStoredFile) {
                  throw new Error("stored attachment import is unavailable");
                }
```
配套事实：`primitives` 仅在 `record.preparedStoredAttachment && record.preparedFiles` 时构建（9568–9569），而 `record.preparedStoredAttachment` 只来自 `args.resources?.deferredStoredAttachment?.prepare(...)`（9373–9379）；公共入口不传 resources：
```
    const trusted = getZoteroHostCanonicalMutationControl(broker);
    const prepared = await trusted.prepare({ input, scope, control });
```
（9654–9662，`broker.attachments.create` 即走此路径 17293–17299）
- 分析：`AttachmentCreateRequestDto` 允许 `source.kind === "stored_file"`，但公共成员没有任何办法提供 staged 文件，于是必然抛裸 `Error`；该 Error 穿过 `executeAttachmentMutation`（只处理 `MutationAuthorityAdmissionError`，13682–13687）与 `executeCanonicalMutationLifecycle`（只处理 admission/execution/TypeError，9672–9696）原样冒泡，违反“broker 失败统一 `ZoteroHostCapabilityError`（稳定 code/retryable/strict-JSON details）”。对比 `attachments.replaceFile` 缺 primitive 时抛的是类型化 `unsupported_operation`（13451–13459），说明此处属遗漏。触发条件：任何直接调用 `broker.attachments.create({source:{kind:"stored_file"}})` 的调用方。
- 建议：公共路径显式抛 `unsupported_operation`/`invalid_request` 的 `ZoteroHostCapabilityError`（或让 broker 成员自行准备 prepared files），不要在能力缺失时抛裸 Error。
- 置信度：中（本文件内路径确证；外部是否存在其它调用约定需与 workflow 层核对，见“存疑”）。

### F6 [P3][错误处理] src/modules/zoteroHostCapabilityBroker.ts:9683 —— 把 TypeError 猜成资源超限时填入伪造的 observed 值
- 证据：
```
    if (error instanceof TypeError) {
      if (/exceeds|too large|limit/i.test(error.message)) {
        throw capabilityError(
          "resource_limited",
          "mutation request exceeds a resource limit",
          { resource: "characters", limit: 1_048_576, observed: 1_048_577 },
        );
      }
```
- 分析：`observed: 1_048_577` 是写死的常量，并非测量值；`resource: "characters"` 也只是猜测（TypeError 可能来自任意原生断言）。下游若依据 details 做展示/重试预算/诊断，会得到与真实输入无关的数字与资源种类。触发条件：任何以 TypeError 形式冒出的超长/超限原生报错。
- 建议：要么用可测量的事实（如 `JSON.stringify(input).length`），要么只给 `{ reason: "invalid_schema" }` 并把原因留在 message，不要伪造 observed。
- 置信度：高（代码事实），严重度低。

### F7 [P3][错误处理] src/modules/zoteroHostCapabilityBroker.ts:4073 —— literature.ingest 的 `paper.fields` 未做 Zotero 字段合法性校验，非法字段被归类为可重试 execution_failed
- 证据：
```
  const fields: Record<string, string | number | boolean> = {};
  for (const [field, raw] of Object.entries(value)) {
    const key = String(field || "").trim();
```
（`normalizePaperFields` 只校验标量类型，不校验字段名；随后 `createMetadataPaperItem` 直接 `setItemFieldIfPresent(item, field, value)`，其内部为 `item.setField(field as any, normalized as any);`，line 4051）
- 分析：同文件对 `item.create`/`item.updateMetadata` 都用 `validateFieldPatch`/`applicableMetadataFieldPatch` 做了字段-类型校验（3083–3146），literature.ingest 只对 DOI/ISBN 做了特判（3928–3935），其余字段名（含错拼、跨类型字段）直到原生 `setField` 才抛错；该异常在 4832–4876 被包装为 `execution_failed` + `retry_same_operation`，即把永久性输入错误宣告为可重试（虽然处在 `executeTransaction` 内不会半提交）。触发条件：`literature.ingest` 传 `paper.fields: { notAField: "x" }`。
- 建议：复用 `assertValidFieldForItem`/`applicableMetadataFieldPatch` 在 `prepareCanonicalLiteratureIngest` 阶段做无副作用 preflight，并以 `invalid_request` 拒绝。
- 置信度：中（归类与缺失校验确证；具体原生行为依赖 Zotero setField 实现）。

### F8 [P3][验证] src/modules/zoteroHostCapabilityBroker.ts:10677 —— parent-set 允许 `entry.visibleHtml` 携带未绑定的 `data-zotero-agents-image-slot` 直接落盘
- 证据：
```
              let plannedContent =
                entry.visibleHtml !== undefined
                  ? entry.visibleHtml
                  : built.content;
```
配合 10739–10767 的 `imageContent` 仅在有 `input.preparedImage` 或 `entry.embeddedImages.length > 0` 时才经 `normalizeNoteContentInput`（该函数会校验 “image slots and bindings do not match”，3308–3316）生成；否则 `plannedContent` 原样写入（11326 `content: plan.content`）。
- 分析：当调用方只给 `visibleHtml`（不给 `embeddedImages`/`preparedImage`）且该 HTML 内含 `<img data-zotero-agents-image-slot="x">` 时，不会走任何 slot/binding 校验（`declaredPayloadSlots`/`discoveredPayloadSlots` 只覆盖 payload 类型 slot，10705–10733），于是 managed note 落盘一个永不绑定附件、无 `data-attachment-key` 的图片占位，且不会报错；而普通写入路径（`normalizeNoteContentInput` + `bindNoteImageSlots`，3508–3526）会显式拒绝未绑定 slot。触发条件：受信任 parent-set seam 传入上述组合。
- 建议：无论 `visibleHtml` 是否提供，都先跑一遍 `normalizeNoteContentInput(..., {allowManagedMarkers:true})` 的 slot/binding 一致性校验（允许 bindings 为空但要求 slot 计数为零）。
- 置信度：中（分支与校验缺失确证；是否为有意放宽需与 seam 调用方确认）。

### 存疑（证据不足，未列为缺陷）
- `previewCanonicalMutation` 的 trash 预览把不含 `operationId` 的预览请求强转为 `... & Pick<TrashSetItemsStateRequest,"operationId">`（14585–14591），实际对象在运行时缺该字段；`prepareHostTrashMutation`（本切片外）是否依赖它、是否会产出 `operationId: undefined` 的 plan 无法在本文件判定。
- `broker.attachments.create`/`replaceFile` 走 `executeCanonicalMutationLifecycle` 时永远拿不到 `deferredStoredAttachment`（F5），若这是“有意只走 trusted control”，则 F5 应降级为“公共成员应显式拒绝”。
- 本文件导出的测试 seam（`configureZoteroHostSnapshotRuntimeForTests`/`resetZoteroHostSliceGateForTests`/`configureZoteroHostMutationRuntimeForTests`，1027/1043/17433/17447/17566）在 production 模块可见，任一同进程导入者即可清空 Host slice 队列并重设预算；是否被构建产物裁剪需主代理核对打包配置后再定性。