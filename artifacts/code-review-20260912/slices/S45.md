| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/modules/acp/transport/acpBackendProbe.ts | 423 | 问题 | P2: 超时后 adapter 仍可能存活于临时目录；见 F4 |
| src/modules/acp/transport/acpClientConnection.ts | 449 | 问题 | P2: `close()` 与 `sendMessage` 的 writer 竞争/`closed` 语义；见 F3、F5 |
| src/modules/acp/transport/acpConnectionAdapter.ts | 2134 | 问题 | P1: 权限请求无取消通道；P2: 会话状态与 `latestConfigOptions` 全局单值；见 F1、F6、F7 |
| src/modules/acp/transport/acpExecutionProgress.ts | 192 | 问题 | P2: 模块级全局 Map 无上限且无 TTL；见 F9 |
| src/modules/acp/transport/acpMessageStream.ts | 206 | 问题 | P2: 单次 `read()` 无界解析 + `waiting` 无背压；见 F8 |
| src/modules/acp/transport/acpNpxLaunchCache.ts | 334 | 问题 | P2: `rotate()` 未持有 lease 之外的互斥；见 F10 |
| src/modules/acp/transport/acpPermissionOptions.ts | 54 | OK | — |
| src/modules/acp/transport/acpSilentTerminalAssistantCollector.ts | 51 | OK | — |
| src/modules/acp/transport/acpSyntheticConnectionAdapter.ts | 207 | 问题 | P2: `resolvePermission` 不 resolve 真实挂起 Promise；见 F11 |
| src/modules/acp/transport/acpTranscriptBoundary.ts | 77 | OK | 与 AGENTS.md “不得把 tool_call_update/usage 当硬边界”一致 |
| src/modules/acp/transport/acpTransport.ts | 2604 | 问题 | P1: `waitForPromiseWithTimeout` 定时器泄漏 / Mozilla stderr 双读；见 F2、F12 |
| src/modules/acp/transport/acpWebSocketBridgeService.ts | 461 | 问题 | P2: 全局单例桥接未随会话/插件卸载回收；见 F13 |

### F1 [P1][错误处理/并发] src/modules/acp/transport/acpConnectionAdapter.ts:1035-1095 —— ACP/MCP 权限请求 Promise 无超时、无取消，`close()` 也不 resolve，永久挂起

- 证据：
```ts
    if (this.permissionListeners.size === 0) {
      return {
        outcome: "unavailable" as const,
        reason: "permission_listener_unavailable",
      };
    }
    const requestId = nextOpaqueId("acp-mcp-permission");
    const outcome = await new Promise<RequestPermissionOutcome>((resolve) => {
```
- 分析：`requestZoteroMcpToolPermission` 与 `buildClient().requestPermission`（1231 行同一模式）都构造一个只能由 listener 调 `resolve` 的 Promise。若 UI listener 从未回调（抽屉关闭、会话切换、页面卸载、plugin disable），该 Promise 永不 settle；`prompt()` 的 `finally { clearPromptCapture }` 也就永不执行，`activePromptSessions`/`rawAssistantTextBySession`/`promptCapturesBySession` 条目长期驻留，ACP 会话无法继续发 prompt（`resolveMcpServers` → MCP 写工具调用会阻塞在审批上）。`close()`（2105-2127）只 abort startupController 并关闭连接，**不**清空/拒绝 pending 权限 Promise。触发条件明确：审批抽屉未响应 + 关闭会话。这违反 AGENTS.md 中“取消/超时不得提前释放槽”所隐含的取消语义对称性要求（取消路径必须存在）。
- 建议：在 `beginPromptCapture` 时记录 active permission resolver 的 aliveness（例如存到 Map，`close()`/`clearPromptCapture()` 时以 `{outcome:"cancelled"}` resolve），或给权限等待加与 `startupTimeoutMs` 同源的 bounded wait。
- 置信度：高

### F2 [P1][资源] src/modules/acp/transport/acpTransport.ts:1159-1184 —— `waitForPromiseWithTimeout` / `withTimeoutValue` 每次调用泄漏一个不受控 setTimeout，热路径上累积

- 证据：
```ts
  return await Promise.race([
    promise.then(
      () => true,
      () => true,
    ),
    new Promise<boolean>((resolve) => {
      setTimeout(() => resolve(false), timeoutMs);
    }),
  ]);
```
- 分析：`Promise.race` 胜出后 timeout 定时器仍会触发（未 `clearTimeout`），且没有任何引用被取消。`waitForPromiseWithTimeout` 被 `waitForExit` 使用 —— `waitForExit` 在 `waitForCleanupKillExit`、`createControlledAcpTransport.close`、以及每次 close 的 grace 判断里被反复调用（关闭路径一条链上可达 4~6 次）。`withTimeoutValue` 同样在 `queryPosixProcessIdentityWithMozilla` 每次身份查询时创建定时器。虽然单个定时器代价小，但这是确定性的句柄泄漏模式，且在长生命周期 Zotero 进程中随会话数累积；关闭窗口期（kill 等待/身份查询超时）是高频路径。
- 建议：实现 `withTimeout` 返回 `{promise, cancel}` 或使用 `AbortSignal.timeout` 配合 `Timeout` 清理；至少在 race 完成后 `clearTimeout`。
- 置信度：高

### F3 [P1][并发] src/modules/acp/transport/acpClientConnection.ts:227-270 —— `sendMessage` 的 writeQueue 链与 `close()` 的 getWriter 竞争，`acceptingWrites=false` 无法阻止已排队的写

- 证据：
```ts
    this.writeQueue = this.writeQueue.then(async () => {
      const writer = this.stream.writable.getWriter();
      try {
        this.traceMessage("out", message);
        await writer.write(message);
      } finally {
        writer.releaseLock();
      }
    });
```
以及
```ts
      const writer = this.stream.writable.getWriter();
      try {
        if (typeof writer.close === "function") {
```
- 分析：`sendMessage` 只在入队前检查 `acceptingWrites`，排队后的任务不复查。`close()` 在 `acceptingWrites=false` 后先等 `writeQueue`（最多 2s），若写操作卡住/超时，则第 257 行再 `getWriter()` 并 `close()`。此时仍在排队的写任务会随后 `getWriter()` 并写向已被 `close()` 的底层 pipe —— 在 node 实现里 `child.stdin.end()` 后 `write` 会抛 `ERR_STREAM_WRITE_AFTER_END`，在 mozilla/web socket 实现里是静默失败或局部错误状态；`pendingResponses` 已在 `resolveClosed` 中被 reject，但写队列的 rejection 无人 await（`sendMessage` 返回的 Promise 只被原调用方 await，若该调用方已因 close 走掉则会成为 unhandled rejection）。
- 建议：队列任务内复查 `acceptingWrites` 并 throw；`close()` 前用一个独立的 abort 通道（`writer.abort`）而不是 `close()`；把 `writeQueue` 末尾的 rejection 收敛（`.catch`）。
- 置信度：中

### F4 [P2][资源] src/modules/acp/transport/acpBackendProbe.ts:217-235 —— `withProbeTimeout` 超时只造成 reject，后台 `initialize()`/`newSession()` 仍在运行，与 `finally` 中 `adapter.close()` 竞态

- 证据：
```ts
  return Promise.race([
    promise,
    new Promise<T>((_, reject) => {
      timer = setTimeout(() => {
        reject(new Error(`${label} timed out after ${timeoutMs}ms`));
      }, timeoutMs);
    }),
  ]).finally(() => {
```
- 分析：`Promise.race` 不取消被 race 的 promise。超时后 `catch` 返回 `{ok:false}`，`finally` 执行 `adapter?.close()`，但同时 `adapter.initialize()` 的 `initializeOnce` 可能仍在 `launchAcpTransport` 阶段（本身带 `startup.signal`）或已在 `newSession` 中创建会话；`close()` 会 `abort()` startupController 并把 `connection/transport` 置 null，而仍在推进的 `initializeOnce` 的 `finally { cacheLease?.release() }` 与 `this.transport = transport` 赋值可能晚于 `close()`（`initializeOnce` 中无 `closePromise` 复查于赋值点之后——1499 行 `throwIfStartupStopped` 检查在 launch 后但 `this.transport = transport` 在其后紧邻赋值，仍可能与 close 交错）。更实际的问题是：超时返回后探针临时目录 `root/workspace`、`root/runtime` 不会被清理（423 行 finally 只 release audit writes），每次失败的探测都会在 `paths.tmpDir/acp-backend-probe/<backendId>` 留下 npx 缓存 generation 目录，随重试次数无界增长。
- 建议：探测结束（含失败）时 `removeRuntimePath(root)`；把超时设计为可取消（传入 AbortSignal 到 createAdapter/startup）而非 race。
- 置信度：中

### F5 [P2][并发] src/modules/acp/transport/acpClientConnection.ts:100-119 —— `resolveClosed` 只在首次生效，`close()` 主动调用会把 origin 固定为 "local"，掩盖真实 remote-eof/receive-error

- 证据：
```ts
  private resolveClosed(result: AcpClientConnectionCloseResult) {
    if (this.closedResolved) {
      return;
    }
    this.closedResolved = true;
```
- 分析：`close()`（243-251 行）在关闭连接前先主动 `resolveClosed({origin:"local"})`。若此刻对端已经断开（receiveLoop 已捕获到错误或 EOF 但尚未走到 finally），真实关闭原因被丢弃，`selectInitializeFailure` 依赖 `closeResult.origin === "receive-error"` 来选取根因（1345 行），因此真实的 `receive-error` 会被降级为通用 "ACP connection closed"，诊断信息丢失（表现为 initialize 失败日志只给出 stderr 或 exit code，而非真实 JSON-RPC 解析/读取错误）。这类时序在探针/启动失败路径上很常见。
- 建议：不要抢占式 resolveClosed；让 `close()` 只设 `acceptingWrites=false` 并驱动 pipe 关闭，由 receiveLoop 的 finally 发布结果，或标记 “local 请求” 而保留 receiver 的 error。
- 置信度：中

### F6 [P2][正确性] src/modules/acp/transport/acpConnectionAdapter.ts:2049-2090 —— `latestConfigOptions` 是适配器单值，跨会话复用会向错误会话发送 configId

- 证据：
```ts
    const option = findAcpSessionConfigOptionByCategory(
      this.latestConfigOptions,
      args.category,
    );
    if (!option) {
      return false;
    }
```
- 分析：`latestConfigOptions` 由 `updateLatestConfigOptions` 在 `config_option_update` 通知、`newSession`、`loadSession`/`resumeSession`、`setConfigOption` 响应处统一覆盖，是**单实例**字段，而 `setConfigOption/setMode/setModel` 都接收任意 `sessionId`。在多会话场景（一个 ACP 后端承载多个会话；`currentSessionId` 也被后者覆盖）下，对会话 B 调用 `setMode` 会取到会话 A 的 configId 并发送，或返回 `false` 使调用方回落到 `setSessionMode`（旧 API）—— 后者在只支持 config option 的后端上失败。`session_update` 事件里的 `params.sessionId` 被完全忽略（819-821 行只读 `update?.configOptions`）。
- 建议：把 `latestConfigOptions` 改为 `Map<sessionId, options>`，`setConfigOption` 按 `args.sessionId` 取；`config_option_update` 用通知的 sessionId 归档。
- 置信度：中

### F7 [P2][契约] src/modules/acp/transport/acpConnectionAdapter.ts:742-758、898-933 —— Claude raw SDK 缓冲按 `sessionId` 无界增长，且只在 prompt 结束时 flush/clear

- 证据：
```ts
    const existing = this.rawAssistantTextBySession.get(sessionId) || [];
    this.rawAssistantTextBySession.set(sessionId, [...existing, ...chunks]);
```
- 分析：`bufferClaudeRawSdkAssistantText` 仅在 `activePromptSessions.has(sessionId)` 时缓冲，但同一次 prompt 内每个 `_claude/sdkMessage` 都做 `[...existing, ...chunks]` 全量复制 —— 长回答（原始 SDK 文本包含完整 assistant 消息）在 O(n²) 复制下增长，且缓冲的文本长度不受 `ACP_STDERR_MAX_CHARS` 之类上限约束。`clearPromptCapture` 只在 `prompt()` 的 finally 或下一次 `prompt()` 开头清理；若 `prompt()` 因 F1 的权限挂起而永不返回，这些数组驻留。触发条件是 Claude family 后端 + 长/多块回答。
- 建议：改为 `push(...chunks)` 或 `existing.push(...)`（避免整数组重建）；并在 flush 后立刻 `delete` 该 sessionId 的条目。
- 置信度：中

### F8 [P2][性能/背压] src/modules/acp/transport/acpMessageStream.ts:78-109 —— NDJSON 解析对单次 `read()` 交付的整个 chunk 做 `split("\n")`，无行数/字节上限

- 证据：
```ts
        content += decoder.decode(value, { stream: true });
        const lines = content.split("\n");
        content = lines.pop() || "";
        for (const line of lines) {
```
- 分析：`value` 是底层 pipe 一次交付的任意大小字节块（web socket bridge/超大 stdout 帧可达数 MB），`content.split("\n")` 会为整个 chunk 分配行数组，并且 `content += ...` 在无换行时也会持续累积（未交付完整行的部分会无上限地存在 `content` 中 —— 恶意/异常后端不发送换行即可让 `content` 无界增长，`queue` 也随之无界，因为 `flushPending` 只在 `waiting` 有请求者时消费）。`queue` 本身无上限，`read()` 才是消费者，若上层（`AcpClientConnection.receiveLoop`）处理慢则 `queue` 无背压增长。
- 建议：对 `content` 施加最大行长上限（超出即报 `ndjson_parse` 错误并终止），并对 `queue` 长度设上限或在达到阈值时对上游施加 read 背压。
- 置信度：中

### F9 [P2][资源] src/modules/acp/transport/acpExecutionProgress.ts:30-54 —— 模块级 `states` Map 仅在显式 release 时删除，无上限/无 TTL

- 证据：
```ts
const states = new Map<string, AcpExecutionProgressState>();
...
export function releaseAcpExecutionProgress(scopeKeyRaw: string) {
  states.delete(String(scopeKeyRaw || ""));
}
```
- 分析：`getOrCreateState` 每次遇到新 `scopeKey`（会话/对话/请求维度）就插入一条，`updateAcpExecutionProgress` 在每次 session update 时都会调用它。若调用方在会话终止路径上漏掉 `releaseAcpExecutionProgress`（例如进程级关闭、异常终止、panel 重载），条目永久驻留。这是纯内存泄漏型问题，累积后表现为长会话进程内存增长。是否实际泄漏取决于调用方是否覆盖所有终止分支——切片内无法确认，但模块本身缺少兜底（无 LRU/无 size 上限）。
- 建议：为 `states` 加 LRU 上限（如 256）或按 scope 生命周期注册清理；至少在达到上限时淘汰最旧条目。
- 置信度：中

### F10 [P2][正确性] src/modules/acp/transport/acpNpxLaunchCache.ts:181-219、262-267 —— `rotate()` 不参与 keyed lease，且 `persistActiveGeneration` 失败降级为非原子写

- 证据：
```ts
      async rotate() {
        generation = generation >= MAX_GENERATION ? 0 : generation + 1;
        cachePath = joinPath(identityRoot, generationDirectoryName(generation));
        await ensureRuntimeDirectory(cachePath);
        await persistActiveGeneration(identityRoot, generation);
      },
```
- 分析：`acquireKeyedLease` 用 `leaseTails` 串行化租约获取，但 `rotate()` 直接在已持有的 lease 上改写 `generation` 并持久化 active-generation 文件，而 `release()` 只在调用方（`acpConnectionAdapter.initializeOnce` 的 finally）执行一次。因此在重试窗口内（1596-1599 行 `cacheLease.rotate()`），同一 `cacheKey` 的下一个 `initialize` 可能读到新旧混合的 generation 目录（例如 B 进程在 `persistActiveGeneration` 前读到旧值并开始使用旧 generation，而 A 已把 B 的 npx 缓存写进去），破坏“每个 cacheKey 同一时刻最多一个生成被使用”的隔离前提——这正是 `classifyAcpNpxCacheRenameConflict` 试图规避的 ENOTEMPTY 冲突来源。另：`persistActiveGeneration` 的 catch 分支（169-172）先 `moveRuntimePath` 失败再退化为直接 `writeRuntimeTextFile(activePath)`，若这一步也失败会抛出，`rotate()` 的调用方 `waitForStartupPhase` 会把整个 initialize 打成失败——与 `moveRuntimePath` 的原子性意图相比是行为回退。
- 建议：让 `rotate()` 也走 `leaseTails` 串行化，或在文件内记录 owner/leaseId 以便并发检测；`persistActiveGeneration` 的 fallback 失败应保留原错误并允许重试而非直接失败。
- 置信度：中

### F11 [P2][正确性] src/modules/acp/transport/acpSyntheticConnectionAdapter.ts:166-194 —— `emitPermissionRequest` 的 `resolve` 被吞掉，`resolvePermission` 只清状态不通知请求方

- 证据：
```ts
        resolve: (outcome) => {
          if (activePermission?.requestId !== requestId) return;
          activePermission = undefined;
        },
```
- 分析：`emitPermissionRequest` 构造的 `pending` 对象把 `resolve` 覆盖为一个仅清空 `activePermission` 的函数，而真正的 `RequestPermissionOutcome` 只被丢弃。下游 `resolvePermission(outcome)`（189-194 行）调用 `pending.resolve(outcome)` 时 outcome 参数被忽略，因此任何依赖该 resolve 完成审批回路的调用方（重放/测试路径中模拟 ACP 权限流）拿不到结果；语义上“resolve”名字与行为不符（仅取消挂起状态）。与 `AcpConnectionAdapter` 契约中 `resolve: (outcome) => void` 的意图不一致。
- 建议：让 `resolve` 真正转发 outcome（例如保存一组 deferred 或直接 resolve 原始 pending），`resolvePermission` 应保证 outcome 被传递。
- 置信度：中

### F12 [P2][正确性] src/modules/acp/transport/acpTransport.ts:1268-1296 —— `captureStdout=true` 时 stdout 走 `createReadableStreamFromMozillaPipe({})`，但实际读取者已被 capture 抢占

- 证据：
```ts
  const pumpedStdout = args.diagnosticCapture?.captureStdout
    ? null
    : createPumpedReadableStreamFromMozillaPipe(proc.stdout || {}, (chunk) => {
...
    stdout: args.diagnosticCapture?.captureStdout
      ? createReadableStreamFromMozillaPipe({})
      : pumpedStdout!.readable,
```
- 分析：当 `captureStdout` 为真时，`stdoutCapture` 用 `captureMozillaPipeTail(proc.stdout, ...)` 持续读取并丢弃/记录 stdout，同时对外暴露的 `stdout` 是 `createReadableStreamFromMozillaPipe({})` —— 其 `readString` 为 `undefined`，任何 `read()` 立即返回 `{done:true}`。因此“captureStdout”模式（探针的详细审计路径）下 ACP 协议消息永远不会被消费，`AcpClientConnection.receiveLoop` 立刻 EOF，连接被视为 remote-eof 关闭。这是探针 `diagnosticCapture` 打开时必然触发的功能失效（探针会得到 `ok:false` 或空会话），属于确定的行为缺陷而非风格问题。
- 建议：capture 路径应把读取到的 chunk 同时喂给真正的 NDJSON 消费者（tee），或改为 `onStdoutChunk` 旁路而不独占 pipe。
- 置信度：中

### F13 [P2][资源] src/modules/acp/transport/acpWebSocketBridgeService.ts:80-82、266-339 —— 桥接服务是进程级单例，`bridgeServicePromise` 在失败清理与 `ensure` 之间存在窗口，且无插件卸载钩子

- 证据：
```ts
let bridgeServicePromise: Promise<AcpWebSocketBridgeService> | null = null;
let bridgeService: AcpWebSocketBridgeService | null = null;
...
  bridgeServicePromise ||= startBridgeService().catch((error) => {
    bridgeServicePromise = null;
    bridgeService = null;
    throw error;
  });
```
- 分析：`startBridgeService` 在 `waitForReadyFile` 超时（5s）后抛错，但此前已 `subprocess.call` 启动 `zotero-acp-bridge.exe` 并用 `writeRuntimeTextFile(readyFile/logFile)` 落盘；错误路径没有 `proc.kill()`/`removeRuntimePath(readyFile)`，因此每次失败会遗留一个 bridge 进程与 ready/log 文件（logFile 还被固定命名 `zotero-acp-bridge.log`，多次启动互相覆盖/竞争）。此外 `bridgeService` 只在进程 `wait()` 完成时自清；若无 `shutdownAcpWebSocketBridgeService()` 调用者（插件卸载/进程退出路径未见此处引用），子进程与端口会随插件禁用残留。`ready-file` 命名为 `ready-${Date.now()}.json`，同毫秒并发启动会冲突覆盖。
- 建议：启动失败路径 `proc.kill?.()` + 删除 ready/log 文件；在插件卸载生命周期里调用 `shutdownAcpWebSocketBridgeService()`；ready 文件名加入随机 token。
- 置信度：中

SLICE: S45
FILES: 12 个 / 6,844 行；ledger: /tmp/review/ledger/S45.md
COVERAGE: 已细读 12 个（acpConnectionAdapter.ts、acpTransport.ts 为重点逐段读）；略读 0 个
TOP FINDINGS（只列 P0/P1/P2，按严重度排序）:
- [P1][错误处理] acpConnectionAdapter.ts:1042 权限 Promise 无超时/取消，close() 不 resolve → prompt 永久挂起并泄漏 capture 状态
- [P1][资源] acpTransport.ts:1171 waitForPromiseWithTimeout/withTimeoutValue 的 setTimeout 从不 clear，关闭路径反复调用
- [P1][并发] acpClientConnection.ts:231 sendMessage 排队任务不复查 acceptingWrites，close() 后仍可写向已关闭 pipe
- [P2][正确性] acpTransport.ts:1285 captureStdout=true 时 stdout 从未被消费，连接立即 EOF（探针详细审计模式必然失败）
- [P2][正确性] acpConnectionAdapter.ts:2065 latestConfigOptions 为单值字段，多会话下 setMode/setModel 用错 configId
- [P2][资源] acpBackendProbe.ts:289 探测结束不清理 tmp 目录，npx generation 目录无界增长
- [P2][性能] acpConnectionAdapter.ts:757 Claude raw SDK 缓冲 O(n²) 复制且无长度上限
- [P2][资源] acpTransport.ts:1268/1286 Mozilla stdout 在 capture 与 pumped 之间无 tee；acpMessageStream.ts:90 行长与 queue 无上限

NOISE: 最值得主代理复核的三点：(1) F12 —— 若 `captureStdout` 在生产探针路径被启用，则 ACP 探针从来不成功，需要确认 `shouldWriteDetailedAcpAuditArtifacts()` 默认值；(2) F1 的“权限无取消”是否由上层 ACP 会话管理器通过 close 会话兜底（本切片看不到调用方）；(3) F10 rotate 是否真的可能并发（`leaseTails` 释放时机 vs `initializeOnce` 重试窗口）。