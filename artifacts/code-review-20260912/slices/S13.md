### A. 文件级台账

| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/workflows/archive.ts | 842 | 问题 | P2: Gecko 抽取路径的条目上限在枚举之后才校验（F4）；P3: catch/finally 中清理失败会覆盖主错误（F7）。路径规范化、ZIP 解析边界、原子写回校验读过未发现其他缺陷 |
| src/workflows/bibliography.ts | 399 | 问题 | P3: 错误透传判据 `typeof code === "string"` 过宽，非 host 错误会绕过 `execution_failed` 契约（F8）。条目/格式/选项校验、回退语义自洽 |
| src/workflows/declarativeRequestCompiler.ts | 704 | 存疑 | 参数模板、内联输入/上传键冲突、handoff 分支均自洽；但 `entry.from` 仅做类型断言后直接传入 selector，selector 未命中时落入 `return true`（第 100 行）等价于 `selected.source`。需确认 manifest 解析层（loaderContracts）是否已限制该字段取值 |
| src/workflows/loader.ts | 1153 | 问题 | P3: `debug_only` 工作流在隐藏分支前仍执行 hooks 加载（F5）；重复 workflow id 静默覆盖、无诊断（F6）。目录过滤、包清单、locale 资源与诊断排序逻辑读过未发现其他问题 |
| src/workflows/runtime.ts | 1294 | 问题 | P2: try 覆盖范围之外的 interactive 资源/上游 abort 监听未清理（F1）；P2: `__stats.skippedUnits` 重复计数（F2）。全局 scope 串行化只覆盖 work，未见嵌套重入路径，暂列为存疑 |
| src/workflows/triggerPolicy.ts | 9 | OK | — |
| src/workflows/types.ts | 3073 | 问题 | P3: mutation 操作名集合被手写 4 份（F9）；`size`/`sizeBytes` 双字段语义漂移（F10）。另存疑：本文件定义的大量 DTO（`MutationReceipt`、`ItemDetailDto` 等）是否与 `src/shared/*WireContract.ts` 重复，未读到 shared 侧无法判定 SSOT |
| src/workflows/workflowHostOwners.ts | 1453 | 问题 | P3: 笔记内嵌图槽位的“检测面”与“重写面”支持的属性写法不一致（F13）。补偿链、幂等 operationId、快照分页循环读过；`createWorkflowHostCapabilityBroker(_resources)` 形参被忽略属死参数，未单独记为缺陷 |
| src/workflows/workflowInputPlanning.ts | 1251 | 问题 | P2: `readNotes` 把已记录的 artifact issue 转成硬失败，过滤语义失效（F3）。计数规则、分组、mime 过滤、分页续读检查读过未发现其他缺陷 |
| src/workflows/workflowNoteImagePreparation.ts | 801 | 问题 | P3: `inferImageMimeType` 用 `includes("image/")` 会把路径当作 MIME（F11）；P3: 输入字节上限在整文件读入之后才校验（F12）。ref 作用域隔离、随机 id、画布回退路径读过 |

### B. Findings

### F1 [P2][并发/资源] src/workflows/runtime.ts:631 —— 监听器与 interactive 资源创建位于 try 之外，抛错即泄漏且不进诊断/清理路径
- 证据：
```ts
    upstreamSignal.addEventListener("abort", onUpstreamAbort, { once: true });
```
```ts
    interactiveResources = await createHostBridgeWorkflowResourceApi({
      workflowId: args.workflow.manifest.id,
```
- 分析：`addEventListener`（622）、`createHostBridgeWorkflowResourceApi`（638）、`summarizeWorkflowHostApiCapabilities`/`emitWorkflowPackageDiagnostic`（648/655）全部发生在 `try {`（676）之前；只有该 try 的 finally（814-822）里才做 `removeEventListener` 与 `interactiveResources.cleanup()`。任一前置步骤 reject（例如 bridge 资源 API 创建失败、诊断发射失败、宿主 API 摘要遍历抛错）时，上游 signal 上的 abort 监听器永久残留（`{once:true}` 仅在真正 abort 时自摘），已创建的 interactive 资源不释放，同时该次 hook 既不落 `workflow-hook-execute-failed` 诊断也没有 `executionController.abort()`。调用方 signal 常为长生命周期对象，重复失败会累积监听器。
- 建议：把监听注册与 interactive 资源创建整体移入 try，或对这些前置步骤单独用 try/catch-finally 保证 removeEventListener 与 cleanup。
- 置信度：中

### F2 [P2][正确性] src/workflows/runtime.ts:1174 —— `skippedUnits` 对 preflight skip 双重计数，可超过 `totalUnits`
- 证据：
```ts
      const skippedUnits =
        Math.max(
          0,
          resolved.totalUnits -
            requests.length -
            preflightState.shortCircuitApplies.length,
        ) + preflightState.skippedUnits;
```
- 分析：`resolveSelectionContexts` 返回的 `totalUnits` 等于输入单元总数（`workflowInputPlanning.freezePlan` 永远把 `units.skipped` 置 0，第 1113 行），因此第一项 `totalUnits - requests.length - shortCircuits` 已经把“被 preflight 判 skip 的单元”算作 skipped；而第 1062-1065 行的 `outcome.kind === "skip"` 又对同一单元 `preflightState.skippedUnits += 1`，两者相加。触发条件：任意工作流 preflight 返回 `{kind:"skip"}`。实测口径：1 个单元、1 次 skip、0 request 时得到 `skippedUnits = 2`，而 `totalUnits = 1`，`requestCount = 0`，即 `skipped > total`，下游若用 `total - skipped` 推导已接受单元会得到负数。仅在有 replace-units 扩张 request 数时第一项才被 clamp 到 0 而“偶然正确”。
- 建议：二者取其一——要么 first-term 只统计规划层跳过单元（改为 `resolved.totalUnits - resolved.contexts.length`），要么完全依赖 `preflightState.skippedUnits`。
- 置信度：高

### F3 [P2][正确性/错误处理] src/workflows/workflowInputPlanning.ts:333 —— 已捕获的 artifact issue 在 `readNotes` 中被转成硬失败，使过滤/可用性判定整体中断
- 证据：
```ts
  return (await readGeneratedNoteFacts(parentRef, runtime)).map((note) => {
    if (note.issue) throw Object.assign(new Error("Managed note is unavailable"), { code: note.issue });
```
- 分析：`readGeneratedNoteFacts` 专门把 `invalid_artifact`/`legacy_artifact_requires_migration` 归约为 `issue` 字段（第 375-386 行）而不是抛错，说明调用方本应有降级路径；但 `readNotes` 立即把它还原成抛错，而 `readNotes` 被 `parentHasAllGeneratedNotes`（346）、`selectGeneratedNoteCandidates`（548）、`selectDigestRepresentativeImage`（573）、`relatedEntries`（730）与 `generated-note-readiness` 过滤器（892）共同使用。触发条件：库里存在一个 legacy/invalid managed note 的父项；后果是父项本应被过滤或跳过，却让 `planWorkflowInput` 直接抛出裸 `Error`（无 `ZoteroHostCapabilityError` 形状），菜单/执行入口整体失败，且 issue 码只在 `code` 上闪现。
- 建议：让 `readNotes` 返回 fact（含 issue），由各过滤器把 issue 记为 skip 原因；或在调用点统一 catch 该 code 并按“不可用”处理。
- 置信度：中

### F4 [P2][资源/信任边界] src/workflows/archive.ts:653 —— 条目数量上限在全部条目名枚举完成之后才校验
- 证据：
```ts
      if (!name || name.endsWith("/")) continue;
      rawNames.push(normalizeWorkflowArchiveEntryName(name));
```
```ts
    if (rawNames.length > WORKFLOW_ARCHIVE_LIMITS.entries) {
```
- 分析：`extractInGecko` 先把 `findEntries(null)` 的每个条目规范化并压入 `rawNames`，之后才比对 `WORKFLOW_ARCHIVE_LIMITS.entries`（20_000）。触发条件：任意用户提供的 ZIP 源文件（`withExtractedZip` 的 `sourcePath` 来自工作流参数/附件）包含远超上限的极短条目名。此时在抛出 `resource_limited` 之前已完成 O(entries) 的字符串分配与数组增长，上限失去保护内存的作用；对照 `extractStoredZip`/`parseStoredZip` 是“先读 EOCD 计数、再逐条处理”，两条路径的资源纪律不一致。
- 建议：在 while 循环内计数并在超过上限时立即抛出（或先读 entry 数再决定是否继续枚举）。
- 置信度：中

### F5 [P3][正确性] src/workflows/loader.ts:1056 —— `debug_only` 工作流在隐藏判断前仍执行 hooks 加载
- 证据：
```ts
          const hookResult = await loadHooks({
```
```ts
          if (hiddenByDebugMode) {
            continue;
          }
```
- 分析：`hiddenByDebugMode` 在第 1023 行就算出来了，但真正 `continue` 在 hooks 加载之后。非 debug 模式下加载隐藏工作流会经 `loadHooksModule` 走 `importPrecompiledPackageHooksModule`（bundle 用户代码 + 写临时脚本 + `loadSubScript` 执行），既做无用功，又会在失败时把 `Skip workflow ...` 警告写进 `diagnostics`，与 `debug_only` 的“对用户不可见”语义冲突。
- 建议：把 `hiddenByDebugMode` 的 continue 提前到 `buildStrategy` 校验之后、`loadHooks` 之前。
- 置信度：中

### F6 [P3][正确性] src/workflows/loader.ts:1073 —— 重复 workflow id 静默覆盖，无任何诊断
- 证据：
```ts
          workflowsById.set(candidate.manifest.id, {
```
- 分析：`loadWorkflowManifests` 对缺 provider、缺 build strategy、hook 缺失/导入失败都产出诊断，唯独对两个不同目录/包声明同一个 `manifest.id` 只做 Map 覆盖（保留先插入的顺序、替换值），后加载者静默胜出；调用方拿到的 `workflows` 长度与目录内容不符且无 warning 可查。
- 建议：命中已存在 id 时推入 `manifest_validation_error` 级诊断（并决定 first-wins / last-wins 语义）。
- 置信度：中

### F7 [P3][错误处理] src/workflows/archive.ts:449 —— 临时文件清理失败会覆盖原始主错误
- 证据：
```ts
  } catch (error) {
    await removePath(temporary);
    throw error;
  }
```
- 分析：`writeStoredZipAtomic` 与 `writeZipInGecko`（第 487-497 行，含 `finally { await removePath(materializedRoot); }`）在异常传播路径上直接 `await` 清理调用；若 `removeRuntimePath` 自身抛错（权限/占用/网络盘），抛出的将是清理错误，真正的写失败原因（磁盘满、路径非法、ZIP 冲突）丢失。同一文件中的 `withExtractedZip` 反而正确地用 `cleanupFailed/cleanupError` 分离并优先抛 `operationError`（第 829-838 行），说明此处是遗漏而非设计。
- 建议：与 `withExtractedZip` 一致，把清理错误单独捕获，主错误优先抛出（清理错误可作为 cause/附加字段）。
- 置信度：高

### F8 [P3][契约] src/workflows/bibliography.ts:382 —— 错误透传判据过宽，非 host 错误会绕过 `execution_failed` 归一化
- 证据：
```ts
        if (
          error &&
          typeof error === "object" &&
          "code" in error &&
          typeof (error as { code?: unknown }).code === "string"
        ) {
          throw error;
        }
```
- 分析：try 块内既有 `createWorkflowHostError`（必须原样透传），也有 `new Export()`/`translation.translate()` 这类宿主/翻译器调用。任何带字符串 `code` 的非 host 错误（Node/Gecko IO 错误、第三方库错误）都会被判定为“已是 host 错误”而原样抛出，跨越 `WorkflowBibliographyOwner` 边界后调用方按 `WorkflowHostErrorCode` 分派会得到未定义分支，`WorkflowHostErrorDetailsByCode` 的形状保证也被破坏。
- 建议：改为校验是否为已知 host 错误形状（例如复用 `workflowHostErrorContract` 的判定/`instanceof` 或白名单 code），否则统一包装。
- 置信度：中

### F9 [P3][可维护性] src/workflows/types.ts:907 —— mutation 操作名集合在同一文件被手写 4 份，存在漂移未被类型系统约束
- 证据：
```ts
export type WorkflowHostMutationReceiptOperation =
  | "item.create"
```
```ts
export type MutationOperation =
  | "item.create"
```
- 分析：`WorkflowHostMutationReceiptOperation`（907-939，26 项，含两个内部 operation）、`MutationOperation`（941-970，25 项）、`MutationExecuteRequest`（1216-1259，带 payload 的联合）、`MutationRequestByOperation`（1261-1319）与 `MutationResultByOperation`（1321-1351）各自独立罗列操作名。只有 `MutationPreviewRequestByOperation`/`MutationPlanByOperation` 是派生的。新增/删除一个操作需要同步 ≥4 处，且没有任何编译期等式检查（例如 `MutationExecuteRequest["operation"]` 与 `keyof MutationRequestByOperation` 的互含断言），漏改表现为运行时 `execute` 收不到该 operation 的窄化类型或产生不可达分支。
- 建议：以 `keyof MutationRequestByOperation` 作为唯一操作名来源，其余联合由映射类型派生，并加一条互含断言。
- 置信度：高（重复事实）/ 中（是否已由其他文件守住，未在切片内验证）

### F10 [P3][契约] src/workflows/types.ts:2118 —— `WorkflowResourceFile`/`WorkflowResourceOutputDescriptor` 同时保留 `size` 与 `sizeBytes`，消费端被迫双读
- 证据：
```ts
  size?: number;
  sha256?: string;
  kind?: "file" | "archive";
  sizeBytes?: number;
```
- 分析：两个同义可选字段并存且都非必填，语义（谁权威、是否可能不一致）无注释约束。实际消费端已经在打补丁：`src/workflows/workflowHostOwners.ts:677` 写作 `const sizeBytes = Number(resource.sizeBytes ?? resource.size);`，一旦两者都有值就静默取前者；若上游只填 `size`，任何直接读 `sizeBytes` 的投影（DTO 序列化、`WorkflowResourceOutputDescriptorDto`）会丢字段或拿到 `undefined`。
- 建议：确定单一必填字段（`sizeBytes`），把 `size` 标为 `@deprecated` 并在 adapter 层一次性归一化。
- 置信度：中

### F11 [P3][正确性] src/workflows/workflowNoteImagePreparation.ts:270 —— `inferImageMimeType` 用路径片段判断 MIME，含 `image/` 的目录会被整体当成 MIME
- 证据：
```ts
  if (text.includes("image/")) return text;
```
- 分析：该函数既接收路径也接收 MIME（`source.mimeType || source.path`，见第 331 行）。对形如 `/data/image/photo.jpg` 的合法路径，`includes("image/")` 命中，函数返回整条路径字符串当作 “MIME 类型”；随后 owner 内的签名一致性校验（第 665-674 行）比较 `declaredMimeType !== detectedMimeType` 必然不等，抛出 `invalid_request: Image MIME type does not match its signature`，使位于名为 `image` 的目录下的图片无法准备。`application/octet-stream` 白名单与 `source.blob.type` 分支掩盖不了 path 输入这一路径。
- 建议：只在字符串以 `image/` 开头时判定为 MIME，否则按扩展名判断。
- 置信度：中

### F12 [P3][资源] src/workflows/workflowNoteImagePreparation.ts:654 —— 输入体积上限在整文件读入内存之后才校验
- 证据：
```ts
      if (sourceBlob.size > MAX_INPUT_BYTES) {
        throw imageResourceLimit(MAX_INPUT_BYTES, sourceBlob.size);
      }
```
- 分析：`MAX_INPUT_BYTES`（32 MiB）是防大文件/防 DoS 的入口闸，但 `file`/`resource` 两个分支在此之前已把文件整体物化：resource 分支的读取实现在 `src/workflows/workflowHostOwners.ts:382-384` 为 `new Blob([await readRuntimeBytes(resource.path)], { type: resource.contentType })`，file 分支经 `adapter.readPathBlob`（fetch → `response.blob()` / `readRuntimeBytes`）。触发条件：工作流对一个远大于 32 MiB 的附件/资源调用 `images.prepareForNoteEmbedding`；后果是先发生内存峰值，再抛出 `resource_limited`，上限失去保护作用（base64 分支相反，是先估算后解码，做法正确）。
- 建议：在物化前取 size（`stat`/资源 DTO 的 `sizeBytes`）并先行拒绝。
- 置信度：中

### F13 [P3][正确性] src/workflows/workflowHostOwners.ts:1181 —— 内嵌图槽位“检测”接受无引号属性，但“重写”只处理带引号形式，导致图片丢失
- 证据：
```ts
            new RegExp(
              `\\bdata-attachment-key\\s*=\\s*(?:["']${attachment.ref.key}["']|${attachment.ref.key}(?=\\s|>))`,
              "i",
            ).test(html),
```
```ts
            .replaceAll(
              `data-attachment-key="${attachment.ref.key}"`,
```
- 分析：检测正则显式支持三种写法（双引号、单引号、无引号），随后的 `replaceAll` 只替换双引号与单引号两种（第 1201-1209 行）。触发条件：笔记 HTML 中出现无引号的 `data-attachment-key=ABCD1234`。后果是资源已被 `stageFile` 暂存并登记进 `embeddedImages`，但正文里仍是旧 attachment-key 属性，导入到目标库的新笔记图片引用悬空（且暂存资源成为孤儿，只能靠 cleanup 兜底）。
- 建议：替换统一走同一条正则（`replace(/…/gi, …)`），或在检测侧只承认将被重写的两种写法。
- 置信度：中

SLICE: S13
FILES: 10 个 / 9989 行；ledger: /tmp/review/ledger/S13.md
COVERAGE: 已细读 10 个（archive/bibliography/declarativeRequestCompiler/loader/runtime/types/workflowHostOwners/workflowInputPlanning/workflowNoteImagePreparation 全文；triggerPolicy 极短全读）；略读 0 个。
TOP FINDINGS（只列 P0/P1/P2，按严重度排序，每条 ≤ 25 词）:
- [P2][资源] src/workflows/runtime.ts:631 上游 abort 监听与 interactive 资源创建在 try 之外，抛错即泄漏且无诊断/清理
- [P2][正确性] src/workflows/runtime.ts:1174 skippedUnits 把 preflight skip 双重计数，可超过 totalUnits
- [P2][错误处理] src/workflows/workflowInputPlanning.ts:333 已记录的 artifact issue 被转成硬失败，父项本应跳过却整体报错
- [P2][资源] src/workflows/archive.ts:653 条目上限在枚举全部条目标题之后才校验，上限失守内存保护
NOISE: (1) runtime.ts:520 全局 `workflowRuntimeScopeTail` 串行化把整个 hook 执行（含 IO/网络）置于全局锁内，当前切片内未找到同 realm 嵌套启动第二次 hook 的路径，故未记为缺陷，但任何未来 re-entrant 调用都会永久死锁，建议主代理确认。(2) types.ts 的 DTO 是否与 `src/shared/*WireContract.ts` 重复无法在切片内判定，需在 shared 侧复核 SSOT。(3) declarativeRequestCompiler 的 `entry.from` 未做取值校验（未知 selector 落入 `return true`），需确认 manifest 解析层是否已限制。