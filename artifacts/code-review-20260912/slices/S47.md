## A. 文件级台账

| 文件 | 行数 | 结论 | 说明 |
|---|---|---|---|
| src/modules/assistant/workspace/assistantPanelLabels.ts | 666 | OK | 纯本地化标签表（单一 builder、无分支/状态）；核对无同对象内重复键，跨对象复用 dashboard/workflow 既有键属有意共享。 |
| src/modules/assistant/workspace/assistantSidebarViewModel.ts | 182 | 存疑 | `panes[tab].active` 取 `args.full`，其余 pane 取 `activeTab === tab`，同一字段两种语义；且未收到 ownerKey/selectedOwnerKey，无法判定 `full ⇔ tab===activeTab` 不变式是否成立（见 NOISE）。 |
| src/modules/assistant/workspace/assistantWorkspaceActionRouter.ts | 1098 | OK | 注册表（action×source×payloadKeys）→ scope/owner 存在性 → 选中 owner 三段校验顺序正确；`actionPayload` 用 owner 身份覆盖 child payload，身份不可被 child 伪造。无来源 envelope 走 legacy 回退分支（注释标注有意保留），会绕过 payloadKeys 校验，属同一 iframe 内契约软化而非信任边界缺陷，未列 finding。 |
| src/modules/assistant/workspace/assistantWorkspacePublicationHost.ts | 1234 | OK | 生命周期/基线 init/ack 归并逻辑自洽；`flushScheduledWorkspacePost` 内 `postSnapshotForTab` 未 await 也未 `void`（浮空 promise），判断为低风险（initialize 失败已有 `onInitializationFailed` 吸收），未升为 finding。 |
| src/modules/assistant/workspace/assistantWorkspaceSidebar.ts | 2069 | 问题 | P2：mountLibraryPane/mountReaderPane 在 Zotero sidenav 上注册的 capture 监听器在 `removeAssistantWorkspaceSidebarShell` 中从不移除（F2）。另：`snapshotRevision` 声明后从未递增（死状态）。 |
| src/modules/assistant/workspace/assistantWorkspaceSurfaceSkeleton.ts | 213 | 问题 | P2：`mapWorkspaceChangeKindsToPublicationKinds` 对未映射 kind 会产出 `undefined` 元素（F3）。`createWorkspaceOwnerControl` 注释称“8-field block”，实际 9 个字段（注释漂移）。 |
| src/modules/hostBridge/cli/hostBridgeCliInjection.ts | 360 | OK | 密钥只经 env/`tokenEnv` 传递，profile/README 不落 token；grant 在写入失败时回滚。shim 模板与 installer 重复（见 F5）。 |
| src/modules/hostBridge/cli/hostBridgeCliInstallPrompt.ts | 398 | 问题 | P2：先执行待校验的目标二进制读版本、后比对摘要，自报版本可抑制重装（F1）。 |
| src/modules/hostBridge/cli/hostBridgeCliInstaller.ts | 715 | 问题 | P3：`sha256Bytes`/shell shim 模板与另外两个 CLI 模块逐字重复（F5）。另：`defaultPathIncludes` 无视注入的 `pathEnv`，与 `resolveHostBridgeCliInstallTarget` 读取同源信息却不同源（可测试性不一致）。 |
| src/modules/hostBridge/cli/hostBridgeCliResolver.ts | 217 | 存疑 | `resolveRuntimePlatform` 只从 `process.arch` 取架构，缺省回落 `"x64"`，无 `Services.appinfo` 类回退；在 arm64 且无 `process` 的环境会静默挑错平台二进制，需确认主窗口是否暴露 `process.arch`。 |
| src/modules/hostBridge/cli/hostBridgePluginSkillBundle.ts | 371 | 存疑 | staging→backup→root→receipt 顺序正确、校验严密；但 `promoted=true` 后 receipt 写入失败时既不回收 `backup` 目录，又返回 `ok:false`（bundle 实际已发布），需确认调用方是否容忍该组合状态。 |
| src/modules/hostBridge/cli/hostBridgeProfileStore.ts | 157 | 问题 | P2：chmod 失败仍返回 `ok:true`，token 文件可能保留默认权限（F4）。 |
| src/modules/hostBridge/cli/hostBridgeSkillRunnerEnv.ts | 305 | OK | local/remote 分流、LAN+pinned port+非 loopback 三道门禁齐全，无绕过路径发现；`advertisedHost` 若为未加方括号的 IPv6 字面量，`buildEndpoint` 会生成非法 URL（检测函数在切片外，未证实可达，故不列 finding）。 |

## B. Findings

### F1 [P2][信任边界] src/modules/hostBridge/cli/hostBridgeCliInstallPrompt.ts:288 —— 完整性校验先执行被校验对象，其自报版本可抑制重装提示

- 证据：
```ts
  const installedVersion = normalizeString(
    await readInstalledVersion(target.targetPath).catch(() => ""),
  );
  try {
    targetSha256 = await hashBytes(await readRuntimeFile(target.targetPath));
```
```ts
    status:
      targetSha256 === bundledSha256 ||
      isInstalledVersionNewer({ installedVersion, bundledVersion })
```
- 分析：`defaultReadInstalledVersion` → `readVersionWithOneShotSubprocess(targetPath)` 会**执行** `target.targetPath`（`--version`），且这一步发生在摘要比对之前、无条件执行。两条后果：①“校验”本身成为对被替换文件的执行路径（启动即运行任意可写该目录者投放的二进制）；②`isInstalledVersionNewer` 只在摘要不一致（或读文件失败）时才会成为 `"current"` 的判定依据，而这正是文件“不是我们装的”那一支——被检查对象自报一个大版本号即可让状态变 `current`，从而不再提示覆盖安装。触发前提是能在 `~/.local/bin` / `%LOCALAPPDATA%\zotero-agents\bin` 写文件（用户级写权限；同用户进程通常已具备）。
- 建议：先比对摘要；摘要不一致时不得以目标二进制自报版本作为权威（改为读取安装时写入的 receipt/版本旁文件，或始终判 `stale` 并保持提示）。
- 置信度：中（代码路径确定；严重度取决于安装目录写权限假设）

### F2 [P2][并发/资源] src/modules/assistant/workspace/assistantWorkspaceSidebar.ts:1417 —— sidenav click 监听器从不移除，宿主/重装累积

- 证据：
```ts
  roots.sidenav.addEventListener(
    "click",
    (event: Event) => {
```
（1461 行 reader 侧同样；`removeAssistantWorkspaceSidebarShell` 只清理 `host.removeMessageListener`、frame 的 `load` 监听与元素，见 1939-1948）
- 分析：`mountLibraryPane`/`mountReaderPane` 把 capture 监听器挂在 Zotero 自身的 `#zotero-view-item-sidenav` / `#zotero-context-pane-sidenav` 上，闭包强引用 `host`；`removeAssistantWorkspaceSidebarShell` 只删除 button/container/frame 并 `hosts.delete`，未 `removeEventListener`。同一窗口内每次 remove→install 都会新增一份监听器，旧 host（含 publicationRuntime、lifecycles Map、订阅集合）无法被回收；旧监听器还会对已删除 DOM 的容器调用 `deactivateTarget`。触发条件是同一窗口内的重装/移除流程。
- 建议：把两个 handler 提到模块级或存到 host（如 `librarySidenavListener`）并在 `removeAssistantWorkspaceSidebarShell` 中 `removeEventListener(..., true)`。
- 置信度：高

### F3 [P2][正确性] src/modules/assistant/workspace/assistantWorkspaceSurfaceSkeleton.ts:44 —— 未映射的 change kind 会以 `undefined` 混入 publication kinds

- 证据：
```ts
  return Array.from(new Set(kinds.flatMap((kind) => mapping[kind])));
```
- 分析：`mapping` 被类型化为覆盖全部 `TKind` 的 `Record`，运行时若某个 change kind 未列入映射（新增 kind 漏配、store 漂移），`mapping[kind]` 为 `undefined`，`flatMap` 会把 `undefined` 作为元素铺平（`[].flat()` 不去除 `undefined`），`Set` 保留它，返回类型 `AssistantWorkspacePublicationKind[]` 把该 `undefined` 隐藏。下游按 kind 查 readers/构造 publication 时会拿到未知 kind（`readWorkspaceOwnerRegions` 之外没有兜底过滤），属于“静默污染”而非丢弃。
- 建议：`kinds.flatMap((kind) => mapping[kind] ?? [])`，或对未映射 kind 记一条 debug 日志后跳过。
- 置信度：高（语言语义确定）/ 中（可达性取决于各 surface 映射是否与实际 change kind 同步）

### F4 [P2][信任边界] src/modules/hostBridge/cli/hostBridgeProfileStore.ts:139 —— 权限收紧失败仍返回成功，token 文件可能保留默认 umask 权限

- 证据：
```ts
  try {
    permissionProtected =
      (await chmodOwnerReadWrite(path)) || permissionProtected;
  } catch {
    permissionProtected = false;
  }
```
（随后 `return { ok: true, path, permissionProtected }`）
- 分析：该文件内容含 bearer token（`auth.token`），是秘密载体。写入使用默认权限创建，随后才 chmod 0600；若 chmod 抛错（不支持的 FS、SELinux 等），函数仍返回 `ok:true`，只用一个布尔字段提示。调用方若不检查 `permissionProtected`，会把“token 已就位”当成成功，而文件可能停在 umask 默认（常见 0644，同机其他用户可读）。这与“秘密不得落盘到宽松权限”的取向相反。
- 建议：权限收紧失败时删除已写入文件并返回 `ok:false`（附 reason），或改为以限制性模式创建后再写入（O_CREAT|0600）。
- 置信度：中（失败分支与返回语义确定；最终影响取决于切片外调用方是否检查该布尔）

### F5 [P3][可维护性] src/modules/hostBridge/cli/hostBridgeCliInstaller.ts:374 —— sha256 与 shim 模板在 3 个 CLI 模块中逐字重复

- 证据：
```ts
async function sha256Bytes(bytes: Uint8Array) {
  const runtime = globalThis as {
    crypto?: {
```
```ts
function buildWindowsShellShim(binaryPath: string) {
  return `#!/usr/bin/env sh\nexec "${formatShellPath(binaryPath)}" "$@"\n`;
}
```
- 分析：`sha256Bytes` 在 `hostBridgeCliInstallPrompt.ts:156-178` 与 `hostBridgeCliInstaller.ts:374-396` 逐字重复（含 `crypto.subtle` 与 `new Function` 动态 import 回退）；`#!/usr/bin/env sh\nexec "<path>" "$@"` 模板在 `hostBridgeCliInjection.ts:93-95` 与 `hostBridgeCliInstaller.ts:136-138` 重复；`formatPortablePath/formatShellPath/normalizeString` 亦各有一份。仓库已有 `utils/sha256.sha256Hex`（`hostBridgePluginSkillBundle.ts:4` 正在使用），即同一能力存在第 3 份实现。代价是可量化的：3 处/2 对重复，任一处修 bug（如摘要回退分支）都不会同步。
- 建议：抽到 `src/utils/sha256.ts`（或复用 `sha256Hex`）与单一 shim 构造器，prompt/installer/injection 共同引用。
- 置信度：高（重复事实确定；归类为可维护性、非缺陷）

---

SLICE: S47
FILES: 13 个 / 7985 行；ledger: /tmp/review/ledger/S47.md
COVERAGE: 已细读 12 个；略读 1 个（assistantPanelLabels.ts，纯本地化标签表，无逻辑分支）
TOP FINDINGS（只列 P0/P1/P2，按严重度排序）:
- [P2][信任边界] hostBridgeCliInstallPrompt.ts:288 校验前先执行目标二进制；自报版本可使被替换文件判为 current
- [P2][并发] assistantWorkspaceSidebar.ts:1417/1461 sidenav click 监听器从不移除：旧 host 泄漏、重装累积
- [P2][正确性] assistantWorkspaceSurfaceSkeleton.ts:44 未映射 change kind 经 flatMap 变成 undefined 混入 kinds
- [P2][信任边界] hostBridgeProfileStore.ts:139 chmod 失败仍 ok:true，token 文件可能保留默认权限
NOISE: ① assistantSidebarViewModel.ts:54 `panes[tab].active = args.full` 与其它 pane 的 `activeTab === tab` 不同源，请确认 `full ⇔ 该 tab 为 activeTab` 不变式；② hostBridgeCliResolver.ts:140/162 架构缺省 "x64" 且无 Zotero 侧回退，arm64 上可能挑错二进制；③ hostBridgePluginSkillBundle.ts:331 promotion 后 receipt 写失败不回收 backup 且报 ok:false（bundle 实际已发布）。