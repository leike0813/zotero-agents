# Export Literature Bundle

Exports one or more selected parent literature items into one portable ZIP bundle. The bundle contains portable item metadata, tags, all child notes, readable attachments, note payload images, embedded images, local images referenced by Markdown attachments, and relationships among selected parents.

Stored and linked local files are bundled as content. Missing local files are skipped and reported as warnings. Remote Markdown images remain remote links.

