{
  "schemaVersion": 2,
  "id": "add-digest-representative-image",
  "label": "Add Digest Representative Image",
  "description": "Choose an image from analyzed literature and attach it as the digest representative image.",
  "executionModes": ["auto"],
  "version": "1.0.0",
  "provider": "pass-through",
  "debug_only": true,
  "trigger": {
    "requiresSelection": true
  },
  "inputs": {
    "member": {
      "kind": "digest-image-target"
    },
    "grouping": {
      "mode": "all"
    }
  },
  "validateSelection": {
    "select": {
      "policy": "digest-representative-image"
    },
    "filters": []
  },
  "parameters": {
    "markdown_src": {
      "type": "string",
      "title": "Markdown Image Source",
      "description": "Relative image path from the source Markdown file, for example figures/overview.jpg.",
      "default": ""
    }
  },
  "hooks": {
    "buildRequest": "hooks/buildRequest.mjs",
    "applyResult": "hooks/applyResult.mjs"
  }
}
