{
  "id": "literature-search-ingest",
  "label": "Literature Search Ingest",
  "description": "Search scholarly sources, review candidates, and ingest deduplicated literature with provenance into the Zotero library.",
  "executionModes": ["auto"],
  "provider": "skillrunner",
  "version": "0.2.0",
  "display": {
    "core": true,
    "emoji": "🔎"
  },
  "taskNameTemplate": "Search and ingest literature: {query}",
  "trigger": {
    "requiresSelection": false
  },
  "inputs": {
    "unit": "workflow"
  },
  "parameters": {
    "query": {
      "type": "string",
      "title": "Search Query",
      "description": "Optional search query or seed. Leave blank with auto mode to start a guided search-planning conversation.",
      "default": ""
    },
    "searchMode": {
      "type": "string",
      "title": "Search Mode",
      "description": "Choose auto detection, guided search planning, topic expansion, paper seed expansion, or exact targeted ingest.",
      "default": "auto",
      "enum": [
        "auto",
        "guided",
        "topic_expansion",
        "paper_seed_expansion",
        "targeted_ingest"
      ]
    },
    "searchBreadth": {
      "type": "string",
      "title": "Search Breadth",
      "description": "Choose broad multi-lane discovery, balanced coverage, or a quick first pass.",
      "default": "broad",
      "enum": [
        "broad",
        "balanced",
        "quick"
      ]
    },
    "languageHints": {
      "type": "array",
      "title": "Language Hints",
      "description": "Optional BCP 47 language hints such as en, zh-CN, ja, or de. They expand queries and sources but never filter other languages.",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "targetCollection": {
      "type": "string",
      "title": "Target Collection",
      "description": "Optional Zotero collection for created or existing items.",
      "default": "",
      "allowCustom": false,
      "optionsSource": {
        "kind": "zotero.collections",
        "library": "current",
        "includeEmpty": true,
        "valueFormat": "collectionRef",
        "labelFormat": "path"
      }
    }
  },
  "request": {
    "kind": "skillrunner.job.v1",
    "create": {
      "skill_id": "literature-search-ingest",
      "mode": "interactive"
    }
  },
  "execution": {
    "zoteroHostAccess": {
      "required": true,
      "allowWriteApprovalBypass": true
    }
  },
  "result": {
    "fetch": {
      "type": "result"
    },
    "expects": {
      "result_json": "result/result.json"
    }
  },
  "hooks": {
    "applyResult": "hooks/applyResult.mjs"
  }
}
