export async function applyResult() {
  return {
    ok: true,
    applied: false,
    reason:
      "literature-search-ingest applies changes through permission-gated single-paper literature ingest calls",
  };
}
