The user supplied files for the pending interaction. Use only these workspace-relative files and continue the current task:

{file_bindings}

Do not infer or request the user's original local paths. Refer to the files by their declared field names or workspace-relative paths.
