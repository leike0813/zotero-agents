{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://zotero-agents.dev/schemas/host-bridge.release-receipt.v1.schema.json",
  "title": "Host Bridge Release Receipt",
  "type": "object",
  "required": [
    "schema",
    "status",
    "releaseSetId",
    "sourceCommit",
    "workflowRun",
    "surfaces"
  ],
  "properties": {
    "schema": { "const": "host-bridge.release-receipt.v1" },
    "status": { "enum": ["complete", "failed"] },
    "releaseSetId": { "type": "string" },
    "sourceCommit": { "type": "string" },
    "workflowRun": { "type": "string" },
    "pipelineRevision": { "type": "string" },
    "prebuild": {
      "type": "object",
      "required": ["branch", "commit"],
      "properties": {
        "branch": { "const": "host-bridge-cli-prebuilds" },
        "commit": { "type": "string" }
      },
      "additionalProperties": false
    },
    "surfaces": { "type": "object" }
  },
  "additionalProperties": false
}
